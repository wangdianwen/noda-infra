-- ============================================
-- Noda Monorepo - PostgreSQL Schema 初始化
-- ============================================
-- 此脚本在每个数据库中创建基础 schema
-- 注意: 此脚本会在每个数据库中执行一次

-- ============================================
-- 创建扩展（如果需要）
-- ============================================

-- UUID 生成（Prisma 需要）
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- ============================================
-- 创建基础 schema
-- ============================================

-- public schema 已存在，无需创建

-- 创建应用专用 schema（可选，用于更清晰的权限隔离）
-- CREATE SCHEMA IF NOT EXISTS app_data;

-- ============================================
-- 配置权限
-- ============================================

-- 授予用户在 public schema 上的权限
-- 注意: 此部分由 PostgreSQL 的默认权限管理
-- 如果需要更细粒度的权限控制，可以在此添加

-- ============================================
-- 创建基础表（占位，实际表由 Prisma migrate 创建）
-- ============================================

-- 示例: 创建版本表（用于跟踪迁移历史）
-- CREATE TABLE IF NOT EXISTS schema_migrations (
--     version VARCHAR(255) PRIMARY KEY,
--     applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- ============================================
-- 输出信息
-- ============================================
\echo '=========================================='
\echo 'Schema 初始化完成'
\echo '=========================================='
\echo '当前数据库:' current_database()
\echo 'Schema 列表:'
\echo '  - public (默认)'
\echo '扩展列表:'
SELECT extname FROM pg_extension ORDER BY extname;
\echo '=========================================='
