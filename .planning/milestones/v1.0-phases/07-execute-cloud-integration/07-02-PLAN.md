---
phase: 07-execute-cloud-integration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: true
requirements: [UPLOAD-01, UPLOAD-02, UPLOAD-03, UPLOAD-04, UPLOAD-05, SECURITY-01, SECURITY-02]

must_haves:
  truths:
    - "test_rclone.sh 退出码为 0（所有 5 个测试通过）"
    - "test_upload.sh 退出码为 0（端到端上传测试通过）"
    - "B2 bucket 中能成功上传和列出文件"
    - "校验和验证通过（rclone check --one-way）"
    - "旧备份清理功能正常（cleanup_old_backups_b2）"
  artifacts:
    - path: "scripts/backup/tests/test_rclone.sh"
      provides: "rclone 配置测试（修复后运行）"
      exits: 0
    - path: "scripts/backup/tests/test_upload.sh"
      provides: "端到端上传测试"
      exits: 0
  key_links:
    - from: "test_rclone.sh"
      to: "B2 API"
      via: "rclone lsd/copy/delete"
      pattern: "rclone.*(lsd|copy|delete)"
    - from: "test_upload.sh"
      to: "cloud.sh upload_to_b2()"
      via: "函数调用"
      pattern: "upload_to_b2"
    - from: "test_upload.sh"
      to: "cloud.sh verify_upload_checksum()"
      via: "rclone check --one-way"
      pattern: "verify_upload_checksum|setup_rclone_config"
---

<objective>
运行完整测试套件验证云存储集成功能：先运行 test_rclone.sh 验证 rclone 配置和 B2 连接，然后运行 test_upload.sh 验证端到端上传功能。

Purpose: Plan 01 修复了 test_rclone.sh 的 3 个 BUG 和 cloud.sh 的 util.sh 依赖。现在需要实际运行测试验证所有功能正常工作，包括 B2 连接、文件上传、校验和验证和清理功能。

Output: 测试结果报告（所有测试通过）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execute-cloud-integration/07-CONTEXT.md
@.planning/phases/07-execute-cloud-integration/07-01-SUMMARY.md

<interfaces>
<!-- Plan 01 修复后的文件状态，executor 需要了解 -->

From scripts/backup/tests/test_rclone.sh（Plan 01 修复后）:
- 使用 `type = b2` 和 `account`/`key` 属性名（与 cloud.sh 一致）
- main() 函数运行全部 5 个测试（test_rclone_installed, test_b2_credentials, test_rclone_config, test_b2_connection, test_b2_operations）

From scripts/backup/lib/cloud.sh（Plan 01 修复后）:
- 包含 util.sh 的条件加载
- upload_to_b2(): 3 次重试 + 指数退避 + 校验和验证
- cleanup_old_backups_b2(): rclone delete --min-age 7d
- setup_rclone_config(): 创建临时配置文件（type = b2）

From scripts/backup/tests/test_upload.sh:
- 6 步测试：rclone 检查 → 凭证验证 → 创建测试文件 → 上传 → 验证 → 清理
- 直接调用 cloud.sh 的函数（upload_to_b2, setup_rclone_config, cleanup_old_backups_b2）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 test_rclone.sh 验证 rclone 配置和 B2 连接</name>
  <files>（无文件修改，只读验证）</files>
  <read_first>
    - scripts/backup/tests/test_rclone.sh（确认 Plan 01 的修复已应用）
    - scripts/backup/.env.backup（确认 B2 凭证已配置）
  </read_first>
  <action>
运行 test_rclone.sh 验证 rclone 配置和 B2 连接。这个脚本测试 5 个方面：

1. **rclone 安装检查** — 确认 rclone 已安装且版本 >= 1.60
2. **B2 凭证配置** — 确认 B2_ACCOUNT_ID、B2_APPLICATION_KEY、B2_BUCKET_NAME 已设置
3. **rclone 配置创建** — 使用修复后的 `type = b2` 格式创建临时配置
4. **B2 连接测试** — 连接到 B2 bucket 并列出目录
5. **B2 基本操作** — 上传/列表/删除测试文件

执行命令：
```bash
cd /Users/dianwenwang/Project/noda-infra && bash scripts/backup/tests/test_rclone.sh
```

如果测试失败：
- 检查 .env.backup 中 B2 凭证是否正确配置
- 检查网络连接是否正常
- 检查 B2 bucket 名称是否正确
- 记录失败原因到 SUMMARY
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && bash scripts/backup/tests/test_rclone.sh; echo "EXIT_CODE=$?"</automated>
  </verify>
  <done>
    - test_rclone.sh 退出码为 0
    - 所有 5/5 测试通过
    - B2 连接正常
    - 文件上传/列表/删除操作正常
  </done>
  <acceptance_criteria>
    - `bash scripts/backup/tests/test_rclone.sh` 退出码为 0
    - 输出包含 "5/5 测试通过" 或 "所有 5/5 测试通过"
    - 输出包含 "B2 bucket 连接成功"
    - 输出包含 "文件上传成功" 和 "文件删除成功"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 运行 test_upload.sh 端到端上传测试</name>
  <files>（无文件修改，只读验证）</files>
  <read_first>
    - scripts/backup/tests/test_upload.sh（了解测试流程）
    - scripts/backup/lib/cloud.sh（了解被测试的函数）
    - scripts/backup/.env.backup（确认 B2 配置）
  </read_first>
  <action>
运行 test_upload.sh 验证完整的端到端上传流程。这个脚本测试 6 个步骤：

1. **rclone 安装检查** — 确认 rclone 可用
2. **B2 凭证配置** — 调用 validate_b2_credentials 验证
3. **创建测试备份** — 在临时目录创建模拟备份文件（.dump、.sql、.json）
4. **上传到 B2** — 调用 upload_to_b2() 上传整个测试目录
5. **验证上传文件** — 使用 rclone ls 验证 B2 上有文件
6. **清理测试文件** — 删除本地测试文件 + 调用 cleanup_old_backups_b2 清理 B2 测试文件

执行命令：
```bash
cd /Users/dianwenwang/Project/noda-infra && bash scripts/backup/tests/test_upload.sh
```

如果测试失败：
- 检查 B2 凭证和网络连接（Task 1 应已验证）
- 检查 cloud.sh 的 upload_to_b2 日志输出，定位失败步骤
- 如果是超时问题，检查网络速度
- 记录失败原因到 SUMMARY

测试完成后验证：
- 确认 B2 上测试文件已被清理（cleanup_old_backups_b2 调用参数为 0，清理所有 0 天前的文件）
- 确认本地临时目录已被删除
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && bash scripts/backup/tests/test_upload.sh; echo "EXIT_CODE=$?"</automated>
  </verify>
  <done>
    - test_upload.sh 退出码为 0
    - 所有 6/6 测试通过
    - upload_to_b2 成功上传测试文件到 B2
    - 校验和验证通过（rclone check --one-way）
    - 测试文件已从 B2 和本地清理
  </done>
  <acceptance_criteria>
    - `bash scripts/backup/tests/test_upload.sh` 退出码为 0
    - 输出包含 "测试 4/6: 上传到 B2" 后跟成功信息
    - 输出包含 "测试 5/6: 验证上传文件" 后跟成功信息
    - 输出包含 "所有测试通过！云上传功能正常"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试脚本 → B2 API | 使用生产凭证进行测试操作（上传测试文件到 test/ 路径） |
| 测试脚本 → 本地文件系统 | 创建临时测试文件（mktemp） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-06 | Information Disclosure | test_upload.sh 上传的测试文件 | accept | 测试文件内容为随机数据，无敏感信息 |
| T-7-07 | Tampering | test_upload.sh 清理 B2 测试文件 | mitigate | cleanup_old_backups_b2 限制到 test/ 路径前缀 [test_upload.sh 第 91 行] |
| T-7-08 | Denial of Service | 测试影响 B2 存储配额 | accept | 测试文件 < 1KB，影响可忽略 |
</threat_model>

<verification>
1. `bash scripts/backup/tests/test_rclone.sh` 退出码为 0
2. `bash scripts/backup/tests/test_upload.sh` 退出码为 0
3. 两个测试输出中无 ERROR 字样
</verification>

<success_criteria>
- test_rclone.sh 所有 5/5 测试通过（退出码 0）
- test_upload.sh 所有 6/6 测试通过（退出码 0）
- B2 上传、校验和验证、清理功能均正常工作
- 无安全异常（无凭证泄漏、无生产数据影响）
</success_criteria>

<output>
After completion, create `.planning/phases/07-execute-cloud-integration/07-02-SUMMARY.md`
</output>
