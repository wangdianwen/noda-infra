---
phase: 08-execute-restore-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/lib/restore.sh
  - scripts/backup/tests/test_restore_quick.sh
autonomous: true
requirements:
  - RESTORE-01
  - RESTORE-03
  - RESTORE-04

must_haves:
  truths:
    - "restore_database() 在宿主机上通过 docker exec 成功恢复数据库"
    - "verify_backup_integrity() 在宿主机上通过 docker exec 成功验证备份"
    - "test_restore_quick.sh 在宿主机上全部 5 项测试通过"
  artifacts:
    - path: "scripts/backup/lib/restore.sh"
      provides: "恢复核心库（添加 docker exec 封装）"
      contains: "docker exec noda-infra-postgres-1"
    - path: "scripts/backup/tests/test_restore_quick.sh"
      provides: "快速恢复测试脚本（宿主机兼容）"
      contains: "test_restore_quick"
  key_links:
    - from: "scripts/backup/lib/restore.sh"
      to: "noda-infra-postgres-1 container"
      via: "docker exec"
      pattern: "docker exec noda-infra-postgres-1 (psql|pg_restore)"
---

<objective>
修复 lib/restore.sh 中 restore_database() 和 verify_backup_integrity() 函数缺少 docker exec 封装的问题，使其能在 macOS 宿主机上正确运行。

Purpose: restore.sh 中的这两个函数直接调用 psql/pg_restore 命令，但 macOS 宿主机上没有安装 PostgreSQL 客户端工具，导致恢复操作在宿主机上无法执行。verify.sh 已有正确的环境检测模式（通过 /.dockerenv 判断），restore.sh 需要对齐这一模式。

Output: 修复后的 lib/restore.sh 和 test_restore_quick.sh，宿主机上所有测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-execute-restore-scripts/08-CONTEXT.md
@.planning/phases/08-execute-restore-scripts/08-RESEARCH.md

<interfaces>
<!-- 已有的宿主机/容器检测模式，来自 lib/verify.sh 第 38-55 行 -->
```bash
# verify.sh 的环境检测模式
verify_backup_readable() {
  local backup_file=$1
  if [[ -f /.dockerenv ]]; then
    # 容器内：直接使用 pg_restore
    PGPASSWORD=$POSTGRES_PASSWORD pg_restore --list -h noda-infra-postgres-1 -U postgres "$backup_file"
  else
    # 宿主机：使用 docker exec
    docker exec noda-infra-postgres-1 pg_restore --list "$backup_file"
  fi
}
```

<!-- constants.sh 中的退出码常量 -->
readonly EXIT_SUCCESS=0
readonly EXIT_RESTORE_FAILED=7
readonly EXIT_INVALID_ARGS=8
readonly EXIT_VERIFICATION_FAILED=5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 restore.sh 的 docker exec 兼容性</name>
  <files>scripts/backup/lib/restore.sh</files>
  <read_first>
    scripts/backup/lib/restore.sh
    scripts/backup/lib/verify.sh
    scripts/backup/lib/constants.sh
    scripts/backup/lib/config.sh
  </read_first>
  <behavior>
    - 宿主机环境：restore_database() 使用 docker exec noda-infra-postgres-1 psql 执行 DROP/CREATE DATABASE
    - 宿主机环境：restore_database() 使用 docker exec noda-infra-postgres-1 pg_restore 恢复 .dump 文件
    - 宿主机环境：restore_database() 使用 docker exec noda-infra-postgres-1 psql 恢复 .sql 文件
    - 宿主机环境：restore_database() 使用 docker exec noda-infra-postgres-1 psql 查询表数量验证
    - 宿主机环境：verify_backup_integrity() 使用 docker exec noda-infra-postgres-1 pg_restore -l 验证 .dump 文件
    - 容器内环境：所有函数保持原有行为不变（直接调用 psql/pg_restore）
    - 文件存在性检查（stat -f%z / stat -c%s）保持宿主机兼容
  </behavior>
  <action>
修复 `scripts/backup/lib/restore.sh` 中的 `restore_database()` 和 `verify_backup_integrity()` 函数，添加与 `verify.sh` 一致的宿主机/容器环境检测逻辑。

**修改 restore_database() 函数（第 155-258 行）：**

1. 在函数开头（第 160 行之后）添加环境检测：
```bash
local is_host=false
if [[ ! -f /.dockerenv ]]; then
  is_host=true
fi
```

2. 修改第 214 行的 DROP DATABASE 命令 -- 替换：
```bash
psql $pg_params -d postgres -c "DROP DATABASE IF EXISTS $target_db" 2>/dev/null
```
为：
```bash
if [[ "$is_host" == true ]]; then
  docker exec noda-infra-postgres-1 psql -U postgres -d postgres -c "DROP DATABASE IF EXISTS $target_db" 2>/dev/null
else
  psql $pg_params -d postgres -c "DROP DATABASE IF EXISTS $target_db" 2>/dev/null
fi
```

3. 修改第 220 行的 CREATE DATABASE 命令 -- 同样模式，替换：
```bash
psql $pg_params -d postgres -c "CREATE DATABASE $target_db" 2>/dev/null
```
为：
```bash
if [[ "$is_host" == true ]]; then
  docker exec noda-infra-postgres-1 psql -U postgres -d postgres -c "CREATE DATABASE $target_db" 2>/dev/null
else
  psql $pg_params -d postgres -c "CREATE DATABASE $target_db" 2>/dev/null
fi
```

4. 修改第 230 行的 pg_restore 命令 -- 替换：
```bash
pg_restore $pg_params -d "$target_db" -j 4 "$backup_file"
```
为：
```bash
if [[ "$is_host" == true ]]; then
  docker exec noda-infra-postgres-1 pg_restore -U postgres -d "$target_db" -j 4 "$backup_file"
else
  pg_restore $pg_params -d "$target_db" -j 4 "$backup_file"
fi
```

5. 修改第 238 行的 psql 恢复 SQL 文件命令 -- 替换：
```bash
psql $pg_params -d "$target_db" -f "$backup_file"
```
为：
```bash
if [[ "$is_host" == true ]]; then
  docker exec -i noda-infra-postgres-1 psql -U postgres -d "$target_db" < "$backup_file"
else
  psql $pg_params -d "$target_db" -f "$backup_file"
fi
```
注意：SQL 文件通过 stdin 管道传入容器，使用 `docker exec -i`。

6. 修改第 249 行的表数量查询 -- 替换：
```bash
table_count=$(psql $pg_params -d "$target_db" -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public'" 2>/dev/null | xargs || echo "0")
```
为：
```bash
if [[ "$is_host" == true ]]; then
  table_count=$(docker exec noda-infra-postgres-1 psql -U postgres -d "$target_db" -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public'" 2>/dev/null | xargs || echo "0")
else
  table_count=$(psql $pg_params -d "$target_db" -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public'" 2>/dev/null | xargs || echo "0")
fi
```

**修改 verify_backup_integrity() 函数（第 268-308 行）：**

1. 在函数开头（第 270 行之后）添加环境检测：
```bash
local is_host=false
if [[ ! -f /.dockerenv ]]; then
  is_host=true
fi
```

2. 修改第 294 行的 pg_restore -l 验证 -- 替换：
```bash
if ! pg_restore -l "$backup_file" >/dev/null 2>&1; then
```
为：
```bash
local pg_restore_result=0
if [[ "$is_host" == true ]]; then
  docker exec noda-infra-postgres-1 pg_restore -l "$backup_file" >/dev/null 2>&1 || pg_restore_result=$?
else
  pg_restore -l "$backup_file" >/dev/null 2>&1 || pg_restore_result=$?
fi
if [[ $pg_restore_result -ne 0 ]]; then
```

注意：所有 docker exec 命令中不传递 $pg_params，因为容器内 PostgreSQL 使用本地 socket 连接，只需指定 -U postgres。容器名硬编码为 `noda-infra-postgres-1`，与 verify.sh 保持一致。
  </action>
  <verify>
    <automated>bash scripts/backup/tests/test_restore_quick.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/backup/lib/restore.sh 包含字符串 `is_host=false`（至少出现 2 次，分别在 restore_database 和 verify_backup_integrity 中）
    - scripts/backup/lib/restore.sh 包含字符串 `docker exec noda-infra-postgres-1 psql`
    - scripts/backup/lib/restore.sh 包含字符串 `docker exec noda-infra-postgres-1 pg_restore`
    - scripts/backup/lib/restore.sh 包含 `docker exec -i noda-infra-postgres-1 psql`（用于 SQL 文件恢复）
    - `bash scripts/backup/tests/test_restore_quick.sh` 退出码为 0
    - test_restore_quick.sh 输出包含 "所有测试通过"
  </acceptance_criteria>
  <done>restore_database() 和 verify_backup_integrity() 在宿主机上通过 docker exec 正确运行，test_restore_quick.sh 全部 5 项测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机 -> Docker 容器 | 通过 docker exec 传递 SQL 命令，需防范 SQL 注入 |
| 宿主机文件系统 -> 容器内 | 备份文件路径需在容器内可访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | restore_database() | mitigate | 数据库名通过文件名正则验证 `[^_]+_[0-9]{8}_[0-9]{6}`，防止 SQL 注入 |
| T-08-02 | Information Disclosure | docker exec 调用 | accept | 错误输出重定向到 /dev/null，不泄露敏感信息 |
| T-08-03 | Denial of Service | restore_database() DROP DATABASE | mitigate | 保留交互确认机制（read -p yes/no），防止误操作生产数据库 |
</threat_model>

<verification>
1. `bash scripts/backup/tests/test_restore_quick.sh` 全部通过
2. `grep -c 'docker exec noda-infra-postgres-1' scripts/backup/lib/restore.sh` 返回 >= 5
3. 恢复脚本在宿主机上能正确创建测试数据库、恢复数据、验证表数量
</verification>

<success_criteria>
- restore.sh 中的 restore_database() 和 verify_backup_integrity() 在宿主机和容器内两种环境下都能正确运行
- test_restore_quick.sh 在 macOS 宿主机上全部 5 项测试通过
- 修改不影响容器内运行时的行为（向后兼容）
</success_criteria>

<output>
After completion, create `.planning/phases/08-execute-restore-scripts/08-01-SUMMARY.md`
</output>
