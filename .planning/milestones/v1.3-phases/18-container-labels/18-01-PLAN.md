---
phase: 18-container-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.yml
  - docker/docker-compose.prod.yml
  - docker/docker-compose.dev.yml
  - docker/docker-compose.simple.yml
  - docker/docker-compose.dev-standalone.yml
autonomous: true
requirements:
  - GRP-01
  - GRP-02

must_haves:
  truths:
    - "docker ps --filter label=noda.environment=prod 仅显示生产容器"
    - "docker ps --filter label=noda.environment=dev 仅显示开发容器"
    - "所有 findclass-ssr 服务的 noda.service-group 值为 apps（不是 noda-apps）"
    - "所有容器同时拥有 noda.service-group 和 noda.environment 两个标签"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "5 个服务添加 noda.environment=prod，findclass-ssr 标签修正"
      contains: "noda.environment=prod"
    - path: "docker/docker-compose.prod.yml"
      provides: "3 个有 labels 的服务添加 noda.environment=prod，findclass-ssr 标签修正"
      contains: "noda.environment=prod"
    - path: "docker/docker-compose.dev.yml"
      provides: "postgres-dev 补全标签，keycloak-dev 添加 environment 标签"
      contains: "noda.environment=dev"
    - path: "docker/docker-compose.simple.yml"
      provides: "所有服务添加环境标签"
      contains: "noda.environment"
    - path: "docker/docker-compose.dev-standalone.yml"
      provides: "postgres-dev 添加 environment 标签"
      contains: "noda.environment=dev"
  key_links:
    - from: "docker/docker-compose.yml"
      to: "docker/docker-compose.prod.yml"
      via: "overlay 合并，labels 必须一致"
      pattern: "noda\\.environment"
---

<objective>
统一所有 Docker Compose 文件中的容器标签：修复 noda.service-group 值不一致，添加 noda.environment 标签。

Purpose: 让 `docker ps --filter label=noda.environment=prod/dev` 能正确按环境筛选容器，消除标签命名不一致。
Output: 5 个 compose 文件修改完成，标签统一规范。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-container-labels/18-CONTEXT.md

<interfaces>
<!-- Docker Compose labels 格式规范 -->
<!-- 所有标签在 labels: 块中使用 "- key=value" 列表格式 -->
<!-- 已有模式：noda.service-group=infra / noda.service-group=apps -->
<!-- 新增标签：noda.environment=prod / noda.environment=dev -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 noda.service-group 不一致 + 为生产服务添加 environment 标签</name>
  <files>docker/docker-compose.yml, docker/docker-compose.prod.yml</files>

  <read_first>
    docker/docker-compose.yml
    docker/docker-compose.prod.yml
  </read_first>

  <acceptance_criteria>
    <!-- docker-compose.yml 验证 -->
    docker-compose.yml: postgres 服务有 "noda.environment=prod" 标签
    docker-compose.yml: nginx 服务有 "noda.environment=prod" 标签
    docker-compose.yml: noda-ops 服务有 "noda.environment=prod" 标签
    docker-compose.yml: findclass-ssr 服务标签为 "noda.service-group=apps"（不是 noda-apps）
    docker-compose.yml: findclass-ssr 服务有 "noda.environment=prod" 标签
    docker-compose.yml: keycloak 服务有 "noda.environment=prod" 标签
    docker-compose.yml: 文件中不包含 "noda-apps" 字符串
    <!-- docker-compose.prod.yml 验证 -->
    docker-compose.prod.yml: keycloak 服务有 "noda.environment=prod" 标签
    docker-compose.prod.yml: findclass-ssr 服务标签为 "noda.service-group=apps"（不是 noda-apps）
    docker-compose.prod.yml: findclass-ssr 服务有 "noda.environment=prod" 标签
    docker-compose.prod.yml: 文件中不包含 "noda-apps" 字符串
  </acceptance_criteria>

  <action>
**docker/docker-compose.yml 修改（per D-01, D-02, D-04, D-06）：**

1. postgres 服务（约第 16-17 行）：在现有 `"noda.service-group=infra"` 后添加 `"noda.environment=prod"`
2. nginx 服务（约第 41-42 行）：在现有 `"noda.service-group=infra"` 后添加 `"noda.environment=prod"`
3. noda-ops 服务（约第 65-66 行）：在现有 `"noda.service-group=infra"` 后添加 `"noda.environment=prod"`
4. findclass-ssr 服务（约第 110-111 行）：将 `"noda.service-group=noda-apps"` 改为 `"noda.service-group=apps"`，然后添加 `"noda.environment=prod"`
5. keycloak 服务（约第 143-144 行）：在现有 `"noda.service-group=infra"` 后添加 `"noda.environment=prod"`

每个服务的 labels 块修改后格式示例：
```yaml
    labels:
      - "noda.service-group=infra"
      - "noda.environment=prod"
```

findclass-ssr 修改后：
```yaml
    labels:
      - "noda.service-group=apps"
      - "noda.environment=prod"
```

**docker/docker-compose.prod.yml 修改（per D-02, D-04）：**

1. keycloak 服务（约第 60-61 行）：在现有 `"noda.service-group=infra"` 后添加 `"noda.environment=prod"`
2. findclass-ssr 服务（约第 115-116 行）：将 `"noda.service-group=noda-apps"` 改为 `"noda.service-group=apps"`，然后添加 `"noda.environment=prod"`

注意：postgres、nginx、noda-ops 服务在 prod overlay 中没有 labels 块（它们通过 YAML anchor *common-security 或没有定义 labels），这些服务的标签只在 base docker-compose.yml 中定义，所以 base 文件中的修改已经覆盖。
  </action>

  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && grep -c "noda.environment=prod" docker/docker-compose.yml | grep -q "5" && grep -c "noda.environment=prod" docker/docker-compose.prod.yml | grep -q "2" && ! grep -q "noda-apps" docker/docker-compose.yml docker/docker-compose.prod.yml && echo "PASS"</automated>
  </verify>

  <done>
docker-compose.yml: 5 个服务各有 noda.environment=prod，findclass-ssr 使用 service-group=apps，文件中无 "noda-apps" 字符串。
docker-compose.prod.yml: keycloak 和 findclass-ssr 有 noda.environment=prod，findclass-ssr 使用 service-group=apps，文件中无 "noda-apps" 字符串。
  </done>
</task>

<task type="auto">
  <name>Task 2: 为开发服务和补充文件添加标签</name>
  <files>docker/docker-compose.dev.yml, docker/docker-compose.simple.yml, docker/docker-compose.dev-standalone.yml</files>

  <read_first>
    docker/docker-compose.dev.yml
    docker/docker-compose.simple.yml
    docker/docker-compose.dev-standalone.yml
  </read_first>

  <acceptance_criteria>
    <!-- docker-compose.dev.yml 验证 -->
    docker-compose.dev.yml: postgres-dev 服务有 "noda.service-group=infra" 和 "noda.environment=dev" 两个标签
    docker-compose.dev.yml: keycloak-dev 服务有 "noda.environment=dev" 标签（已有 service-group=infra）
    <!-- docker-compose.simple.yml 验证 -->
    docker-compose.simple.yml: postgres 服务有 "noda.environment=prod" 标签
    docker-compose.simple.yml: postgres-dev 服务有 "noda.environment=dev" 标签
    docker-compose.simple.yml: nginx 服务有 "noda.environment=prod" 标签
    docker-compose.simple.yml: keycloak 服务有 "noda.environment=prod" 标签
    docker-compose.simple.yml: cloudflared 服务有 "noda.environment=prod" 标签
    <!-- docker-compose.dev-standalone.yml 验证 -->
    docker-compose.dev-standalone.yml: postgres-dev 服务有 "noda.environment=dev" 标签
  </acceptance_criteria>

  <action>
**docker/docker-compose.dev.yml 修改（per D-05, D-06, D-07, D-08）：**

1. postgres-dev 服务（约第 18 行起）：当前没有 labels 块。在 `container_name: noda-infra-postgres-dev` 之后、`restart:` 之前添加 labels 块：
```yaml
    labels:
      - "noda.service-group=infra"
      - "noda.environment=dev"
```

2. keycloak-dev 服务（约第 95-99 行）：已有 `"noda.service-group=infra"` 标签，在其后添加 `"noda.environment=dev"`

**docker/docker-compose.simple.yml 修改（per D-04, D-05）：**

所有 5 个服务都已有 `"noda.service-group=infra"` 标签，逐个添加 environment 标签：

1. postgres（约第 14-15 行）：添加 `"noda.environment=prod"`
2. postgres-dev（约第 39-40 行）：添加 `"noda.environment=dev"`
3. nginx（约第 63-64 行）：添加 `"noda.environment=prod"`
4. keycloak（约第 85-86 行）：添加 `"noda.environment=prod"`
5. cloudflared（约第 117-118 行）：添加 `"noda.environment=prod"`

**docker/docker-compose.dev-standalone.yml 修改（per D-05）：**

1. postgres-dev（约第 14-15 行）：已有 `"noda.service-group=infra"`，在其后添加 `"noda.environment=dev"`
  </action>

  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && grep -c "noda.environment=dev" docker/docker-compose.dev.yml | grep -q "2" && grep -c "noda.environment" docker/docker-compose.simple.yml | grep -q "5" && grep -c "noda.environment=dev" docker/docker-compose.dev-standalone.yml | grep -q "1" && echo "PASS"</automated>
  </verify>

  <done>
docker-compose.dev.yml: postgres-dev 有完整标签（service-group=infra + environment=dev），keycloak-dev 有 environment=dev。
docker-compose.simple.yml: 5 个服务各有对应环境标签（prod/dev）。
docker-compose.dev-standalone.yml: postgres-dev 有 environment=dev 标签。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 本 Phase 仅修改 Docker Compose 标签，不涉及代码变更、网络暴露或权限修改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | I (Information Disclosure) | container labels | accept | 标签仅含环境名称（prod/dev），无敏感信息，`docker inspect` 需宿主机访问权限 |
</threat_model>

<verification>
1. 语法验证：`docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config > /dev/null` 不报错
2. 标签计数：所有 compose 文件中 "noda-apps" 出现次数为 0
3. 标签完整性：docker-compose.yml 中 "noda.environment=prod" 出现 5 次，覆盖全部 5 个服务
4. 简单配置语法：`docker compose -f docker/docker-compose.simple.yml config > /dev/null` 不报错
</verification>

<success_criteria>
1. 所有 5 个 compose 文件通过 `docker compose config` 语法验证
2. grep -r "noda-apps" docker/*.yml 返回空（无不一致命名）
3. docker-compose.yml 中 grep "noda.environment=prod" 匹配 5 行
4. docker-compose.dev.yml 中 grep "noda.environment=dev" 匹配 2 行
5. docker-compose.simple.yml 中 grep "noda.environment" 匹配 5 行
6. docker-compose.dev-standalone.yml 中 grep "noda.environment=dev" 匹配 1 行
7. docker-compose.app.yml 无需修改（确认 grep "noda.service-group=apps" 匹配 1 行）
</success_criteria>

<output>
After completion, create `.planning/phases/18-container-labels/18-01-SUMMARY.md`
</output>
