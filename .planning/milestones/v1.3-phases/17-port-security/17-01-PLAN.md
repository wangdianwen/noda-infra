---
phase: 17-port-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.dev.yml
  - docker/docker-compose.simple.yml
  - docker/docker-compose.dev-standalone.yml
autonomous: false
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "postgres-dev 5433 端口仅绑定 127.0.0.1，外部网络无法直接连接"
    - "Keycloak 9000 管理端口不在任何 compose 文件中以 0.0.0.0 暴露到宿主机"
    - "本地开发通过 127.0.0.1:5433 仍可正常连接 dev 数据库"
  artifacts:
    - path: "docker/docker-compose.dev.yml"
      provides: "postgres-dev 端口绑定 127.0.0.1"
      contains: "127.0.0.1:5433:5432"
    - path: "docker/docker-compose.simple.yml"
      provides: "postgres-dev 端口绑定 127.0.0.1 + Keycloak 9000 端口绑定 127.0.0.1"
      contains: "127.0.0.1:5433:5432"
    - path: "docker/docker-compose.dev-standalone.yml"
      provides: "postgres-dev 端口绑定 127.0.0.1"
      contains: "127.0.0.1:5433:5432"
  key_links:
    - from: "docker/docker-compose.dev.yml"
      to: "postgres-dev container"
      via: "ports 映射"
      pattern: "127\\.0\\.0\\.1:5433:5432"
    - from: "docker/docker-compose.simple.yml"
      to: "keycloak container"
      via: "ports 映射"
      pattern: "127\\.0\\.0\\.1:9000:9000"
---

<objective>
将所有 Docker Compose 文件中 postgres-dev 5433 端口和 Keycloak 9000 管理端口从 0.0.0.0 绑定改为 127.0.0.1，确保仅本地可访问。

Purpose: 消除开发数据库和管理端口的网络暴露风险，外部网络无法直接连接这些端口。
Output: 3 个 compose 文件的端口绑定修改 + 验证确认。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-port-security/17-CONTEXT.md
@.planning/phases/16-keycloak/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改三个 compose 文件的端口绑定</name>
  <files>
    docker/docker-compose.dev.yml
    docker/docker-compose.simple.yml
    docker/docker-compose.dev-standalone.yml
  </files>
  <read_first>
    docker/docker-compose.dev.yml
    docker/docker-compose.simple.yml
    docker/docker-compose.dev-standalone.yml
    docker/docker-compose.yml
  </read_first>
  <action>
按照用户决策（D-01 至 D-07），逐文件修改端口绑定：

1. **docker-compose.dev.yml**（per D-01）:
   - 第 23 行：将 `"5433:5432"` 改为 `"127.0.0.1:5433:5432"`

2. **docker-compose.simple.yml**（per D-02, D-06）:
   - 第 43 行：将 `"5433:5432"` 改为 `"127.0.0.1:5433:5432"`
   - 第 89 行：将 `"9000:9000"` 改为 `"127.0.0.1:9000:9000"`

3. **docker-compose.dev-standalone.yml**（per D-03）:
   - 第 27 行：将 `"5433:5432"` 改为 `"127.0.0.1:5433:5432"`

4. **确认无需修改的文件**（per D-05, D-07）:
   - docker-compose.yml: Keycloak 已在 Phase 16 移除 ports 段，无需操作
   - docker-compose.dev.yml: keycloak-dev 已使用 `"127.0.0.1:18080:8080"` 和 `"127.0.0.1:19000:9000"`，无需操作

5. **运行配置验证**（per D-08）:
   ```bash
   docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.dev.yml config --quiet
   ```
   确认无语法错误。

6. **grep 验证**：
   ```bash
   grep -n "5433:5432" docker/docker-compose.dev.yml docker/docker-compose.simple.yml docker/docker-compose.dev-standalone.yml
   ```
   确认所有 postgres-dev 端口都带有 `127.0.0.1:` 前缀。

   ```bash
   grep -n "9000:9000" docker/docker-compose.simple.yml
   ```
   确认 Keycloak 9000 端口带有 `127.0.0.1:` 前缀。
  </action>
  <acceptance_criteria>
    - grep -c "127.0.0.1:5433:5432" docker/docker-compose.dev.yml 输出 1
    - grep -c "127.0.0.1:5433:5432" docker/docker-compose.simple.yml 输出 1
    - grep -c "127.0.0.1:5433:5432" docker/docker-compose.dev-standalone.yml 输出 1
    - grep -c "127.0.0.1:9000:9000" docker/docker-compose.simple.yml 输出 1
    - grep -rn "\"5433:5432\"" docker/ 无输出（无残留的 0.0.0.0 绑定）
    - grep -rn "\"9000:9000\"" docker/ 无输出（无残留的 0.0.0.0 绑定）
    - docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.dev.yml config --quiet 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>grep -rn '"5433:5432"\|"9000:9000"' docker/ && echo "FAIL: found non-localhost bindings" || echo "PASS: all bindings are localhost-only"</automated>
  </verify>
  <done>
    - docker-compose.dev.yml postgres-dev 端口为 127.0.0.1:5433:5432
    - docker-compose.simple.yml postgres-dev 端口为 127.0.0.1:5433:5432
    - docker-compose.simple.yml Keycloak 9000 端口为 127.0.0.1:9000:9000
    - docker-compose.dev-standalone.yml postgres-dev 端口为 127.0.0.1:5433:5432
    - Docker Compose 配置语法验证通过
    - 无残留的 0.0.0.0 端口绑定（"5433:5432" 和 "9000:9000" 不出现在任何 compose 文件中）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证部署后端口绑定</name>
  <files>无（运行时验证）</files>
  <action>等待人工部署后验证端口绑定和本地连接。Claude 已完成所有配置变更，此步骤需要人工在目标环境执行验证命令。</action>
  <what-built>
    三个 Docker Compose 文件的端口绑定已从 0.0.0.0 改为 127.0.0.1：
    - postgres-dev 5433 端口：仅本地可访问
    - Keycloak 9000 管理端口（simple.yml）：仅本地可访问
  </what-built>
  <how-to-verify>
    部署后执行以下验证（per D-08, D-09, D-10）：

    1. **查看容器端口映射**：
       ```bash
       docker ps --format 'table {{.Names}}\t{{.Ports}}' | grep -E "postgres-dev|keycloak"
       ```
       预期：postgres-dev 端口显示 `127.0.0.1:5433->5432/tcp`，keycloak 无 9000 端口映射

    2. **确认端口仅监听 localhost**：
       ```bash
       ss -tlnp | grep 5433
       ```
       预期：显示 `127.0.0.1:5433`（不是 `0.0.0.0:5433` 或 `*:5433`）

    3. **本地连接测试**：
       ```bash
       psql -h 127.0.0.1 -p 5433 -U dev_user -d noda_dev -c "SELECT 1"
       ```
       预期：连接成功，返回 ?column? = 1
       （如果 dev-standalone 未部署，跳过此步骤）
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated test applicable"</automated>
  </verify>
  <done>
    - docker ps 显示 postgres-dev 端口为 127.0.0.1:5433->5432/tcp
    - ss -tlnp 显示 5433 端口仅监听 127.0.0.1
    - psql 本地连接测试通过
  </done>
  <resume-signal>输入 "approved" 确认验证通过，或描述发现的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机网络 -> Docker 容器端口 | 修改前 postgres-dev 5433 监听所有接口（0.0.0.0），任何可达宿主机的网络均可连接 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | postgres-dev 5433 端口 | mitigate | D-01/D-02/D-03: 绑定 127.0.0.1，仅本地可访问 |
| T-17-02 | Information Disclosure | Keycloak 9000 管理端口 | mitigate | D-06: simple.yml 中绑定 127.0.0.1:9000:9000 |
| T-17-03 | Elevation of Privilege | postgres-dev 无认证暴露 | accept | 绑定 localhost 后仅本机用户可连接，服务器本机已通过 SSH 密钥认证保护 |
</threat_model>

<verification>
1. grep 确认无 `"5433:5432"` 和 `"9000:9000"` 的 0.0.0.0 绑定残留在 docker/ 目录
2. grep 确认所有目标端口都带有 `127.0.0.1:` 前缀
3. Docker Compose config 验证语法正确
4. 人工验证：部署后 docker ps + ss + psql 连接测试
</verification>

<success_criteria>
1. 所有 compose 文件中 postgres-dev 5433 端口为 `127.0.0.1:5433:5432` 格式
2. simple.yml 中 Keycloak 9000 端口为 `127.0.0.1:9000:9000` 格式
3. Docker Compose 配置语法验证通过
4. 部署后 docker ps 确认 postgres-dev 端口仅绑定 localhost
5. 本地 psql 连接测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/17-port-security/17-01-SUMMARY.md`
</output>
