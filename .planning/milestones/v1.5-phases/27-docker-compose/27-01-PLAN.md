---
phase: 27-docker-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.dev.yml
  - docker/docker-compose.simple.yml
  - docker/docker-compose.dev-standalone.yml
autonomous: true
requirements:
  - CLEANUP-01
  - CLEANUP-02
  - CLEANUP-03
  - CLEANUP-05

must_haves:
  truths:
    - "docker-compose.dev.yml 不包含 postgres-dev 和 keycloak-dev 服务定义"
    - "docker-compose.dev.yml 仍包含 nginx 开发覆盖（端口 8081）和 keycloak 开发覆盖"
    - "docker-compose.dev.yml 不再声明 postgres_dev_data volume"
    - "docker-compose.simple.yml 不包含 postgres-dev 服务定义和 postgres_dev_data volume"
    - "docker-compose.dev-standalone.yml 文件不存在"
  artifacts:
    - path: "docker/docker-compose.dev.yml"
      provides: "开发环境 overlay，仅保留 nginx/keycloak/cloudflared 开发覆盖"
      contains: "8081:80"
    - path: "docker/docker-compose.simple.yml"
      provides: "简化版 compose，仅包含生产服务"
      contains: "postgres:"
    - path: "docker/docker-compose.dev-standalone.yml"
      provides: "已删除的独立开发环境文件"
      contains: "FILE_DELETED"
  key_links:
    - from: "docker/docker-compose.dev.yml"
      to: "docker/docker-compose.yml"
      via: "Docker Compose overlay 合并"
      pattern: "services:.*nginx:.*8081"
---

<objective>
清理 Docker Compose 配置文件，移除 postgres-dev 和 keycloak-dev 服务定义

Purpose: 本地 PostgreSQL（Phase 26）已替代 Docker 开发数据库，dev 容器定义和 dev-standalone.yml 不再需要。清理后 Docker Compose overlay 仅保留生产部署必需配置和 nginx/keycloak 开发覆盖。

Output: 清理后的 docker-compose.dev.yml、docker-compose.simple.yml，删除的 dev-standalone.yml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-docker-compose/27-CONTEXT.md
@.planning/phases/27-docker-compose/27-RESEARCH.md

<interfaces>
<!-- 需要修改的文件的关键结构 -->

docker/docker-compose.dev.yml 当前结构:
- L7-13: postgres 段注释（保留）
- L18-41: postgres-dev 服务定义（移除）
- L46-51: nginx 开发覆盖（保留，端口 8081）
- L56-58: cloudflared profiles 禁用（保留）
- L63-81: keycloak 开发覆盖（保留）
- L87-126: keycloak-dev 服务定义（移除）
- L131-133: postgres_dev_data volume 声明（移除）

docker/docker-compose.simple.yml 当前结构:
- L11-32: postgres 生产服务（保留）
- L37-58: postgres-dev 服务定义（移除）
- L63-82: nginx 服务（保留）
- L86-114: keycloak 服务（保留）
- L116-129: cloudflared 服务（保留）
- L131-135: volumes 段含 postgres_data + postgres_dev_data（移除 postgres_dev_data）

docker/docker-compose.dev-standalone.yml: 整个文件删除
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 docker-compose.dev.yml 和删除 dev-standalone.yml</name>
  <files>docker/docker-compose.dev.yml, docker/docker-compose.dev-standalone.yml</files>
  <read_first>
    - docker/docker-compose.dev.yml
    - docker/docker-compose.dev-standalone.yml
    - docker/docker-compose.yml（参考基线，不修改）
  </read_first>
  <action>
按 D-01 和 D-02 决策执行以下操作：

1. **编辑 docker-compose.dev.yml**，移除以下段落：
   - 移除 postgres-dev 服务定义（L18-41 整段，从 `postgres-dev:` 到 `retries: 5`）
   - 移除 keycloak-dev 服务定义（L87-126 整段，从 `keycloak-dev:` 到 `start_period: 60s`）
   - 移除底部 `volumes:` 段中的 `postgres_dev_data:` 和 `driver: local` 两行（L131-133）
   - 如果 volumes 段移除后变为空，则删除整个 `volumes:` 段及其注释

2. **保留的内容**（不修改）：
   - 文件顶部注释块（L1-6）
   - `services:` 段声明
   - `postgres:` 段注释（L9-13，保持结构一致性）
   - `nginx:` 开发覆盖（L43-51，端口 8081 + 前端 dist 挂载）
   - `cloudflared:` profiles 禁用（L53-58）
   - `keycloak:` 开发覆盖（L60-81，空 hostname、健康检查）

3. **删除 docker-compose.dev-standalone.yml** 整个文件（D-02 决策）
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config 2>&1 | grep -cE "postgres-dev|keycloak-dev" && echo "---" && docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config 2>&1 | grep "8081" && echo "---" && test -f docker/docker-compose.dev-standalone.yml && echo "FAIL: dev-standalone.yml still exists" || echo "PASS: dev-standalone.yml deleted"</automated>
    验证预期：
    - postgres-dev/keycloak-dev 匹配数为 0
    - 8081 端口映射存在（nginx 开发覆盖保留）
    - dev-standalone.yml 不存在
  </verify>
  <acceptance_criteria>
    - grep -cE "postgres-dev|keycloak-dev" docker/docker-compose.dev.yml 返回 0
    - grep "8081:80" docker/docker-compose.dev.yml 返回匹配（nginx 覆盖保留）
    - grep "KC_HOSTNAME" docker/docker-compose.dev.yml 返回匹配（keycloak 覆盖保留）
    - grep "postgres_dev_data" docker/docker-compose.dev.yml 返回空
    - test -f docker/docker-compose.dev-standalone.yml 返回非零（文件已删除）
    - docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config 无错误输出
  </acceptance_criteria>
  <done>docker-compose.dev.yml 不包含 postgres-dev 和 keycloak-dev 定义，仍包含 nginx/keycloak 开发覆盖；dev-standalone.yml 已删除；docker compose config 合并验证通过</done>
</task>

<task type="auto">
  <name>Task 2: 清理 docker-compose.simple.yml 中的 postgres-dev</name>
  <files>docker/docker-compose.simple.yml</files>
  <read_first>
    - docker/docker-compose.simple.yml
  </read_first>
  <action>
按 D-03 决策清理 docker-compose.simple.yml：

1. **移除 postgres-dev 服务定义**（L37-58，从 `postgres-dev:` 到 `retries: 5`），包括其上方的注释行 `# PostgreSQL 开发数据库` 和 `# ----------------------------------------`

2. **更新 volumes 段**：移除 `postgres_dev_data:` 和 `driver: local` 两行。保留 `postgres_data:` volume 声明

3. **保留的内容**：postgres 生产服务、nginx、keycloak、cloudflared、noda-network 网络声明全部保留不变

注意：simple.yml 移除 postgres-dev 后仅包含生产服务，文件仍然保留（deferred 决策：simple.yml 合并到 base 超出 Phase 27 范围）
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && grep -cE "postgres-dev" docker/docker-compose.simple.yml && echo "---" && grep -c "postgres_dev_data" docker/docker-compose.simple.yml && echo "---" && docker compose -f docker/docker-compose.simple.yml config 2>&1 | grep -c "postgres-dev"</automated>
    验证预期：三个值均为 0
  </verify>
  <acceptance_criteria>
    - grep -cE "postgres-dev" docker/docker-compose.simple.yml 返回 0
    - grep -c "postgres_dev_data" docker/docker-compose.simple.yml 返回 0
    - docker compose -f docker/docker-compose.simple.yml config 无错误输出
    - grep "postgres:" docker/docker-compose.simple.yml 返回匹配（生产 postgres 保留）
    - grep "nginx:" docker/docker-compose.simple.yml 返回匹配
    - grep "keycloak:" docker/docker-compose.simple.yml 返回匹配
  </acceptance_criteria>
  <done>simple.yml 不包含 postgres-dev 服务和 postgres_dev_data volume；docker compose config 验证通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增信任边界 | 本计划仅移除开发容器配置，不引入新的网络访问点或认证机制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | E (Elevation of Privilege) | dev.yml 清理 | accept | 移除 dev 容器减少攻击面，无新增风险 |
| T-27-02 | I (Information Disclosure) | dev-standalone.yml 删除 | accept | 文件不含生产凭据，仅含开发默认值 |
</threat_model>

<verification>
1. docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config 输出不包含 postgres-dev 和 keycloak-dev
2. docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config 输出包含 nginx 8081 端口映射
3. docker compose -f docker/docker-compose.simple.yml config 输出不包含 postgres-dev
4. docker-compose.dev-standalone.yml 文件不存在
5. base + prod 双文件 config 验证通过：docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config
</verification>

<success_criteria>
1. docker compose config 合并结果中 postgres-dev 和 keycloak-dev 出现次数为 0
2. nginx 开发覆盖（8081 端口）和 keycloak 开发覆盖（空 KC_HOSTNAME）保留在 dev.yml 中
3. dev-standalone.yml 文件已删除
4. simple.yml 中 postgres-dev 和 postgres_dev_data 已移除
5. 所有 docker compose config 命令无语法错误
</success_criteria>

<output>
After completion, create `.planning/phases/27-docker-compose/27-01-SUMMARY.md`
</output>
