---
phase: 27-docker-compose
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - README.md
  - docs/DEVELOPMENT.md
  - docs/CONFIGURATION.md
  - docs/architecture.md
  - docs/GETTING-STARTED.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "文档中不包含对已移除文件的过时引用（dev-standalone.yml、postgres-dev 容器）"
    - "README.md 目录结构反映当前文件状态"
    - "开发环境文档引导用户使用本地 PostgreSQL"
  artifacts:
    - path: "README.md"
      provides: "项目概览，反映清理后文件结构"
    - path: "docs/DEVELOPMENT.md"
      provides: "开发环境指南，无 dev-standalone 引用"
    - path: "docs/CONFIGURATION.md"
      provides: "配置文档，无 postgres-dev 段落"
    - path: "docs/architecture.md"
      provides: "架构文档，反映当前 compose 文件列表"
    - path: "docs/GETTING-STARTED.md"
      provides: "入门指南，容器状态示例无 postgres-dev 行"
  key_links: []
---

<objective>
同步更新项目文档，移除对已清理文件和服务的过时引用

Purpose: Docker Compose 文件和部署脚本已清理完成，文档需要同步更新以避免误导用户。所有对 dev-standalone.yml、postgres-dev 容器、keycloak-dev 容器的引用都需要更新或移除。

Output: 更新后的 README.md 和 4 个文档文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-docker-compose/27-CONTEXT.md
@.planning/phases/27-docker-compose/27-01-SUMMARY.md
@.planning/phases/27-docker-compose/27-02-SUMMARY.md

<interfaces>
<!-- 文档中需要修改的具体位置 -->

README.md L96:
  包含: docker-compose.dev-standalone.yml 引用
  操作: 移除该行

docs/DEVELOPMENT.md L76:
  包含: `docker compose -f docker/docker-compose.dev-standalone.yml up -d`
  操作: 移除或替换该段落（独立开发环境说明）

docs/CONFIGURATION.md L177-178:
  包含: Docker Compose 文件结构表中 dev-standalone.yml 行
  操作: 移除该行

docs/CONFIGURATION.md L244-246:
  包含: postgres-dev 开发数据库配置说明
  操作: 更新为本地 PostgreSQL 说明

docs/GETTING-STARTED.md L118-119:
  包含: 容器状态示例中 postgres-dev 行
  操作: 移除该行

docs/architecture.md L104-105:
  包含: 目录结构中 dev-standalone.yml 引用
  操作: 移除该行

docs/architecture.md L232-233:
  包含: Compose 文件用途表中 dev-standalone.yml 行
  操作: 移除该行
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新所有文档中的 dev 容器和 dev-standalone 引用</name>
  <files>README.md, docs/DEVELOPMENT.md, docs/CONFIGURATION.md, docs/architecture.md, docs/GETTING-STARTED.md</files>
  <read_first>
    - README.md
    - docs/DEVELOPMENT.md
    - docs/CONFIGURATION.md
    - docs/architecture.md
    - docs/GETTING-STARTED.md
  </read_first>
  <action>
按 D-06 决策更新 5 个文档文件。对每个文件执行以下精确修改：

**README.md:**
1. 移除目录结构树中的 `docker-compose.dev-standalone.yml` 行（约 L96 附近，格式类似 `└── docker-compose.dev-standalone.yml  # 独立开发环境（仅数据库）`）

**docs/DEVELOPMENT.md:**
1. 找到引用 `docker-compose.dev-standalone.yml` 的段落（约 L76 附近的独立开发环境说明）
2. 移除或替换该段落内容为本地 PostgreSQL 说明：
   - 移除 `docker compose -f docker/docker-compose.dev-standalone.yml up -d` 命令
   - 如果有 "独立开发环境" 段落标题，替换其内容为指向 setup-postgres-local.sh 的说明
   - 文案建议："开发环境使用本地 PostgreSQL（通过 setup-postgres-local.sh 安装），不再需要 Docker 开发数据库容器"

**docs/CONFIGURATION.md:**
1. 找到 Docker Compose 文件结构表（约 L177-178），移除 `docker-compose.dev-standalone.yml` 行
2. 找到 postgres-dev 开发数据库配置说明（约 L244-246），更新为本地 PG 说明：
   - 移除 `独立容器 postgres-dev` 引用
   - 替换为：开发数据库使用本地 PostgreSQL 17（通过 `bash scripts/setup-postgres-local.sh install` 安装）
   - 保留端口 5432 说明（本地 PG 使用默认端口）

**docs/architecture.md:**
1. 移除目录结构树中的 `docker-compose.dev-standalone.yml` 行（约 L105）
2. 移除 Compose 文件用途表中的 `docker-compose.dev-standalone.yml` 行（约 L233）

**docs/GETTING-STARTED.md:**
1. 找到容器状态示例（约 L118-119），移除 `noda-infra-postgres-dev` 行
2. 如果有端口表格包含 5433 端口映射，移除该行或更新说明

每个文件修改后需保持 Markdown 格式正确（表格对齐、缩进一致）。
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && echo "=== Checking for stale references ===" && grep -rn "dev-standalone" README.md docs/ 2>/dev/null || echo "PASS: no dev-standalone references" && echo "---" && grep -rn "postgres-dev" docs/GETTING-STARTED.md docs/architecture.md README.md 2>/dev/null || echo "PASS: no postgres-dev in key docs" && echo "---" && grep -rn "keycloak-dev" docs/ README.md 2>/dev/null || echo "PASS: no keycloak-dev references"</automated>
    验证预期：所有旧引用均被清除
  </verify>
  <acceptance_criteria>
    - grep -r "dev-standalone" README.md docs/ 返回空（无过时文件引用）
    - grep "postgres-dev" docs/GETTING-STARTED.md 返回空（容器状态示例已更新）
    - grep "postgres-dev" docs/architecture.md 返回空（架构表已更新）
    - grep "dev-standalone" docs/CONFIGURATION.md 返回空（配置表已更新）
    - grep "dev-standalone" docs/DEVELOPMENT.md 返回空（独立环境说明已更新）
    - grep "dev-standalone" README.md 返回空（目录结构已更新）
    - 文档中包含 setup-postgres-local.sh 的引用（引导用户使用本地 PG）
  </acceptance_criteria>
  <done>所有文档中不再包含对 dev-standalone.yml、postgres-dev 容器、keycloak-dev 容器的过时引用；开发环境文档引导用户使用本地 PostgreSQL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增信任边界 | 纯文档更新，不涉及代码或配置变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | — | 文档更新 | accept | 纯文档变更，无安全影响 |
</threat_model>

<verification>
1. grep -r "dev-standalone" README.md docs/ 返回空
2. grep -r "postgres-dev" docs/GETTING-STARTED.md docs/architecture.md README.md 返回空
3. grep -r "keycloak-dev" docs/ README.md 返回空
</verification>

<success_criteria>
1. 所有文档中无 dev-standalone.yml 引用
2. 所有文档中无 postgres-dev / keycloak-dev 容器引用
3. 开发环境文档引导用户使用 setup-postgres-local.sh
4. Markdown 格式正确（无断裂的表格或列表）
</success_criteria>

<output>
After completion, create `.planning/phases/27-docker-compose/27-03-SUMMARY.md`
</output>
