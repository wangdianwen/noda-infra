---
phase: 28-keycloak
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/env-keycloak.env
  - config/nginx/snippets/upstream-keycloak.conf
  - scripts/manage-containers.sh
autonomous: true
requirements:
  - KCBLUE-01
  - KCBLUE-02
  - KCBLUE-03

must_haves:
  truths:
    - "env-keycloak.env 包含所有 Keycloak 环境变量（KC_DB, KC_HOSTNAME, KC_PROXY, SMTP 等），敏感值用 ${VAR} 占位符"
    - "upstream-keycloak.conf 使用 keycloak-{color}:8080 格式，可被 manage-containers.sh update_upstream 原子替换"
    - "manage-containers.sh run_container 支持 Keycloak 特有参数（1G 内存、非 read-only、主题卷挂载）"
  artifacts:
    - path: "docker/env-keycloak.env"
      provides: "Keycloak 环境变量模板"
      min_lines: 25
    - path: "config/nginx/snippets/upstream-keycloak.conf"
      provides: "Keycloak upstream 蓝绿切换配置"
      contains: "keycloak-"
    - path: "scripts/manage-containers.sh"
      provides: "支持 Keycloak 安全参数覆盖的蓝绿容器管理"
  key_links:
    - from: "scripts/manage-containers.sh"
      to: "docker/env-keycloak.env"
      via: "ENV_TEMPLATE 变量引用 env-keycloak.env"
      pattern: "env-\\$\\{SERVICE_NAME\\}"
    - from: "scripts/manage-containers.sh"
      to: "config/nginx/snippets/upstream-keycloak.conf"
      via: "update_upstream 原子写入"
      pattern: "update_upstream"
---

<objective>
创建 Keycloak 蓝绿部署的三个基础组件：环境变量模板、nginx upstream 蓝绿配置、manage-containers.sh 的 Keycloak 适配。

Purpose: Keycloak 蓝绿部署需要独立的环境变量文件（从 compose 环境变量中提取），nginx upstream 需要支持蓝绿切换格式，manage-containers.sh 需要支持 Keycloak 特有的安全参数（更高内存、非 read-only、主题卷挂载）。
Output: env-keycloak.env 模板文件、upstream-keycloak.conf 蓝绿格式、manage-containers.sh Keycloak 参数覆盖支持
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-keycloak/28-CONTEXT.md
@.planning/phases/28-keycloak/28-RESEARCH.md

<interfaces>
<!-- manage-containers.sh 关键环境变量（Keycloak 需要覆盖的） -->
SERVICE_NAME="${SERVICE_NAME:-findclass-ssr}"
SERVICE_PORT="${SERVICE_PORT:-3001}"
UPSTREAM_NAME="${UPSTREAM_NAME:-findclass_backend}"
HEALTH_PATH="${HEALTH_PATH:-/api/health}"
ACTIVE_ENV_FILE="${ACTIVE_ENV_FILE:-/opt/noda/active-env}"
UPSTREAM_CONF="${UPSTREAM_CONF:-$PROJECT_ROOT/config/nginx/snippets/upstream-findclass.conf}"
ENV_TEMPLATE="$PROJECT_ROOT/docker/env-${SERVICE_NAME}.env"

<!-- run_container 当前硬编码的安全参数（需要支持覆盖） -->
--security-opt no-new-privileges
--cap-drop ALL
--read-only
--tmpfs /tmp
--memory 512m
--memory-reservation 128m
--cpus 1

<!-- env-findclass-ssr.env 格式参考 -->
静态硬编码值（不随环境变化）
动态替换 ${VAR} 语法（POSTGRES_USER, POSTGRES_PASSWORD, RESEND_API_KEY）

<!-- docker-compose.prod.yml Keycloak 环境变量完整列表 -->
KC_DB=postgres
KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak
KC_DB_USERNAME=${POSTGRES_USER}
KC_DB_PASSWORD=${POSTGRES_PASSWORD}
KC_HOSTNAME=https://auth.noda.co.nz
KC_HOSTNAME_STRICT=false
KC_HTTP_ENABLED=true
KC_PROXY=edge
KC_PROXY_HEADERS=xforwarded
KC_HEALTH_ENABLED=true
KEYCLOAK_ADMIN=${KEYCLOAK_ADMIN_USER}
KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD}
KC_MAIL_HOST=${SMTP_HOST}
KC_MAIL_PORT=${SMTP_PORT}
KC_MAIL_FROM=${SMTP_FROM}
KC_MAIL_FROM_DISPLAY_NAME=Noda
KC_SMTP_AUTH=true
KC_SMTP_USER=${SMTP_USER}
KC_SMTP_PASSWORD=${SMTP_PASSWORD}
KC_SMTP_SSL=false
KC_SMTP_STARTTLS=true

<!-- Keycloak 容器资源需求（from docker-compose.prod.yml） -->
memory: 1G（limits）
memory: 512M（reservations）
cpus: '1'（limits）
主题卷: ./services/keycloak/themes:/opt/keycloak/themes/noda:ro

<!-- manage-containers.sh prepare_env_file 当前 envsubst 变量列表 -->
envsubst '${POSTGRES_USER} ${POSTGRES_PASSWORD} ${RESEND_API_KEY}' < "$ENV_TEMPLATE" > "$tmp_file"
<!-- Keycloak 需要的 envsubst 变量 -->
envsubst '${POSTGRES_USER} ${POSTGRES_PASSWORD} ${KEYCLOAK_ADMIN_USER} ${KEYCLOAK_ADMIN_PASSWORD} ${SMTP_HOST} ${SMTP_PORT} ${SMTP_FROM} ${SMTP_USER} ${SMTP_PASSWORD}'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 env-keycloak.env 环境变量模板</name>
  <files>docker/env-keycloak.env</files>
  <read_first>
    docker/env-findclass-ssr.env
    docker/docker-compose.yml (L107-143, Keycloak 环境变量)
    docker/docker-compose.prod.yml (L57-109, Keycloak 生产配置)
  </read_first>
  <action>
创建 `docker/env-keycloak.env` 文件，参照 `docker/env-findclass-ssr.env` 的格式。文件包含注释说明用途和变量分类。

具体内容（按 docker-compose.prod.yml L63-89 提取，per D-02）：

```
# ============================================
# Keycloak 环境变量文件
# ============================================
# 用途：manage-containers.sh 使用 envsubst 替换 ${VAR} 占位符后，
#       生成的临时文件传给 docker run --env-file
#
# 变量分类：
#   静态硬编码 - 不随环境变化
#   动态替换   - ${VAR} 语法，由宿主机环境变量填充
#     POSTGRES_USER, POSTGRES_PASSWORD,
#     KEYCLOAK_ADMIN_USER, KEYCLOAK_ADMIN_PASSWORD,
#     SMTP_HOST, SMTP_PORT, SMTP_FROM, SMTP_USER, SMTP_PASSWORD

# 数据库配置
KC_DB=postgres
KC_DB_URL=jdbc:postgresql://noda-infra-postgres-prod:5432/keycloak
KC_DB_USERNAME=${POSTGRES_USER}
KC_DB_PASSWORD=${POSTGRES_PASSWORD}

# 主机名配置（v2 SPI - 使用完整 URL）
KC_HOSTNAME=https://auth.noda.co.nz
KC_HOSTNAME_STRICT=false
KC_HTTP_ENABLED=true
KC_PROXY=edge
KC_PROXY_HEADERS=xforwarded
KC_FRONTEND_URL=https://auth.noda.co.nz

# 健康检查（per D-01）
KC_HEALTH_ENABLED=true

# 管理员账号
KEYCLOAK_ADMIN=${KEYCLOAK_ADMIN_USER}
KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD}

# SMTP 配置（密码重置邮件）
KC_MAIL_HOST=${SMTP_HOST}
KC_MAIL_PORT=${SMTP_PORT}
KC_MAIL_FROM=${SMTP_FROM}
KC_MAIL_FROM_DISPLAY_NAME=Noda
KC_SMTP_AUTH=true
KC_SMTP_USER=${SMTP_USER}
KC_SMTP_PASSWORD=${SMTP_PASSWORD}
KC_SMTP_SSL=false
KC_SMTP_STARTTLS=true
```

关键注意事项：
- KC_DB_URL 使用 `noda-infra-postgres-prod` 而非 `postgres`（compose 网络别名在 docker run 模式下不适用）
- SMTP 变量使用 `${VAR}` 占位符（与 env-findclass-ssr.env 模式一致）
- 包含 KC_FRONTEND_URL（docker-compose.yml 有，prod.yml 没有显式列出但 Keycloak 需要）
- 不包含 command: start（这通过 docker run 参数传递，不是环境变量）
  </action>
  <verify>
    <automated>test -f docker/env-keycloak.env && grep -c "KC_DB\|KC_HOSTNAME\|KC_PROXY\|KC_HEALTH\|KEYCLOAK_ADMIN\|KC_MAIL\|KC_SMTP" docker/env-keycloak.env | grep -q "1[3-9]\|[2-9][0-9]"</automated>
  </verify>
  <done>docker/env-keycloak.env 存在，包含所有 Keycloak 环境变量（数据库、主机名、代理、健康检查、管理员、SMTP），敏感值使用 ${VAR} 占位符</done>
  <acceptance_criteria>
    - 文件存在: `test -f docker/env-keycloak.env`
    - 包含数据库配置: `grep -q "KC_DB=postgres" docker/env-keycloak.env`
    - 包含主机名配置: `grep -q "KC_HOSTNAME=https://auth.noda.co.nz" docker/env-keycloak.env`
    - 包含代理配置: `grep -q "KC_PROXY=edge" docker/env-keycloak.env`
    - 包含健康检查: `grep -q "KC_HEALTH_ENABLED=true" docker/env-keycloak.env`
    - 包含管理员变量: `grep -q "KEYCLOAK_ADMIN=\${KEYCLOAK_ADMIN_USER}" docker/env-keycloak.env`
    - 包含 SMTP 配置: `grep -q "KC_SMTP_AUTH=true" docker/env-keycloak.env`
    - 使用 compose 容器名: `grep -q "noda-infra-postgres-prod" docker/env-keycloak.env`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 upstream-keycloak.conf 为蓝绿格式</name>
  <files>config/nginx/snippets/upstream-keycloak.conf</files>
  <read_first>
    config/nginx/snippets/upstream-keycloak.conf
    config/nginx/snippets/upstream-findclass.conf
    config/nginx/snippets/upstream-noda-site.conf
  </read_first>
  <action>
将 `config/nginx/snippets/upstream-keycloak.conf` 从 compose 单容器格式改为蓝绿格式（per D-06）。

当前内容：
```
upstream keycloak_backend {
    server keycloak:8080 max_fails=3 fail_timeout=30s;
}
```

改为（与 upstream-findclass.conf 格式一致）：
```
upstream keycloak_backend {
    server keycloak-blue:8080 max_fails=3 fail_timeout=30s;
}
```

注意事项：
- 默认值设为 `keycloak-blue`（与 findclass 的默认值一致，init 后 blue 是第一个活跃环境）
- 保持 `max_fails=3 fail_timeout=30s` 参数不变
- nginx 会通过 `include snippets/upstream-keycloak.conf` 引用此文件
- 此文件会在 init 和 switch 时被 manage-containers.sh 的 `update_upstream` 函数原子替换
  </action>
  <verify>
    <automated>grep -q "keycloak-blue:8080" config/nginx/snippets/upstream-keycloak.conf && grep -q "upstream keycloak_backend" config/nginx/snippets/upstream-keycloak.conf</automated>
  </verify>
  <done>upstream-keycloak.conf 使用 keycloak-blue:8080 格式，可被 update_upstream 函数原子替换为 keycloak-green:8080</done>
  <acceptance_criteria>
    - 包含 upstream 块: `grep -q "upstream keycloak_backend" config/nginx/snippets/upstream-keycloak.conf`
    - 包含蓝绿容器名: `grep -q "keycloak-blue:8080" config/nginx/snippets/upstream-keycloak.conf`
    - 不包含旧别名: `! grep -q "server keycloak:8080" config/nginx/snippets/upstream-keycloak.conf`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: manage-containers.sh 支持 Keycloak 参数覆盖</name>
  <files>scripts/manage-containers.sh</files>
  <read_first>
    scripts/manage-containers.sh
    scripts/manage-containers.sh (L158-204, run_container 函数)
    scripts/manage-containers.sh (L96-111, prepare_env_file 函数)
    scripts/manage-containers.sh (L248-324, cmd_init 函数)
    docker/docker-compose.prod.yml (L57-109, Keycloak 生产安全配置)
  </read_first>
  <action>
修改 `scripts/manage-containers.sh` 的三个函数，支持 Keycloak 的参数差异（per D-04, D-03）。

**修改 1: prepare_env_file 函数（L96-111）— 支持动态 envsubst 变量列表**

当前 prepare_env_file 硬编码了 `${POSTGRES_USER} ${POSTGRES_PASSWORD} ${RESEND_API_KEY}`。需要支持通过环境变量 `ENVSUBST_VARS` 覆盖。

修改方案（保持向后兼容）：
```bash
prepare_env_file() {
  local env="$1"
  # 使用服务容器名而非 SERVICE_NAME 来生成临时文件，避免多服务冲突
  local tmp_file="/tmp/${SERVICE_NAME}-${env}.env.$$"

  if ! command -v envsubst &>/dev/null; then
    log_error "envsubst 不可用，请安装 gettext: sudo apt install gettext"
    exit 1
  fi

  # 支持通过 ENVSUBST_VARS 环境变量覆盖需要替换的变量列表
  # 默认值保持 findclass-ssr 兼容
  local vars="${ENVSUBST_VARS:-\${POSTGRES_USER} \${POSTGRES_PASSWORD} \${RESEND_API_KEY}}"
  envsubst "$vars" < "$ENV_TEMPLATE" > "$tmp_file"
  echo "$tmp_file"
}
```

**修改 2: run_container 函数（L158-204）— 支持 Keycloak 参数覆盖**

需要支持以下环境变量覆盖（不改变默认值，保持 findclass-ssr 和 noda-site 不受影响）：

```bash
# 在 run_container 函数开头（local tmp_env_file 之后）添加：
local container_memory="${CONTAINER_MEMORY:-512m}"
local container_memory_reservation="${CONTAINER_MEMORY_RESERVATION:-128m}"
local container_readonly="${CONTAINER_READONLY:-true}"
local extra_docker_args="${EXTRA_DOCKER_ARGS:-}"
```

在 docker run 命令中替换硬编码值：
- `--memory 512m` → `--memory "$container_memory"`
- `--memory-reservation 128m` → `--memory-reservation "$container_memory_reservation"`
- `--read-only` → 改为条件判断：`if [ "$container_readonly" = "true" ]; then echo "--read-only"; fi`
- 在 `"$image"` 之前添加：`${extra_docker_args}`

docker run 命令变为（仅列出变更部分）：
```bash
  docker run -d \
    --name "$container_name" \
    --network "$NETWORK_NAME" \
    --restart unless-stopped \
    --stop-timeout 30 \
    --security-opt no-new-privileges \
    --cap-drop ALL \
    $([ "$container_readonly" = "true" ] && echo "--read-only") \
    --tmpfs /tmp \
    --memory "$container_memory" \
    --memory-reservation "$container_memory_reservation" \
    --cpus 1 \
    --log-driver json-file \
    --log-opt max-size=10m \
    --log-opt max-file=3 \
    ${tmp_env_file:+--env-file "$tmp_env_file"} \
    --label noda.service-group=apps \
    --label noda.environment=prod \
    --label "noda.blue-green=${env}" \
    --label com.docker.compose.project=noda-apps \
    --label "com.docker.compose.service=${SERVICE_NAME}" \
    --health-cmd "wget --quiet --tries=1 --spider http://localhost:${SERVICE_PORT}${HEALTH_PATH} || exit 1" \
    --health-interval 30s \
    --health-timeout 10s \
    --health-retries 3 \
    --health-start-period 60s \
    ${extra_docker_args} \
    "$image"
```

**修改 3: cmd_init 函数（L252-324）— 支持检测 compose 容器名差异**

当前 `cmd_init` 检测容器 `$SERVICE_NAME`（即 `keycloak`），但 compose 中的 Keycloak 容器名是 `noda-infra-keycloak-prod`。需要在检测不到 SERVICE_NAME 容器时，回退检测 compose 项目名前缀的容器名。

修改 `cmd_init` 步骤 1 的容器检测逻辑（L258-261）：
```bash
  # 步骤 1：检测 compose 管理的旧容器
  # 先尝试直接 SERVICE_NAME（findclass-ssr 等），回退检查 compose 项目名前缀的容器
  local current_image=""
  local compose_container_name="noda-infra-${SERVICE_NAME}-prod"
  
  if docker inspect "$SERVICE_NAME" &>/dev/null; then
    current_image=$(docker inspect --format='{{.Config.Image}}' "$SERVICE_NAME" 2>/dev/null || echo "")
    log_info "检测到 compose 容器 $SERVICE_NAME (镜像: ${current_image:-未知})"
  elif docker inspect "$compose_container_name" &>/dev/null; then
    current_image=$(docker inspect --format='{{.Config.Image}}' "$compose_container_name" 2>/dev/null || echo "")
    log_info "检测到 compose 容器 $compose_container_name (镜像: ${current_image:-未知})"
  fi
```

同时修改步骤 4 的停止操作（L288-291），需要停止检测到的那个容器：
```bash
  # 步骤 4：停止 compose 容器
  log_info "步骤 4/10: 停止 compose 容器"
  local stop_container="$SERVICE_NAME"
  if ! docker inspect "$SERVICE_NAME" &>/dev/null 2>&1 && docker inspect "$compose_container_name" &>/dev/null 2>&1; then
    stop_container="$compose_container_name"
  fi
  docker stop "$stop_container"
  docker rm "$stop_container"
  log_success "compose 容器 $stop_container 已停止并移除"
```

**修改 4: update_upstream 函数日志（L233）— 修复硬编码端口**

当前 L233 有硬编码端口 `:3001`：
```bash
log_info "upstream 已更新: $container_name:3001"
```
改为：
```bash
log_info "upstream 已更新: $container_name:$SERVICE_PORT"
```

**修改 5: 在脚本头部的注释中添加 Keycloak 示例**

在 usage 函数中的示例部分添加 Keycloak 用法示例：
```bash
  SERVICE_NAME=keycloak SERVICE_PORT=8080 UPSTREAM_NAME=keycloak_backend \\
    HEALTH_PATH=/health/ready ACTIVE_ENV_FILE=/opt/noda/active-env-keycloak \\
    UPSTREAM_CONF=\$PROJECT_ROOT/config/nginx/snippets/upstream-keycloak.conf \\
    CONTAINER_MEMORY=1g CONTAINER_MEMORY_RESERVATION=512m CONTAINER_READONLY=false \\
    ENVSUBST_VARS='\${POSTGRES_USER} \${POSTGRES_PASSWORD} \${KEYCLOAK_ADMIN_USER} \${KEYCLOAK_ADMIN_PASSWORD} \${SMTP_HOST} \${SMTP_PORT} \${SMTP_FROM} \${SMTP_USER} \${SMTP_PASSWORD}' \\
    EXTRA_DOCKER_ARGS='-v /path/to/themes:/opt/keycloak/themes/noda:ro --tmpfs /opt/keycloak/data' \\
    bash scripts/manage-containers.sh status
```

关键注意事项：
- 所有修改保持向后兼容，findclass-ssr 和 noda-site 的现有行为不变（使用默认值）
- Keycloak 通过环境变量覆盖默认值，不需要修改 manage-containers.sh 的核心逻辑
- EXTRA_DOCKER_ARGS 允许传入额外的 docker run 参数（Keycloak 需要主题卷挂载和 data tmpfs）
- label noda.service-group 当前硬编码为 `apps`，Keycloak 应该是 `infra`，但也通过 EXTRA_DOCKER_ARGS 覆盖不了。需要把 label 也参数化：添加 `SERVICE_GROUP="${SERVICE_GROUP:-apps}"` 并在 docker run 中使用 `$SERVICE_GROUP`
  </action>
  <verify>
    <automated>grep -q "CONTAINER_MEMORY" scripts/manage-containers.sh && grep -q "CONTAINER_READONLY" scripts/manage-containers.sh && grep -q "EXTRA_DOCKER_ARGS" scripts/manage-containers.sh && grep -q "ENVSUBST_VARS" scripts/manage-containers.sh && grep -q "compose_container_name" scripts/manage-containers.sh && grep -q "SERVICE_GROUP" scripts/manage-containers.sh && grep "upstream 已更新" scripts/manage-containers.sh | grep -q "SERVICE_PORT"</automated>
  </verify>
  <done>manage-containers.sh 支持通过环境变量覆盖内存限制（CONTAINER_MEMORY）、是否只读（CONTAINER_READONLY）、额外 docker 参数（EXTRA_DOCKER_ARGS）、envsubst 变量列表（ENVSUBST_VARS）、服务标签（SERVICE_GROUP），并能检测 compose 项目名前缀的容器名</done>
  <acceptance_criteria>
    - 支持内存覆盖: `grep -q "CONTAINER_MEMORY" scripts/manage-containers.sh`
    - 支持只读覆盖: `grep -q "CONTAINER_READONLY" scripts/manage-containers.sh`
    - 支持额外参数: `grep -q "EXTRA_DOCKER_ARGS" scripts/manage-containers.sh`
    - 支持 envsubst 覆盖: `grep -q "ENVSUBST_VARS" scripts/manage-containers.sh`
    - 支持 compose 容器名检测: `grep -q "compose_container_name" scripts/manage-containers.sh`
    - 支持服务组标签: `grep -q "SERVICE_GROUP" scripts/manage-containers.sh`
    - 日志使用参数化端口: `grep "upstream 已更新" scripts/manage-containers.sh | grep -q "SERVICE_PORT"`
    - findclass-ssr 默认行为不变: `grep -q 'findclass-ssr' scripts/manage-containers.sh`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env 模板 -> envsubst -> docker run | 敏感变量通过 envsubst 替换后传入容器环境变量 |
| manage-containers.sh -> docker run | 脚本以当前用户权限执行 docker 命令 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | env-keycloak.env | mitigate | envsubst 只替换明确列出的变量名（ENVSUBST_VARS），不使用无参数 envsubst |
| T-28-02 | Tampering | upstream-keycloak.conf | mitigate | update_upstream 使用原子写入（tmp file + mv），nginx -t 验证后才 reload |
| T-28-03 | Elevation of Privilege | Keycloak docker run | mitigate | 保持 --security-opt no-new-privileges, --cap-drop ALL；CONTAINER_READONLY=false 但通过 tmpfs 限制写入路径 |
</threat_model>

<verification>
1. env-keycloak.env 存在且包含所有必需环境变量
2. upstream-keycloak.conf 使用蓝绿格式
3. manage-containers.sh 支持所有新增环境变量覆盖
4. `grep -c "CONTAINER_MEMORY\|CONTAINER_READONLY\|EXTRA_DOCKER_ARGS\|ENVSUBST_VARS\|compose_container_name\|SERVICE_GROUP" scripts/manage-containers.sh` >= 6
</verification>

<success_criteria>
- docker/env-keycloak.env 包含 Keycloak 全部环境变量（KC_DB, KC_HOSTNAME, KC_PROXY, KC_HEALTH_ENABLED, SMTP, 管理员凭据）
- upstream-keycloak.conf 使用 keycloak-blue:8080 蓝绿格式
- manage-containers.sh 通过环境变量支持 Keycloak 参数差异，且不影响 findclass-ssr/noda-site 现有行为
</success_criteria>

<output>
After completion, create `.planning/phases/28-keycloak/28-01-SUMMARY.md`
</output>
