---
phase: 10-b2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/crontab
  - deploy/Dockerfile.noda-ops
  - deploy/entrypoint-ops.sh
autonomous: false
requirements:
  - BFIX-01

must_haves:
  truths:
    - "crontab 中所有脚本路径与 Dockerfile COPY 目标路径一致"
    - "cron 调度的三个脚本（备份、验证、清理）都能被正确找到并执行"
    - "容器重建后 rclone 配置和备份脚本路径均正确"
  artifacts:
    - path: "deploy/crontab"
      provides: "修正后的 crontab 配置"
      contains: "/app/backup/"
    - path: "deploy/Dockerfile.noda-ops"
      provides: "确认 COPY 路径与 crontab 一致"
      contains: "COPY scripts/backup/ /app/backup/"
    - path: "deploy/entrypoint-ops.sh"
      provides: "容器启动时显示正确路径的定时任务"
  key_links:
    - from: "deploy/crontab"
      to: "deploy/Dockerfile.noda-ops COPY 目标"
      via: "脚本路径必须与 COPY 目标目录匹配"
      pattern: "/app/backup/"
---

<objective>
修复 B2 备份中断根因 — crontab 路径不匹配

Purpose: 自 v1.1 迁移后，Dockerfile.noda-ops 将脚本复制到 `/app/backup/` 目录，但 crontab 中引用的路径仍是 `/app/backup-postgres.sh`、`/app/test-verify-weekly.sh`、`/app/lib/metrics.sh`，导致 cron 找不到脚本，备份完全中断。修复 crontab 路径后，备份自动恢复。

Output: 修正后的 `deploy/crontab` 文件，所有路径指向 `/app/backup/` 下的正确位置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-b2/10-CONTEXT.md
@.planning/phases/10-b2/10-RESEARCH.md

<interfaces>
<!-- Dockerfile.noda-ops COPY 指令（脚本的实际安装位置） -->
```dockerfile
COPY scripts/backup/ /app/backup/
RUN chmod +x /app/backup/*.sh 2>/dev/null || true
```

<!-- 当前 crontab（错误的路径） -->
```
0 3 * * * /app/backup-postgres.sh >> /var/log/noda-backup/backup.log 2>&1
0 3 * * 0 /app/test-verify-weekly.sh >> /var/log/noda-backup/test.log 2>&1
0 */6 * * * /app/lib/metrics.sh cleanup 2>/dev/null || true
0 4 * * * find /tmp/postgres_backups -type f -name "*.dump" -mtime +7 -delete 2>/dev/null || true
```

<!-- 备份系统脚本目录结构 -->
```
scripts/backup/
  backup-postgres.sh       -> 容器内 /app/backup/backup-postgres.sh
  test-verify-weekly.sh    -> 容器内 /app/backup/test-verify-weekly.sh
  lib/
    metrics.sh             -> 容器内 /app/backup/lib/metrics.sh
    health.sh              -> 容器内 /app/backup/lib/health.sh
    cloud.sh               -> 容器内 /app/backup/lib/cloud.sh
    config.sh              -> 容器内 /app/backup/lib/config.sh
    ...其他 lib/ 脚本
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: 确认 B2 备份中断根因</name>
  <files>N/A</files>
  <action>
    RESEARCH.md 分析：Dockerfile.noda-ops 将 `scripts/backup/` 复制到 `/app/backup/`，但 crontab 引用 `/app/backup-postgres.sh`（无 `backup/` 前缀），导致 cron 找不到脚本。

    可能性判断：这是 90%+ 可能性的根因，因为 (1) 时间吻合（v1.1 迁移后 4/8 中断） (2) 路径不匹配是明确的代码错误。

    如果尚未验证，可跳过验证直接修复（风险：如果还有其他根因，修复后仍需进一步调查），或先执行 `docker exec noda-ops cat /etc/crontabs/root` 和 `docker exec noda-ops ls -la /app/backup/` 确认。
  </action>
  <verify>
    <automated>echo "checkpoint:decision — awaiting user input"</automated>
  </verify>
  <done>用户选择 skip-verify（直接修复）或 verify-first（先验证根因）</done>
</task>

<task type="auto">
  <name>Task 1: 修复 crontab 脚本路径</name>
  <files>deploy/crontab, deploy/Dockerfile.noda-ops</files>
  <read_first>
    - deploy/crontab（当前错误的路径）
    - deploy/Dockerfile.noda-ops（确认 COPY 目标路径）
    - scripts/backup/（确认脚本目录结构，ls scripts/backup/ 和 scripts/backup/lib/）
  </read_first>
  <action>
    修改 `deploy/crontab` 文件，将所有脚本路径从 `/app/` 修正为 `/app/backup/`：

    **第 5 行** — 备份脚本：
    - 旧：`0 3 * * * /app/backup-postgres.sh >> /var/log/noda-backup/backup.log 2>&1`
    - 新：`0 3 * * * /app/backup/backup-postgres.sh >> /var/log/noda-backup/backup.log 2>&1`

    **第 8 行** — 验证测试脚本：
    - 旧：`0 3 * * 0 /app/test-verify-weekly.sh >> /var/log/noda-backup/test.log 2>&1`
    - 新：`0 3 * * 0 /app/backup/test-verify-weekly.sh >> /var/log/noda-backup/test.log 2>&1`

    **第 11 行** — metrics 清理：
    - 旧：`0 */6 * * * /app/lib/metrics.sh cleanup 2>/dev/null || true`
    - 新：`0 */6 * * * /app/backup/lib/metrics.sh cleanup 2>/dev/null || true`

    **第 14 行** — 备份文件清理（保持不变，这是 find 命令不是脚本路径）：
    - 保持：`0 4 * * * find /tmp/postgres_backups -type f -name "*.dump" -mtime +7 -delete 2>/dev/null || true`

    同时修改 `deploy/Dockerfile.noda-ops` 中第 39 行的 chmod 命令，确保覆盖所有子目录下的脚本：
    - 旧：`RUN chmod +x /app/backup/*.sh 2>/dev/null || true`
    - 新：`RUN find /app/backup -name "*.sh" -exec chmod +x {} \; 2>/dev/null || true`
  </action>
  <acceptance_criteria>
    - `deploy/crontab` 包含 `/app/backup/backup-postgres.sh`
    - `deploy/crontab` 包含 `/app/backup/test-verify-weekly.sh`
    - `deploy/crontab` 包含 `/app/backup/lib/metrics.sh`
    - `deploy/crontab` 不包含任何 `/app/backup-postgres.sh`（无 backup/ 前缀的路径）
    - `deploy/crontab` 不包含任何 `/app/test-verify-weekly.sh`（无 backup/ 前缀的路径）
    - `deploy/crontab` 不包含任何 `/app/lib/metrics.sh`（无 backup/ 前缀的路径）
    - `deploy/Dockerfile.noda-ops` 中 chmod 命令使用 `find -exec` 模式覆盖 lib/ 子目录下的脚本
  </acceptance_criteria>
  <verify>
    <automated>grep -c "/app/backup/" deploy/crontab | grep -q "3" && grep -vq "^0.* /app/[^b]" deploy/crontab && grep -q "find /app/backup" deploy/Dockerfile.noda-ops && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>crontab 中三个脚本路径全部指向 /app/backup/ 下的正确位置，与 Dockerfile COPY 目标一致；Dockerfile chmod 覆盖所有子目录脚本</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 验证修复并重建部署</name>
  <files>N/A</files>
  <action>
    Claude 已修复 crontab 路径和 Dockerfile chmod。这是 BFIX-01 的核心修复，需要重建镜像并在生产环境验证。

    验证步骤：
    1. 重建 noda-ops 镜像：`docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml build noda-ops`
    2. 重启 noda-ops 容器：`docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up -d noda-ops`
    3. 验证 crontab 路径：`docker exec noda-ops cat /etc/crontabs/root`（预期：所有路径为 /app/backup/...）
    4. 验证脚本存在：`docker exec noda-ops ls -la /app/backup/backup-postgres.sh`
    5. Dry-run 测试（可选）：`docker exec noda-ops /app/backup/backup-postgres.sh --dry-run`
    6. 检查 B2 控制台确认新备份上传
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user approval"</automated>
  </verify>
  <done>用户确认 noda-ops 容器重建后 crontab 路径正确，备份脚本可执行，B2 备份恢复正常</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron 调度 -> 备份脚本 | cron 以 root 身份执行 crontab 中的命令，路径错误导致命令不存在 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | crontab 路径 | mitigate | 路径修复后通过 Dockerfile COPY 确保不可变（构建时写入） |
| T-10-02 | Elevation of Privilege | cron 以 root 执行 | accept | 容器内本身以 root 运行，crontab 路径修复不改变权限模型 |
</threat_model>

<verification>
1. `docker exec noda-ops cat /etc/crontabs/root` — 确认路径为 `/app/backup/...`
2. `docker exec noda-ops ls -la /app/backup/backup-postgres.sh` — 确认脚本存在且可执行
3. `docker exec noda-ops /app/backup/backup-postgres.sh --dry-run` — 确认 dry-run 成功
4. B2 控制台可见新备份文件（下一个 cron 周期后）
</verification>

<success_criteria>
- crontab 所有脚本路径与 Dockerfile COPY 目标一致
- noda-ops 容器重建后 cron 能正确找到并执行备份脚本
- 下一个备份周期（03:00）成功上传备份到 B2
- B2 控制台可见最新备份文件
</success_criteria>

<output>
After completion, create `.planning/phases/10-b2/10-01-SUMMARY.md`
</output>
