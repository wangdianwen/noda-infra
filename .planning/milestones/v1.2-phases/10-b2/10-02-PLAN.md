---
phase: 10-b2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/lib/health.sh
autonomous: true
requirements:
  - BFIX-02

must_haves:
  truths:
    - "容器内运行时，磁盘空间检查不再跳过，而是实际检查挂载点可用空间"
    - "磁盘空间不足时，check_disk_space 返回 EXIT_DISK_SPACE_INSUFFICIENT (6)，备份不执行"
    - "磁盘空间充足时，检查通过并继续备份"
    - "无法获取空间信息时，发出警告但继续备份（不阻断）"
  artifacts:
    - path: "scripts/backup/lib/health.sh"
      provides: "容器内磁盘空间检查逻辑"
      contains: "df -B1"
    - path: "scripts/backup/lib/health.sh"
      provides: "容器内数据库大小查询（psql 直连）"
      contains: "pg_database_size"
  key_links:
    - from: "scripts/backup/lib/health.sh::check_disk_space()"
      to: "psql 直连 PostgreSQL"
      via: "PGPASSWORD 环境变量（docker-compose.yml 注入）"
      pattern: "psql.*pg_database_size"
    - from: "scripts/backup/lib/health.sh::check_disk_space()"
      to: "df -B1"
      via: "检查备份目录挂载点可用空间"
      pattern: "df -B1.*backup_dir"
---

<objective>
修复容器内磁盘空间检查被跳过的 bug（BFIX-02）

Purpose: 当前 `health.sh:161-165` 容器内分支直接 `return 0` 跳过磁盘检查，导致磁盘空间不足时备份仍会执行，可能因空间不足而失败。修复后，容器内将通过 psql 直连查询数据库大小，用 df 检查挂载点空间，空间不足时拒绝执行备份。

Output: 修正后的 `scripts/backup/lib/health.sh`，容器内 `check_disk_space()` 函数包含完整的空间检查逻辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-b2/10-CONTEXT.md
@.planning/phases/10-b2/10-RESEARCH.md

<interfaces>
<!-- health.sh 当前 bug 位置（第 161-166 行） -->
```bash
# 检查是否在容器内运行
if [[ -f /.dockerenv ]]; then
  # 在容器内，简化检查（仅检查挂载点）
  echo "ℹ️  容器内运行，跳过详细磁盘检查"
  echo "ℹ️  备份目录: $backup_dir"
  return 0  # <-- BUG: 直接返回成功
fi
```

<!-- 容器内可用的工具和连接方式 -->
```
容器环境：
  - psql (postgresql-client): 已安装，可通过 PGPASSWORD 环境变量连接
  - df (coreutils): 已安装，支持 -B1 字节精度
  - PostgreSQL 连接: POSTGRES_HOST, POSTGRES_PORT, POSTGRES_USER, POSTGRES_PASSWORD
    均通过 docker-compose.yml environment 注入
  - 备份目录: /tmp/postgres_backups (映射到宿主机 docker/volumes/backup)
```

<!-- config.sh 中可用的配置获取函数 -->
```bash
get_postgres_host()   # 返回: noda-infra-postgres-prod
get_postgres_port()   # 返回: 5432
get_postgres_user()   # 返回: postgres
get_backup_dir()      # 返回: /tmp/postgres_backups (容器内)
```

<!-- constants.sh 中的退出码 -->
```bash
readonly EXIT_DISK_SPACE_INSUFFICIENT=6
```

<!-- 注意事项 -->
```
容器内不能使用 docker exec（没有 Docker socket）
get_database_size() 和 get_total_database_size() 使用 docker exec，容器内不可用
必须使用 psql 直连替代
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复容器内磁盘空间检查逻辑</name>
  <files>scripts/backup/lib/health.sh</files>
  <read_first>
    - scripts/backup/lib/health.sh（当前完整文件，重点 148-166 行）
    - scripts/backup/lib/config.sh（确认 get_postgres_host 等函数签名）
    - scripts/backup/lib/constants.sh（确认 EXIT_DISK_SPACE_INSUFFICIENT 值）
  </read_first>
  <action>
    修改 `scripts/backup/lib/health.sh` 的 `check_disk_space()` 函数，替换第 161-166 行的容器内分支（`if [[ -f /.dockerenv ]]` 块）：

    **替换为以下逻辑（保留注释风格和格式）：**

    ```bash
    if [[ -f /.dockerenv ]]; then
      echo "ℹ️  容器内运行，检查挂载点磁盘空间"

      local pg_host
      local pg_user
      pg_host=$(get_postgres_host)
      pg_user=$(get_postgres_user)

      # 1. 查询所有用户数据库总大小（容器内用 psql 直连，不用 docker exec）
      echo "ℹ️  查询所有用户数据库大小..." >&2

      local total_db_size=0
      local databases
      databases=$(PGPASSWORD="$POSTGRES_PASSWORD" psql -h "$pg_host" -U "$pg_user" \
        -d postgres -t -c \
        "SELECT datname FROM pg_database WHERE datistemplate = false AND datname NOT IN ('postgres', 'template0', 'template1') ORDER BY datname;" 2>/dev/null | tr -d ' \n' | sed 's/$/\n/' | grep -v '^$')

      for db in $databases; do
        local db_size
        db_size=$(PGPASSWORD="$POSTGRES_PASSWORD" psql -h "$pg_host" -U "$pg_user" \
          -d postgres -t -c "SELECT pg_database_size('$db');" 2>/dev/null | tr -d ' ')
        if [[ -n "$db_size" && "$db_size" =~ ^[0-9]+$ ]]; then
          total_db_size=$((total_db_size + db_size))
          local db_size_mb
          db_size_mb=$((db_size / 1024 / 1024))
          echo "  - $db: ${db_size_mb} MB" >&2
        fi
      done

      if [[ $total_db_size -eq 0 ]]; then
        echo "⚠️  无法获取数据库大小，跳过磁盘空间检查"
        return 0
      fi

      # 2. 计算所需空间 = 数据库总大小 × 2
      local required_space=$((total_db_size * 2))
      local required_space_mb=$((required_space / 1024 / 1024))
      echo "ℹ️  所需磁盘空间: ${required_space_mb} MB (数据库大小 × 2)"

      # 3. 检查备份目录挂载点可用空间
      local available
      available=$(df -B1 "$backup_dir" 2>/dev/null | tail -1 | awk '{print $4}')

      if [[ -z "$available" || ! "$available" =~ ^[0-9]+$ ]]; then
        echo "⚠️  无法获取磁盘空间信息，继续备份"
        return 0
      fi

      local available_mb=$((available / 1024 / 1024))
      echo "  - 可用空间: ${available_mb} MB"

      if [[ $available -lt $required_space ]]; then
        echo "❌ 错误: 磁盘空间不足"
        echo "  - 可用: ${available_mb} MB"
        echo "  - 需要: ${required_space_mb} MB"
        echo ""
        echo "解决建议："
        echo "  - 清理旧的备份文件: find $backup_dir -type f -mtime +7 -delete"
        echo "  - 检查: df -h $backup_dir"
        return $EXIT_DISK_SPACE_INSUFFICIENT
      fi

      echo "✅ 磁盘空间检查通过"
      return 0
    fi
    ```

    **关键设计决策（per Claude's Discretion）：**
    1. 使用 `PGPASSWORD="$POSTGRES_PASSWORD"` 而非 `PGPASSWORD=$POSTGRES_PASSWORD`，确保密码含特殊字符时正确传递
    2. 数据库大小为 0 时跳过检查（防御性编程），不返回错误
    3. 无法获取磁盘空间时跳过检查（graceful degradation），不阻断备份
    4. 输出 MB 级别信息（MB 对此场景足够直观，避免引入 bc 依赖）
    5. 不使用 `bc` 计算 GB（避免容器内 bc 不可用的问题）

    **不要修改的部分：**
    - 第 168-249 行的宿主机检查逻辑保持不变
    - `get_database_size()` 和 `get_total_database_size()` 函数保持不变（宿主机使用）
  </action>
  <acceptance_criteria>
    - `scripts/backup/lib/health.sh` 不包含 `跳过详细磁盘检查` 字符串
    - `scripts/backup/lib/health.sh` 第 161 行开始的容器内分支包含 `PGPASSWORD` 和 `psql` 直连
    - `scripts/backup/lib/health.sh` 容器内分支包含 `df -B1 "$backup_dir"`
    - `scripts/backup/lib/health.sh` 容器内分支包含 `EXIT_DISK_SPACE_INSUFFICIENT`
    - `scripts/backup/lib/health.sh` 容器内分支包含 `pg_database_size`
    - `bash -n scripts/backup/lib/health.sh` 语法检查通过（退出码 0）
    - `scripts/backup/lib/health.sh` 仍然包含宿主机分支 `get_total_database_size`（第 168 行后未改变）
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/backup/lib/health.sh && grep -q "PGPASSWORD" scripts/backup/lib/health.sh && grep -q "pg_database_size" scripts/backup/lib/health.sh && grep -q "df -B1" scripts/backup/lib/health.sh && grep -q "EXIT_DISK_SPACE_INSUFFICIENT" scripts/backup/lib/health.sh && ! grep -q "跳过详细磁盘检查" scripts/backup/lib/health.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>health.sh 容器内分支使用 psql 直连查询数据库大小 + df 检查挂载点空间，空间不足时返回 EXIT_DISK_SPACE_INSUFFICIENT，语法检查通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 容器内脚本 → PostgreSQL | psql 使用 PGPASSWORD 环境变量认证（Docker Compose 注入） |
| 数据库大小查询 | 用户输入的数据库名通过 SQL 查询获取大小，需防注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | PGPASSWORD 环境变量 | accept | Docker 环境变量仅在容器内可见，不跨容器泄露 |
| T-10-04 | Tampering | 数据库名传入 psql | accept | 数据库名来自 pg_database 系统表查询结果，非用户输入 |
</threat_model>

<verification>
1. `bash -n scripts/backup/lib/health.sh` — 语法检查通过
2. 容器内分支不再 `return 0` 跳过检查
3. 容器内分支使用 psql 直连查询数据库大小（无 docker exec）
4. 容器内分支使用 df -B1 检查挂载点空间
5. 空间不足时返回 EXIT_DISK_SPACE_INSUFFICIENT (6)
</verification>

<success_criteria>
- health.sh 容器内分支实现完整的磁盘空间检查（数据库大小 × 2 vs 可用空间）
- 语法检查通过（bash -n 无错误）
- 不引入新依赖（使用已有的 psql、df、awk）
- 宿主机分支逻辑未受影响
</success_criteria>

<output>
After completion, create `.planning/phases/10-b2/10-02-SUMMARY.md`
</output>
