---
phase: 25-cleanup-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy/deploy-infrastructure-prod.sh
  - scripts/deploy/deploy-apps-prod.sh
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ENH-04

must_haves:
  truths:
    - "deploy-infrastructure-prod.sh 和 deploy-apps-prod.sh 包含手动回退方案注释块"
    - "CLAUDE.md 部署命令章节以 Jenkins Pipeline 为主，旧脚本标注为手动回退"
    - "ROADMAP.md 中 v1.4 里程碑归档为折叠块格式"
    - "PROJECT.md 中 v1.4 标记为 shipped"
    - "STATE.md milestone 状态为 completed"
  artifacts:
    - path: "scripts/deploy/deploy-infrastructure-prod.sh"
      provides: "手动回退方案文件头注释"
      contains: "手动回退"
    - path: "scripts/deploy/deploy-apps-prod.sh"
      provides: "手动回退方案文件头注释"
      contains: "手动回退"
    - path: "CLAUDE.md"
      provides: "Jenkins Pipeline 优先的部署命令章节"
      contains: "Jenkins Pipeline"
    - path: ".planning/ROADMAP.md"
      provides: "v1.4 里程碑归档"
      contains: "v1.4 CI/CD 零停机部署"
    - path: ".planning/PROJECT.md"
      provides: "v1.4 shipped 状态"
      contains: "v1.4 CI/CD 零停机部署"
    - path: ".planning/STATE.md"
      provides: "milestone completed 状态"
      contains: "completed"
  key_links:
    - from: "CLAUDE.md"
      to: "jenkins/Jenkinsfile"
      via: "部署命令章节引用 Jenkins Pipeline"
      pattern: "Jenkins Pipeline"
    - from: "CLAUDE.md"
      to: "scripts/deploy/deploy-infrastructure-prod.sh"
      via: "回退方案引用"
      pattern: "deploy-infrastructure-prod.sh"
---

<objective>
v1.4 CI/CD 零停机部署里程碑收尾：旧部署脚本标记为手动回退方案，CLAUDE.md 部署章节更新为 Jenkins Pipeline 优先，里程碑归档为已完成状态。

Purpose: 完成从手动部署到 CI/CD Pipeline 的文档迁移，确保 CLAUDE.md 反映当前实际部署流程，里程碑状态归档为下个版本做准备。

Output: 7 个文件更新（2 脚本注释 + 1 部署文档 + 3 归档文件 + 1 需求文件）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-cleanup-migration/25-CONTEXT.md
@.planning/phases/25-cleanup-migration/25-RESEARCH.md
@.planning/milestones/v1.3-ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 旧部署脚本添加手动回退注释块</name>
  <files>
    scripts/deploy/deploy-infrastructure-prod.sh
    scripts/deploy/deploy-apps-prod.sh
    scripts/deploy/deploy-findclass-zero-deps.sh
  </files>
  <read_first>
    scripts/deploy/deploy-infrastructure-prod.sh
    scripts/deploy/deploy-apps-prod.sh
    scripts/deploy/deploy-findclass-zero-deps.sh
  </read_first>
  <action>
Per D-01 和 D-02，为两个旧部署脚本添加文件头注释块，标注为手动回退方案。不添加运行时警告（无 echo/printf），不改变脚本逻辑行为。

1. **deploy-infrastructure-prod.sh** -- 在第 2 行（`set -euo pipefail` 之前）插入注释块：
```bash
# ============================================
# 手动回退部署脚本（生产环境）
# ============================================
# NOTE: 此脚本作为 Jenkins Pipeline 不可用时的紧急回退方案保留。
# 正常部署请使用 Jenkins Pipeline（Build Now -> findclass-deploy）。
#
# 原有功能：自动部署并配置基础设施服务
# 包括：PostgreSQL (Prod/Dev), Keycloak, Nginx, Noda-Ops, Findclass-SSR
#
# 此脚本行为不变，可直接手动执行。
# ============================================
```
保留原有注释块（`# 功能：自动部署并配置基础设施服务` 等）在其下方，不删除。

2. **deploy-apps-prod.sh** -- 替换第 2-6 行的注释块（`# 部署应用服务到生产环境` 到 `# 使用独立的 docker-compose.app.yml`）为：
```bash
# ============================================
# 手动回退部署脚本（应用服务）
# ============================================
# NOTE: 此脚本作为 Jenkins Pipeline 不可用时的紧急回退方案保留。
# 正常部署请使用 Jenkins Pipeline（Build Now -> findclass-deploy）。
#
# 原有功能：部署 findclass-ssr 应用服务（重新构建镜像 + 部署）
# 使用独立的 docker-compose.app.yml（name: noda-apps）
#
# 此脚本行为不变，可直接手动执行。
# ============================================
```

3. **deploy-findclass-zero-deps.sh** -- Per D-03，确认第 2 行已有 `# DEPRECATED:` 标记。无需额外处理，仅读取确认。
  </action>
  <verify>
    <automated>grep -c "手动回退" scripts/deploy/deploy-infrastructure-prod.sh && grep -c "手动回退" scripts/deploy/deploy-apps-prod.sh && grep -c "DEPRECATED" scripts/deploy/deploy-findclass-zero-deps.sh</automated>
  </verify>
  <acceptance_criteria>
    - `deploy-infrastructure-prod.sh` 包含字符串 "手动回退部署脚本（生产环境）"
    - `deploy-infrastructure-prod.sh` 包含字符串 "Jenkins Pipeline 不可用时的紧急回退方案保留"
    - `deploy-infrastructure-prod.sh` 不包含 `echo` 或 `printf` 警告（仅注释）
    - `deploy-apps-prod.sh` 包含字符串 "手动回退部署脚本（应用服务）"
    - `deploy-apps-prod.sh` 包含字符串 "Jenkins Pipeline 不可用时的紧急回退方案保留"
    - `deploy-findclass-zero-deps.sh` 包含字符串 "DEPRECATED"（确认已有，不修改）
    - `deploy-infrastructure-prod.sh` 原有功能逻辑未改变（set -euo pipefail 仍在，source 行未改）
    - `deploy-apps-prod.sh` 原有功能逻辑未改变
  </acceptance_criteria>
  <done>两个旧部署脚本添加了手动回退注释块（无运行时警告），deploy-findclass-zero-deps.sh 已确认 DEPRECATED 状态无需额外处理。</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md 部署章节更新 + 里程碑归档</name>
  <files>
    CLAUDE.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    CLAUDE.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
    .planning/milestones/v1.3-ROADMAP.md
    .planning/milestones/v1.3-REQUIREMENTS.md
  </read_first>
  <action>
Per D-04/D-05/D-06/D-07/D-08/D-09，执行以下 5 项文档更新。

### 2a. CLAUDE.md 部署命令章节更新 (D-04/D-05/D-06)

仅修改 CLAUDE.md 第 131-142 行的 "## 部署命令" 章节。不触碰其他章节（架构、部署规则、修复记录等）。

将第 131-142 行的代码块替换为：

```markdown
## 部署命令

### 主要部署方式：Jenkins Pipeline

通过 Jenkins UI 手动触发蓝绿部署 Pipeline：

1. 浏览器访问 Jenkins（默认 http://\<server-ip\>:8888）
2. 点击 `findclass-deploy` 任务
3. 点击 "Build Now" 按钮
4. Pipeline 自动执行 9 阶段：Pre-flight -> Build -> Test -> Deploy -> Health Check -> Switch -> Verify -> CDN Purge -> Cleanup
5. 查看 Stage View 确认各阶段状态

### 紧急回退：手动部署脚本

Jenkins 不可用时，可使用旧部署脚本手动部署：

```bash
# 全量部署（基础设施 + 应用）
bash scripts/deploy/deploy-infrastructure-prod.sh

# 仅部署应用（findclass-ssr）
bash scripts/deploy/deploy-apps-prod.sh
```

### 查看状态（只读，允许直接使用）

```bash
# Docker Compose 容器状态
docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.dev.yml ps

# 蓝绿容器状态
cat /opt/noda/active-env  # 当前活跃环境（blue/green）
docker ps --filter name=findclass-ssr  # 蓝绿容器列表
```
```

注意：上述 markdown 中内嵌的代码块要用正确的反引号嵌套。实际编辑时 CLAUDE.md 需要保留 "## 部署命令" 后面的 `<!-- GSD:project-start -->` 注释不动。

### 2b. ROADMAP.md v1.4 里程碑归档 (D-07)

Per D-07，v1.4 归档格式与 v1.0~v1.3 一致。

具体操作：
1. 将 Milestones 列表中 v1.4 行更新为 shipped 格式：
```
- ✅ **v1.4 CI/CD 零停机部署** — Phases 19-25 (shipped 2026-04-16)
```

2. 将 `### v1.4 CI/CD 零停机部署 (In Progress)` 改为折叠块归档格式，参考 v1.3 格式：
```markdown
### ✅ v1.4 CI/CD 零停机部署 (Shipped 2026-04-16)

**Milestone Goal:** Jenkins + 蓝绿部署实现编译失败不 down 站，自动回滚保护

- [x] **Phase 19: Jenkins 安装与基础配置** — 宿主机原生安装 Jenkins LTS，可操作 Docker daemon (completed 2026-04-14)
- [x] **Phase 20: Nginx 蓝绿路由基础** — 将 upstream 定义抽离为独立 include 文件，支持动态切换 (completed 2026-04-15)
- [x] **Phase 21: 蓝绿容器管理** — docker run 独立管理蓝绿容器，状态文件追踪活跃环境 (completed 2026-04-15)
- [x] **Phase 22: 蓝绿部署核心流程** — 完整的蓝绿部署脚本（容器启停 + 健康检查 + nginx 切换 + 回滚） (completed 2026-04-15)
- [x] **Phase 23: Pipeline 集成与测试门禁** — Jenkinsfile 九阶段 Pipeline + lint/test 质量门禁 (completed 2026-04-15)
- [x] **Phase 24: Pipeline 增强特性** — 部署前备份检查 + CDN 缓存清除 + 镜像清理 (completed 2026-04-15)
- [x] **Phase 25: 清理与迁移** — 旧脚本保留为手动回退 + 文档更新 + 里程碑归档 (completed 2026-04-16)
```

3. Phase 25 详情区域更新：
   - `**Plans**: 2 plans` 改为 `**Plans**: 1 plan`
   - Plans 列表中 `25-01` 勾选为 `[x]`
   - Success Criteria 保持不变

4. Progress 表格中 Phase 25 行更新：
   - Plans Complete: `0/1` -> `1/1`
   - Status: `Not started` -> `Complete`
   - Completed: `2026-04-16`

### 2c. PROJECT.md 里程碑状态更新 (D-08)

1. 将 `## Current Milestone: v1.4 CI/CD 零停机部署` 部分更新：

- 标题改为 `## Current State`
- 删除 v1.4 的 Goal/Target features 列表（已归档到 Shipped Milestones）
- `**Next focus:**` 后面改为 `待定`

2. 在 `## Shipped Milestones` 下，v1.3 之前插入 v1.4 归档：
```markdown
### v1.4 CI/CD 零停机部署 ✅ (2026-04-16)

7 phases, 11 plans, 95 commits, 89 files changed (+15,967/-1,511 LOC)

- Jenkins 宿主机原生安装/卸载脚本（setup-jenkins.sh 7 子命令 + groovy 自动化）
- Nginx upstream include 抽离（蓝绿路由基础，支持 nginx -s reload 切换）
- 蓝绿容器管理（manage-containers.sh 8 子命令 + env-findclass-ssr.env 模板）
- 蓝绿部署核心流程（blue-green-deploy.sh + rollback-findclass.sh，零停机 + 自动回滚）
- Jenkinsfile 9 阶段 Pipeline + pipeline-stages.sh 函数库（lint/test 质量门禁）
- Pipeline 增强特性（备份时效性检查 + CDN 缓存清除 + 镜像时间阈值清理）
- 旧脚本保留为手动回退 + 部署文档更新 + 里程碑归档
```

3. Requirements 部分：
   - Active 中的 v1.4 相关项（前 5 项）全部移到 Validated，标注 `— v1.4`
   - Prisma 7 兼容性保留在 Active

4. Key Decisions 表新增行：
   - `蓝绿容器 docker run 管理 | 避免与 compose 冲突 | ✅ Good`
   - `Pipeline 手动触发 | 生产环境安全控制 | ✅ Good`
   - `Jenkinsfile Declarative Pipeline | 可读性和可维护性 | ✅ Good`

5. 更新底部 `Last updated` 日期为 `2026-04-16`

### 2d. STATE.md 状态更新 (D-09)

更新 YAML front matter 中的字段：
- `status: completed`
- `stopped_at: Phase 25 complete`
- `last_updated: "2026-04-16T..."` （使用当前 ISO 时间戳）
- `last_activity: 2026-04-16`
- `progress.total_plans: 11`
- `progress.completed_plans: 11`
- `progress.percent: 100`

Body 中：
- `## Current Position` 下更新：
  - `Phase: 25`
  - `Plan: 01 (complete)`
  - `Status: Milestone v1.4 shipped`
- 在 `## Session Continuity` 之前添加 `## Milestone Complete` 段落：
```
## Milestone Complete

v1.4 CI/CD 零停机部署 -- shipped 2026-04-16
7 phases, 11 plans, 95 commits, 89 files changed
```

### 2e. REQUIREMENTS.md 需求归档

1. ENH-04 勾选为 `[x]`
2. Traceability 表中 ENH-04 行 Status 改为 `Complete`
3. 创建归档文件 `.planning/milestones/v1.4-REQUIREMENTS.md`，内容参考 `.planning/milestones/v1.3-REQUIREMENTS.md` 格式，标题改为 `# Requirements Archive: v1.4 CI/CD 零停机部署`，包含所有 v1.4 需求（JENK/PIPE/BLUE/TEST/ENH），状态全部为 `[x]`
  </action>
  <verify>
    <automated>grep -c "Jenkins Pipeline" CLAUDE.md && grep -c "Shipped 2026-04-16" .planning/ROADMAP.md && grep -c "v1.4 CI/CD" .planning/PROJECT.md && grep -c "completed" .planning/STATE.md && grep -c "\[x\] \*\*ENH-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md 包含 "### 主要部署方式：Jenkins Pipeline" 字符串
    - CLAUDE.md 包含 "### 紧急回退：手动部署脚本" 字符串
    - CLAUDE.md 包含 "Build Now" 字符串
    - CLAUDE.md 包含 "cat /opt/noda/active-env" 字符串
    - CLAUDE.md "## 部署命令" 到 "<!-- GSD:project-start" 之间包含上述新内容
    - CLAUDE.md 第 131 行之前的内容（架构、部署规则等）未被修改
    - ROADMAP.md 包含 "✅ **v1.4 CI/CD 零停机部署** — Phases 19-25 (shipped 2026-04-16)"
    - ROADMAP.md 包含 "✅ v1.4 CI/CD 零停机部署 (Shipped 2026-04-16)"
    - ROADMAP.md Phase 25 Plans 行显示 "1 plan"（不是 "2 plans"）
    - ROADMAP.md Progress 表格 Phase 25 状态为 "Complete"
    - PROJECT.md 包含 "### v1.4 CI/CD 零停机部署 ✅ (2026-04-16)"
    - PROJECT.md Validated 部分包含 "部署脚本迁移为 Jenkins Pipeline — v1.4" 等 v1.4 项
    - PROJECT.md Active 中 Prisma 7 仍保留
    - STATE.md front matter `status` 字段为 `completed`
    - STATE.md 包含 "Milestone v1.4 shipped"
    - REQUIREMENTS.md ENH-04 状态为 `[x]`
    - `.planning/milestones/v1.4-REQUIREMENTS.md` 文件存在
  </acceptance_criteria>
  <done>
CLAUDE.md 部署章节更新为 Jenkins Pipeline 优先（含手动回退和状态命令）。ROADMAP.md v1.4 归档为 shipped 折叠块。PROJECT.md v1.4 标记为 shipped。STATE.md milestone 状态更新为 completed。REQUIREMENTS.md ENH-04 标记完成并归档。
  </done>
</task>

</tasks>

<verification>
1. `grep "手动回退" scripts/deploy/deploy-infrastructure-prod.sh scripts/deploy/deploy-apps-prod.sh` -- 确认注释已添加
2. `grep -A 20 "## 部署命令" CLAUDE.md` -- 确认部署章节已更新
3. `grep "v1.4" .planning/ROADMAP.md | head -5` -- 确认归档格式
4. `grep "v1.4" .planning/PROJECT.md | head -5` -- 确认 shipped 标记
5. `head -5 .planning/STATE.md` -- 确认 status: completed
6. `grep "ENH-04" .planning/REQUIREMENTS.md` -- 确认需求完成
7. `ls .planning/milestones/v1.4-REQUIREMENTS.md` -- 确认归档文件存在
</verification>

<success_criteria>
1. deploy-infrastructure-prod.sh 和 deploy-apps-prod.sh 包含手动回退注释块，脚本逻辑未改变
2. CLAUDE.md 部署命令章节以 Jenkins Pipeline 为主，包含紧急回退和状态查看说明
3. ROADMAP.md v1.4 以 shipped 折叠块归档，与 v1.0~v1.3 格式一致
4. PROJECT.md v1.4 在 Shipped Milestones 中，Requirements 中 Active 项正确迁移到 Validated
5. STATE.md status 为 completed
6. REQUIREMENTS.md ENH-04 标记为完成，归档文件已创建
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup-migration/25-01-SUMMARY.md`
</output>
