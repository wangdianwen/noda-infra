---
phase: 39-infisical-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-doppler.sh
autonomous: false
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Jenkins 宿主机上 doppler 命令可用，返回有效版本号 >= 3.75"
    - "doppler CLI 可通过 DOPPLER_TOKEN 环境变量认证，能执行 secrets download"
    - "安装过程有可复现的脚本，未来重建服务器时可直接运行"
  artifacts:
    - path: "scripts/install-doppler.sh"
      provides: "Doppler CLI 自动化安装脚本"
      min_lines: 30
  key_links:
    - from: "scripts/install-doppler.sh"
      to: "brew (Jenkins 宿主机)"
      via: "brew tap + brew install"
      pattern: "brew (tap|install) dopplerhq/cli"
---

<objective>
在 Jenkins 宿主机上安装 Doppler CLI，并创建可复现的安装脚本。

目的：Doppler CLI 是整个密钥管理基础设施的基础，所有后续操作（密钥拉取、认证验证、离线备份）都依赖它。

输出：scripts/install-doppler.sh 安装脚本 + CLI 安装验证

**对应决策：** D-01（使用 Doppler）、D-02（brew install 安装方式）

**注意：** 此计划需要在远程 Jenkins 宿主机上执行安装脚本。本计划先创建安装脚本，然后通过 checkpoint 要求用户在服务器上执行并反馈结果。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-infisical-infra/39-CONTEXT.md
@.planning/phases/39-infisical-infra/39-RESEARCH.md
@scripts/utils/decrypt-secrets.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Doppler CLI 安装脚本</name>
  <files>scripts/install-doppler.sh</files>
  <read_first>
    - scripts/install-doppler.sh (新文件，不存在)
    - .planning/phases/39-infisical-infra/39-RESEARCH.md (安装命令和常见陷阱)
  </read_first>
  <behavior>
    - 脚本检查 Homebrew 是否可用
    - 脚本先执行 `brew install gnupg` 确保签名验证依赖存在
    - 脚本执行 `brew tap dopplerhq/cli` 然后 `brew install dopplerhq/cli/doppler`
    - 脚本验证 `doppler --version` 输出有效版本号
    - 脚本包含备选方案：如果 brew 不可用，使用 curl install.sh
  </behavior>
  <action>创建 scripts/install-doppler.sh 脚本，实现以下逻辑：

1. `set -euo pipefail` 安全模式
2. 检查 Homebrew：`command -v brew`，如果可用走 brew 路径
   a. `brew install gnupg 2>/dev/null || true` — 确保签名验证依赖
   b. `brew tap dopplerhq/cli` — 注册 tap 源
   c. `brew install dopplerhq/cli/doppler` — 安装 Doppler CLI
3. 如果 brew 不可用，走 curl 路径：
   a. `(curl -Ls --tlsv1.2 --proto "=https" --retry 3 https://cli.doppler.com/install.sh || wget -t 3 -qO- https://cli.doppler.com/install.sh) | sudo sh`
4. 安装后验证：`doppler --version`，输出版本号
5. 提示用户下一步：设置 DOPPLER_TOKEN 环境变量

脚本需要 `chmod +x` 权限。输出使用中文。

参考 RESEARCH.md 的安装命令和 Common Pitfalls（Pitfall 1: gnupg 依赖、Pitfall 2: brew tap 顺序）。</action>
  <verify>
    <automated>test -f scripts/install-doppler.sh && bash -n scripts/install-doppler.sh && echo "脚本语法正确"</automated>
  </verify>
  <done>
    - scripts/install-doppler.sh 文件存在且可执行
    - 脚本包含 brew 安装路径和 curl 备选路径
    - 脚本包含 gnupg 依赖处理
    - 脚本包含 doppler --version 验证步骤
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Doppler CLI 安装脚本 scripts/install-doppler.sh 已创建。需要在 Jenkins 宿主机（远程服务器）上执行该脚本安装 Doppler CLI。</what-built>
  <how-to-verify>
    1. 将安装脚本复制到 Jenkins 宿主机（或直接在服务器上运行）：
       ```bash
       bash scripts/install-doppler.sh
       ```
    2. 验证安装成功：
       ```bash
       doppler --version
       ```
       期望输出：`3.75.3` 或更高版本号
    3. 验证 CLI 帮助命令正常：
       ```bash
       doppler --help
       ```
       期望输出：Doppler CLI 使用说明
  </how-to-verify>
  <resume-signal>输入 "已安装" 并附上 doppler --version 的输出，或描述遇到的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地 → Jenkins 宿主机 | 安装脚本通过 SSH/手动方式传输到远程服务器执行 |
| brew tap 源 | 第三方 tap 源 dopplerhq/cli，信任 Homebrew 的 formula 审计机制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | brew tap 源 | accept | Homebrew formula 有 GPG 签名验证；dopplerhq 是官方维护的 tap |
| T-39-02 | Denial of Service | Doppler CLI 安装失败 | mitigate | 安装脚本包含 brew + curl 双路径备选方案 |
</threat_model>

<verification>
1. `bash -n scripts/install-doppler.sh` — 脚本语法正确
2. `grep -c 'brew tap dopplerhq/cli' scripts/install-doppler.sh` — 包含 brew tap 步骤
3. `grep -c 'gnupg' scripts/install-doppler.sh` — 包含 gnupg 依赖处理
4. 用户确认：Jenkins 宿主机上 `doppler --version` 返回有效版本号
</verification>

<success_criteria>
- scripts/install-doppler.sh 脚本存在且可执行
- Jenkins 宿主机上 doppler CLI 已安装，`doppler --version` 返回 >= 3.75
</success_criteria>

<output>
After completion, create `.planning/phases/39-infisical-infra/39-01-SUMMARY.md`
</output>
