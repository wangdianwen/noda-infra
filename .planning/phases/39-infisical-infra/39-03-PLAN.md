---
phase: 39-infisical-infra
plan: 03
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - scripts/backup/backup-doppler-secrets.sh
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Doppler 密钥可导出为 .env 格式并通过 age 加密"
    - "加密后的密钥快照可上传到 Backblaze B2"
    - "Service Token 明文已存储到密码管理器"
  artifacts:
    - path: "scripts/backup/backup-doppler-secrets.sh"
      provides: "Doppler 密钥离线备份脚本 — age 加密 + B2 上传"
      min_lines: 50
  key_links:
    - from: "scripts/backup/backup-doppler-secrets.sh"
      to: "Doppler Cloud"
      via: "doppler secrets download --format=env"
      pattern: "doppler secrets download"
    - from: "scripts/backup/backup-doppler-secrets.sh"
      to: "Backblaze B2"
      via: "b2 upload-file"
      pattern: "b2 upload-file"
---

<objective>
创建 Doppler 密钥离线备份脚本，实现 age 加密 + B2 上传的两层备份策略。

目的：确保密钥有离线副本，即使 Doppler 服务不可用也能恢复。这是灾难恢复的最后防线。

输出：scripts/backup/backup-doppler-secrets.sh 备份脚本

**对应决策：** D-05（两层备份策略：密码管理器 + B2 加密快照）

**说明：** Service Token 存储到密码管理器是人工操作，在 Plan 02 的 checkpoint 中已完成。本计划聚焦于自动化 B2 加密快照脚本。

**与其他计划的关系：** 本计划与 Plan 02 无文件依赖，理论上可并行执行。但逻辑上依赖 Plan 02 完成后才有密钥可备份。脚本本身可独立编写和测试（使用 mock 数据）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-infisical-infra/39-CONTEXT.md
@.planning/phases/39-infisical-infra/39-RESEARCH.md
@.sops.yaml
@scripts/backup/lib/config.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Doppler 密钥离线备份脚本</name>
  <files>scripts/backup/backup-doppler-secrets.sh</files>
  <read_first>
    - scripts/backup/backup-doppler-secrets.sh (新文件，不存在)
    - .planning/phases/39-infisical-infra/39-RESEARCH.md (离线备份代码示例，第 296-315 行)
    - .sops.yaml (age 公钥：age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w)
    - scripts/backup/lib/config.sh (B2 配置加载模式)
  </read_first>
  <behavior>
    - 脚本检查必要环境变量：DOPPLER_TOKEN、AGE_PUBLIC_KEY 或从 .sops.yaml 读取
    - 脚本执行 `doppler secrets download --format=env --no-file --project noda --config prod` 获取明文密钥
    - 脚本通过管道将明文密钥传给 age 加密：`age -r "$AGE_PUBLIC_KEY" -o "$OUTPUT_FILE"`
    - 脚本使用 b2 upload-file 上传加密文件到 B2
    - 脚本清理临时文件
    - 脚本支持 --dry-run 模式（仅下载和加密，不上传到 B2）
  </behavior>
  <action>创建 scripts/backup/backup-doppler-secrets.sh 脚本，实现以下逻辑：

1. `set -euo pipefail` 安全模式
2. 解析参数：
   - `--dry-run`: 仅下载和加密，不上传 B2
   - `--project noda`: Doppler 项目名（默认 noda）
   - `--config prod`: Doppler 环境名（默认 prod）
3. 环境变量检查：
   - `DOPPLER_TOKEN` — 必须设置，否则报错退出
   - `AGE_PUBLIC_KEY` — 如果未设置，从 `.sops.yaml` 文件中读取（`grep '^age:' .sops.yaml | awk '{print $2}'`）。age 公钥值：`age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w`
4. 检查依赖：`doppler`、`age`、`b2`（仅在非 dry-run 模式下检查 b2）
5. 生成输出文件名：`/tmp/doppler-backup-$(date +%Y%m%d-%H%M%S).env.age`
6. 执行密钥下载 + 加密（管道方式，明文不落盘）：
   ```bash
   doppler secrets download --format=env --no-file --project "$PROJECT" --config "$CONFIG" \
     | age -r "$AGE_PUBLIC_KEY" -o "$OUTPUT_FILE"
   ```
7. 验证加密文件已生成且非空
8. 如果非 dry-run 模式：
   a. 上传到 B2：
      ```bash
      b2 upload-file noda-backups "$OUTPUT_FILE" "doppler-backup/$(basename "$OUTPUT_FILE")"
      ```
   b. 验证上传成功
9. 清理临时文件：`rm -f "$OUTPUT_FILE"`
10. 输出备份完成摘要

脚本需要 `chmod +x` 权限。输出使用中文。

参考 RESEARCH.md 的离线备份代码示例（第 296-315 行）。参考 scripts/backup/lib/config.sh 的配置加载模式（但不使用其 config.sh，因为备份系统保持独立）。

**Claude's Discretion 处理：**
- 加密方式选择 age（与现有 SOPS 工具链一致，项目已有 .sops.yaml 中的 age 公钥）
- B2 bucket 使用现有的 noda-backups，路径使用 doppler-backup/ 前缀</action>
  <verify>
    <automated>test -f scripts/backup/backup-doppler-secrets.sh && bash -n scripts/backup/backup-doppler-secrets.sh && echo "脚本语法正确"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/backup/backup-doppler-secrets.sh 文件存在且可执行
    - 脚本包含 `doppler secrets download` 命令（grep 'doppler secrets download' scripts/backup/backup-doppler-secrets.sh）
    - 脚本包含 `age -r` 加密步骤（grep 'age -r' scripts/backup/backup-doppler-secrets.sh）
    - 脚本包含 `b2 upload-file` 上传步骤（grep 'b2 upload-file' scripts/backup/backup-doppler-secrets.sh）
    - 脚本包含 `--dry-run` 参数支持（grep '\-\-dry-run' scripts/backup/backup-doppler-secrets.sh）
    - 脚本包含 DOPPLER_TOKEN 环境变量检查
    - 脚本包含临时文件清理步骤（grep 'rm -f' scripts/backup/backup-doppler-secrets.sh）
    - 脚本包含 age 公钥回退读取 .sops.yaml 的逻辑
  </acceptance_criteria>
  <done>
    - scripts/backup/backup-doppler-secrets.sh 文件存在且可执行
    - 脚本实现 doppler secrets download → age 加密 → B2 上传的完整流程
    - 脚本支持 --dry-run 模式
    - 脚本使用 .sops.yaml 中的 age 公钥作为默认加密密钥
    - 脚本语法检查通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doppler Cloud → 本地脚本 | 密钥明文通过 HTTPS 传输到本地进程管道 |
| 本地脚本 → age 加密 | 明文在管道中直接传递给 age，不落盘 |
| age 加密文件 → B2 | 加密文件上传到 B2，即使泄露也无法解密（需要 age 私钥） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | Information Disclosure | 备份过程中明文密钥泄露 | mitigate | 管道方式处理，明文不写入磁盘文件；临时文件仅存储加密后的内容 |
| T-39-08 | Information Disclosure | B2 加密快照被截获 | accept | age 加密使用公钥，需要对应的私钥才能解密；私钥存储在 age-key 文件中 |
| T-39-09 | Denial of Service | B2 上传失败 | mitigate | 脚本支持 --dry-run 模式；可本地保存加密文件后手动上传 |
</threat_model>

<verification>
1. `bash -n scripts/backup/backup-doppler-secrets.sh` — 脚本语法正确
2. `grep 'doppler secrets download' scripts/backup/backup-doppler-secrets.sh` — 包含密钥下载
3. `grep 'age -r' scripts/backup/backup-doppler-secrets.sh` — 包含 age 加密
4. `grep 'b2 upload-file' scripts/backup/backup-doppler-secrets.sh` — 包含 B2 上传
5. `grep '\-\-dry-run' scripts/backup/backup-doppler-secrets.sh` — 支持 dry-run 模式
</verification>

<success_criteria>
- scripts/backup/backup-doppler-secrets.sh 脚本存在且可执行
- 脚本实现完整流程：doppler secrets download → age 加密 → B2 上传
- 脚本支持 --dry-run 模式
- 明文密钥不落盘（管道传递给 age）
</success_criteria>

<output>
After completion, create `.planning/phases/39-infisical-infra/39-03-SUMMARY.md`
</output>
