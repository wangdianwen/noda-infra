---
phase: 14-container-protection-and-deployment-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy/deploy-infrastructure-prod.sh
  - scripts/deploy/deploy-apps-prod.sh
autonomous: true
requirements:
  - D-05
  - D-06

must_haves:
  truths:
    - "部署前自动保存当前运行镜像的 digest 到文件"
    - "部署失败时自动回退到保存的上一版本镜像"
    - "部署前检查最近备份时间，12 小时内已有成功备份则跳过"
    - "12 小时内无备份时自动触发一次备份"
  artifacts:
    - path: "scripts/deploy/deploy-infrastructure-prod.sh"
      provides: "镜像保存 + 回滚 + 部署前自动备份"
      contains: "save_image_tags"
    - path: "scripts/deploy/deploy-apps-prod.sh"
      provides: "应用部署的镜像保存 + 回滚"
      contains: "save_image_tags"
  key_links:
    - from: "scripts/deploy/deploy-infrastructure-prod.sh"
      to: "noda-ops 容器内的 backup-postgres.sh"
      via: "docker exec noda-ops /app/backup/backup-postgres.sh"
      pattern: "docker exec.*backup-postgres"
    - from: "scripts/deploy/deploy-infrastructure-prod.sh"
      to: "ROLLBACK_FILE"
      via: "部署失败时读取保存的 digest 进行回滚"
      pattern: "ROLLBACK_FILE"
---

<objective>
为两个部署脚本添加镜像回滚机制（D-05）和部署前自动备份（D-06），确保部署失败时可以安全回退，且每次部署前数据都有备份保护。

Purpose: 防止部署失败导致服务不可用，确保数据在部署前始终有备份。
Output: 修改后的 deploy-infrastructure-prod.sh（回滚+备份）和 deploy-apps-prod.sh（回滚）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-container-protection-and-deployment-safety/14-CONTEXT.md
@.planning/phases/14-container-protection-and-deployment-safety/14-RESEARCH.md
@scripts/deploy/deploy-infrastructure-prod.sh
@scripts/deploy/deploy-apps-prod.sh
@scripts/backup/lib/config.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy-infrastructure-prod.sh 回滚 + 部署前备份 (D-05, D-06)</name>
  <files>
    scripts/deploy/deploy-infrastructure-prod.sh
  </files>
  <read_first>
    scripts/deploy/deploy-infrastructure-prod.sh
    scripts/backup/backup-postgres.sh
  </read_first>
  <action>
在 deploy-infrastructure-prod.sh 中添加三个新函数和对应调用点。

**1. 在 `source "$PROJECT_ROOT/scripts/lib/log.sh"` 之后，`COMPOSE_FILES` 定义之前添加：**

```bash
# 回滚目录和文件
ROLLBACK_DIR="/tmp/noda-rollback"
ROLLBACK_FILE="$ROLLBACK_DIR/images-$(date +%s).txt"
```

**2. 在 EXPECTED_CONTAINERS 和 START_SERVICES 定义之后添加三个函数：**

```bash
# ============================================
# 镜像回滚函数 (D-05)
# ============================================

# save_image_tags - 保存当前运行容器的镜像 digest
# 参数：无（保存到 ROLLBACK_FILE）
# 返回：0=成功
save_image_tags() {
  mkdir -p "$ROLLBACK_DIR"
  : > "$ROLLBACK_FILE"
  for container in "${EXPECTED_CONTAINERS[@]}"; do
    local image_id
    image_id=$(docker inspect --format='{{.Image}}' "$container" 2>/dev/null || echo "")
    if [ -n "$image_id" ]; then
      echo "${container}=${image_id}" >> "$ROLLBACK_FILE"
      log_info "已保存 ${container} 的镜像: ${image_id:0:12}..."
    fi
  done
  log_success "镜像标签已保存到 ${ROLLBACK_FILE}"
}

# rollback_images - 回退到保存的镜像版本
# 参数：无（从 ROLLBACK_FILE 读取）
# 返回：0=成功，1=失败
rollback_images() {
  if [ ! -f "$ROLLBACK_FILE" ]; then
    log_error "回滚文件不存在: ${ROLLBACK_FILE}"
    return 1
  fi

  log_info "开始回滚到上一版本镜像..."
  while IFS='=' read -r container image_id; do
    [ -z "$container" ] && continue
    log_info "回滚 ${container} 到镜像 ${image_id:0:12}..."
    docker stop "$container" 2>/dev/null || true
    docker rm "$container" 2>/dev/null || true
    docker run -d --name "$container" "$image_id" 2>/dev/null || {
      log_error "回滚 ${container} 失败"
    }
  done < "$ROLLBACK_FILE"

  log_success "回滚完成"
  return 0
}

# ============================================
# 部署前备份检查函数 (D-06)
# ============================================

# check_recent_backup - 检查最近备份是否在 12 小时内
# 参数：无（检查 noda-ops 容器内的 history.json）
# 返回：0=备份足够新（跳过），1=需要备份
check_recent_backup() {
  local history_json
  history_json=$(docker exec noda-ops cat /app/history/history.json 2>/dev/null || echo "")

  if [ -z "$history_json" ]; then
    log_info "无备份历史记录，需要执行备份"
    return 1
  fi

  local last_backup_ts
  last_backup_ts=$(echo "$history_json" | jq -r '
    [(. // []) | select(.operation=="backup" and .duration > 0)] |
    sort_by(.timestamp) | reverse |
    .[0].timestamp // empty
  ' 2>/dev/null || echo "")

  if [ -z "$last_backup_ts" ]; then
    log_info "无成功备份记录，需要执行备份"
    return 1
  fi

  # 计算备份时间差（兼容 macOS 和 Linux）
  local last_epoch now_epoch age
  if date -u -v0S +%s >/dev/null 2>&1; then
    # macOS
    last_epoch=$(date -j -f "%Y-%m-%dT%H:%M:%SZ" "${last_backup_ts%%.*}Z" +%s 2>/dev/null || echo "0")
  else
    # Linux
    last_epoch=$(date -d "${last_backup_ts%%.*}Z" +%s 2>/dev/null || echo "0")
  fi
  now_epoch=$(date +%s)
  age=$((now_epoch - last_epoch))

  local threshold_seconds=43200  # 12 hours

  if [ "$age" -lt "$threshold_seconds" ]; then
    local age_hours=$((age / 3600))
    log_success "最近备份在 ${age_hours} 小时前，跳过部署前备份"
    return 0
  else
    log_info "最近备份超过 12 小时前，需要执行备份"
    return 1
  fi
}

# run_pre_deploy_backup - 执行部署前备份
# 参数：无
# 返回：0=成功，1=失败
run_pre_deploy_backup() {
  log_info "执行部署前数据库备份..."
  if ! docker exec noda-ops /app/backup/backup-postgres.sh; then
    log_error "部署前备份失败"
    return 1
  fi
  log_success "部署前备份完成"
  return 0
}
```

**3. 修改步骤顺序，将原来的 5 步扩展为 7 步：**

在 "步骤 1/5: 验证环境" 中将编号改为 "步骤 1/7"。

在 "步骤 2/5: 重启容器" **之前** 插入两个新步骤：

```bash
# ============================================
# 步骤 2/7: 保存当前镜像标签 (D-05)
# ============================================
log_info "=========================================="
log_info "步骤 2/7: 保存当前镜像标签"
log_info "=========================================="

save_image_tags

# ============================================
# 步骤 3/7: 部署前自动备份 (D-06)
# ============================================
log_info "=========================================="
log_info "步骤 3/7: 部署前自动备份"
log_info "=========================================="

if ! check_recent_backup; then
  if ! run_pre_deploy_backup; then
    log_error "部署前备份失败，中止部署"
    exit 1
  fi
fi
```

将原来的 "步骤 2/5: 重启容器" 改为 "步骤 4/7: 重启容器"。
将原来的 "步骤 3/5: 等待所有服务健康" 改为 "步骤 5/7: 等待所有服务健康"。
将原来的 "步骤 4/5: 配置 Keycloak" 改为 "步骤 6/7: 配置 Keycloak"。
将原来的 "步骤 5/5: 最终验证" 改为 "步骤 7/7: 最终验证"。

**4. 在脚本中的健康检查失败处添加回滚触发：**

在 "步骤 5/7" 中，`if [ $WAITED -ge $HEALTH_TIMEOUT ]` 的分支内，`exit 1` 之前添加：
```bash
    log_info "尝试回滚到上一版本..."
    rollback_images || true
```

在 "步骤 7/7" 中，`if [ $RESTART_ISSUES -gt 0 ]` 的分支内，`exit 1` 之前添加：
```bash
  log_info "尝试回滚到上一版本..."
  rollback_images || true
```

同样在 "步骤 6/7: 配置 Keycloak" 的 `if ! bash scripts/setup-keycloak-full.sh; then` 分支内，`exit 1` 之前添加：
```bash
  log_info "尝试回滚到上一版本..."
  rollback_images || true
```

**5. 在部署成功消息的末尾添加：**
```bash
log_info "回滚文件: ${ROLLBACK_FILE}（部署成功，可安全忽略）"
```

**注意事项：**
- 使用 `docker inspect --format='{{.Image}}'` 获取当前运行容器的镜像 ID（短 ID），这是最可靠的方式
- 回滚使用 `docker run -d --name` 而非 `docker compose up`，因为 compose 配置可能已经改变
- 备份检查通过 `docker exec noda-ops cat /app/history/history.json` 获取容器内的备份历史
- 部署前备份通过 `docker exec noda-ops /app/backup/backup-postgres.sh` 在容器内执行
  </action>
  <verify>
    <automated>grep -c 'save_image_tags' scripts/deploy/deploy-infrastructure-prod.sh && grep -c 'rollback_images' scripts/deploy/deploy-infrastructure-prod.sh && grep -c 'check_recent_backup' scripts/deploy/deploy-infrastructure-prod.sh && grep -c 'run_pre_deploy_backup' scripts/deploy/deploy-infrastructure-prod.sh && grep -c 'ROLLBACK_FILE' scripts/deploy/deploy-infrastructure-prod.sh && grep -c 'docker exec noda-ops /app/backup/backup-postgres.sh' scripts/deploy/deploy-infrastructure-prod.sh</automated>
  </verify>
  <done>
    - deploy-infrastructure-prod.sh 包含 save_image_tags、rollback_images、check_recent_backup、run_pre_deploy_backup 四个函数
    - 步骤扩展为 7 步（验证 -> 保存镜像 -> 备份 -> 重启 -> 健康检查 -> Keycloak -> 最终验证）
    - 失败点（健康检查超时、Keycloak 配置失败、频繁重启）触发回滚
    - 部署前备份检查 12 小时阈值
  </done>
</task>

<task type="auto">
  <name>Task 2: deploy-apps-prod.sh 回滚机制 (D-05)</name>
  <files>
    scripts/deploy/deploy-apps-prod.sh
  </files>
  <read_first>
    scripts/deploy/deploy-apps-prod.sh
  </read_first>
  <action>
在 deploy-apps-prod.sh 中添加镜像保存和回滚机制。

**1. 在 `source "$PROJECT_ROOT/scripts/lib/log.sh"` 之后添加回滚相关变量：**

```bash
# 回滚目录和文件
ROLLBACK_DIR="/tmp/noda-rollback"
ROLLBACK_FILE="$ROLLBACK_DIR/images-apps-$(date +%s).txt"
```

**2. 在变量定义之后添加回滚函数：**

```bash
# ============================================
# 应用镜像回滚函数 (D-05)
# ============================================

# save_app_image_tags - 保存 findclass-ssr 当前镜像
save_app_image_tags() {
  mkdir -p "$ROLLBACK_DIR"
  : > "$ROLLBACK_FILE"
  local image_id
  image_id=$(docker inspect --format='{{.Image}}' findclass-ssr 2>/dev/null || echo "")
  if [ -n "$image_id" ]; then
    echo "findclass-ssr=${image_id}" >> "$ROLLBACK_FILE"
    log_info "已保存 findclass-ssr 镜像: ${image_id:0:12}..."
  fi
  log_success "应用镜像标签已保存"
}

# rollback_app - 回退到上一版本 findclass-ssr
rollback_app() {
  if [ ! -f "$ROLLBACK_FILE" ]; then
    log_error "回滚文件不存在: ${ROLLBACK_FILE}"
    return 1
  fi

  log_info "开始回滚 findclass-ssr 到上一版本..."
  local image_id
  image_id=$(grep "findclass-ssr=" "$ROLLBACK_FILE" | cut -d'=' -f2)
  if [ -n "$image_id" ]; then
    docker stop findclass-ssr 2>/dev/null || true
    docker rm findclass-ssr 2>/dev/null || true
    docker run -d --name findclass-ssr "$image_id" 2>/dev/null || {
      log_error "回滚 findclass-ssr 失败"
      return 1
    }
    log_success "findclass-ssr 已回滚"
  fi
  return 0
}
```

**3. 在现有部署流程中集成：**

在 "# 2. 停止并删除现有应用容器" 之前添加：
```bash
# 1.5 保存当前镜像标签
save_app_image_tags
```

在 "# 4. 等待应用就绪" 超时处理之后（TIMEOUT 循环结束后），添加失败检测和回滚：

```bash
if [ $ELAPSED -ge $TIMEOUT ]; then
  log_error "应用启动超时（${TIMEOUT}s）"
  log_info "尝试回滚到上一版本..."
  rollback_app || true
  exit 1
fi
```

在文件末尾 `log_success` 之前添加：
```bash
log_info "回滚文件: ${ROLLBACK_FILE}（部署成功，可安全忽略）"
```
  </action>
  <verify>
    <automated>grep -c 'save_app_image_tags' scripts/deploy/deploy-apps-prod.sh && grep -c 'rollback_app' scripts/deploy/deploy-apps-prod.sh && grep -c 'ROLLBACK_FILE' scripts/deploy/deploy-apps-prod.sh</automated>
  </verify>
  <done>
    - deploy-apps-prod.sh 包含 save_app_image_tags 和 rollback_app 函数
    - 部署前保存当前 findclass-ssr 镜像 ID
    - 启动超时时自动触发回滚
    - 使用与 deploy-infrastructure-prod.sh 一致的回滚模式
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 部署脚本 -> Docker daemon | 部署脚本通过 Docker CLI 管理容器 |
| 部署脚本 -> noda-ops 容器 | 通过 docker exec 调用备份脚本 |
| 回滚文件 -> /tmp | 镜像 digest 保存在临时目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Denial of Service | 部署失败导致服务不可用 | mitigate | 镜像 digest 回滚机制（失败时自动恢复上一版本） |
| T-14-09 | Denial of Service | 部署过程中数据丢失 | mitigate | 部署前自动备份（12 小时内跳过） |
| T-14-10 | Tampering | 回滚文件被篡改 | accept | 回滚文件在 /tmp，服务器被入侵时回滚已不是主要问题 |

</threat_model>

<verification>
1. `grep -c 'save_image_tags\|save_app_image_tags' scripts/deploy/deploy-infrastructure-prod.sh scripts/deploy/deploy-apps-prod.sh` — 两个文件都包含
2. `grep -c 'rollback_images\|rollback_app' scripts/deploy/deploy-infrastructure-prod.sh scripts/deploy/deploy-apps-prod.sh` — 两个文件都包含
3. `grep 'check_recent_backup' scripts/deploy/deploy-infrastructure-prod.sh` — 存在
4. `grep 'docker exec noda-ops /app/backup/backup-postgres.sh' scripts/deploy/deploy-infrastructure-prod.sh` — 存在
5. `bash -n scripts/deploy/deploy-infrastructure-prod.sh && echo "Syntax OK"` — OK
6. `bash -n scripts/deploy/deploy-apps-prod.sh && echo "Syntax OK"` — OK
</verification>

<success_criteria>
- deploy-infrastructure-prod.sh 步骤扩展为 7 步（验证 -> 保存镜像 -> 备份 -> 重启 -> 健康检查 -> Keycloak -> 最终验证）
- deploy-apps-prod.sh 包含镜像保存和回滚逻辑
- 两个脚本在失败时触发回滚
- 部署前备份在 12 小时内跳过，超过 12 小时自动执行
- bash 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/14-container-protection-and-deployment-safety/14-03-SUMMARY.md`
</output>
