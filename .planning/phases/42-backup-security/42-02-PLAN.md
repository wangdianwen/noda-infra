---
phase: 42-backup-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/utils/bfg-clean-history.sh
autonomous: false
requirements:
  - BACKUP-02

must_haves:
  truths:
    - "git-filter-repo 清理脚本可以自动删除 .env.production、.sops.yaml、config/secrets.sops.yaml 的历史"
    - "脚本执行前列出目标文件的提交次数供用户确认"
    - "脚本执行后验证目标文件不再出现在 git log 中"
    - "脚本生成验证报告证明清理成功"
  artifacts:
    - path: "scripts/utils/bfg-clean-history.sh"
      provides: "Git 历史敏感文件自动化清理脚本"
      contains: "git filter-repo"
      min_lines: 40
  key_links:
    - from: "scripts/utils/bfg-clean-history.sh"
      to: "git-filter-repo"
      via: "调用 git filter-repo --path ... --invert-paths --force"
      pattern: "git filter-repo.*--invert-paths.*--force"
    - from: "scripts/utils/bfg-clean-history.sh"
      to: "GitHub 远端"
      via: "git push --force --mirror origin（需用户确认）"
      pattern: "git push.*--force.*--mirror"
---

<objective>
创建 Git 历史敏感文件清理脚本，自动化执行 git-filter-repo 清理 + 验证 + force push。

Purpose: per D-04, D-05, D-06 — 清除 Git 历史中的 .env.production（3 次提交）、.sops.yaml、config/secrets.sops.yaml，使用脚本自动化整个流程
Output: scripts/utils/bfg-clean-history.sh 脚本（可执行，含分析、确认、清理、验证、推送步骤）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-backup-security/42-CONTEXT.md
@.planning/phases/42-backup-security/42-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 创建 git-filter-repo 清理脚本</name>
  <files>scripts/utils/bfg-clean-history.sh</files>
  <read_first>
    .planning/phases/42-backup-security/42-RESEARCH.md (Code Examples section)
    .planning/phases/42-backup-security/42-CONTEXT.md (D-04, D-05, D-06, D-07)
  </read_first>
  <action>
创建 `scripts/utils/bfg-clean-history.sh` 脚本，使用 git-filter-repo 清理 Git 历史中的敏感文件。

per D-04: 清理目标为 .env.production、.sops.yaml、config/secrets.sops.yaml
per D-05: 脚本自动化执行清理 + force push
per D-06: 执行后自动验证 + 生成验证报告
per D-07: docker/.env 不在清理范围内

脚本内容（必须包含以下步骤）：

```bash
#!/usr/bin/env bash
# Git 历史敏感文件清理脚本
# 使用 git-filter-repo 替代 BFG Repo Cleaner（无需 Java）
# 用途：从 Git 历史中彻底删除敏感文件
set -euo pipefail

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
NC='\033[0m'

info()  { echo -e "${GREEN}[INFO]${NC} $*"; }
warn()  { echo -e "${YELLOW}[WARN]${NC} $*"; }
error() { echo -e "${RED}[ERROR]${NC} $*" >&2; }

# 目标文件列表（per D-04, D-07）
TARGET_FILES=(
    ".env.production"
    ".sops.yaml"
    "config/secrets.sops.yaml"
)

echo "=========================================="
echo "Git 历史敏感文件清理"
echo "=========================================="
echo ""
echo "将清除以下文件的完整历史:"
for f in "${TARGET_FILES[@]}"; do
    count=$(git log --all --oneline -- "$f" 2>/dev/null | wc -l | tr -d ' ')
    echo "  - $f ($count 次提交)"
done
echo ""
echo "注意: docker/.env 从未被 git 追踪，无需清理 (per D-07)"
echo ""
warn "此操作将重写 Git 历史，不可逆！"
warn "所有 commit hash 将会改变。"
echo ""

# 步骤 1: 确认
read -p "确认继续？(输入 yes 继续): " confirm
[[ "$confirm" != "yes" ]] && echo "已取消" && exit 0

# 步骤 2: 检查 git-filter-repo
if ! command -v git-filter-repo &>/dev/null; then
    error "git-filter-repo 未安装"
    error "安装: brew install git-filter-repo"
    exit 1
fi

# 步骤 3: 检查未提交的更改
if ! git diff --quiet 2>/dev/null || ! git diff --cached --quiet 2>/dev/null; then
    error "存在未提交的更改，请先提交或暂存"
    git status --short
    exit 1
fi

# 步骤 4: 执行清理（per D-05）
info "执行 git-filter-repo 清理..."
git filter-repo \
    --path .env.production \
    --path .sops.yaml \
    --path config/secrets.sops.yaml \
    --invert-paths \
    --force

# 步骤 5: 验证（per D-06）
echo ""
info "=== 验证结果 ==="
all_clean=true
for f in "${TARGET_FILES[@]}"; do
    result=$(git log --all --oneline -- "$f" 2>/dev/null || true)
    if [[ -z "$result" ]]; then
        echo "  [OK] $f 已从历史中清除"
    else
        echo "  [FAIL] $f 仍存在于历史中:"
        echo "$result"
        all_clean=false
    fi
done

# 步骤 6: 生成验证报告
REPORT_FILE="git-history-cleanup-report.txt"
echo "Git 历史清理验证报告" > "$REPORT_FILE"
echo "生成时间: $(date '+%Y-%m-%d %H:%M:%S')" >> "$REPORT_FILE"
echo "清理文件: ${TARGET_FILES[*]}" >> "$REPORT_FILE"
echo "" >> "$REPORT_FILE"
for f in "${TARGET_FILES[@]}"; do
    result=$(git log --all --oneline -- "$f" 2>/dev/null || true)
    if [[ -z "$result" ]]; then
        echo "[OK] $f 已清除" >> "$REPORT_FILE"
    else
        echo "[FAIL] $f 未清除" >> "$REPORT_FILE"
    fi
done

if [[ "$all_clean" == true ]]; then
    echo ""
    info "验证通过！所有敏感文件已从历史中清除。"
    info "验证报告: $REPORT_FILE"
    echo ""
    warn "下一步需要手动执行 force push 更新远端:"
    echo "  git push --force --mirror origin"
    echo ""
    echo "注意: 如果在其他机器上有此仓库的克隆，需要重新克隆。"
else
    echo ""
    error "验证失败，部分文件未清除。请检查报告: $REPORT_FILE"
    exit 1
fi
```

关键实现细节：
- `--invert-paths` 表示排除指定路径（即删除这些文件）
- `--force` 覆盖 git-filter-repo 的 fresh-clone 检查（当前是完整工作仓库）
- force push 使用 `--mirror` 确保所有 ref 都更新
- 验证报告输出到 git-history-cleanup-report.txt
  </action>
  <verify>
    <automated>test -x scripts/utils/bfg-clean-history.sh && grep -q 'git filter-repo' scripts/utils/bfg-clean-history.sh && grep -q '\.env\.production' scripts/utils/bfg-clean-history.sh && grep -q '\.sops\.yaml' scripts/utils/bfg-clean-history.sh && grep -q 'config/secrets\.sops\.yaml' scripts/utils/bfg-clean-history.sh && grep -q '\-\-invert-paths' scripts/utils/bfg-clean-history.sh && grep -q '\-\-force' scripts/utils/bfg-clean-history.sh && grep -q 'git push.*--force.*--mirror' scripts/utils/bfg-clean-history.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - scripts/utils/bfg-clean-history.sh 可执行
    - 脚本包含 3 个目标文件（.env.production, .sops.yaml, config/secrets.sops.yaml）
    - 脚本包含执行前确认、git-filter-repo 清理、自动验证、验证报告生成
    - 脚本包含 force push 提示（不自动执行，需用户手动确认）
    - 不包含 docker/.env（per D-07）
  </done>
  <what-built>Git 历史敏感文件清理脚本（scripts/utils/bfg-clean-history.sh），使用 git-filter-repo 清除 .env.production、.sops.yaml、config/secrets.sops.yaml 的完整历史。</what-built>
  <how-to-verify>
    1. 检查脚本内容：`cat scripts/utils/bfg-clean-history.sh` 确认包含所有目标文件和 git-filter-repo 命令
    2. 检查脚本可执行：`ls -la scripts/utils/bfg-clean-history.sh` 确认有执行权限
    3. **不要执行脚本**（执行会重写 Git 历史，待部署后由用户决定执行时机）
  </how-to-verify>
  <resume-signal>Type "approved" 确认脚本内容正确，或描述需要修改的部分</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地 Git 仓库 → GitHub 远端 | force push 重写远端历史，所有协作者必须重新克隆 |
| Git 历史重写操作 | 不可逆操作，所有 commit hash 改变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Information Disclosure | .env.production 含过期 JWT (VERCEL_OIDC_TOKEN) | mitigate | git-filter-repo 彻底重写历史 + force push，GitHub CDN 缓存会在 24h 内清除 |
| T-42-07 | Information Disclosure | .sops.yaml + config/secrets.sops.yaml 含加密配置 | mitigate | 虽然内容已加密，但清理历史是最佳实践；密钥已轮换 |
| T-42-08 | Tampering | Force push 后仓库状态不一致 | mitigate | 脚本验证清理结果；项目仅一个协作者（wangdianwen），无同步风险 |
| T-42-09 | Denial of Service | Force push 失败导致远端与本地不一致 | accept | 脚本不自动执行 force push，由用户手动执行 |

</threat_model>

<verification>
1. 脚本存在且可执行：`test -x scripts/utils/bfg-clean-history.sh`
2. 包含 git-filter-repo 命令：`grep 'git filter-repo.*--invert-paths.*--force' scripts/utils/bfg-clean-history.sh`
3. 包含所有 3 个目标文件：`grep -c '\.env\.production\|\.sops\.yaml\|config/secrets\.sops\.yaml' scripts/utils/bfg-clean-history.sh` 应 >= 3
4. 包含验证逻辑：`grep 'git log --all' scripts/utils/bfg-clean-history.sh`
5. 包含 force push 提示：`grep 'git push.*--force.*--mirror' scripts/utils/bfg-clean-history.sh`
6. 不包含 docker/.env：`grep 'docker/\.env' scripts/utils/bfg-clean-history.sh` 无输出
</verification>

<success_criteria>
- scripts/utils/bfg-clean-history.sh 已创建并具有可执行权限
- 脚本目标文件列表包含 .env.production、.sops.yaml、config/secrets.sops.yaml
- 脚本不包含 docker/.env（per D-07）
- 脚本包含执行前确认、清理、验证、报告生成的完整流程
- 脚本不自动执行 force push（由用户手动确认）
</success_criteria>

<output>
After completion, create `.planning/phases/42-backup-security/42-02-SUMMARY.md`
</output>
