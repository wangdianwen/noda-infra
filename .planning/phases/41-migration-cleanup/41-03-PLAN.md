---
phase: 41-migration-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["41-01", "41-02"]
files_modified:
  - docker/.env (DELETE)
  - .env.production (DELETE)
  - .sops.yaml (DELETE)
  - config/secrets.sops.yaml (DELETE)
  - scripts/utils/decrypt-secrets.sh (DELETE)
  - scripts/deploy/deploy-findclass-zero-deps.sh (DELETE)
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-04]

must_haves:
  truths:
    - "docker/.env 和 .env.production 已从文件系统删除"
    - "所有 SOPS 相关文件已删除（.sops.yaml、config/secrets.sops.yaml、decrypt-secrets.sh）"
    - "已废弃脚本 deploy-findclass-zero-deps.sh 已删除"
    - "备份系统 scripts/backup/.env.backup 保持不变"
    - "模板文件 config/environments/ 保持不变"
  artifacts:
    - path: "scripts/backup/.env.backup"
      provides: "备份系统独立密钥"
      exists: true
    - path: "docker/.env"
      provides: "（已删除）"
      exists: false
    - path: ".env.production"
      provides: "（已删除）"
      exists: false
    - path: ".sops.yaml"
      provides: "（已删除）"
      exists: false
    - path: "config/secrets.sops.yaml"
      provides: "（已删除）"
      exists: false
  key_links:
    - from: "scripts/verify-doppler-secrets.sh"
      to: "Doppler API"
      via: "doppler secrets --only-names"
      pattern: "doppler secrets"
---

<objective>
验证 Doppler 密钥完整后删除明文文件和 SOPS 相关文件

Purpose: 在 Plan 01（验证脚本更新）和 Plan 02（SOPS 引用清理）完成后，执行最终的文件删除。确保 Doppler 中密钥完整后，安全删除所有明文 .env 文件和 SOPS 相关代码。
Output: 6 个文件被 git rm 删除
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-migration-cleanup/41-CONTEXT.md
@.planning/phases/41-migration-cleanup/41-RESEARCH.md
@.planning/phases/41-migration-cleanup/41-01-SUMMARY.md
@.planning/phases/41-migration-cleanup/41-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 Doppler 密钥完整性后删除明文 .env 文件</name>
  <files>docker/.env, .env.production</files>
  <read_first>
    - docker/.env（确认存在，记录当前内容以便最终验证）
    - .env.production（确认存在）
    - scripts/verify-doppler-secrets.sh（Plan 01 更新后的版本）
  </read_first>
  <action>
per D-01, D-02, D-07, D-08:

**前置条件验证：**
注意：verify-doppler-secrets.sh 需要有效的 DOPPLER_TOKEN 才能运行。在本地开发环境可能没有 token。此验证在服务器上执行时通过 DOPPLER_TOKEN 认证。

1. 确认 verify-doppler-secrets.sh 脚本存在且语法正确：
   ```bash
   bash -n scripts/verify-doppler-secrets.sh
   ```

2. 确认 docker/.env 和 .env.production 当前存在：
   ```bash
   ls -la docker/.env .env.production
   ```

3. 如果 DOPPLER_TOKEN 环境变量可用，运行验证脚本：
   ```bash
   DOPPLER_TOKEN='dp.st.prd.xxx' bash scripts/verify-doppler-secrets.sh
   ```
   验证必须通过（所有 17 个密钥在 Doppler 中）。

   如果 DOPPLER_TOKEN 不可用（本地开发环境），跳过远程验证，直接记录：
   "跳过 Doppler 验证（DOPPLER_TOKEN 未设置）。在服务器部署时需先运行 verify-doppler-secrets.sh 确认密钥完整。"

4. 删除明文密钥文件（per D-07, D-08）：
   ```bash
   git rm docker/.env .env.production
   ```

5. 确认备份系统不受影响（per D-09, MIGR-02）：
   ```bash
   ls -la scripts/backup/.env.backup
   ```
   必须存在。

6. 确认模板文件保留（per D-10）：
   ```bash
   ls -la config/environments/.env.production.template config/environments/.env.example
   ```
   必须存在。
  </action>
  <acceptance_criteria>
    - `ls docker/.env 2>&1` 输出 "No such file or directory"（已删除）
    - `ls .env.production 2>&1` 输出 "No such file or directory"（已删除）
    - `ls scripts/backup/.env.backup` 文件存在（备份系统不受影响）
    - `ls config/environments/.env.production.template` 文件存在（模板保留）
    - `git status --short` 显示 "D docker/.env" 和 "D .env.production"（已 git rm）
  </acceptance_criteria>
  <verify>
    <automated>! test -f docker/.env && ! test -f .env.production && test -f scripts/backup/.env.backup && test -f config/environments/.env.production.template</automated>
  </verify>
  <done>docker/.env 和 .env.production 已通过 git rm 删除，备份系统 .env.backup 和模板文件完好</done>
</task>

<task type="auto">
  <name>Task 2: 删除 SOPS 相关文件和废弃脚本</name>
  <files>.sops.yaml, config/secrets.sops.yaml, scripts/utils/decrypt-secrets.sh, scripts/deploy/deploy-findclass-zero-deps.sh</files>
  <read_first>
    - .sops.yaml（确认存在，包含 age 公钥）
    - config/secrets.sops.yaml（确认存在，包含加密密钥）
    - scripts/utils/decrypt-secrets.sh（确认存在，SOPS 解密脚本）
    - scripts/deploy/deploy-findclass-zero-deps.sh（确认存在，已标记废弃）
  </read_first>
  <action>
per D-12, D-13:

1. 确认所有待删除文件存在：
   ```bash
   ls -la .sops.yaml config/secrets.sops.yaml scripts/utils/decrypt-secrets.sh scripts/deploy/deploy-findclass-zero-deps.sh
   ```

2. 使用 git rm 删除 SOPS 核心文件（per D-12）：
   ```bash
   git rm .sops.yaml config/secrets.sops.yaml scripts/utils/decrypt-secrets.sh
   ```

3. 使用 git rm 删除已废弃脚本（per D-13）：
   ```bash
   git rm scripts/deploy/deploy-findclass-zero-deps.sh
   ```

4. 最终验证 — 确认文件系统中无 SOPS 残留：
   ```bash
   # 验证删除结果
   ls .sops.yaml 2>&1 | grep -q "No such file"
   ls config/secrets.sops.yaml 2>&1 | grep -q "No such file"
   ls scripts/utils/decrypt-secrets.sh 2>&1 | grep -q "No such file"
   ls scripts/deploy/deploy-findclass-zero-deps.sh 2>&1 | grep -q "No such file"
   ```

5. 全局 grep 扫描确认无 SOPS 引用残留：
   ```bash
   grep -rl 'sops\|SOPS\|decrypt-secrets' scripts/ --include='*.sh' 2>/dev/null
   ```
   应无输出（Plan 02 已清理所有脚本引用）。

6. 确认 config/keys/ 目录保留（age 私钥仍用于 backup-doppler-secrets.sh）：
   ```bash
   ls config/keys/git-age-key.txt 2>/dev/null
   ```
   文件应存在（backup-doppler-secrets.sh 仍需 age 加密）。

7. 确认 scripts/backup/templates/ 和 scripts/backup/.env.example 保留（per D-11）：
   ```bash
   ls scripts/backup/templates/.env.backup scripts/backup/.env.example
   ```
  </action>
  <acceptance_criteria>
    - `ls .sops.yaml 2>&1` 输出包含 "No such file"（已删除）
    - `ls config/secrets.sops.yaml 2>&1` 输出包含 "No such file"（已删除）
    - `ls scripts/utils/decrypt-secrets.sh 2>&1` 输出包含 "No such file"（已删除）
    - `ls scripts/deploy/deploy-findclass-zero-deps.sh 2>&1` 输出包含 "No such file"（已删除）
    - `grep -rl 'sops\|SOPS' scripts/ --include='*.sh' 2>/dev/null` 无输出
    - `test -f config/keys/git-age-key.txt` 退出码为 0（age 密钥保留）
    - `test -f scripts/backup/templates/.env.backup` 退出码为 0（备份模板保留）
  </acceptance_criteria>
  <verify>
    <automated>! test -f .sops.yaml && ! test -f config/secrets.sops.yaml && ! test -f scripts/utils/decrypt-secrets.sh && ! test -f scripts/deploy/deploy-findclass-zero-deps.sh && ! grep -rq 'sops\|SOPS' scripts/ --include='*.sh'</automated>
  </verify>
  <done>SOPS 核心文件（.sops.yaml、config/secrets.sops.yaml、decrypt-secrets.sh）和废弃脚本（deploy-findclass-zero-deps.sh）已删除，scripts/ 中无 SOPS 引用残留，age 密钥和备份模板保留</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → Git | 文件通过 git rm 删除，同时从 Git 跟踪中移除 |
| Doppler → 部署 | 删除明文文件后，Doppler 成为唯一密钥源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Denial of Service | docker/.env 删除 | mitigate | 删除前 verify-doppler-secrets.sh 确认 Doppler 中密钥完整（per D-01） |
| T-41-02 | Information Disclosure | 明文密钥残留 | mitigate | git rm 确保从 Git 跟踪中移除，文件系统不再存在明文密钥 |
| T-41-04 | Information Disclosure | SOPS 代码残留 | mitigate | 全局 grep 扫描 scripts/ 和 docs/ 确认无 SOPS/sops/decrypt-secrets 引用 |
</threat_model>

<verification>
1. `! test -f docker/.env` — 明文密钥已删除
2. `! test -f .env.production` — 前端密钥已删除
3. `! test -f .sops.yaml` — SOPS 配置已删除
4. `! test -f config/secrets.sops.yaml` — 加密密钥文件已删除
5. `! test -f scripts/utils/decrypt-secrets.sh` — 解密脚本已删除
6. `! test -f scripts/deploy/deploy-findclass-zero-deps.sh` — 废弃脚本已删除
7. `test -f scripts/backup/.env.backup` — 备份系统密钥完好
8. `test -f config/environments/.env.production.template` — 模板文件完好
9. `! grep -rq 'sops\|SOPS' scripts/ --include='*.sh'` — 无 SOPS 代码残留
10. `! grep -rq 'sops\|SOPS' docs/ README.md --include='*.md'` — 无 SOPS 文档残留（Plan 02 已清理）
</verification>

<success_criteria>
- docker/.env 和 .env.production 已删除（文件系统 + Git 跟踪）
- .sops.yaml、config/secrets.sops.yaml、scripts/utils/decrypt-secrets.sh 已删除
- scripts/deploy/deploy-findclass-zero-deps.sh 已删除
- scripts/backup/.env.backup 保持不变（per MIGR-02）
- 模板文件（config/environments/）保持不变
- 全局 grep 扫描 scripts/ 和 docs/ 无 SOPS 引用
- config/keys/git-age-key.txt 保留（backup-doppler-secrets.sh 使用）
</success_criteria>

<output>
After completion, create `.planning/phases/41-migration-cleanup/41-03-SUMMARY.md`
</output>
