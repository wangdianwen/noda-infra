---
phase: 41-migration-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup-keycloak-full.sh
  - scripts/deploy/deploy-infrastructure-prod.sh
  - docs/secrets-management.md
  - docs/DEVELOPMENT.md
  - docs/GETTING-STARTED.md
  - docs/CONFIGURATION.md
  - docs/KEYCLOAK_SCRIPTS.md
  - docs/DEPLOYMENT_GUIDE.md
  - docs/architecture.md
  - docs/README.md
  - README.md
  - .gitignore
autonomous: true
requirements: [MIGR-04]

must_haves:
  truths:
    - "所有脚本中无 SOPS/sops/decrypt-secrets 引用"
    - "所有文档描述 Doppler 密钥管理方案而非 SOPS"
    - ".gitignore 中无 SOPS 相关模式"
  artifacts:
    - path: "scripts/setup-keycloak-full.sh"
      provides: "从环境变量获取 Google OAuth 凭据"
      contains: "GOOGLE_CLIENT_ID"
      not_contains: "sops"
    - path: ".gitignore"
      provides: "干净的忽略规则"
      not_contains: ".sops.yaml"
  key_links:
    - from: "scripts/setup-keycloak-full.sh"
      to: "环境变量（Doppler 注入）"
      via: "GOOGLE_CLIENT_ID/SECRET 环境变量"
      pattern: "GOOGLE_CLIENT_ID.*:-"
---

<objective>
清理脚本和文档中的 SOPS 引用，更新 .gitignore

Purpose: 移除所有对废弃 SOPS 加密体系的引用，更新文档为 Doppler 密钥管理方案，清理 .gitignore 中不再需要的 SOPS 相关模式。与 Plan 01 并行执行。
Output: 更新后的脚本、文档、.gitignore
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-migration-cleanup/41-CONTEXT.md
@.planning/phases/41-migration-cleanup/41-RESEARCH.md

<interfaces>
<!-- scripts/setup-keycloak-full.sh — 当前 SOPS 解密逻辑 -->

当前步骤 1（第 22-38 行）：
```bash
log_info "步骤 1/6: 解密 Google OAuth 凭据"
export SOPS_AGE_KEY_FILE="$PROJECT_ROOT/config/keys/git-age-key.txt"
SECRETS=$(sops --decrypt "$PROJECT_ROOT/config/secrets.sops.yaml" 2>/dev/null)
GOOGLE_CLIENT_ID=$(echo "$SECRETS" | grep "google_oauth_client_id:" | cut -d' ' -f2)
GOOGLE_CLIENT_SECRET=$(echo "$SECRETS" | grep "google_oauth_client_secret:" | cut -d' ' -f2)
```

改造后（per D-14）：
```bash
log_info "步骤 1/6: 获取 Google OAuth 凭据"
GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID:-}"
GOOGLE_CLIENT_SECRET="${GOOGLE_CLIENT_SECRET:-}"
if [ -z "$GOOGLE_CLIENT_ID" ] || [ -z "$GOOGLE_CLIENT_SECRET" ]; then
    log_error "缺少 Google OAuth 凭据（GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET）"
    log_error "请确保 DOPPLER_TOKEN 已设置，且 Doppler 中已配置这些密钥"
    exit 1
fi
log_success "凭据已从环境变量获取"
log_info "Client ID: ${GOOGLE_CLIENT_ID:0:20}..."
```

<!-- scripts/deploy/deploy-infrastructure-prod.sh — 第 216 行残留 SOPS 注释 -->
当前第 216 行：
```
# config/secrets.sops.yaml 检查已移除（SOPS 将在 Phase 41 清理）
```
需要删除此行（per D-17）。

<!-- .gitignore — 当前 SOPS 相关条目 -->
需要移除的行（per D-16）：
- 第 22 行: `*.decrypted`（SOPS 解密产物）
- 第 33 行: `config/secrets.sops.yaml.bak`（SOPS 备份）
- 第 34 行: `config/keys/`（age 密钥目录 — 注意 config/keys/ 仍需保留，但不再需要忽略整个目录，因为 age 密钥用于 backup-doppler-secrets.sh 加密。实际应保留此条目以防止误提交私钥）
- 第 37 行: `!config/secrets.sops.yaml`（SOPS 加密文件例外）
- 第 38 行: `config/secrets.local.yaml`（SOPS 本地密钥）

需要保留的行：
- 第 2 行: `docker/.env`（隐含在 `.env` 通配符中，但第 32 行注释 "# 未加密的环境配置" 下没有显式 docker/.env。实际上 `.env` 通配符已覆盖，但需要确认）
- `scripts/backup/.env.backup`（第 29 行，保留）

<!-- 文档中的 SOPS 引用分布（grep 扫描结果） -->
7 个文档文件中有 SOPS 引用：
1. docs/secrets-management.md — 完全重写为 Doppler 密钥管理文档
2. docs/DEVELOPMENT.md — 移除 SOPS 相关章节和引用
3. docs/GETTING-STARTED.md — 移除 SOPS/age 前置要求
4. docs/CONFIGURATION.md — 更新密钥文件表和加密章节
5. docs/KEYCLOAK_SCRIPTS.md — 移除 SOPS 解密说明
6. docs/DEPLOYMENT_GUIDE.md — 移除 SOPS 解密步骤
7. docs/architecture.md — 更新密钥管理描述
8. docs/README.md — 移除 SOPS 相关链接和说明
9. README.md — 移除 SOPS 依赖提及
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 setup-keycloak-full.sh 和 deploy-infrastructure-prod.sh 移除 SOPS 引用</name>
  <files>scripts/setup-keycloak-full.sh, scripts/deploy/deploy-infrastructure-prod.sh</files>
  <read_first>
    - scripts/setup-keycloak-full.sh（完整文件，特别是第 22-38 行 SOPS 解密逻辑）
    - scripts/deploy/deploy-infrastructure-prod.sh（第 216 行 SOPS 注释）
  </read_first>
  <action>
per D-14 和 D-17:

**scripts/setup-keycloak-full.sh:**

1. 替换步骤 1（第 22-38 行）：
   移除所有 SOPS 相关代码（SOPS_AGE_KEY_FILE、sops --decrypt、SECRETS 变量），改为从环境变量读取：
   ```bash
   log_info "步骤 1/6: 获取 Google OAuth 凭据"

   # 密钥由 Doppler 通过 load_secrets() 注入到环境变量中
   GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID:-}"
   GOOGLE_CLIENT_SECRET="${GOOGLE_CLIENT_SECRET:-}"

   if [ -z "$GOOGLE_CLIENT_ID" ] || [ -z "$GOOGLE_CLIENT_SECRET" ]; then
       log_error "缺少 Google OAuth 凭据（GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET）"
       log_error "请确保 DOPPLER_TOKEN 已设置，且 Doppler 中已配置这些密钥"
       exit 1
   fi

   log_success "凭据已从环境变量获取"
   log_info "Client ID: ${GOOGLE_CLIENT_ID:0:20}..."
   ```

2. 在文件头部添加 source secrets.sh（如果尚未存在）：
   在第 13 行 `source "$SCRIPT_DIR/lib/log.sh"` 之后添加：
   ```bash
   source "$SCRIPT_DIR/lib/secrets.sh"
   ```

3. 在步骤 1 之前（步骤 1 改造后的代码之前）添加 load_secrets 调用：
   ```bash
   # 加载密钥（Doppler Only）
   load_secrets
   ```
   或者将 load_secrets() 放在步骤 1 中，让 GOOGLE_CLIENT_ID/SECRET 从 Doppler 注入的环境变量中获取。

**scripts/deploy/deploy-infrastructure-prod.sh:**

1. 删除第 216 行：
   ```
   # config/secrets.sops.yaml 检查已移除（SOPS 将在 Phase 41 清理）
   ```
   （这行是残留的 SOPS 注释，per D-17 需要移除）
  </action>
  <acceptance_criteria>
    - `grep -i 'sops\|decrypt' scripts/setup-keycloak-full.sh` 无输出
    - `grep 'SOPS_AGE_KEY_FILE' scripts/setup-keycloak-full.sh` 无输出
    - `grep 'GOOGLE_CLIENT_ID.*:-' scripts/setup-keycloak-full.sh` 输出包含环境变量读取
    - `grep 'load_secrets' scripts/setup-keycloak-full.sh` 输出包含调用
    - `grep -i 'sops' scripts/deploy/deploy-infrastructure-prod.sh` 无输出
    - `bash -n scripts/setup-keycloak-full.sh` 退出码为 0
    - `bash -n scripts/deploy/deploy-infrastructure-prod.sh` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/setup-keycloak-full.sh && bash -n scripts/deploy/deploy-infrastructure-prod.sh && ! grep -qi 'sops' scripts/setup-keycloak-full.sh && ! grep -qi 'sops' scripts/deploy/deploy-infrastructure-prod.sh</automated>
  </verify>
  <done>setup-keycloak-full.sh 从环境变量获取 Google OAuth 凭据（不再使用 SOPS），deploy-infrastructure-prod.sh 无 SOPS 残留注释</done>
</task>

<task type="auto">
  <name>Task 2: 更新文档和 .gitignore 清理 SOPS 引用</name>
  <files>docs/secrets-management.md, docs/DEVELOPMENT.md, docs/GETTING-STARTED.md, docs/CONFIGURATION.md, docs/KEYCLOAK_SCRIPTS.md, docs/DEPLOYMENT_GUIDE.md, docs/architecture.md, docs/README.md, README.md, .gitignore</files>
  <read_first>
    - docs/secrets-management.md（完整文件 — 需完全重写）
    - docs/DEVELOPMENT.md（需定位 SOPS 相关行）
    - docs/GETTING-STARTED.md（需定位 SOPS 相关行）
    - docs/CONFIGURATION.md（需定位 SOPS 相关行）
    - docs/KEYCLOAK_SCRIPTS.md（需定位 SOPS 相关行）
    - docs/DEPLOYMENT_GUIDE.md（需定位 SOPS 相关行）
    - docs/architecture.md（需定位 SOPS 相关行）
    - docs/README.md（需定位 SOPS 相关行）
    - README.md（需定位 SOPS 相关行）
    - .gitignore（完整文件）
  </read_first>
  <action>
per D-15, D-16:

**docs/secrets-management.md — 完全重写为 Doppler 密钥管理文档：**

替换整个文件内容为 Doppler 密钥管理方案文档，包含以下章节：
1. 概述 — 项目使用 Doppler 集中管理密钥
2. 架构 — 密钥流：Doppler CLI → load_secrets() → shell 环境变量 → Docker Compose
3. Doppler 项目配置 — 项目名 noda，环境 prd
4. 密钥列表 — 分组列出所有密钥（基础设施 / 应用 / Google OAuth）
5. 使用方式 — Jenkins Pipeline（自动注入 DOPPLER_TOKEN）+ 手动部署（export DOPPLER_TOKEN）
6. 离线备份 — backup-doppler-secrets.sh（age 加密 + B2 上传）
7. 新环境设置 — install-doppler.sh + Service Token 配置
8. 故障排除 — 常见问题和解决方案

**docs/DEVELOPMENT.md — 移除/替换 SOPS 引用：**

根据 grep 扫描结果，需要修改的行：
- 第 15 行: `| SOPS | 最新版 | 密钥解密（仅生产环境需要） |` → 改为 `| Doppler CLI | 3.x+ | 密钥拉取（生产环境需要） |`
- 第 16 行: `| age | 最新版 | SOPS 密钥加密工具 |` → 改为 `| age | 1.3+ | Doppler 备份加密 |`
- 第 46 行: `生产环境使用 config/secrets.sops.yaml（SOPS 加密）...` → 改为 `生产环境通过 Doppler CLI 拉取密钥（scripts/lib/secrets.sh load_secrets()）`
- 第 60 行: `生产部署时，通过 scripts/utils/decrypt-secrets.sh 解密...` → 改为 `生产部署时，通过 scripts/lib/secrets.sh load_secrets() 从 Doppler 拉取密钥`
- 第 142 行: `scripts/deploy/deploy-findclass-zero-deps.sh` 行 → 删除（脚本已废弃）
- 第 151 行: `scripts/utils/decrypt-secrets.sh` 行 → 删除（脚本已删除）
- 第 252 行: `config/secrets.sops.yaml（SOPS + age 加密，提交到 Git）` → 改为 `Doppler 云端密钥管理（project=noda, config=prd）`
- 第 255 行: `通过 scripts/utils/decrypt-secrets.sh 解密到临时目录...` → 改为 `通过 scripts/lib/secrets.sh load_secrets() 从 Doppler 拉取到环境变量`
- 第 283 行: 移除 SOPS 相关描述
- 第 298 行: 移除 SOPS 解密密钥相关说明

**docs/GETTING-STARTED.md — 移除 SOPS 前置要求：**

- 第 18-19 行: 移除 SOPS 和 age 前置要求行，添加 Doppler CLI
- 第 21 行: 更新说明为 "开发环境只需 Docker 和 Docker Compose。Doppler CLI 仅在生产部署时需要。"
- 第 207-228 行: "问题 4: SOPS 解密失败" 章节改为 "问题 4: Doppler 密钥拉取失败"
- 第 265 行: 更新密钥管理链接描述

**docs/CONFIGURATION.md — 更新密钥相关描述：**

- 第 156 行: `config/secrets.sops.yaml` → 改为 Doppler 云端
- 第 158 行: `config/keys/git-age-key.txt` → 保留（age 密钥仍用于 backup-doppler-secrets.sh），更新说明
- 第 160 行: 更新密钥来源描述
- 第 263-300 行: 更新加密章节为 Doppler 描述

**docs/KEYCLOAK_SCRIPTS.md — 移除 SOPS 解密说明：**

- 第 189-194 行: 移除 sops --decrypt 命令示例
- 第 234 行: 更新步骤描述，移除 "解密 secrets.sops.yaml"

**docs/DEPLOYMENT_GUIDE.md — 移除 SOPS 解密步骤：**

- 第 47/81/91 行: 移除 config/secrets.sops.yaml 引用
- 第 107-110 行: 移除 sops --decrypt 命令
- 第 309-324 行: "问题 4: SOPS 解密失败" 改为 Doppler 故障排除
- 第 371 行: vim config/secrets.sops.yaml 改为 doppler 命令
- 第 382-384 行: 更新验证清单

**docs/architecture.md — 更新密钥管理描述：**

- 第 77 行: `SOPS + age` → 改为 `Doppler`
- 第 89 行: `secrets.sops.yaml` → 改为 Doppler 云端

**docs/README.md — 移除 SOPS 相关链接：**

- 第 30 行: `SOPS + age 加密` → 改为 `Doppler 密钥管理`
- 第 102 行: 移除 SOPS 加密工具链接
- 第 131-136 行: 更新密钥管理命令为 Doppler

**README.md — 移除 SOPS 依赖：**

- 第 25 行: `SOPS 3.12.2 + age 1.3.1（密钥加密，生产环境需要）` → 改为 `Doppler CLI（密钥管理，生产环境需要）`
- 第 100 行: `utils/... decrypt-secrets.sh` → 移除 decrypt-secrets.sh
- 第 140 行: `密钥管理 ... SOPS + age 密钥加密方案` → 改为 `密钥管理 ... Doppler 密钥管理方案`

**.gitignore — 清理 SOPS 相关模式（per D-16）：**

移除以下行：
- `*.decrypted`（第 22 行，SOPS 解密产物）
- `config/secrets.sops.yaml.bak`（第 33 行，SOPS 备份）
- `!config/secrets.sops.yaml`（第 37 行，SOPS 加密文件例外）
- `config/secrets.local.yaml`（第 38 行，SOPS 本地密钥）

保留以下行：
- `docker/.env` — 防止未来误提交（通过 `.env` 通配符覆盖）
- `scripts/backup/.env.backup` — 备份系统密钥
- `config/keys/` — age 私钥仍用于 backup-doppler-secrets.sh，保留忽略规则
- `config/nginx/snippets/upstream-*.conf` — 蓝绿配置
  </action>
  <acceptance_criteria>
    - `grep -rl 'sops\|SOPS' docs/ README.md --include='*.md' | grep -v '.planning/'` 无输出（文档中无 SOPS 引用）
    - `grep -c 'Doppler' docs/secrets-management.md` 至少返回 3（重写后文档提到 Doppler）
    - `grep '.sops.yaml' .gitignore` 无输出
    - `grep 'decrypt-secrets' .gitignore` 无输出
    - `grep 'docker/.env' .gitignore` 输出包含 .env（保留 docker/.env 忽略）
    - `grep 'config/keys/' .gitignore` 输出包含 config/keys/（保留 age 密钥忽略）
    - `bash -n scripts/setup-keycloak-full.sh` 退出码为 0（同 Task 1 联合验证）
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq 'sops\|SOPS' docs/ README.md --include='*.md' && ! grep -q '.sops.yaml' .gitignore && grep -q 'Doppler' docs/secrets-management.md</automated>
  </verify>
  <done>所有文档中无 SOPS 引用，docs/secrets-management.md 已重写为 Doppler 文档，.gitignore 已清理 SOPS 模式</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 用户操作 | 文档指导用户操作方式，错误文档导致操作失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | Information Disclosure | README.md | accept | 移除 SOPS 引用不引入新风险，仅降低文档过时导致的混淆 |
</threat_model>

<verification>
1. `grep -rl 'sops\|SOPS' docs/ README.md --include='*.md' | grep -v '.planning/'` — 无输出
2. `! grep -qi 'sops' scripts/setup-keycloak-full.sh` — 无 SOPS 引用
3. `! grep -qi 'sops' scripts/deploy/deploy-infrastructure-prod.sh` — 无 SOPS 残留
4. `! grep '.sops.yaml' .gitignore` — gitignore 已清理
5. `grep 'Doppler' docs/secrets-management.md | wc -l` — 至少 3 处 Doppler 引用
</verification>

<success_criteria>
- 所有 7 个文档 + README.md + docs/README.md 中无 sops/SOPS/decrypt-secrets 引用
- docs/secrets-management.md 已完全重写为 Doppler 密钥管理方案
- setup-keycloak-full.sh 从环境变量获取 Google OAuth 凭据
- deploy-infrastructure-prod.sh 无 SOPS 注释残留
- .gitignore 移除 SOPS 模式但保留 docker/.env 和 config/keys/ 忽略
- 所有修改的脚本 bash -n 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/41-migration-cleanup/41-02-SUMMARY.md`
</output>
