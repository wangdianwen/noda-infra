---
phase: 41-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-doppler-secrets.sh
  - scripts/lib/secrets.sh
  - scripts/backup/backup-doppler-secrets.sh
autonomous: true
requirements: [MIGR-01, MIGR-03]

must_haves:
  truths:
    - "verify-doppler-secrets.sh 验证覆盖所有密钥（包括 GOOGLE_CLIENT_ID 和 GOOGLE_CLIENT_SECRET）"
    - "secrets.sh load_secrets() 不再回退 docker/.env，DOPPLER_TOKEN 未设置时报错退出"
    - "backup-doppler-secrets.sh 不再依赖 .sops.yaml 获取 age 公钥"
  artifacts:
    - path: "scripts/verify-doppler-secrets.sh"
      provides: "密钥完整性验证（含 Google OAuth）"
      contains: "GOOGLE_CLIENT_ID"
    - path: "scripts/lib/secrets.sh"
      provides: "Doppler-only 密钥加载"
      contains: "DOPPLER_TOKEN 未设置"
    - path: "scripts/backup/backup-doppler-secrets.sh"
      provides: "age 公钥硬编码的 Doppler 备份"
      contains: "age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w"
  key_links:
    - from: "scripts/lib/secrets.sh"
      to: "doppler CLI"
      via: "doppler secrets download --no-file --format=env"
      pattern: "doppler secrets download"
    - from: "scripts/backup/backup-doppler-secrets.sh"
      to: "age CLI"
      via: "硬编码公钥加密"
      pattern: "AGE_PUBLIC_KEY.*age1869smm"
---

<objective>
密钥验证扩展 + 密钥加载 Doppler-only 化 + backup 脚本公钥来源改造

Purpose: 为后续删除明文文件和 SOPS 代码做准备。扩展验证脚本确保 Doppler 中密钥完整，移除 secrets.sh 的 docker/.env 回退使 Doppler 成为唯一密钥源，更新 backup 脚本使其不依赖即将删除的 .sops.yaml。
Output: 3 个更新后的脚本文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-migration-cleanup/41-CONTEXT.md
@.planning/phases/41-migration-cleanup/41-RESEARCH.md

<interfaces>
<!-- scripts/lib/secrets.sh — 当前 load_secrets() 双模式实现 -->
<!-- Executor 必须先读取此文件了解当前实现 -->

当前 secrets.sh load_secrets() 逻辑：
1. DOPPLER_TOKEN 存在 → doppler secrets download --no-file --format=env --project noda --config prd
2. DOPPLER_TOKEN 不存在 → 回退 source docker/.env

改造目标（per D-03/D-04/D-05/D-06）：
1. DOPPLER_TOKEN 存在 → doppler secrets download（不变）
2. DOPPLER_TOKEN 不存在 → 输出 "[ERROR] DOPPLER_TOKEN 未设置。请 export DOPPLER_TOKEN=<service-token> 后重试。" 并 return 1

<!-- scripts/verify-doppler-secrets.sh — 当前 EXPECTED_SECRETS 列表 -->
当前 15 个预期密钥（不含 GOOGLE_CLIENT_ID/SECRET）：
POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, KEYCLOAK_ADMIN_USER, KEYCLOAK_ADMIN_PASSWORD, KEYCLOAK_DB_PASSWORD, CLOUDFLARE_TUNNEL_TOKEN, ANTHROPIC_AUTH_TOKEN, ANTHROPIC_BASE_URL, SMTP_HOST, SMTP_PORT, SMTP_FROM, SMTP_USER, SMTP_PASSWORD, RESEND_API_KEY

需要添加（per RESEARCH Pitfall 2/3）：
GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET

<!-- scripts/backup/backup-doppler-secrets.sh — 当前 age 公钥来源 -->
当前第 65-77 行从 .sops.yaml 读取 age 公钥：
```bash
AGE_PUBLIC_KEY="${AGE_PUBLIC_KEY:-}"
if [[ -z "$AGE_PUBLIC_KEY" ]]; then
    SOPS_FILE="${SOPS_FILE:-.sops.yaml}"
    if [[ -f "$SOPS_FILE" ]]; then
        AGE_PUBLIC_KEY=$(grep '^age:' "$SOPS_FILE" | awk '{print $2}')
    fi
fi
```

.sops.yaml 中 age 公钥值（第 4 行）：age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 verify-doppler-secrets.sh 添加 Google OAuth 密钥验证</name>
  <files>scripts/verify-doppler-secrets.sh</files>
  <read_first>
    - scripts/verify-doppler-secrets.sh（当前完整实现）
    - .sops.yaml（确认 age 公钥值）
  </read_first>
  <action>
per D-02, RESEARCH Pitfall 2/3:

1. 在 EXPECTED_SECRETS 数组末尾添加 GOOGLE_CLIENT_ID 和 GOOGLE_CLIENT_SECRET（共 17 个密钥）
2. 更新注释 "# 预期密钥列表（15 个" 为 "# 预期密钥列表（17 个"
3. 这两个密钥存储在 config/secrets.sops.yaml 中，setup-keycloak-full.sh 需要从 Doppler 获取它们

具体修改：
- 第 34 行注释改为 "# 预期密钥列表（17 个，排除 VITE_* 和备份系统密钥）"
- 在 EXPECTED_SECRETS 数组第 51 行 "RESEND_API_KEY" 之后添加：
  ```
      "GOOGLE_CLIENT_ID"
      "GOOGLE_CLIENT_SECRET"
  ```
  </action>
  <acceptance_criteria>
    - `grep 'GOOGLE_CLIENT_ID' scripts/verify-doppler-secrets.sh` 输出包含 GOOGLE_CLIENT_ID
    - `grep 'GOOGLE_CLIENT_SECRET' scripts/verify-doppler-secrets.sh` 输出包含 GOOGLE_CLIENT_SECRET
    - `grep -c '"[A-Z_]*"' scripts/verify-doppler-secrets.sh` 返回 17（17 个密钥）
    - `bash -n scripts/verify-doppler-secrets.sh` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-doppler-secrets.sh && grep -c 'GOOGLE_CLIENT_ID\|GOOGLE_CLIENT_SECRET' scripts/verify-doppler-secrets.sh | grep -q 2</automated>
  </verify>
  <done>verify-doppler-secrets.sh 包含 17 个预期密钥（含 GOOGLE_CLIENT_ID 和 GOOGLE_CLIENT_SECRET），语法正确</done>
</task>

<task type="auto">
  <name>Task 2: 改造 secrets.sh 为 Doppler-only 模式</name>
  <files>scripts/lib/secrets.sh</files>
  <read_first>
    - scripts/lib/secrets.sh（当前双模式实现）
  </read_first>
  <action>
per D-03/D-04/D-05/D-06:

1. 更新文件头注释：
   - 第 5 行 "密钥加载库（Doppler 双模式）" 改为 "密钥加载库（Doppler Only）"
   - 第 8 行移除 "模式：" 中的第 2 点 "DOPPLER_TOKEN 不存在 → 回退到 source docker/.env"
   - 第 12 行 "设计决策" 改为 "per D-03 (Doppler only), D-04 (--no-file 不落盘), D-05 (config=prd)"

2. 重写 load_secrets() 函数体（保留 log 函数 fallback 不变），替换整个函数逻辑：
   - 保留 log 函数 fallback 代码块（第 28-33 行不变）
   - DOPPLER_TOKEN 检查提前到最前面：如果 DOPPLER_TOKEN 为空，输出 "[ERROR] DOPPLER_TOKEN 未设置。请 export DOPPLER_TOKEN=<service-token> 后重试。" 并 return 1（per D-06）
   - doppler CLI 检查（保留）
   - doppler secrets download（保留）
   - 移除整个 else 分支（第 57-76 行，即 .env 回退逻辑）
   - 更新 load_secrets() 的函数注释：移除 "DOPPLER_TOKEN 为空时：回退到 source docker/.env 文件" 说明，改为 "DOPPLER_TOKEN 为空时：报错退出"

3. 更新第 25 行函数注释：
   ```
   # Doppler-only 密钥加载：
   #   - DOPPLER_TOKEN 非空时：从 Doppler API 拉取密钥注入 shell 环境
   #   - DOPPLER_TOKEN 为空时：输出错误提示并 return 1
   ```
  </action>
  <acceptance_criteria>
    - `grep 'docker/.env' scripts/lib/secrets.sh` 无输出（不再引用 docker/.env）
    - `grep 'DOPPLER_TOKEN 未设置' scripts/lib/secrets.secrets.sh` 输出包含错误提示（注：检查实际文件）
    - `grep 'export DOPPLER_TOKEN' scripts/lib/secrets.sh` 输出包含 "请 export DOPPLER_TOKEN"
    - `grep -c 'return 1' scripts/lib/secrets.sh` 至少返回 3（3 个 return 1 点：TOKEN 缺失、CLI 缺失、拉取失败）
    - `bash -n scripts/lib/secrets.sh` 退出码为 0
    - `grep 'source.*docker' scripts/lib/secrets.sh` 无输出
    - `grep 'PROJECT_ROOT' scripts/lib/secrets.sh` 无输出（不再需要定位 docker/.env）
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/lib/secrets.sh && ! grep -q 'source.*docker' scripts/lib/secrets.sh && grep -q 'DOPPLER_TOKEN 未设置' scripts/lib/secrets.sh</automated>
  </verify>
  <done>secrets.sh load_secrets() 只支持 Doppler 模式，DOPPLER_TOKEN 未设置时输出明确错误并 return 1，不再回退 docker/.env</done>
</task>

<task type="auto">
  <name>Task 3: 改造 backup-doppler-secrets.sh 的 age 公钥来源</name>
  <files>scripts/backup/backup-doppler-secrets.sh</files>
  <read_first>
    - scripts/backup/backup-doppler-secrets.sh（当前完整实现，特别是第 65-79 行）
    - .sops.yaml（确认 age 公钥值：age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w）
  </read_first>
  <action>
per RESEARCH Pitfall 1: 删除 .sops.yaml 后 backup-doppler-secrets.sh 将无法获取 age 公钥。

替换第 65-79 行的公钥获取逻辑：

当前代码（第 65-79 行）：
```bash
# 获取 age 公钥（优先环境变量，其次从 .sops.yaml 读取）
AGE_PUBLIC_KEY="${AGE_PUBLIC_KEY:-}"
if [[ -z "$AGE_PUBLIC_KEY" ]]; then
    SOPS_FILE="${SOPS_FILE:-.sops.yaml}"
    if [[ -f "$SOPS_FILE" ]]; then
        AGE_PUBLIC_KEY=$(grep '^age:' "$SOPS_FILE" | awk '{print $2}')
        info "从 $SOPS_FILE 读取 age 公钥"
    fi
fi

if [[ -z "$AGE_PUBLIC_KEY" ]]; then
    error "AGE_PUBLIC_KEY 未设置且无法从 .sops.yaml 读取"
    error "使用方法: export AGE_PUBLIC_KEY='age1xxx...' 或确保 .sops.yaml 存在"
    exit 1
fi
```

替换为（age 公钥可安全公开，硬编码为默认值）：
```bash
# Doppler 备份加密公钥（age 公钥，可安全公开）
# 公钥来源：原 .sops.yaml 中定义的加密密钥
AGE_PUBLIC_KEY="${AGE_PUBLIC_KEY:-age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w}"

if [[ -z "$AGE_PUBLIC_KEY" ]]; then
    error "AGE_PUBLIC_KEY 未设置"
    error "使用方法: export AGE_PUBLIC_KEY='age1xxx...'"
    exit 1
fi
```

这个修改保留了 AGE_PUBLIC_KEY 环境变量覆盖能力，同时硬编码默认公钥使脚本不再依赖 .sops.yaml。
  </action>
  <acceptance_criteria>
    - `grep 'age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w' scripts/backup/backup-doppler-secrets.sh` 输出包含该公钥
    - `grep '.sops.yaml' scripts/backup/backup-doppler-secrets.sh` 无输出（不再引用 .sops.yaml）
    - `grep 'SOPS_FILE' scripts/backup/backup-doppler-secrets.sh` 无输出
    - `bash -n scripts/backup/backup-doppler-secrets.sh` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/backup/backup-doppler-secrets.sh && ! grep -q '.sops.yaml' scripts/backup/backup-doppler-secrets.sh && grep -q 'age1869smm93r878hzgarhv5uggkg58mttaz54l05wwc0s3zmp264e7qw7rc3w' scripts/backup/backup-doppler-secrets.sh</automated>
  </verify>
  <done>backup-doppler-secrets.sh 使用硬编码 age 公钥作为默认值，不再依赖 .sops.yaml，AGE_PUBLIC_KEY 环境变量仍可覆盖</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| secrets.sh → Doppler API | 密钥通过 DOPPLER_TOKEN 认证从 Doppler 拉取，不再有本地文件回退 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | Denial of Service | scripts/lib/secrets.sh | mitigate | DOPPLER_TOKEN 未设置时输出明确错误提示 "[ERROR] DOPPLER_TOKEN 未设置。请 export DOPPLER_TOKEN=\<service-token\> 后重试。" 并 return 1（per D-04/D-06） |
| T-41-05 | Information Disclosure | scripts/backup/backup-doppler-secrets.sh | accept | age 公钥硬编码到脚本中可安全公开（公钥不等于私钥，无法用于解密） |
</threat_model>

<verification>
1. `bash -n scripts/verify-doppler-secrets.sh` — 语法正确
2. `bash -n scripts/lib/secrets.sh` — 语法正确
3. `bash -n scripts/backup/backup-doppler-secrets.sh` — 语法正确
4. `grep 'GOOGLE_CLIENT_ID' scripts/verify-doppler-secrets.sh` — 包含新增密钥
5. `! grep 'source.*docker' scripts/lib/secrets.sh` — 不再回退 docker/.env
6. `! grep '.sops.yaml' scripts/backup/backup-doppler-secrets.sh` — 不再依赖 .sops.yaml
</verification>

<success_criteria>
- verify-doppler-secrets.sh EXPECTED_SECRETS 数组包含 17 个密钥（含 GOOGLE_CLIENT_ID/SECRET）
- secrets.sh load_secrets() 在 DOPPLER_TOKEN 为空时 return 1 并输出明确错误
- secrets.sh 不再包含任何 docker/.env 或 source $PROJECT_ROOT 引用
- backup-doppler-secrets.sh 硬编码 age 公钥，不再引用 .sops.yaml
- 所有 3 个脚本 bash -n 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/41-migration-cleanup/41-01-SUMMARY.md`
</output>
