---
phase: 45-infra-image-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline-stages.sh
autonomous: true
requirements:
  - IMG-01
  - IMG-02

must_haves:
  truths:
    - "noda-ops 部署后 pipeline_infra_cleanup 调用 cleanup_by_date_threshold 清理旧 SHA 标签镜像"
    - "nginx 部署后 case 分支输出'无需额外清理（dangling 清理由通用 wrapper 处理）'"
    - "nginx 和 noda-ops 的 case 分支已拆分为两个独立分支"
  artifacts:
    - path: "scripts/pipeline-stages.sh"
      provides: "pipeline_infra_cleanup() 中 noda-ops 分支调用 cleanup_by_date_threshold"
      contains: "cleanup_by_date_threshold \"noda-ops\""
  key_links:
    - from: "scripts/pipeline-stages.sh:cleanup_by_date_threshold"
      to: "scripts/lib/image-cleanup.sh:cleanup_by_date_threshold()"
      via: "source image-cleanup.sh (line 19)"
      pattern: "cleanup_by_date_threshold.*noda-ops"
---

<objective>
拆分 pipeline_infra_cleanup() 中合并的 nginx|noda-ops case 分支，为 noda-ops 添加 cleanup_by_date_threshold 调用，nginx 保持不变。

Purpose: 补全 infra Pipeline 中缺失的 noda-ops 镜像清理逻辑，使所有服务部署后无残留镜像堆积。nginx 使用外部镜像，dangling 清理已由通用 wrapper 覆盖。
Output: 修改后的 scripts/pipeline-stages.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-infra-image-cleanup/45-CONTEXT.md
@.planning/phases/45-infra-image-cleanup/45-RESEARCH.md

<interfaces>
<!-- pipeline-stages.sh line 19 已 source image-cleanup.sh，函数可直接调用 -->

From scripts/lib/image-cleanup.sh:
```bash
# cleanup_by_date_threshold - 删除不被任何容器使用的旧镜像和 dangling images
# 策略：只保留正在被容器使用的镜像 + latest 标签，删除所有其他旧标签镜像
# 参数:
#   $1: 镜像名（如 findclass-ssr 或 keycloak）
cleanup_by_date_threshold()
```

From scripts/lib/cleanup.sh:
```bash
# cleanup_after_infra_deploy() - 基础设施部署后清理
# 已包含: cleanup_dangling_images(), cleanup_stopped_containers(), cleanup_anonymous_volumes() 等
# cleanup_anonymous_volumes() 使用 docker volume prune -f（不加 --all），保护 postgres_data
cleanup_after_infra_deploy()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 拆分 pipeline_infra_cleanup case 分支并添加 noda-ops 清理调用</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    - scripts/pipeline-stages.sh (line 891-917, pipeline_infra_cleanup 函数)
    - scripts/lib/image-cleanup.sh (cleanup_by_date_threshold 函数签名和参数)
    - scripts/lib/cleanup.sh (line 260-298, cleanup_after_infra_deploy wrapper)
  </read_first>
  <action>
修改 scripts/pipeline-stages.sh 中 pipeline_infra_cleanup() 函数的 case 分支（当前 line 898-913）。

将合并的 `nginx | noda-ops)` 分支拆分为两个独立分支：

**当前代码（line 898-913）：**
```bash
case "$service" in
    keycloak)
        # 清理 dangling images（keycloak 使用 SERVICE_IMAGE，不会产生 SHA 标签）
        cleanup_dangling
        ;;
    nginx | noda-ops)
        log_info "$service 无需额外清理"
        ;;
    postgres)
        # 无额外清理
        log_info "PostgreSQL 无需额外清理"
        ;;
    *)
        log_info "未知服务: $service，跳过清理"
        ;;
esac
```

**修改为：**
```bash
case "$service" in
    keycloak)
        cleanup_dangling
        ;;
    nginx)
        log_info "$service 无需额外清理（dangling 清理由通用 wrapper 处理）"
        ;;
    noda-ops)
        cleanup_by_date_threshold "noda-ops"
        ;;
    postgres)
        log_info "PostgreSQL 无需额外清理"
        ;;
    *)
        log_info "未知服务: $service，跳过清理"
        ;;
esac
```

具体改动：
1. 删除 `nginx | noda-ops)` 合并分支和其 log_info 行
2. 新增 `nginx)` 分支：`log_info "$service 无需额外清理（dangling 清理由通用 wrapper 处理）"`
3. 新增 `noda-ops)` 分支：`cleanup_by_date_threshold "noda-ops"`
4. 移除 keycloak 分支中的注释行（保持简洁，与其他分支一致）
5. 保持 postgres 和 *) 分支不变
6. 保持 case 后的 `cleanup_after_infra_deploy` 调用（line 915-916）不变

注意事项：
- image-cleanup.sh 已在 pipeline-stages.sh line 19 被 source，无需额外 source
- 不要在 noda-ops 分支添加 `|| true`，cleanup_by_date_threshold 内部的 docker rmi 已自带 `|| true`
- 不要在 noda-ops 分支重复调用通用 wrapper 已有的清理函数（dangling、stopped containers 等）
  </action>
  <acceptance_criteria>
    1. `grep -n 'cleanup_by_date_threshold "noda-ops"' scripts/pipeline-stages.sh` 返回匹配行
    2. `grep -n 'nginx | noda-ops' scripts/pipeline-stages.sh` 无匹配（合并分支已拆分）
    3. `grep -c 'noda-ops)' scripts/pipeline-stages.sh` 返回 1（只有一个 noda-ops 独立分支）
    4. `grep -c 'nginx)' scripts/pipeline-stages.sh` 返回 1（只有一个 nginx 独立分支）
    5. `grep 'dangling 清理由通用 wrapper 处理' scripts/pipeline-stages.sh` 返回匹配（nginx 分支日志信息）
    6. `shellcheck scripts/pipeline-stages.sh` 无错误
  </acceptance_criteria>
  <verify>
    <automated>shellcheck scripts/pipeline-stages.sh && echo "ShellCheck passed"</automated>
  </verify>
  <done>
pipeline_infra_cleanup() 的 case 分支中：nginx 和 noda-ops 已拆分为两个独立分支；noda-ops 调用 cleanup_by_date_threshold "noda-ops"；nginx 输出"无需额外清理（dangling 清理由通用 wrapper 处理）"；ShellCheck 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins Pipeline -> Docker daemon | pipeline_infra_cleanup 通过 shell 调用 docker rmi |
| cleanup_by_date_threshold -> docker images/ps/rmi | 镜像清理操作依赖 Docker API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | cleanup_anonymous_volumes | mitigate | docker volume prune -f 不加 --all，保护 postgres_data 命名卷 [VERIFIED: cleanup.sh line 289] |
| T-45-02 | Denial of Service | cleanup_by_date_threshold noda-ops | mitigate | 函数保留容器在用镜像 ID + latest 标签镜像 ID，不会删除运行中容器引用的镜像 [VERIFIED: image-cleanup.sh line 66-84] |
| T-45-03 | Denial of Service | 清理失败导致 Pipeline 中断 | accept | cleanup_by_date_threshold 内部 docker rmi 自带 `|| true`；Pipeline 最终调用 cleanup_after_infra_deploy 是非关键步骤 |
</threat_model>

<verification>
1. shellcheck scripts/pipeline-stages.sh 通过
2. grep 确认 case 分支拆分正确（见 acceptance_criteria）
3. grep 确认 cleanup_by_date_threshold "noda-ops" 存在
</verification>

<success_criteria>
- pipeline_infra_cleanup() 中 noda-ops 分支调用 cleanup_by_date_threshold "noda-ops"
- nginx 和 noda-ops 已拆分为独立 case 分支
- ShellCheck 无错误
- 通用 wrapper cleanup_after_infra_deploy 调用未被修改
</success_criteria>

<output>
After completion, create `.planning/phases/45-infra-image-cleanup/45-01-SUMMARY.md`
</output>
