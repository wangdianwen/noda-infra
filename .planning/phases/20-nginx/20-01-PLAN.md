---
phase: 20-nginx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/nginx/snippets/upstream-findclass.conf
  - config/nginx/snippets/upstream-noda-site.conf
  - config/nginx/snippets/upstream-keycloak.conf
  - config/nginx/conf.d/default.conf
autonomous: true
requirements:
  - BLUE-02

must_haves:
  truths:
    - "snippets/ 目录下存在 3 个 upstream-*.conf 文件，每个文件包含纯 upstream 块"
    - "default.conf 中不再包含任何 upstream 块定义"
    - "nginx -t 通过，配置语法正确"
    - "nginx -s reload 成功，配置生效"
    - "class.noda.co.nz /health 端点返回 HTTP 200（功能等价）"
  artifacts:
    - path: "config/nginx/snippets/upstream-findclass.conf"
      provides: "findclass_backend upstream 定义"
      contains: "upstream findclass_backend"
    - path: "config/nginx/snippets/upstream-noda-site.conf"
      provides: "noda_site_backend upstream 定义"
      contains: "upstream noda_site_backend"
    - path: "config/nginx/snippets/upstream-keycloak.conf"
      provides: "keycloak_backend upstream 定义"
      contains: "upstream keycloak_backend"
    - path: "config/nginx/conf.d/default.conf"
      provides: "server 块配置（无 upstream 定义）"
      excludes: "upstream "
  key_links:
    - from: "config/nginx/nginx.conf"
      to: "config/nginx/snippets/upstream-*.conf"
      via: "include /etc/nginx/snippets/*.conf; (第 27 行)"
      pattern: "include /etc/nginx/snippets/\\*\\.conf"
    - from: "config/nginx/conf.d/default.conf"
      to: "upstream-*.conf 中定义的 upstream"
      via: "proxy_pass http://findclass_backend 等引用"
      pattern: "proxy_pass http://(findclass_backend|noda_site_backend|keycloak_backend)"
---

<objective>
将 nginx 的三个 upstream 定义从 default.conf 抽离到独立 include 文件，建立蓝绿部署的路由基础。

Purpose: Phase 21/22 的蓝绿部署需要 Pipeline 通过重写 upstream include 文件 + nginx reload 来切换流量。本 Phase 建立此基础结构。
Output: 3 个 upstream-*.conf 文件 + 精简后的 default.conf
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-nginx/20-CONTEXT.md
@.planning/phases/20-nginx/20-RESEARCH.md

<interfaces>
<!-- 现有 nginx 配置文件关键接口 -->

config/nginx/nginx.conf 第 27-29 行（include 加载点）:
```nginx
include /etc/nginx/snippets/*.conf;   # 先加载 snippets（含 upstream）
include /etc/nginx/conf.d/*.conf;     # 后加载 conf.d（含 server 块）
```

config/nginx/conf.d/default.conf 第 1-14 行（需要移除的 upstream 块）:
```nginx
# ============================================
# Upstream 定义（故障转移）
# ============================================
upstream keycloak_backend {
    server keycloak:8080 max_fails=3 fail_timeout=30s;
}

upstream findclass_backend {
    server findclass-ssr:3001 max_fails=3 fail_timeout=30s;
}

upstream noda_site_backend {
    server noda-site:3000 max_fails=3 fail_timeout=30s;
}
```

default.conf 第 16 行起（保留不变的内容起始行）:
```nginx
# 动态设置转发协议和端口（Cloudflare TLS 终止，内部 HTTP）
map $host $forwarded_proto {
    default $scheme;
    class.noda.co.nz "https";
    auth.noda.co.nz "https";
    noda.co.nz "https";
    www.noda.co.nz "https";
}
```

default.conf 中的 proxy_pass 引用（保持不变）:
- server auth.noda.co.nz: proxy_pass http://keycloak_backend;
- server class.noda.co.nz: proxy_pass http://findclass_backend;
- server noda.co.nz: proxy_pass http://noda_site_backend;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 upstream include 文件并清理 default.conf</name>
  <files>
    config/nginx/snippets/upstream-findclass.conf
    config/nginx/snippets/upstream-noda-site.conf
    config/nginx/snippets/upstream-keycloak.conf
    config/nginx/conf.d/default.conf
  </files>
  <read_first>
    config/nginx/conf.d/default.conf
    config/nginx/nginx.conf
    config/nginx/snippets/proxy-common.conf
  </read_first>
  <acceptance_criteria>
    1. `grep -c "^upstream " config/nginx/snippets/upstream-findclass.conf` 输出 "1"
    2. `grep -c "^upstream " config/nginx/snippets/upstream-noda-site.conf` 输出 "1"
    3. `grep -c "^upstream " config/nginx/snippets/upstream-keycloak.conf` 输出 "1"
    4. `grep -c "^upstream " config/nginx/conf.d/default.conf` 输出 "0"
    5. `grep "server findclass-ssr:3001 max_fails=3 fail_timeout=30s" config/nginx/snippets/upstream-findclass.conf` 匹配成功
    6. `grep "server noda-site:3000 max_fails=3 fail_timeout=30s" config/nginx/snippets/upstream-noda-site.conf` 匹配成功
    7. `grep "server keycloak:8080 max_fails=3 fail_timeout=30s" config/nginx/snippets/upstream-keycloak.conf` 匹配成功
    8. `grep "proxy_pass http://findclass_backend" config/nginx/conf.d/default.conf` 匹配成功（proxy_pass 引用未变）
    9. `grep "proxy_pass http://noda_site_backend" config/nginx/conf.d/default.conf` 匹配成功
    10. `grep "proxy_pass http://keycloak_backend" config/nginx/conf.d/default.conf` 匹配成功
    11. upstream-*.conf 文件不包含 "#" 字符（无注释头部，per D-02）
  </acceptance_criteria>
  <action>
**Step 1: 创建 3 个 upstream include 文件**

在 config/nginx/snippets/ 目录下创建 3 个文件。Per D-01 三个 upstream 全部抽离。Per D-02 纯 upstream 块格式，不加注释头部。Per T-20-01 缓解策略，使用原子写入（先写临时文件再 mv）。

`config/nginx/snippets/upstream-keycloak.conf` 内容（精确内容，无注释）:
```nginx
upstream keycloak_backend {
    server keycloak:8080 max_fails=3 fail_timeout=30s;
}
```

`config/nginx/snippets/upstream-findclass.conf` 内容（精确内容，无注释）:
```nginx
upstream findclass_backend {
    server findclass-ssr:3001 max_fails=3 fail_timeout=30s;
}
```

`config/nginx/snippets/upstream-noda-site.conf` 内容（精确内容，无注释）:
```nginx
upstream noda_site_backend {
    server noda-site:3000 max_fails=3 fail_timeout=30s;
}
```

**Step 2: 编辑 default.conf — 删除第 1-15 行**

从 config/nginx/conf.d/default.conf 中精确删除第 1-15 行:
- 第 1-3 行: `# ============================================` 注释头部
- 第 4 行: `upstream keycloak_backend {`
- 第 5 行: `    server keycloak:8080 max_fails=3 fail_timeout=30s;`
- 第 6 行: `}`
- 第 7 行: 空行
- 第 8 行: `upstream findclass_backend {`
- 第 9 行: `    server findclass-ssr:3001 max_fails=3 fail_timeout=30s;`
- 第 10 行: `}`
- 第 11 行: 空行
- 第 12 行: `upstream noda_site_backend {`
- 第 13 行: `    server noda-site:3000 max_fails=3 fail_timeout=30s;`
- 第 14 行: `}`
- 第 15 行: 空行

删除后，文件直接从 `# 动态设置转发协议和端口（Cloudflare TLS 终止，内部 HTTP）` 开始。

**关键约束:**
- 不要在 default.conf 中添加任何 include 语句（nginx.conf 第 27 行已有 `include /etc/nginx/snippets/*.conf;` 通配符加载，重复 include 会导致 upstream 重复定义错误）
- 不要修改 map 指令、server 块、proxy_pass 引用中的任何内容
- 不要修改文件中第 16 行之后的任何内容
  </action>
  <verify>
    <automated>grep -c "^upstream " config/nginx/snippets/upstream-findclass.conf config/nginx/snippets/upstream-noda-site.conf config/nginx/snippets/upstream-keycloak.conf && echo "--- upstream files OK ---" && grep -c "^upstream " config/nginx/conf.d/default.conf | grep -q "^0$" && echo "--- default.conf upstream removed ---" || echo "FAIL: default.conf still has upstream"</automated>
  </verify>
  <done>
    1. config/nginx/snippets/upstream-findclass.conf 存在，内容为纯 upstream 块（无注释头部）
    2. config/nginx/snippets/upstream-noda-site.conf 存在，内容为纯 upstream 块（无注释头部）
    3. config/nginx/snippets/upstream-keycloak.conf 存在，内容为纯 upstream 块（无注释头部）
    4. config/nginx/conf.d/default.conf 中 grep "^upstream " 返回 0 行
    5. default.conf 仍包含所有 map 指令和 server 块（内容完整）
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 nginx 配置语法和 reload 切换</name>
  <files></files>
  <read_first>
    config/nginx/conf.d/default.conf
    config/nginx/snippets/upstream-findclass.conf
    config/nginx/snippets/upstream-noda-site.conf
    config/nginx/snippets/upstream-keycloak.conf
    config/nginx/nginx.conf
  </read_first>
  <acceptance_criteria>
    1. `docker exec noda-infra-nginx nginx -t` 输出包含 "syntax is ok" 和 "test is successful"（若容器运行中）
    2. `docker exec noda-infra-nginx nginx -s reload` 返回退出码 0（若容器运行中）
    3. `docker exec noda-infra-nginx curl -sf http://localhost/health -H "Host: class.noda.co.nz" -o /dev/null -w "%{http_code}"` 返回 "200"（若容器运行中）
    4. 若容器未运行，SUMMARY 中标注"需部署后验证"且不阻塞完成
  </acceptance_criteria>
  <action>
**Step 1: 语法验证**

运行 `docker exec noda-infra-nginx nginx -t` 确认配置语法正确。

如果容器未运行（`docker exec` 报错），输出 "容器未运行，跳过在线验证" 并继续 Step 4。

**Step 2: Reload 验证（per D-03 功能验证）**

运行 `docker exec noda-infra-nginx nginx -s reload` 触发重载。期望退出码 0。

**Step 3: 流量验证**

运行以下命令确认 class.noda.co.nz 访问正常:
```bash
docker exec noda-infra-nginx curl -sf http://localhost/health -H "Host: class.noda.co.nz" -o /dev/null -w "%{http_code}"
```

期望输出: `200`

**Step 4: 记录验证结果**

在 SUMMARY 中记录:
- nginx -t 结果（通过/失败/跳过）
- reload 结果（成功/失败/跳过）
- /health HTTP 状态码（200/其他/跳过）

**注意:** 如果 Docker 容器未运行（本地开发环境），跳过 Steps 1-3，在 SUMMARY 中标注"需部署后验证"。这不阻塞计划完成——配置文件的正确性已通过代码审查和 grep 验证确认。
  </action>
  <verify>
    <automated>test -f config/nginx/snippets/upstream-findclass.conf && test -f config/nginx/snippets/upstream-noda-site.conf && test -f config/nginx/snippets/upstream-keycloak.conf && echo "All upstream files exist" || echo "FAIL: missing upstream files"</automated>
  </verify>
  <done>
    1. docker exec noda-infra-nginx nginx -t 返回 "syntax is ok" 和 "test is successful"（或容器未运行时标注待验证）
    2. docker exec noda-infra-nginx nginx -s reload 执行成功（或容器未运行时标注待验证）
    3. /health 端点返回 HTTP 200（或容器未运行时标注待验证）
    4. 3 个 upstream-*.conf 文件全部存在
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主机文件系统 -> nginx 容器 | Pipeline 修改宿主机 upstream 文件，nginx 通过 bind mount 读取 |
| nginx 配置解析 -> 上游服务 | upstream 块定义后端地址，语法错误会导致服务中断 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | config/nginx/snippets/upstream-*.conf | mitigate | 使用原子写入（写临时文件 + mv），避免写入中断导致配置损坏 |
| T-20-02 | Denial of Service | config/nginx/conf.d/default.conf | mitigate | 变更后必须通过 nginx -t 验证语法，失败则不执行 reload；验证 reload 后 /health 端点返回 200 |
</threat_model>

<verification>
1. `grep -c "^upstream " config/nginx/conf.d/default.conf` 返回 0（upstream 已从 default.conf 移除）
2. `ls config/nginx/snippets/upstream-*.conf` 列出 3 个文件
3. 每个 upstream-*.conf 文件中 `grep "^upstream "` 匹配恰好 1 行（纯 upstream 块）
4. `docker exec noda-infra-nginx nginx -t` 通过（或容器未运行时标注部署后验证）
5. default.conf 中 proxy_pass 引用的 upstream 名称与 snippets 文件中定义的名称完全一致
</verification>

<success_criteria>
1. 三个 upstream-*.conf 文件存在于 config/nginx/snippets/ 目录
2. default.conf 中不包含任何 upstream 块定义
3. nginx -t 验证配置语法正确
4. nginx -s reload 后 class.noda.co.nz /health 返回 HTTP 200
5. 所有 proxy_pass 引用的 upstream 名称与 include 文件中定义一致
6. 变更前后功能完全等价——无新增/删除/修改的 server 块内容
</success_criteria>

<output>
After completion, create `.planning/phases/20-nginx/20-01-SUMMARY.md`
</output>
