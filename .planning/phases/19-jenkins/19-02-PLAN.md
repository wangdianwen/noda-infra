---
phase: 19-jenkins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/jenkins/init.groovy.d/01-security.groovy
  - scripts/jenkins/init.groovy.d/02-plugins.groovy
  - scripts/jenkins/init.groovy.d/03-pipeline-job.groovy
  - scripts/jenkins/config/jenkins-admin.env.example
  - scripts/jenkins/config/.gitignore
autonomous: true
requirements:
  - JENK-04

must_haves:
  truths:
    - "install 子命令复制 groovy 脚本到 Jenkins 后，首次启动自动创建管理员用户"
    - "首次启动自动安装 Git、Pipeline、Pipeline Stage View、Credentials Binding、Timestamaller 插件"
    - "首次启动自动配置 CSRF 保护、禁用匿名读取"
    - "首次启动自动创建 noda-apps-deploy Pipeline 作业"
    - "管理员凭据模板文件存在，实际凭据文件被 .gitignore 排除"
  artifacts:
    - path: "scripts/jenkins/init.groovy.d/01-security.groovy"
      provides: "管理员用户创建 + 安全策略（CSRF + 禁用匿名读取 + 跳过 Setup Wizard）"
      contains: "HudsonPrivateSecurityRealm"
    - path: "scripts/jenkins/init.groovy.d/02-plugins.groovy"
      provides: "插件预安装（Git、Pipeline、Pipeline Stage View、Credentials Binding、Timestamper）"
      contains: "workflow-aggregator"
    - path: "scripts/jenkins/init.groovy.d/03-pipeline-job.groovy"
      provides: "预创建 noda-apps-deploy Pipeline 作业"
      contains: "createProject"
    - path: "scripts/jenkins/config/jenkins-admin.env.example"
      provides: "管理员凭据模板文件"
      contains: "JENKINS_ADMIN_USER"
  key_links:
    - from: "scripts/setup-jenkins.sh"
      to: "scripts/jenkins/init.groovy.d/*.groovy"
      via: "install 子命令 cp 到 JENKINS_HOME/init.groovy.d/"
      pattern: "GROOVY_SRC_DIR"
    - from: "01-security.groovy"
      to: "JENKINS_HOME/.admin.env"
      via: "Groovy 脚本读取管理员凭据"
      pattern: "\\.admin\\.env"
---

<objective>
创建 Jenkins 首次启动自动化配置文件：3 个 init.groovy.d 脚本（管理员用户 + 安全策略、插件安装、Pipeline 作业创建）+ 管理员凭据模板文件 + .gitignore 保护实际凭据。

Purpose: 实现 D-04 决策 — Jenkins 安装后首次启动自动完成管理员创建、插件安装、安全加固、Pipeline 作业预创建，无需手动操作 Web UI。
Output: scripts/jenkins/ 目录下的 5 个配置文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-jenkins/19-CONTEXT.md
@.planning/phases/19-jenkins/19-RESEARCH.md

<interfaces>
<!-- setup-jenkins.sh install 子命令会读取这些文件 -->
<!-- groovy 脚本路径约定 -->
From scripts/setup-jenkins.sh (Plan 01):
```bash
GROOVY_SRC_DIR="$SCRIPT_DIR/jenkins/init.groovy.d"
ADMIN_ENV_TEMPLATE="$SCRIPT_DIR/jenkins/config/jenkins-admin.env.example"
# install 中执行:
# sudo cp "$GROOVY_SRC_DIR"/*.groovy "$JENKINS_HOME/init.groovy.d/"
# 检查 scripts/jenkins/config/jenkins-admin.env 实际文件
# sudo cp 实际文件到 $JENKINS_HOME/.admin.env
```

<!-- Jenkins init.groovy.d 约定 -->
- 脚本按字母顺序执行: 01-*, 02-*, 03-*
- 脚本在每次 Jenkins 启动时执行（需幂等检查）
- install 子命令在首次配置完成后删除 init.groovy.d 目录
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 01-security.groovy + 02-plugins.groovy + 03-pipeline-job.groovy</name>
  <files>scripts/jenkins/init.groovy.d/01-security.groovy, scripts/jenkins/init.groovy.d/02-plugins.groovy, scripts/jenkins/init.groovy.d/03-pipeline-job.groovy</files>
  <read_first>
    .planning/phases/19-jenkins/19-RESEARCH.md — Groovy 脚本代码示例、pitfalls（Pitfall 3: 幂等检查、Pitfall 5: 插件安装后重启）
    .planning/phases/19-jenkins/19-CONTEXT.md — D-04 首次启动配置决策
  </read_first>
  <action>
创建目录 `scripts/jenkins/init.groovy.d/`，然后创建以下 3 个 Groovy 脚本。

**01-security.groovy — 管理员用户 + 安全策略 (per D-04):**

```groovy
// Jenkins 首次启动安全配置
// 功能：创建管理员用户、启用 CSRF 保护、禁用匿名读取、跳过 Setup Wizard
import jenkins.model.*
import hudson.security.*
import jenkins.security.s2m.*

// 读取管理员凭据 — 从 JENKINS_HOME/.admin.env 文件或环境变量
def jenkinsHome = System.getenv('JENKINS_HOME') ?: '/var/lib/jenkins'
def adminUser = 'admin'
def adminPass = null

// 优先从 .admin.env 文件读取
def envFile = new File(jenkinsHome, '.admin.env')
if (envFile.exists()) {
    def props = new Properties()
    envFile.withInputStream { stream -> props.load(stream) }
    adminUser = props.getProperty('JENKINS_ADMIN_USER', 'admin')
    adminPass = props.getProperty('JENKINS_ADMIN_PASSWORD')
}

// 回退到环境变量
if (adminPass == null) {
    adminPass = System.getenv('JENKINS_ADMIN_PASSWORD')
}
if (adminUser == 'admin' && System.getenv('JENKINS_ADMIN_USER')) {
    adminUser = System.getenv('JENKINS_ADMIN_USER')
}

if (adminPass == null) {
    println 'WARNING: No admin password configured. Skipping security setup.'
    return
}

def instance = Jenkins.getInstance()

// 创建管理员用户（幂等：检查用户是否已存在）
def hudsonRealm = new HudsonPrivateSecurityRealm(false)
def existingUser = hudsonRealm.getUser(adminUser)
if (existingUser == null) {
    hudsonRealm.createAccount(adminUser, adminPass)
    println "Created admin user: ${adminUser}"
} else {
    println "Admin user already exists: ${adminUser}, skipping creation"
}
instance.setSecurityRealm(hudsonRealm)

// 授权策略：登录后完全控制，禁用匿名读取
def strategy = new FullControlOnceLoggedInAuthorizationStrategy()
strategy.setAllowAnonymousRead(false)
instance.setAuthorizationStrategy(strategy)

// CSRF 保护
instance.setCrumbIssuer(new DefaultCrumbIssuer(true))

// 跳过 Setup Wizard
def setupWizard = instance.getSetupWizard()
if (setupWizard != null) {
    setupWizard.completeSetup()
}

instance.save()
println 'Security configuration completed.'
```

**02-plugins.groovy — 插件安装 (per D-04):**

```groovy
// Jenkins 首次启动插件安装
// 功能：安装 Git、Pipeline、Pipeline Stage View、Credentials Binding、Timestamper
import jenkins.model.*
import hudson.init.*
import hudson.PluginWrapper

def instance = Jenkins.getInstance()
def pm = instance.getPluginManager()
def uc = instance.getUpdateCenter()

// 初始化 Update Center
uc.updateAllSites()

def plugins = [
    'git',
    'workflow-aggregator',
    'pipeline-stage-view',
    'credentials-binding',
    'timestamper'
]

def allInstalled = true

plugins.each { pluginId ->
    if (!pm.getPlugin(pluginId)) {
        println "Installing plugin: ${pluginId}"
        def plugin = uc.getPlugin(pluginId)
        if (plugin) {
            def result = plugin.deploy(true)
            allInstalled = false
        } else {
            println "WARNING: Plugin ${pluginId} not found in Update Center"
        }
    } else {
        println "Plugin already installed: ${pluginId}"
    }
}

instance.save()

if (!allInstalled) {
    println 'Plugins installed. Jenkins will restart to complete installation.'
    // 注意：不需要在此处触发重启，setup-jenkins.sh install 子命令会处理重启
}
```

**03-pipeline-job.groovy — 创建 Pipeline 作业 (per D-04):**

```groovy
// Jenkins 首次启动 Pipeline 作业创建
// 功能：预创建 noda-apps-deploy Pipeline 作业（占位模板，Phase 23 填充 Jenkinsfile）
import jenkins.model.*
import org.jenkinsci.plugins.workflow.job.*
import hudson.XmlFile

def instance = Jenkins.getInstance()
def jobName = 'noda-apps-deploy'

// 幂等检查：作业已存在则跳过
if (instance.getItem(jobName) != null) {
    println "Pipeline job '${jobName}' already exists, skipping creation."
    return
}

// 创建 Pipeline 作业
def parent = instance
def job = new WorkflowJob(parent, jobName)

// 设置 Pipeline 定义（占位 — 后续 Phase 23 填充实际 Jenkinsfile 路径）
// 使用内联 Pipeline script 作为占位
def configXml = '''<?xml version='1.1' encoding='UTF-8'?>
<flow-definition plugin="workflow-job">
  <description>Noda Apps 蓝绿部署 Pipeline（Phase 23 将填充 Jenkinsfile）</description>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsFlowDefinition" plugin="workflow-cps">
    <script>// Noda Apps 部署 Pipeline
// 此 Pipeline 将在 Phase 23 中替换为完整版本
pipeline {
    agent any
    stages {
        stage(&apos;Placeholder&apos;) {
            steps {
                echo &apos;Pipeline configured. Jenkinsfile will be added in Phase 23.&apos;
            }
        }
    }
}</script>
    <sandbox>true</sandbox>
  </definition>
  <triggers/>
  <disabled>false</disabled>
</flow-definition>'''

// 使用 XML 创建作业
def xmlStream = new ByteArrayInputStream(configXml.getBytes('UTF-8'))
instance.createProjectFromXML(jobName, xmlStream)

println "Pipeline job '${jobName}' created."
instance.save()
```

注意事项（per RESEARCH.md Pitfalls）：
- 所有脚本包含幂等检查（用户/插件/作业已存在则跳过）
- 01-security.groovy 先于 02-plugins.groovy 执行（管理员用户必须在插件安装前创建）
- 插件安装后 setup-jenkins.sh install 子命令负责重启 Jenkins
  </action>
  <verify>
    <automated>test -f /Users/dianwenwang/Project/noda-infra/scripts/jenkins/init.groovy.d/01-security.groovy && test -f /Users/dianwenwang/Project/noda-infra/scripts/jenkins/init.groovy.d/02-plugins.groovy && test -f /Users/dianwenwang/Project/noda-infra/scripts/jenkins/init.groovy.d/03-pipeline-job.groovy && echo "All groovy files exist"</automated>
  </verify>
  <done>
    - scripts/jenkins/init.groovy.d/01-security.groovy 存在
    - 01-security.groovy 包含 HudsonPrivateSecurityRealm（创建管理员用户）
    - 01-security.groovy 包含 FullControlOnceLoggedInAuthorizationStrategy + setAllowAnonymousRead(false)
    - 01-security.groovy 包含 DefaultCrumbIssuer（CSRF 保护）
    - 01-security.groovy 包含 setupWizard.completeSetup()（跳过 Setup Wizard）
    - 01-security.groovy 包含幂等检查（existingUser == null 才创建）
    - 01-security.groovy 从 .admin.env 文件读取凭据（回退到环境变量）
    - scripts/jenkins/init.groovy.d/02-plugins.groovy 存在
    - 02-plugins.groovy 包含 git、workflow-aggregator、pipeline-stage-view、credentials-binding、timestamper 5 个插件
    - 02-plugins.groovy 包含幂等检查（pm.getPlugin(pluginId) 为 null 才安装）
    - scripts/jenkins/init.groovy.d/03-pipeline-job.groovy 存在
    - 03-pipeline-job.groovy 创建名为 noda-apps-deploy 的 Pipeline 作业
    - 03-pipeline-job.groovy 包含幂等检查（instance.getItem(jobName) != null 则跳过）
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建管理员凭据模板 + .gitignore 保护</name>
  <files>scripts/jenkins/config/jenkins-admin.env.example, scripts/jenkins/config/.gitignore</files>
  <read_first>
    .planning/phases/19-jenkins/19-CONTEXT.md — specifics 部分（管理员凭据存储方式）
    .planning/phases/19-jenkins/19-RESEARCH.md — Open Questions 1（管理员凭据存储方式推荐）
  </read_first>
  <action>
创建目录 `scripts/jenkins/config/`，然后创建以下 2 个文件。

**scripts/jenkins/config/jenkins-admin.env.example — 管理员凭据模板：**
```
# Jenkins 管理员凭据配置
# 复制此文件为 jenkins-admin.env 并填写实际值
# jenkins-admin.env 不会被提交到 Git（已在 .gitignore 中排除）
#
# 用途：setup-jenkins.sh install 子命令将此文件复制到
#       /var/lib/jenkins/.admin.env（Jenkins init.groovy.d 脚本读取）

JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
```

**scripts/jenkins/config/.gitignore — 排除实际凭据文件：**
```
# 排除实际凭据文件（仅保留 .example 模板）
jenkins-admin.env
```
  </action>
  <verify>
    <automated>test -f /Users/dianwenwang/Project/noda-infra/scripts/jenkins/config/jenkins-admin.env.example && test -f /Users/dianwenwang/Project/noda-infra/scripts/jenkins/config/.gitignore && echo "Config files exist"</automated>
  </verify>
  <done>
    - scripts/jenkins/config/jenkins-admin.env.example 存在
    - jenkins-admin.env.example 包含 JENKINS_ADMIN_USER=admin
    - jenkins-admin.env.example 包含 JENKINS_ADMIN_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
    - jenkins-admin.env.example 包含使用说明注释
    - scripts/jenkins/config/.gitignore 存在
    - .gitignore 排除 jenkins-admin.env（实际凭据文件）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| groovy 脚本 → Jenkins 配置 | init.groovy.d 脚本在 Jenkins 启动时以 Jenkins 进程权限执行 |
| .admin.env 文件 → groovy 脚本 | groovy 脚本从文件系统读取明文密码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | .admin.env 明文存储管理员密码 | mitigate | 文件权限 600 (jenkins:jenkins)；实际凭据文件不入 Git（.gitignore 排除）；生产服务器本地文件系统保护 |
| T-19-06 | Tampering | init.groovy.d 脚本每次启动执行 | mitigate | 所有 groovy 脚本包含幂等检查（用户/插件/作业已存在则跳过）；install 子命令在首次配置完成后删除 init.groovy.d 目录 |
| T-19-07 | Spoofing | groovy 脚本无凭据时跳过安全配置 | accept | 无凭据时仅打印警告，不阻止 Jenkins 启动；管理员后续可手动配置 |
</threat_model>

<verification>
1. 所有文件存在: `ls scripts/jenkins/init.groovy.d/01-security.groovy scripts/jenkins/init.groovy.d/02-plugins.groovy scripts/jenkins/init.groovy.d/03-pipeline-job.groovy scripts/jenkins/config/jenkins-admin.env.example scripts/jenkins/config/.gitignore`
2. 安全脚本包含幂等检查: `grep 'getUser' scripts/jenkins/init.groovy.d/01-security.groovy`
3. 插件脚本包含 5 个插件: `grep -c "'" scripts/jenkins/init.groovy.d/02-plugins.groovy` 或直接检查插件列表
4. Pipeline 作业脚本创建 noda-apps-deploy: `grep 'noda-apps-deploy' scripts/jenkins/init.groovy.d/03-pipeline-job.groovy`
5. 凭据模板不包含实际密码: `grep 'CHANGE_ME' scripts/jenkins/config/jenkins-admin.env.example`
6. .gitignore 排除实际凭据: `grep 'jenkins-admin.env' scripts/jenkins/config/.gitignore`
</verification>

<success_criteria>
- scripts/jenkins/init.groovy.d/ 目录包含 3 个 groovy 脚本（01-security、02-plugins、03-pipeline-job）
- 01-security.groovy：创建管理员用户（幂等）+ CSRF 保护 + 禁用匿名读取 + 跳过 Setup Wizard
- 02-plugins.groovy：安装 5 个插件（Git、Pipeline、Pipeline Stage View、Credentials Binding、Timestamper），幂等检查
- 03-pipeline-job.groovy：创建 noda-apps-deploy Pipeline 作业（占位模板），幂等检查
- scripts/jenkins/config/jenkins-admin.env.example 存在且包含说明注释
- scripts/jenkins/config/.gitignore 排除实际凭据文件
</success_criteria>

<output>
After completion, create `.planning/phases/19-jenkins/19-02-SUMMARY.md`
</output>
