---
phase: 43-cleanup-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - scripts/pipeline-stages.sh
autonomous: true
requirements:
  - DOCK-01
  - DOCK-02
  - DOCK-03
  - DOCK-04
  - CACHE-01
  - FILE-01
  - FILE-02

must_haves:
  truths:
    - "pipeline-stages.sh 头部 source cleanup.sh（在 image-cleanup.sh 之后）"
    - "pipeline_cleanup() 末尾调用 cleanup_after_deploy($WORKSPACE)"
    - "pipeline_infra_cleanup() 末尾调用 cleanup_after_infra_deploy($SERVICE, $WORKSPACE)"
    - "pipeline_deploy() 开头调用 disk_snapshot('部署前')（per D-06）"
    - "pipeline_infra_deploy() 开头调用 disk_snapshot('部署前')（per D-06）"
    - "4 个 Jenkinsfile 不需要修改——所有变更在 pipeline-stages.sh 函数内部完成"
  artifacts:
    - path: "scripts/pipeline-stages.sh"
      provides: "增强 pipeline_cleanup/pipeline_infra_cleanup 集成 cleanup.sh"
      contains: "cleanup_after_deploy"
  key_links:
    - from: "scripts/pipeline-stages.sh"
      to: "scripts/lib/cleanup.sh"
      via: "source + 函数调用"
      pattern: "source.*cleanup\\.sh"
    - from: "pipeline_cleanup()"
      to: "cleanup_after_deploy()"
      via: "函数调用"
      pattern: "cleanup_after_deploy"
    - from: "pipeline_infra_cleanup()"
      to: "cleanup_after_infra_deploy()"
      via: "函数调用"
      pattern: "cleanup_after_infra_deploy"
---

<objective>
增强 pipeline-stages.sh 中的 pipeline_cleanup() 和 pipeline_infra_cleanup()，集成 Plan 43-01 创建的 cleanup.sh 共享库。在 pipeline_deploy() 和 pipeline_infra_deploy() 开头添加 disk_snapshot("部署前") 调用。

Purpose: 将清理函数接入 Pipeline 自动化流程（per D-02: pipeline 同时 source 两个库；per D-03: 只需调用 wrapper 函数）
Output: 修改后的 scripts/pipeline-stages.sh（4 处变更）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-cleanup-pipeline/43-CONTEXT.md
@.planning/phases/43-cleanup-pipeline/43-RESEARCH.md
@.planning/research/CLEANUP-RESEARCH.md
@scripts/lib/cleanup.sh

<interfaces>
<!-- Plan 43-01 创建的 cleanup.sh API -->

From scripts/lib/cleanup.sh (to be created by Plan 43-01):
```bash
# 高层 Wrapper 函数
cleanup_after_deploy()        # 参数: $1 = workspace 路径（默认 $WORKSPACE）
cleanup_after_infra_deploy()  # 参数: $1 = 服务名（必须）, $2 = workspace 路径（默认 $WORKSPACE）

# 磁盘快照
disk_snapshot()               # 参数: $1 = 标签（"部署前" / "清理后"）

# 底层清理函数（由 wrapper 编排，通常不直接调用）
cleanup_docker_build_cache()     # $1 = 保留小时数
cleanup_dangling_images()        # 无参数
cleanup_stopped_containers()     # $1 = 保留小时数
cleanup_unused_networks()        # 无参数
cleanup_anonymous_volumes()      # 无参数
cleanup_node_modules()           # $1 = workspace 路径
cleanup_jenkins_temp_files()     # $1 = workspace 路径
cleanup_old_infra_backups()      # $1 = 服务名, $2 = 保留天数
```

<!-- pipeline-stages.sh 当前 source 加载顺序 -->
From scripts/pipeline-stages.sh:15-19:
```bash
source "$PROJECT_ROOT/scripts/lib/log.sh"
source "$PROJECT_ROOT/scripts/manage-containers.sh"
source "$PROJECT_ROOT/scripts/lib/secrets.sh"
source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"
source "$PROJECT_ROOT/scripts/lib/image-cleanup.sh"
```

<!-- pipeline_deploy() 当前结构 (pipeline-stages.sh:291-326) -->
From scripts/pipeline-stages.sh:291-326:
```bash
pipeline_deploy()
{
    local target_env="$1"
    local git_sha="${2:-}"
    local service="${SERVICE_NAME:-findclass-ssr}"
    local target_container
    target_container=$(get_container_name "$target_env")
    # ... 停止旧容器、启动新容器 ...
    log_success "部署完成: $target_container ($image_name)"
}
```

<!-- pipeline_cleanup() 当前结构 (pipeline-stages.sh:411-441) -->
From scripts/pipeline-stages.sh:411-441:
```bash
pipeline_cleanup()
{
    # 停掉非活跃容器
    # SHA 镜像日期阈值清理 / dangling images 清理
}
```

<!-- pipeline_infra_deploy() 当前结构 (pipeline-stages.sh:589-611) -->
From scripts/pipeline-stages.sh:589-611:
```bash
pipeline_infra_deploy()
{
    local service="$1"
    case "$service" in
        keycloak)  pipeline_deploy_keycloak ;;
        nginx)     pipeline_deploy_nginx ;;
        noda-ops)  pipeline_deploy_noda_ops ;;
        postgres)  pipeline_deploy_postgres ;;
        *)         log_error "未知服务: $service"; return 1 ;;
    esac
}
```

<!-- pipeline_infra_cleanup() 当前结构 (pipeline-stages.sh:883-905) -->
From scripts/pipeline-stages.sh:883-905:
```bash
pipeline_infra_cleanup()
{
    local service="$1"
    ls -la "...infra-pipeline/${service}/" 2>/dev/null || true
    case "$service" in
        keycloak)      cleanup_dangling ;;
        nginx|noda-ops) log_info "$service 无需额外清理" ;;
        postgres)       log_info "PostgreSQL 无需额外清理" ;;
        *)              log_info "未知服务: $service，跳过清理" ;;
    esac
}
``>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 pipeline-stages.sh 头部添加 cleanup.sh source + 增强 4 个 pipeline 函数</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    scripts/pipeline-stages.sh
    scripts/lib/cleanup.sh
    .planning/phases/43-cleanup-pipeline/43-CONTEXT.md
    .planning/research/CLEANUP-RESEARCH.md
  </read_first>
  <action>
修改 `scripts/pipeline-stages.sh`，共 4 处变更：

**变更 1: 头部添加 cleanup.sh source（第 19 行之后）**

在 `source "$PROJECT_ROOT/scripts/lib/image-cleanup.sh"` 之后追加一行：
```bash
source "$PROJECT_ROOT/scripts/lib/cleanup.sh"
```

最终头部顺序（第 15-20 行）：
```bash
source "$PROJECT_ROOT/scripts/lib/log.sh"
source "$PROJECT_ROOT/scripts/manage-containers.sh"
source "$PROJECT_ROOT/scripts/lib/secrets.sh"
source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"
source "$PROJECT_ROOT/scripts/lib/image-cleanup.sh"
source "$PROJECT_ROOT/scripts/lib/cleanup.sh"
```

注意：cleanup.sh 必须在 log.sh 之后（因为 cleanup.sh 依赖 log_info/log_success 等函数），放在 image-cleanup.sh 之后最安全（per D-02: 两个库独立，不互相依赖）。

**变更 2: pipeline_deploy() 开头添加 disk_snapshot（第 291 行附近）**

在 `pipeline_deploy()` 函数体开头（`local target_env="$1"` 之前）添加：
```bash
    disk_snapshot "部署前"
```

**变更 3: pipeline_cleanup() 末尾添加 cleanup_after_deploy 调用（第 441 行附近）**

在 `pipeline_cleanup()` 函数末尾（最后一个 `}` 之前），追加：
```bash

    # === 部署后全面清理（per D-03）===
    cleanup_after_deploy "${WORKSPACE:-$PWD}"
```

完整的 pipeline_cleanup() 函数结构：
```bash
pipeline_cleanup()
{
    # [保持不变] 停掉非活跃容器
    local active_env
    active_env=$(get_active_env)
    # ... 现有蓝绿容器停止逻辑（不变）...

    # [保持不变] SHA 镜像日期阈值清理 / dangling images 清理
    if [ -z "${SERVICE_IMAGE:-}" ]; then
        cleanup_by_date_threshold "${SERVICE_NAME:-findclass-ssr}" "${IMAGE_RETENTION_DAYS:-7}"
    else
        cleanup_dangling
    fi

    # === 部署后全面清理（per D-03）===
    cleanup_after_deploy "${WORKSPACE:-$PWD}"
}
```

**变更 4: pipeline_infra_deploy() 开头 + pipeline_infra_cleanup() 末尾**

(a) pipeline_infra_deploy() 函数体开头（`local service="$1"` 之后、`case` 之前）添加：
```bash
    disk_snapshot "部署前"
```

(b) pipeline_infra_cleanup() 函数末尾（`esac` 之后、`}` 之前）追加：
```bash

    # === 基础设施部署后全面清理（per D-03）===
    cleanup_after_infra_deploy "$service" "${WORKSPACE:-$PWD}"
```

完整的 pipeline_infra_cleanup() 函数结构：
```bash
pipeline_infra_cleanup()
{
    local service="$1"

    # 创建备份目录索引（用于审计）
    ls -la "${BACKUP_HOST_DIR:-$PROJECT_ROOT/docker/volumes/backup}/infra-pipeline/${service}/" 2>/dev/null || true

    case "$service" in
        keycloak)
            cleanup_dangling
            ;;
        nginx | noda-ops)
            log_info "$service 无需额外清理"
            ;;
        postgres)
            log_info "PostgreSQL 无需额外清理"
            ;;
        *)
            log_info "未知服务: $service，跳过清理"
            ;;
    esac

    # === 基础设施部署后全面清理（per D-03）===
    cleanup_after_infra_deploy "$service" "${WORKSPACE:-$PWD}"
}
```

**重要约束：**
- 不要修改现有的蓝绿容器停止逻辑（pipeline_cleanup 前 30 行）
- 不要修改现有的镜像清理逻辑（cleanup_by_date_threshold / cleanup_dangling）
- 不要修改 4 个 Jenkinsfile 文件（per RESEARCH: 修改 pipeline-stages.sh 函数即可自动生效）
- 使用 `"${WORKSPACE:-$PWD}"` 而非裸 `$WORKSPACE`（兼容非 Jenkins 环境）
  </action>
  <verify>
    <automated>bash -n scripts/pipeline-stages.sh && echo "语法检查通过"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'source.*cleanup\\.sh' scripts/pipeline-stages.sh` 匹配 `source "$PROJECT_ROOT/scripts/lib/cleanup.sh"`
    - `grep 'disk_snapshot.*部署前' scripts/pipeline-stages.sh` 匹配 2 次（pipeline_deploy 和 pipeline_infra_deploy 各一次）
    - `grep 'cleanup_after_deploy' scripts/pipeline-stages.sh` 匹配 1 次（在 pipeline_cleanup 末尾）
    - `grep 'cleanup_after_infra_deploy' scripts/pipeline-stages.sh` 匹配 1 次（在 pipeline_infra_cleanup 末尾）
    - `bash -n scripts/pipeline-stages.sh` 通过
    - pipeline_cleanup() 中现有的蓝绿容器停止逻辑和镜像清理逻辑未被修改
    - pipeline_infra_cleanup() 中现有的 case 分支（keycloak/nginx/noda-ops/postgres）未被修改
  </acceptance_criteria>
  <done>
    - pipeline-stages.sh 语法检查通过
    - 头部 source 顺序：log.sh -> manage-containers.sh -> secrets.sh -> deploy-check.sh -> image-cleanup.sh -> cleanup.sh
    - pipeline_deploy() 开头有 `disk_snapshot "部署前"`
    - pipeline_cleanup() 末尾有 `cleanup_after_deploy "${WORKSPACE:-$PWD}"`
    - pipeline_infra_deploy() 开头有 `disk_snapshot "部署前"`
    - pipeline_infra_cleanup() 末尾有 `cleanup_after_infra_deploy "$service" "${WORKSPACE:-$PWD}"`
    - 现有逻辑（蓝绿容器停止、SHA 镜像清理、case 分支）完全未改动
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline-stages.sh -> cleanup.sh | 函数调用，依赖 cleanup.sh 正确 source |
| pipeline_cleanup -> Docker daemon | 执行 docker prune 系列命令 |
| pipeline_deploy -> disk_snapshot | 输出磁盘信息到日志，不修改任何状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06 | Denial of Service | disk_snapshot 在 deploy 开头 | accept | 只读取 df/docker system df，不修改任何状态，执行耗时 < 1s |
| T-43-07 | Denial of Service | cleanup_after_deploy 失败 | mitigate | cleanup.sh 内部所有函数使用 `|| true`，失败不传播到 pipeline |
| T-43-08 | Tampering | WORKSPACE 变量为空 | mitigate | 使用 `"${WORKSPACE:-$PWD}"` 提供回退值 |
</threat_model>

<verification>
```bash
# 语法检查
bash -n scripts/pipeline-stages.sh

# source 加载顺序
grep -n 'source.*cleanup.sh' scripts/pipeline-stages.sh

# disk_snapshot 调用（应有 2 处：pipeline_deploy + pipeline_infra_deploy）
grep -n 'disk_snapshot.*部署前' scripts/pipeline-stages.sh

# cleanup_after_deploy 调用
grep -n 'cleanup_after_deploy' scripts/pipeline-stages.sh

# cleanup_after_infra_deploy 调用
grep -n 'cleanup_after_infra_deploy' scripts/pipeline-stages.sh

# 确认现有逻辑未被修改
grep -A5 '停掉非活跃容器' scripts/pipeline-stages.sh
grep 'cleanup_by_date_threshold' scripts/pipeline-stages.sh
```
</verification>

<success_criteria>
1. `bash -n scripts/pipeline-stages.sh` 通过
2. `grep -c 'source.*cleanup.sh' scripts/pipeline-stages.sh` 返回 1（唯一 source 点）
3. `grep -c 'disk_snapshot.*部署前' scripts/pipeline-stages.sh` 返回 2
4. `grep 'cleanup_after_deploy' scripts/pipeline-stages.sh` 存在
5. `grep 'cleanup_after_infra_deploy' scripts/pipeline-stages.sh` 存在
6. 现有 pipeline_cleanup() 中的蓝绿容器停止逻辑和 SHA 镜像清理逻辑行数未减少
7. 现有 pipeline_infra_cleanup() 中的 case 分支结构完整
</success_criteria>

<output>
After completion, create `.planning/phases/43-cleanup-pipeline/43-02-SUMMARY.md`
</output>
