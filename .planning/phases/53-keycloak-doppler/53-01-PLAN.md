---
phase: 53-keycloak-doppler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/init-databases.sh
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "noda_preprod 数据库存在于 PostgreSQL 容器中"
    - "preprod_app 用户可独立连接 noda_preprod 数据库并执行 SELECT"
  artifacts:
    - path: "scripts/init-databases.sh"
      provides: "扩展的数据库初始化脚本，包含 noda_preprod"
      contains: "noda_preprod"
  key_links:
    - from: "scripts/init-databases.sh"
      to: "noda-infra-postgres-prod 容器"
      via: "docker exec psql"
      pattern: "docker exec noda-infra-postgres-prod psql"
---

<objective>
创建 noda_preprod 数据库和独立 preprod_app 用户，实现 pre-prod 环境数据与 prod 完全隔离。

Purpose: Pre-prod 环境需要独立的数据库，避免测试数据污染生产环境。
Output: 扩展后的 scripts/init-databases.sh（包含 noda_preprod 数据库）+ 运行后 noda_preprod 数据库和 preprod_app 用户就绪。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/init-databases.sh
@scripts/lib/log.sh

<interfaces>
<!-- scripts/init-databases.sh 现有结构 -->
REQUIRED_DBS 数组: 每个元素格式 "db_name:db_description"
数据库创建模式: docker exec noda-infra-postgres-prod psql -U postgres -d postgres -c "CREATE DATABASE ..."
数据库存在检查: docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc "SELECT 1 FROM pg_database WHERE datname='...'"
日志函数: source "$SCRIPT_DIR/lib/log.sh" → log_info, log_success, log_error, log_warn
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 init-databases.sh 添加 noda_preprod 数据库创建</name>
  <files>scripts/init-databases.sh</files>
  <read_first>
    scripts/init-databases.sh
    scripts/lib/log.sh
  </read_first>
  <action>
在 `scripts/init-databases.sh` 中进行以下修改：

1. 在 `REQUIRED_DBS` 数组中，在 `"noda_prod:Findclass Application Database"` 之后添加一行：
   ```
   "noda_preprod:Findclass Application Database (pre-prod)"
   ```

   修改后 REQUIRED_DBS 变为：
   ```bash
   REQUIRED_DBS=(
       "noda_prod:Findclass Application Database"
       "noda_preprod:Findclass Application Database (pre-prod)"
       "keycloak:Keycloak Authentication Database"
   )
   ```

2. 在步骤 2（"确保所有数据库存在"）的 for 循环之后、步骤 3（总结）之前，添加一个新的步骤用于创建 preprod_app 用户并授权。这段代码必须放在 `done` 行之后、步骤 3 注释之前：

   ```bash
   # ============================================
   # 步骤 2.5: 创建 pre-prod 独立用户（如果需要）
   # ============================================
   PREPROD_USER="preprod_app"
   PREPROD_PASSWORD="${PREPROD_APP_PASSWORD:-$(openssl rand -hex 16)}"
   
   # 检查 preprod_app 用户是否已存在
   USER_EXISTS=$(docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc \
       "SELECT 1 FROM pg_roles WHERE rolname='$PREPROD_USER';" 2>/dev/null || echo "0")
   
   if [ "$USER_EXISTS" = "1" ]; then
       log_info "✓ $PREPROD_USER 用户已存在"
   else
       log_info "✗ 创建 $PREPROD_USER 用户..."
       if docker exec noda-infra-postgres-prod psql -U postgres -d postgres -c \
           "CREATE USER $PREPROD_USER WITH ENCRYPTED PASSWORD '$PREPROD_PASSWORD';" \
           >/dev/null 2>&1; then
           log_success "✓ $PREPROD_USER 用户已创建"
       else
           log_error "✗ $PREPROD_USER 用户创建失败"
           exit 1
       fi
   fi
   
   # 授予 noda_preprod 数据库权限给 preprod_app 用户
   log_info "配置 $PREPROD_USER 对 noda_preprod 的权限..."
   docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c \
       "GRANT ALL PRIVILEGES ON DATABASE noda_preprod TO $PREPROD_USER;" >/dev/null 2>&1 || true
   docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c \
       "GRANT ALL ON SCHEMA public TO $PREPROD_USER;" >/dev/null 2>&1 || true
   docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c \
       "ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO $PREPROD_USER;" >/dev/null 2>&1 || true
   docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c \
       "ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO $PREPROD_USER;" >/dev/null 2>&1 || true
   log_success "✓ $PREPROD_USER 权限配置完成"
   ```

3. 在脚本文件头部注释区域更新功能描述，将 `防止数据库缺失` 保持不变即可（无需修改头部）。

注意：保持现有的 `set -euo pipefail` 和 `source "$SCRIPT_DIR/lib/log.sh"` 不变。新增代码中 `|| true` 是为了处理重复授权时不报错退出（因为 `set -e`）。
  </action>
  <verify>
    <automated>grep -c 'noda_preprod' scripts/init-databases.sh | grep -v '^0$' && grep -c 'preprod_app' scripts/init-databases.sh | grep -v '^0$'</automated>
  </verify>
  <done>
    - scripts/init-databases.sh 的 REQUIRED_DBS 数组包含 "noda_preprod:Findclass Application Database (pre-prod)"
    - 脚本包含创建 preprod_app 用户的代码块
    - 脚本包含 GRANT ALL PRIVILEGES 和 ALTER DEFAULT PRIVILEGES 授权语句
    - 脚本语法正确：`bash -n scripts/init-databases.sh` 无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 执行 init-databases.sh 创建 noda_preprod 数据库和 preprod_app 用户</name>
  <files>scripts/init-databases.sh</files>
  <read_first>
    scripts/init-databases.sh
  </read_first>
  <action>
1. 先验证 PostgreSQL 容器正在运行：
   ```bash
   docker ps --format "{{.Names}}" | grep "noda-infra-postgres-prod"
   ```
   如果没有输出，停止并报告错误。

2. 执行 init-databases.sh 创建数据库和用户：
   ```bash
   bash scripts/init-databases.sh
   ```

3. 验证数据库已创建：
   ```bash
   docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc \
     "SELECT 1 FROM pg_database WHERE datname='noda_preprod';"
   ```
   预期输出: `1`

4. 验证用户已创建：
   ```bash
   docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc \
     "SELECT 1 FROM pg_roles WHERE rolname='preprod_app';"
   ```
   预期输出: `1`

5. 验证 preprod_app 可连接 noda_preprod：
   ```bash
   docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c \
     "GRANT ALL ON SCHEMA public TO preprod_app;" 2>/dev/null || true
   docker exec noda-infra-postgres-prod psql -U preprod_app -d noda_preprod -c "SELECT current_database(), current_user;"
   ```
   预期输出包含: `noda_preprod` 和 `preprod_app`
  </action>
  <verify>
    <automated>docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c "SELECT 1;" 2>&1 | grep -q '1 row' && docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc "SELECT 1 FROM pg_database WHERE datname='noda_preprod';" | grep -q '1'</automated>
  </verify>
  <done>
    - noda_preprod 数据库存在（pg_database 查询返回 1）
    - preprod_app 用户存在（pg_roles 查询返回 1）
    - preprod_app 用户可连接 noda_preprod 并执行 SELECT
    - init-databases.sh 执行无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init-databases.sh -> PostgreSQL 容器 | 通过 docker exec 执行 psql 命令，信任 Docker socket |
| preprod_app 用户 -> noda_preprod 数据库 | 仅授予 noda_preprod 数据库权限，无跨库访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Elevation of Privilege | preprod_app 用户 | mitigate | 仅授予 noda_preprod 数据库权限，不授予 noda_prod 或其他库权限 |
| T-53-02 | Information Disclosure | 数据库密码 | mitigate | preprod_app 密码由 openssl rand 自动生成，不硬编码在脚本中 |
| T-53-03 | Tampering | init-databases.sh | accept | 脚本在 Git 仓库中版本控制，非运行时生成的配置 |
</threat_model>

<verification>
1. `bash -n scripts/init-databases.sh` 语法检查通过
2. `docker exec noda-infra-postgres-prod psql -U postgres -d noda_preprod -c "SELECT 1;"` 返回成功
3. `docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc "SELECT 1 FROM pg_roles WHERE rolname='preprod_app';"` 返回 `1`
</verification>

<success_criteria>
- noda_preprod 数据库存在于 PostgreSQL 容器中
- preprod_app 用户可连接 noda_preprod 并执行查询
- scripts/init-databases.sh 包含 noda_preprod 且可幂等执行（重复运行不报错）
</success_criteria>

<output>
After completion, create `.planning/phases/53-keycloak-doppler/53-01-SUMMARY.md`
</output>
