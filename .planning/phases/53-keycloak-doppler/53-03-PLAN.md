---
phase: 53-keycloak-doppler
plan: 03
type: execute
wave: 2
depends_on:
  - 53-01
  - 53-02
files_modified:
  - scripts/lib/secrets.sh
  - scripts/doppler-setup.sh
  - scripts/verify-preprod-isolation.sh
  - docker/env-noda-apps-preprod.env
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "Doppler pre config 存在且可访问"
    - "Doppler pre config 中 POSTGRES_DB=noda_preprod"
    - "Doppler pre config 中 KEYCLOAK_REALM=noda-preprod"
    - "scripts/lib/secrets.sh 支持 DOPPLER_CONFIG 环境变量，默认为 prd"
    - "docker/env-noda-apps-preprod.env 存在且 DATABASE_URL 指向 noda_preprod"
    - "verify-preprod-isolation.sh 可验证数据库/realm/Doppler 三层隔离"
  artifacts:
    - path: "scripts/lib/secrets.sh"
      provides: "参数化 Doppler config 的密钥加载库"
      contains: "DOPPLER_CONFIG"
    - path: "scripts/doppler-setup.sh"
      provides: "Doppler pre config 创建脚本"
      contains: "configs clone"
    - path: "scripts/verify-preprod-isolation.sh"
      provides: "Pre-prod 隔离完整性验证"
      contains: "noda_preprod"
    - path: "docker/env-noda-apps-preprod.env"
      provides: "Pre-prod 环境变量模板"
      contains: "noda_preprod"
  key_links:
    - from: "scripts/doppler-setup.sh"
      to: "Doppler API"
      via: "doppler CLI"
      pattern: "doppler (configs|secrets)"
    - from: "scripts/doppler-setup.sh"
      to: "Doppler pre config secrets"
      via: "secrets get + sed 替换数据库名 + secrets set"
      pattern: "secrets get DATABASE_URL.*sed.*secrets set DATABASE_URL"
      notes: "DATABASE_URL 转换链路: doppler-setup.sh 通过 `doppler secrets get DATABASE_URL --plain` 获取当前 URL -> sed 三模式替换（/noda_prod$ | /noda_prod? | /noda_prod(行尾空格)）-> `doppler secrets set DATABASE_URL` 写回。覆盖 PostgreSQL URL 格式: postgresql://user:pass@host:port/dbname（无 query params）和 postgresql://user:pass@host:port/dbname?param=value（有 query params）两种情况。"
    - from: "scripts/lib/secrets.sh"
      to: "Doppler API"
      via: "DOPPLER_CONFIG 环境变量 + doppler secrets download"
      pattern: "doppler secrets download.*--config"
    - from: "docker/env-noda-apps-preprod.env"
      to: "findclass-ssr 容器"
      via: "docker compose env_file"
      pattern: "DATABASE_URL.*noda_preprod"
    - from: "scripts/verify-preprod-isolation.sh"
      to: "PostgreSQL / Keycloak / Doppler"
      via: "docker exec psql / kcadm.sh / doppler CLI"
      pattern: "(docker exec|doppler secrets)"
---

<objective>
创建 Doppler pre config、参数化 secrets.sh 支持 DOPPLER_CONFIG、创建 pre-prod 环境变量模板、编写隔离验证脚本。

Purpose: Pre-prod Pipeline 需要独立的 Doppler config 加载独立密钥，secrets.sh 需要支持 config 参数化以兼容 prod 和 pre-prod。验证脚本确保三层隔离完整。
Output: Doppler pre config（含独立密钥）+ 参数化 secrets.sh + pre-prod env 模板 + 隔离验证脚本。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/lib/secrets.sh
@scripts/verify-doppler-secrets.sh
@docker/env-noda-apps.env
@scripts/lib/log.sh

<!-- 依赖 Plan 01 和 02 的产出物 -->
@.planning/phases/53-keycloak-doppler/53-01-SUMMARY.md
@.planning/phases/53-keycloak-doppler/53-02-SUMMARY.md

<interfaces>
<!-- scripts/lib/secrets.sh 当前接口 -->
load_secrets() 函数:
  - 检查 DOPPLER_TOKEN 环境变量
  - 检查 doppler CLI 可用性
  - 执行: doppler secrets download --no-file --format=env --project noda --config prd
  - eval 注入 shell 环境
  - 当前硬编码 --config prd，需要参数化为 --config $DOPPLER_CONFIG

<!-- docker/env-noda-apps.env 当前结构 -->
格式: KEY=VALUE 或 KEY=${VAR}（动态替换）
关键字段: DATABASE_URL, KEYCLOAK_URL, KEYCLOAK_REALM, KEYCLOAK_CLIENT_ID
Prod 值: noda_prod, noda, noda-frontend

<!-- Doppler CLI 命令 -->
configs clone: doppler configs clone prd pre --project noda
secrets set: doppler secrets set KEY VALUE --config pre --project noda
secrets get: doppler secrets get KEY --plain --config pre --project noda
configs tokens create: doppler configs tokens create <name> --config pre --project noda --plain

<!-- Doppler Token 类型（执行前校验） -->
dp.ct.* = CLI/Personal Token（可创建 config/token，doppler-setup.sh 必须使用此类型）
dp.st.* = Service Token（仅读取指定 config，无法创建 config）
执行 doppler-setup.sh 前必须验证 DOPPLER_TOKEN 以 dp.ct. 开头
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 参数化 secrets.sh + 创建 doppler-setup.sh + 创建 env-noda-apps-preprod.env</name>
  <files>scripts/lib/secrets.sh, scripts/doppler-setup.sh, docker/env-noda-apps-preprod.env</files>
  <read_first>
    scripts/lib/secrets.sh
    scripts/verify-doppler-secrets.sh
    docker/env-noda-apps.env
    scripts/init-databases.sh
  </read_first>
  <action>
**1. 修改 scripts/lib/secrets.sh**

将第 44 行的硬编码 `--config prd` 替换为参数化版本：

将：
```bash
    _secrets=$(doppler secrets download --no-file --format=env --project noda --config prd 2>/dev/null)
```

替换为：
```bash
    # DOPPLER_CONFIG 环境变量控制加载哪个 config，默认 prd（向后兼容）
    local _config="${DOPPLER_CONFIG:-prd}"
    _secrets=$(doppler secrets download --no-file --format=env --project noda --config "$_config" 2>/dev/null)
```

将第 55 行的日志输出：
```bash
    log_success "密钥已从 Doppler 加载（project=noda, config=prd）"
```

替换为：
```bash
    log_success "密钥已从 Doppler 加载（project=noda, config=$_config）"
```

同时更新文件头部注释中的 `D-05 (config=prd)` 为 `D-05 (config=$DOPPLER_CONFIG, 默认 prd)`。

**2. 创建 scripts/doppler-setup.sh**

创建新文件 `scripts/doppler-setup.sh`，遵循 init-databases.sh 的脚本结构模式：

```bash
#!/bin/bash
set -euo pipefail

# ============================================
# Doppler Pre-prod Config 初始化脚本
# ============================================
# 功能：创建 Doppler pre config 并配置 pre-prod 密钥
# 用途：pre-prod 环境初始化时手动执行
# 前置：DOPPLER_TOKEN 环境变量（必须为 CLI Token，以 dp.ct. 开头）
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib/log.sh"

PROJECT="noda"
SOURCE_CONFIG="prd"
TARGET_CONFIG="pre"

# ============================================
# 环境变量检查
# ============================================
if [[ -z "${DOPPLER_TOKEN:-}" ]]; then
    log_error "DOPPLER_TOKEN 环境变量未设置"
    log_error "使用方法: export DOPPLER_TOKEN='<cli-token>'"
    exit 1
fi

# 验证 Token 类型：doppler-setup.sh 需要创建 config 和 token，必须使用 CLI Token
if [[ ! "$DOPPLER_TOKEN" =~ ^dp\.ct\. ]]; then
    log_error "DOPPLER_TOKEN 类型不正确"
    log_error "当前 token 以 '$(echo "$DOPPLER_TOKEN" | cut -c1-6)...' 开头"
    log_error "doppler-setup.sh 需要创建 config 和 service token，必须使用 CLI Token（dp.ct.*）"
    log_error "Service Token（dp.st.*）仅有只读权限，无法创建 config"
    log_error "获取 CLI Token: Doppler Dashboard -> Settings -> CLI -> Generate Token"
    exit 1
fi

if ! command -v doppler &>/dev/null; then
    log_error "doppler CLI 未安装"
    log_error "安装方式: brew install dopplerhq/cli/doppler"
    exit 1
fi

# ============================================
# 步骤 1: 检查 pre config 是否已存在
# ============================================
log_info "检查 Doppler $TARGET_CONFIG config..."

CONFIG_EXISTS=$(doppler configs list --project "$PROJECT" --json 2>/dev/null | grep -c "\"name\": \"$TARGET_CONFIG\"" || echo "0")

if [[ "$CONFIG_EXISTS" -ge 1 ]]; then
    log_info "✓ $TARGET_CONFIG config 已存在"
else
    log_info "✗ 从 $SOURCE_CONFIG config 克隆创建 $TARGET_CONFIG config..."

    # 克隆 prd config 到 pre config
    doppler configs clone "$SOURCE_CONFIG" "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1 || {
        log_error "✗ $TARGET_CONFIG config 克隆失败"
        log_error "尝试手动创建..."
        doppler configs create "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1 || {
            log_error "✗ 手动创建也失败，请检查 Doppler Token 权限"
            exit 1
        }
        # 手动创建后需要上传密钥
        log_info "从 $SOURCE_CONFIG 下载密钥..."
        doppler secrets download --format=env --project "$PROJECT" --config "$SOURCE_CONFIG" 2>/dev/null | \
            doppler secrets upload - --project "$PROJECT" --config "$TARGET_CONFIG" >/dev/null 2>&1 || {
            log_error "✗ 密钥上传失败"
            exit 1
        }
    }

    log_success "✓ $TARGET_CONFIG config 已创建"
fi

# ============================================
# 步骤 2: 修改 pre config 中的关键密钥
# ============================================
log_info "配置 pre-prod 特定密钥..."

# 修改 POSTGRES_DB
doppler secrets set POSTGRES_DB "noda_preprod" --config "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1
log_info "✓ POSTGRES_DB = noda_preprod"

# 修改 KEYCLOAK_REALM
doppler secrets set KEYCLOAK_REALM "noda-preprod" --config "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1
log_info "✓ KEYCLOAK_REALM = noda-preprod"

# 修改 KEYCLOAK_CLIENT_ID（如果存在于 config 中）
if doppler secrets get KEYCLOAK_CLIENT_ID --plain --config "$SOURCE_CONFIG" --project "$PROJECT" >/dev/null 2>&1; then
    doppler secrets set KEYCLOAK_CLIENT_ID "noda-frontend-preprod" --config "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1
    log_info "✓ KEYCLOAK_CLIENT_ID = noda-frontend-preprod"
fi

# 修改 KEYCLOAK_URL（如果存在于 config 中）
if doppler secrets get KEYCLOAK_URL --plain --config "$SOURCE_CONFIG" --project "$PROJECT" >/dev/null 2>&1; then
    doppler secrets set KEYCLOAK_URL "https://pre.auth.noda.co.nz" --config "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1
    log_info "✓ KEYCLOAK_URL = https://pre.auth.noda.co.nz"
fi

# 注意：DATABASE_URL 和 DIRECT_URL 需要指向 noda_preprod
# 这些 URL 包含用户名密码，需要从现有 URL 中替换数据库名
# DATABASE_URL 转换链路:
#   doppler secrets get DATABASE_URL --plain
#     -> 获取当前 URL（如 postgresql://user:pass@host:5432/noda_prod 或 postgresql://user:pass@host:5432/noda_prod?sslmode=require）
#     -> sed 三模式替换处理:
#        1) /noda_prod$ — URL 以数据库名结尾（无 query params）
#        2) /noda_prod? — URL 后有 query params（如 ?sslmode=require）
#        3) /noda_prod  — URL 末尾有换行/空格（行尾空格模式，第三个 sed 清理尾部空格）
#     -> doppler secrets set DATABASE_URL <new_url>
# 覆盖格式: 标准 PostgreSQL URL (postgresql://...) 和带 query params 的 URL
CURRENT_DB_URL=$(doppler secrets get DATABASE_URL --plain --config "$TARGET_CONFIG" --project "$PROJECT" 2>/dev/null || echo "")
if [[ -n "$CURRENT_DB_URL" ]]; then
    # 替换 URL 中的数据库名
    # 模式 1: /noda_prod 在 URL 末尾（最常见：无 query params）
    # 模式 2: /noda_prod 后跟 ?（有 query params 如 ?sslmode=require）
    # 模式 3: /noda_prod 后跟空格或换行（边界情况清理）
    NEW_DB_URL=$(echo "$CURRENT_DB_URL" | sed 's|/noda_prod$|/noda_preprod|' | sed 's|/noda_prod?|/noda_preprod?|' | sed 's|/noda_prod |')
    if [[ "$NEW_DB_URL" != "$CURRENT_DB_URL" ]]; then
        doppler secrets set DATABASE_URL "$NEW_DB_URL" --config "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1
        log_info "✓ DATABASE_URL 指向 noda_preprod"
    else
        log_warn "⚠ DATABASE_URL 未能替换（当前值可能不含 /noda_prod 路径段）"
        log_warn "  当前值: $CURRENT_DB_URL"
    fi
fi

CURRENT_DIRECT_URL=$(doppler secrets get DIRECT_URL --plain --config "$TARGET_CONFIG" --project "$PROJECT" 2>/dev/null || echo "")
if [[ -n "$CURRENT_DIRECT_URL" ]]; then
    NEW_DIRECT_URL=$(echo "$CURRENT_DIRECT_URL" | sed 's|/noda_prod$|/noda_preprod|' | sed 's|/noda_prod?|/noda_preprod?|' | sed 's|/noda_prod |')
    if [[ "$NEW_DIRECT_URL" != "$CURRENT_DIRECT_URL" ]]; then
        doppler secrets set DIRECT_URL "$NEW_DIRECT_URL" --config "$TARGET_CONFIG" --project "$PROJECT" >/dev/null 2>&1
        log_info "✓ DIRECT_URL 指向 noda_preprod"
    else
        log_warn "⚠ DIRECT_URL 未能替换（当前值可能不含 /noda_prod 路径段）"
    fi
fi

# ============================================
# 步骤 3: 创建 Service Token（用于 Pipeline）
# ============================================
log_info "创建 pre config Service Token..."

# 检查 token 是否已存在
EXISTING_TOKENS=$(doppler configs tokens list --config "$TARGET_CONFIG" --project "$PROJECT" --json 2>/dev/null || echo "[]")
TOKEN_NAME="pre-prod-jenkins"

if echo "$EXISTING_TOKENS" | grep -q "$TOKEN_NAME" 2>/dev/null; then
    log_info "✓ Service Token '$TOKEN_NAME' 已存在"
else
    TOKEN_OUTPUT=$(doppler configs tokens create "$TOKEN_NAME" \
        --config "$TARGET_CONFIG" \
        --project "$PROJECT" \
        --plain \
        --max-age 0 2>/dev/null || echo "")

    if [[ -n "$TOKEN_OUTPUT" && "$TOKEN_OUTPUT" == dp.st.* ]]; then
        log_success "✓ Service Token 已创建"
        log_warn "=========================================="
        log_warn "请保存以下 Service Token 到安全位置："
        log_warn "$TOKEN_OUTPUT"
        log_warn "后续需配置到 Jenkins Credentials"
        log_warn "=========================================="
    else
        log_warn "⚠ Service Token 创建失败或输出格式异常"
        log_warn "可手动创建: doppler configs tokens create $TOKEN_NAME --config $TARGET_CONFIG --project $PROJECT --plain"
    fi
fi

# ============================================
# 步骤 4: 验证 pre config
# ============================================
log_info "验证 $TARGET_CONFIG config 密钥..."

PREPROD_DB=$(doppler secrets get POSTGRES_DB --plain --config "$TARGET_CONFIG" --project "$PROJECT" 2>/dev/null || echo "")
if [[ "$PREPROD_DB" == "noda_preprod" ]]; then
    log_info "✓ POSTGRES_DB = $PREPROD_DB"
else
    log_error "✗ POSTGRES_DB 配置错误: '$PREPROD_DB'（预期: noda_preprod）"
    exit 1
fi

PREPROD_REALM=$(doppler secrets get KEYCLOAK_REALM --plain --config "$TARGET_CONFIG" --project "$PROJECT" 2>/dev/null || echo "")
if [[ "$PREPROD_REALM" == "noda-preprod" ]]; then
    log_info "✓ KEYCLOAK_REALM = $PREPROD_REALM"
else
    log_error "✗ KEYCLOAK_REALM 配置错误: '$PREPROD_REALM'（预期: noda-preprod）"
    exit 1
fi

# ============================================
# 步骤 5: 总结
# ============================================
log_success "=========================================="
log_success "Doppler pre config 初始化完成"
log_success "=========================================="
log_info "Config: $TARGET_CONFIG"
log_info "POSTGRES_DB: noda_preprod"
log_info "KEYCLOAK_REALM: noda-preprod"
log_info "KEYCLOAK_CLIENT_ID: noda-frontend-preprod"
log_success "=========================================="

exit 0
```

创建文件后设置可执行权限：`chmod +x scripts/doppler-setup.sh`

**3. 创建 docker/env-noda-apps-preprod.env**

复制 `docker/env-noda-apps.env` 的完整内容，修改以下字段：

将 `DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@noda-infra-postgres-prod:5432/noda_prod` 改为 `DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@noda-infra-postgres-prod:5432/noda_preprod`

将 `DIRECT_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@noda-infra-postgres-prod:5432/noda_prod` 改为 `DIRECT_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@noda-infra-postgres-prod:5432/noda_preprod`

将 `KEYCLOAK_URL=https://auth.noda.co.nz` 改为 `KEYCLOAK_URL=https://pre.auth.noda.co.nz`

将 `KEYCLOAK_REALM=noda` 改为 `KEYCLOAK_REALM=noda-preprod`

将 `KEYCLOAK_CLIENT_ID=noda-frontend` 改为 `KEYCLOAK_CLIENT_ID=noda-frontend-preprod`

更新文件头部注释：将 `noda-apps 环境变量文件` 改为 `noda-apps pre-prod 环境变量文件`，用途说明改为 `pre-prod 环境使用`。

其余字段保持不变（NODE_ENV, NEXT_PUBLIC_LOCAL_API_URL, KEYCLOAK_INTERNAL_URL, RESEND_API_KEY, ANTHROPIC_*, KEYCLOAK_ADMIN_URL, EMAIL_SERVICE 等）。
  </action>
  <verify>
    <automated>grep -c 'DOPPLER_CONFIG' scripts/lib/secrets.sh | grep -v '^0$' && bash -n scripts/doppler-setup.sh && grep -c 'noda_preprod' docker/env-noda-apps-preprod.env | grep -v '^0$' && grep 'KEYCLOAK_REALM=noda-preprod' docker/env-noda-apps-preprod.env | grep -c 'noda-preprod' && grep -c 'dp\.ct\.' scripts/doppler-setup.sh | grep -v '^0$'</automated>
  </verify>
  <done>
    - scripts/lib/secrets.sh 中 load_secrets() 使用 `DOPPLER_CONFIG` 环境变量，默认值为 `prd`
    - scripts/lib/secrets.sh 日志输出显示实际 config 名称
    - scripts/doppler-setup.sh 存在且语法正确
    - scripts/doppler-setup.sh 包含 DOPPLER_TOKEN 类型校验（dp.ct.* CLI Token）
    - scripts/doppler-setup.sh 包含 configs clone、secrets set、service token 创建步骤
    - scripts/doppler-setup.sh 的 DATABASE_URL sed 替换覆盖三种格式（无参数/有参数/行尾空格）
    - docker/env-noda-apps-preprod.env 存在且 DATABASE_URL 包含 noda_preprod
    - docker/env-noda-apps-preprod.env 的 KEYCLOAK_REALM=noda-preprod
    - docker/env-noda-apps-preprod.env 的 KEYCLOAK_CLIENT_ID=noda-frontend-preprod
    - docker/env-noda-apps-preprod.env 的 KEYCLOAK_URL=https://pre.auth.noda.co.nz
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 verify-preprod-isolation.sh 并执行 Doppler 设置 + 完整验证</name>
  <files>scripts/verify-preprod-isolation.sh, scripts/doppler-setup.sh</files>
  <read_first>
    scripts/verify-doppler-secrets.sh
    scripts/doppler-setup.sh
    scripts/lib/log.sh
  </read_first>
  <action>
**1. 创建 scripts/verify-preprod-isolation.sh**

遵循 `scripts/verify-doppler-secrets.sh` 的脚本模式（颜色化输出、逐项检查、总结）：

```bash
#!/bin/bash
set -euo pipefail

# ============================================
# Pre-prod 环境隔离完整性验证脚本
# ============================================
# 功能：验证 pre-prod 环境的数据库、Keycloak realm、Doppler config 隔离
# 用途：Phase 53 完成后执行，确认三层隔离无泄漏
# 前置：DOPPLER_TOKEN 环境变量（具有 pre config 读取权限）
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib/log.sh"

FAILURES=0

# ============================================
# 步骤 1: 验证数据库隔离
# ============================================
log_info "=========================================="
log_info "1. 验证数据库隔离"
log_info "=========================================="

# 检查 noda_preprod 数据库存在
DB_EXISTS=$(docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc \
    "SELECT 1 FROM pg_database WHERE datname='noda_preprod';" 2>/dev/null || echo "0")

if [[ "$DB_EXISTS" == "1" ]]; then
    log_success "✓ noda_preprod 数据库存在"
else
    log_error "✗ noda_preprod 数据库不存在"
    ((FAILURES++))
fi

# 检查 noda_prod 数据库存在（确保 prod 未被影响）
PROD_DB_EXISTS=$(docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc \
    "SELECT 1 FROM pg_database WHERE datname='noda_prod';" 2>/dev/null || echo "0")

if [[ "$PROD_DB_EXISTS" == "1" ]]; then
    log_success "✓ noda_prod 数据库未受影响"
else
    log_error "✗ noda_prod 数据库不存在（prod 被破坏！）"
    ((FAILURES++))
fi

# 检查 preprod_app 用户存在
USER_EXISTS=$(docker exec noda-infra-postgres-prod psql -U postgres -d postgres -tAc \
    "SELECT 1 FROM pg_roles WHERE rolname='preprod_app';" 2>/dev/null || echo "0")

if [[ "$USER_EXISTS" == "1" ]]; then
    log_success "✓ preprod_app 用户存在"
else
    log_warn "⚠ preprod_app 用户不存在（可选，使用 postgres 超级用户）"
fi

# ============================================
# 步骤 2: 验证 Keycloak realm 隔离
# ============================================
log_info "=========================================="
log_info "2. 验证 Keycloak realm 隔离"
log_info "=========================================="

KEYCLOAK_CONTAINER=$(docker ps --format "{{.Names}}" | grep "keycloak" | head -1)

if [[ -z "$KEYCLOAK_CONTAINER" ]]; then
    log_error "✗ Keycloak 容器未运行"
    ((FAILURES++))
else
    # 检查 noda-preprod realm 存在
    REALM_EXISTS=$(docker exec "$KEYCLOAK_CONTAINER" /opt/keycloak/bin/kcadm.sh get realms/noda-preprod --fields realm 2>/dev/null | grep -c "noda-preprod" || echo "0")

    if [[ "$REALM_EXISTS" -ge 1 ]]; then
        log_success "✓ noda-preprod realm 存在"
    else
        log_error "✗ noda-preprod realm 不存在"
        ((FAILURES++))
    fi

    # 检查 noda realm 存在（确保 prod 未被影响）
    PROD_REALM_EXISTS=$(docker exec "$KEYCLOAK_CONTAINER" /opt/keycloak/bin/kcadm.sh get realms/noda --fields realm 2>/dev/null | grep -c "noda" || echo "0")

    if [[ "$PROD_REALM_EXISTS" -ge 1 ]]; then
        log_success "✓ noda (prod) realm 未受影响"
    else
        log_error "✗ noda (prod) realm 不存在（prod 被破坏！）"
        ((FAILURES++))
    fi

    # 检查 Google IdP
    IDP_EXISTS=$(docker exec "$KEYCLOAK_CONTAINER" /opt/keycloak/bin/kcadm.sh get \
        identity-provider/instances/google -r noda-preprod --fields alias 2>/dev/null | grep -c "google" || echo "0")

    if [[ "$IDP_EXISTS" -ge 1 ]]; then
        log_success "✓ Google identity provider 已配置"
    else
        log_error "✗ Google identity provider 未配置"
        ((FAILURES++))
    fi

    # 检查 client
    CLIENT_EXISTS=$(docker exec "$KEYCLOAK_CONTAINER" /opt/keycloak/bin/kcadm.sh get \
        clients -r noda-preprod --fields clientId 2>/dev/null | grep -c "noda-frontend-preprod" || echo "0")

    if [[ "$CLIENT_EXISTS" -ge 1 ]]; then
        log_success "✓ noda-frontend-preprod client 存在"
    else
        log_error "✗ noda-frontend-preprod client 不存在"
        ((FAILURES++))
    fi

    # 检查 redirect URIs
    REDIRECT_CHECK=$(docker exec "$KEYCLOAK_CONTAINER" /opt/keycloak/bin/kcadm.sh get \
        clients -r noda-preprod --fields redirectUris 2>/dev/null | grep -c "pre.class.noda.co.nz" || echo "0")

    if [[ "$REDIRECT_CHECK" -ge 1 ]]; then
        log_success "✓ redirect URIs 包含 pre.class.noda.co.nz"
    else
        log_error "✗ redirect URIs 不包含 pre.class.noda.co.nz"
        ((FAILURES++))
    fi
fi

# ============================================
# 步骤 3: 验证 Doppler config 隔离
# ============================================
log_info "=========================================="
log_info "3. 验证 Doppler config 隔离"
log_info "=========================================="

if [[ -z "${DOPPLER_TOKEN:-}" ]]; then
    log_warn "⚠ DOPPLER_TOKEN 未设置，跳过 Doppler 验证"
    log_warn "  使用方法: DOPPLER_TOKEN='dp.st.pre.xxx' bash scripts/verify-preprod-isolation.sh"
else
    if ! command -v doppler &>/dev/null; then
        log_error "✗ doppler CLI 未安装"
        ((FAILURES++))
    else
        # 检查 pre config 可访问
        if doppler secrets --only-names --project noda --config pre >/dev/null 2>&1; then
            log_success "✓ Doppler pre config 可访问"
        else
            log_error "✗ Doppler pre config 不可访问"
            ((FAILURES++))
        fi

        # 检查 POSTGRES_DB
        PREPROD_DB=$(doppler secrets get POSTGRES_DB --plain --project noda --config pre 2>/dev/null || echo "")
        if [[ "$PREPROD_DB" == "noda_preprod" ]]; then
            log_success "✓ POSTGRES_DB = noda_preprod"
        else
            log_error "✗ POSTGRES_DB = '$PREPROD_DB'（预期: noda_preprod）"
            ((FAILURES++))
        fi

        # 检查 KEYCLOAK_REALM
        PREPROD_REALM=$(doppler secrets get KEYCLOAK_REALM --plain --project noda --config pre 2>/dev/null || echo "")
        if [[ "$PREPROD_REALM" == "noda-preprod" ]]; then
            log_success "✓ KEYCLOAK_REALM = noda-preprod"
        else
            log_error "✗ KEYCLOAK_REALM = '$PREPROD_REALM'（预期: noda-preprod）"
            ((FAILURES++))
        fi

        # 确认 prd config 未被修改（安全检查）
        PROD_DB=$(doppler secrets get POSTGRES_DB --plain --project noda --config prd 2>/dev/null || echo "")
        if [[ "$PROD_DB" == "noda_prod" ]]; then
            log_success "✓ prd config POSTGRES_DB 未被修改（仍为 noda_prod）"
        else
            log_error "✗ prd config POSTGRES_DB 已被修改为 '$PROD_DB'（prod 被污染！）"
            ((FAILURES++))
        fi
    fi
fi

# ============================================
# 总结
# ============================================
log_info "=========================================="
if [[ $FAILURES -eq 0 ]]; then
    log_success "Pre-prod 环境隔离验证通过"
    log_success "所有检查项均正常"
else
    log_error "Pre-prod 环境隔离验证失败"
    log_error "$FAILURES 项检查未通过"
fi
log_info "=========================================="

exit $FAILURES
```

创建后设置可执行权限：`chmod +x scripts/verify-preprod-isolation.sh`

**2. 执行 doppler-setup.sh 创建 pre config**

前置条件：需要 DOPPLER_TOKEN 为 CLI Token（以 dp.ct. 开头）。脚本内部会校验 token 类型，如果是 Service Token（dp.st.*）会报错退出。

```bash
# 验证 doppler CLI 可用
command -v doppler && doppler --version

# 验证 token 类型（必须以 dp.ct. 开头）
echo "Token prefix: ${DOPPLER_TOKEN:0:6}"

# 执行（DOPPLER_TOKEN 需要已设置）
bash scripts/doppler-setup.sh
```

**3. 执行 verify-preprod-isolation.sh 验证隔离**

```bash
# 使用 pre config 的 service token 验证
bash scripts/verify-preprod-isolation.sh
```

验证所有检查项通过。
  </action>
  <verify>
    <automated>bash -n scripts/verify-preprod-isolation.sh && grep -c 'noda_preprod' scripts/verify-preprod-isolation.sh | grep -v '^0$' && grep -c 'noda-preprod' scripts/verify-preprod-isolation.sh | grep -v '^0$' && grep -c 'pre.class.noda.co.nz' scripts/verify-preprod-isolation.sh | grep -v '^0$'</automated>
  </verify>
  <done>
    - scripts/verify-preprod-isolation.sh 存在且语法正确
    - 验证脚本包含数据库、Keycloak、Doppler 三层隔离检查
    - 验证脚本包含 prod 安全检查（确保 prod 未被 pre-prod 操作影响）
    - doppler-setup.sh 已执行，pre config 已创建
    - doppler-setup.sh 执行前 DOPPLER_TOKEN 已校验为 CLI Token（dp.ct.*）
    - verify-preprod-isolation.sh 已执行，所有检查通过（exit 0）
    - Doppler pre config 中 POSTGRES_DB=noda_preprod
    - Doppler pre config 中 KEYCLOAK_REALM=noda-preprod
    - Doppler prd config 未被修改（POSTGRES_DB 仍为 noda_prod）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doppler-setup.sh -> Doppler API | 通过 doppler CLI 操作，需要 DOPPLER_TOKEN（CLI Token） |
| Pipeline (future) -> Doppler pre config | 通过 Service Token (dp.st.pre.xxx)，只读访问 |
| pre-prod 容器 -> Doppler pre config | 加载 pre-prod 密钥，不含 prod 密钥 |
| Doppler pre config vs prd config | config 级别隔离，service token 不能跨 config 访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Information Disclosure | Doppler pre service token 泄露 | mitigate | Service Token 仅授予 pre config 只读权限，即使泄露也无法访问 prd config |
| T-53-09 | Spoofing | 环境混淆（pre-prod 加载 prod 密钥） | mitigate | DOPPLER_CONFIG 环境变量显式指定，Pipeline 脚本中硬编码 DOPPLER_CONFIG=pre，verify-preprod-isolation.sh 验证隔离 |
| T-53-10 | Tampering | Doppler pre config 被意外修改 | accept | Doppler 提供审计日志和版本管理，config 级别权限控制已足够 |
| T-53-11 | Elevation of Privilege | pre service token 访问 prd config | mitigate | Doppler Service Token 绑定到特定 config，无法跨 config 访问（Doppler 平台保证） |
| T-53-12 | Elevation of Privilege | 使用 Service Token 执行 doppler-setup.sh | mitigate | 脚本启动时校验 DOPPLER_TOKEN 必须以 dp.ct. 开头（CLI Token），拒绝 dp.st.* Service Token |
</threat_model>

<verification>
## 脚本语法验证（Nyquist 合规）
1. `bash -n scripts/doppler-setup.sh` 语法检查通过
2. `bash -n scripts/verify-preprod-isolation.sh` 语法检查通过
3. `bash -n scripts/lib/secrets.sh` 语法检查通过

## 配置正确性验证
4. `grep 'DOPPLER_CONFIG' scripts/lib/secrets.sh` 显示参数化代码
5. `grep 'noda_preprod' docker/env-noda-apps-preprod.env` 显示 pre-prod 数据库配置
6. `grep 'dp\.ct\.' scripts/doppler-setup.sh` 确认 token 类型校验存在

## 运行时验证（需要 DOPPLER_TOKEN）
7. `doppler secrets get POSTGRES_DB --plain --project noda --config pre` 返回 `noda_preprod`
8. `doppler secrets get KEYCLOAK_REALM --plain --project noda --config pre` 返回 `noda-preprod`
9. `bash scripts/verify-preprod-isolation.sh` 退出码为 0
</verification>

<success_criteria>
- Doppler pre config 存在且可访问
- Doppler pre config 中 POSTGRES_DB=noda_preprod、KEYCLOAK_REALM=noda-preprod
- Doppler pre config 的 Service Token 以 dp.st. 开头
- scripts/lib/secrets.sh 支持 DOPPLER_CONFIG 环境变量，默认 prd（向后兼容）
- docker/env-noda-apps-preprod.env 包含指向 noda_preprod 的 DATABASE_URL
- verify-preprod-isolation.sh 全部检查通过（exit 0）
- Doppler prd config 未被修改（prod 安全）
- doppler-setup.sh 启动时校验 DOPPLER_TOKEN 为 CLI Token（dp.ct.*），拒绝 Service Token
</success_criteria>

<output>
After completion, create `.planning/phases/53-keycloak-doppler/53-03-SUMMARY.md`
</output>
