---
phase: 44-jenkins-maintenance-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/cleanup.sh
autonomous: true
requirements: [JENK-02, CACHE-02, CACHE-03]

must_haves:
  truths:
    - "Jenkins workspace 中 @tmp 残留目录被自动识别和清理"
    - "pnpm store prune 执行前检查 7 天间隔，间隔不足则跳过"
    - "npm cache 被 npm cache clean --force 清理，失败不影响后续操作"
    - "FORCE 参数传入 pnpm store prune 时跳过 7 天间隔检查"
  artifacts:
    - path: "scripts/lib/cleanup.sh"
      provides: "3 个新清理函数: cleanup_jenkins_workspace, cleanup_pnpm_store, cleanup_npm_cache"
      contains: "cleanup_jenkins_workspace"
    - path: "scripts/lib/cleanup.sh"
      provides: "7 天间隔标记文件机制"
      contains: "pnpm-prune-marker"
  key_links:
    - from: "cleanup_pnpm_store"
      to: "pnpm CLI"
      via: "pnpm store prune"
      pattern: "pnpm store prune"
    - from: "cleanup_npm_cache"
      to: "npm CLI"
      via: "npm cache clean --force"
      pattern: "npm cache clean --force"
    - from: "cleanup_jenkins_workspace"
      to: "/var/lib/jenkins/workspace/"
      via: "文件系统遍历 + rm -rf @tmp"
      pattern: "@tmp"
---

<objective>
在 cleanup.sh 中扩展 3 个新函数，覆盖 Jenkins workspace 清理、pnpm store 定期 prune、npm cache 定期清理。

Purpose: 为 Plan 02 的 Jenkinsfile.cleanup Pipeline 提供可复用的清理函数（per D-02, D-05）。这 3 个函数遵循 cleanup.sh 已有的 Source Guard + `|| true` 模式，可直接被 Jenkinsfile sh 块 source 调用。
Output: cleanup.sh 末尾追加 3 个新函数 + 1 个定期清理 wrapper
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-jenkins-maintenance-cleanup/44-CONTEXT.md
@.planning/phases/44-jenkins-maintenance-cleanup/44-RESEARCH.md
@.planning/phases/44-jenkins-maintenance-cleanup/44-PATTERNS.md

@scripts/lib/cleanup.sh
@scripts/lib/log.sh

<interfaces>
<!-- cleanup.sh 现有函数签名（executor 必须遵循的模式） -->

From scripts/lib/cleanup.sh:
```bash
# 函数定义模式（所有新函数必须遵循）
# 1. 注释头：函数名 + 需求 ID + 参数说明 + 返回说明
# 2. local 变量声明
# 3. log_info 开始 + log_success 结束
# 4. 所有外部命令加 2>/dev/null || true

# Source Guard（已有，新函数追加在 guard 之后）
if [[ -n "${_NODA_CLEANUP_LOADED:-}" ]]; then return 0; fi
_NODA_CLEANUP_LOADED=1

# 保留策略环境变量模式
BUILD_CACHE_RETENTION_HOURS="${BUILD_CACHE_RETENTION_HOURS:-24}"

# 磁盘快照函数（可复用）
disk_snapshot() { local label="${1:-快照}"; ... }

# wrapper 函数模式
cleanup_after_deploy() { ... }
cleanup_after_infra_deploy() { ... }
```

From scripts/lib/log.sh:
```bash
log_info()    # 黄色信息
log_success() # 绿色成功
log_warn()    # 黄色警告
log_error()   # 红色错误
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 cleanup.sh 添加 3 个新清理函数 + 定期清理 wrapper</name>
  <files>scripts/lib/cleanup.sh</files>
  <read_first>
    - scripts/lib/cleanup.sh（完整文件，确认追加位置）
  </read_first>
  <action>
在 cleanup.sh 文件末尾（`cleanup_after_infra_deploy()` 函数之后）追加以下 4 个函数。严格遵循现有函数模式（注释头 + local 变量 + log_info/log_success + `|| true`）。

**函数 1: `cleanup_jenkins_workspace`** (per D-02, 覆盖 JENK-02)

```bash
# -------------------------------------------
# Jenkins Workspace 清理 (JENK-02)
# -------------------------------------------
# 参数:
#   $1: Jenkins workspace 根路径（默认 /var/lib/jenkins/workspace）
# 返回：无（清理 @tmp 残留目录，释放磁盘空间）
cleanup_jenkins_workspace()
{
    local workspace_root="${1:-/var/lib/jenkins/workspace}"

    if [ ! -d "$workspace_root" ]; then
        log_info "Jenkins workspace 目录不存在: $workspace_root"
        return 0
    fi

    log_info "检查 Jenkins workspace: $workspace_root"

    local cleaned=0
    for dir in "$workspace_root"/*/; do
        [ -d "$dir" ] || continue
        local dirname
        dirname=$(basename "$dir")
        # 清理策略: 删除 @tmp 目录（Jenkins SCM checkout 临时目录残留）
        if [[ "$dirname" == *@tmp ]]; then
            local size
            size=$(du -sh "$dir" 2>/dev/null | awk '{print $1}' || echo "unknown")
            log_info "清理 @tmp 目录: $dirname ($size)"
            rm -rf "$dir" || true
            cleaned=$((cleaned + 1))
        fi
    done

    log_success "Jenkins workspace 清理完成: ${cleaned} 个临时目录已清理"
}
```

**函数 2: `cleanup_pnpm_store`** (per D-05/D-06, 覆盖 CACHE-02)

```bash
# -------------------------------------------
# pnpm Store 定期清理 (CACHE-02)
# -------------------------------------------
# 参数:
#   $1: 强制模式（"force" 忽略 7 天间隔检查，per D-06）
# 返回：无（移除未引用的 pnpm 包）
# 间隔：7 天（可通过 FORCE 参数强制执行）
cleanup_pnpm_store()
{
    local force_mode="${1:-}"
    local marker_file="${HOME}/.cache/noda-cleanup/pnpm-prune-marker"

    # 间隔检查：非强制模式下，距上次 prune 不足 7 天则跳过
    if [ "$force_mode" != "force" ]; then
        if [ -f "$marker_file" ]; then
            local last_epoch
            last_epoch=$(stat -f '%m' "$marker_file" 2>/dev/null || stat -c '%Y' "$marker_file" 2>/dev/null || echo "0")
            local now_epoch
            now_epoch=$(date +%s)
            local age_days=$(( (now_epoch - last_epoch) / 86400 ))
            if [ "$age_days" -lt 7 ]; then
                log_info "pnpm store prune 跳过（距上次仅 ${age_days} 天，需 >= 7 天）"
                return 0
            fi
        fi
    fi

    log_info "pnpm store prune（清理未引用包）..."

    local store_path
    store_path=$(pnpm store path 2>/dev/null || echo "unknown")
    if [ "$store_path" != "unknown" ]; then
        local before_size
        before_size=$(du -sh "$store_path" 2>/dev/null | awk '{print $1}' || echo "unknown")
        log_info "pnpm store 当前大小: ${before_size}"
    fi

    pnpm store prune 2>/dev/null || true

    # 更新标记文件时间戳
    mkdir -p "$(dirname "$marker_file")" 2>/dev/null || true
    touch "$marker_file" 2>/dev/null || true

    log_success "pnpm store prune 完成"
}
```

**函数 3: `cleanup_npm_cache`** (per D-05, 覆盖 CACHE-03)

```bash
# -------------------------------------------
# npm Cache 定期清理 (CACHE-03)
# -------------------------------------------
# 参数：无
# 返回：无（清理 npm 缓存目录）
cleanup_npm_cache()
{
    log_info "npm cache clean --force..."

    local cache_path
    cache_path=$(npm config get cache 2>/dev/null || echo "unknown")
    if [ "$cache_path" != "unknown" ]; then
        local before_size
        before_size=$(du -sh "$cache_path" 2>/dev/null | awk '{print $1}' || echo "unknown")
        log_info "npm cache 当前大小: ${before_size}"
    fi

    npm cache clean --force 2>/dev/null || true

    log_success "npm cache 清理完成"
}
```

**函数 4: `cleanup_periodic_maintenance`** (定期清理 wrapper，供 Jenkinsfile.cleanup 调用)

```bash
# -------------------------------------------
# 定期维护清理 wrapper
# -------------------------------------------
# 参数:
#   $1: 强制模式（"force" 忽略间隔限制，per D-06）
# 返回：无（编排 Jenkins workspace + pnpm store + npm cache 清理）
cleanup_periodic_maintenance()
{
    local force_mode="${1:-}"

    log_info "=== 开始定期维护清理 ==="

    cleanup_jenkins_workspace

    if [ "$force_mode" = "force" ]; then
        cleanup_pnpm_store "force"
    else
        cleanup_pnpm_store
    fi

    cleanup_npm_cache

    # 磁盘快照不可跳过
    disk_snapshot "定期清理后"

    log_success "=== 定期维护清理完成 ==="
}
```
  </action>
  <verify>
    <automated>bash -n scripts/lib/cleanup.sh && echo "SYNTAX OK"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/cleanup.sh` 包含函数 `cleanup_jenkins_workspace`，grep 命令 `grep -c 'cleanup_jenkins_workspace()' scripts/lib/cleanup.sh` 返回 1
    - `scripts/lib/cleanup.sh` 包含函数 `cleanup_pnpm_store`，grep 命令 `grep -c 'cleanup_pnpm_store()' scripts/lib/cleanup.sh` 返回 1
    - `scripts/lib/cleanup.sh` 包含函数 `cleanup_npm_cache`，grep 命令 `grep -c 'cleanup_npm_cache()' scripts/lib/cleanup.sh` 返回 1
    - `scripts/lib/cleanup.sh` 包含 wrapper 函数 `cleanup_periodic_maintenance`，grep 命令 `grep -c 'cleanup_periodic_maintenance()' scripts/lib/cleanup.sh` 返回 1
    - `cleanup_pnpm_store` 包含 7 天间隔检查逻辑：`grep -c 'age_days.*-lt 7' scripts/lib/cleanup.sh` 返回 1
    - `cleanup_pnpm_store` 包含 marker 文件：`grep 'pnpm-prune-marker' scripts/lib/cleanup.sh` 有输出
    - `cleanup_jenkins_workspace` 包含 @tmp 目录匹配：`grep '@tmp' scripts/lib/cleanup.sh` 有输出
    - 所有外部命令都有 `|| true`：`grep -c '|| true' scripts/lib/cleanup.sh` 大于等于 15（现有 12 + 新增 3+）
    - Source Guard 未被重复添加：`grep -c '_NODA_CLEANUP_LOADED' scripts/lib/cleanup.sh` 等于 2（if 判断 + 赋值，与现有一致）
    - `bash -n scripts/lib/cleanup.sh` 退出码为 0
  </acceptance_criteria>
  <done>cleanup.sh 包含 3 个新清理函数（cleanup_jenkins_workspace, cleanup_pnpm_store, cleanup_npm_cache）和 1 个 wrapper（cleanup_periodic_maintenance），bash 语法检查通过，所有函数遵循现有模式（log_info/log_success + || true + local 变量）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins workspace 文件系统 | cleanup_jenkins_workspace 操作 /var/lib/jenkins/workspace/ 目录 |
| pnpm global store | cleanup_pnpm_store 操作 pnpm 全局 store 目录 |
| npm cache 目录 | cleanup_npm_cache 操作 npm 缓存目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | cleanup_jenkins_workspace | mitigate | workspace_root 使用硬编码默认值 `/var/lib/jenkins/workspace`，不接受外部输入构造路径 |
| T-44-02 | Tampering | cleanup_jenkins_workspace | accept | `rm -rf` 不跟随符号链接（bash 默认行为），且 @tmp 目录名模式固定 |
| T-44-03 | Denial of Service | cleanup_pnpm_store | mitigate | 7 天间隔标记文件 + 凌晨 3 点低峰期执行（Pipeline 层面控制），避免与 pnpm install 冲突 |
| T-44-04 | Denial of Service | cleanup_jenkins_workspace | mitigate | Jenkinsfile.cleanup 使用 `disableConcurrentBuilds()` 防止与构建并发（Plan 02 实现） |
</threat_model>

<verification>
1. `bash -n scripts/lib/cleanup.sh` 语法检查通过
2. `grep -c 'cleanup_jenkins_workspace\|cleanup_pnpm_store\|cleanup_npm_cache\|cleanup_periodic_maintenance' scripts/lib/cleanup.sh` 返回 4（每个函数各一个定义行）
3. Source Guard 完整性：`grep -c '_NODA_CLEANUP_LOADED' scripts/lib/cleanup.sh` 等于 2
</verification>

<success_criteria>
- cleanup.sh 追加 3 个新清理函数 + 1 个 wrapper，bash -n 语法检查通过
- 每个函数遵循现有模式：注释头 + local 变量 + log_info/log_success + || true
- cleanup_pnpm_store 包含 7 天间隔标记文件机制和 FORCE 参数支持
- cleanup_jenkins_workspace 遍历 @tmp 目录并清理
- cleanup_npm_cache 执行 npm cache clean --force
</success_criteria>

<output>
After completion, create `.planning/phases/44-jenkins-maintenance-cleanup/44-01-SUMMARY.md`
</output>
