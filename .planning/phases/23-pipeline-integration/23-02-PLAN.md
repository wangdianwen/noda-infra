---
phase: 23-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - scripts/pipeline-stages.sh
autonomous: false
requirements:
  - PIPE-01
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Jenkins Test 阶段 pnpm lint 失败时 Pipeline 中止，不进入 Deploy 阶段"
    - "Jenkins Test 阶段 pnpm test 失败时 Pipeline 中止，不进入 Deploy 阶段"
    - "Stage View 可区分 install、lint 和 test 失败（三个独立 sh 步骤）"
    - "Pre-flight 阶段检查 Node.js/pnpm 可用性，未安装时给出明确安装指引"
  artifacts:
    - path: "scripts/pipeline-stages.sh"
      provides: "pipeline_preflight 环境检查增强（Node.js/pnpm/noda-apps/package.json 验证）"
      contains: "command -v node"
  key_links:
    - from: "jenkins/Jenkinsfile Test stage"
      to: "noda-apps/package.json"
      via: "独立 sh 步骤直接调用 pnpm lint 和 pnpm test（不通过 pipeline_test 函数）"
      pattern: "pnpm (lint|test)"
    - from: "jenkins/Jenkinsfile Pre-flight stage"
      to: "scripts/pipeline-stages.sh pipeline_preflight()"
      via: "pipeline_preflight 函数执行所有前置检查（单一真相源）"
      pattern: "pipeline_preflight"
---

<objective>
增强 Pre-flight 阶段环境检查，确保 Node.js/pnpm 可用性和 noda-apps 项目完整性。人工验证 Phase 23 所有产出。

Purpose: PIPE-01 要求 8 阶段 Pipeline，TEST-01 和 TEST-02 要求 lint/test 不通过时中止部署。Plan 01 已创建 Jenkinsfile 骨架（含独立 pnpm lint/test sh 步骤），本计划增强 Pre-flight 阶段的环境检查覆盖范围，确保在 Test 阶段执行前发现环境问题。

Output: 更新 pipeline-stages.sh 的 pipeline_preflight 函数（添加 Node.js/pnpm/noda-apps 检查），人工验证 Pipeline 配置完整性
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-pipeline-integration/23-CONTEXT.md
@.planning/phases/23-pipeline-integration/23-RESEARCH.md
@.planning/phases/23-pipeline-integration/23-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的 pipeline-stages.sh 函数接口 -->

### pipeline_preflight() — 当前实现（Plan 01 创建，需增强）:
- 检查 Docker daemon 可用
- 检查 nginx 容器运行中
- 检查 noda-network 存在
- 检查 pnpm 可用性
- **缺少**: Node.js 检查、noda-apps 目录检查、package.json lint/test 脚本验证

### pipeline_test(APPS_DIR) — 当前实现（Plan 01 创建，仅处理 install）:
- cd 到 APPS_DIR
- 执行 pnpm install --frozen-lockfile
- lint/test 由 Jenkinsfile 独立 sh 步骤调用（不在此函数中）

### Jenkinsfile Test stage — 当前实现（Plan 01 创建）:
```groovy
stage('Test') {
    steps {
        dir('noda-apps') {
            sh '''
                source "$WORKSPACE/scripts/lib/log.sh"
                source "$WORKSPACE/scripts/pipeline-stages.sh"
                pipeline_test "$PWD"
            '''
            sh 'pnpm lint'
            sh 'pnpm test'
        }
    }
}
```
lint 和 test 已为独立 sh 步骤，Stage View 可区分失败步骤（RESEARCH.md Pitfall 4）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 pipeline_preflight 环境检查（Node.js/pnpm/noda-apps/package.json 验证）</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    - scripts/pipeline-stages.sh（Plan 01 创建，当前 pipeline_preflight 和 pipeline_test 实现）
    - scripts/lib/log.sh（日志函数接口）
  </read_first>
  <acceptance_criteria>
    - pipeline_preflight 函数检查 Node.js 可用性: `command -v node`
    - pipeline_preflight 函数检查 pnpm 可用性: `command -v pnpm`
    - pipeline_preflight 函数 Node.js/pnpm 不可用时输出明确的安装指引（包含 apt 命令）
    - pipeline_preflight 函数检查 noda-apps 目录存在且包含 package.json
    - pipeline_preflight 函数检查 noda-apps/package.json 包含 lint 和 test 脚本
    - pipeline_preflight 函数接受可选参数 APPS_DIR（默认 $WORKSPACE/noda-apps）
    - pipeline_test 函数仅处理 pnpm install --frozen-lockfile（不包含 lint/test 逻辑）
    - pipeline_test 函数不包含 [lint]/[test] 阶段标签（这些由 Jenkinsfile 独立 sh 步骤处理）
  </acceptance_criteria>
  <action>
修改 `scripts/pipeline-stages.sh` 中的 pipeline_preflight 函数，添加 Node.js/pnpm/noda-apps 环境检查。

**增强 pipeline_preflight() — 添加 Node.js/pnpm/noda-apps 检查**

函数签名改为 `pipeline_preflight([APPS_DIR])`，在现有的 Docker/nginx/network/pnpm 检查之后添加：

```bash
# 检查 Node.js
if ! command -v node >/dev/null 2>&1; then
  log_error "Node.js 未安装"
  log_error "安装方式: curl -fsSL https://deb.nodesource.com/setup_21.x | sudo -E bash - && sudo apt install -y nodejs"
  return 1
fi
log_info "Node.js: $(node --version)"

# 检查 pnpm 版本输出（pnpm 可用性已在上方检查）
log_info "pnpm: $(pnpm --version)"

# 检查 noda-apps 目录
local apps_dir="${1:-$WORKSPACE/noda-apps}"
if [ ! -d "$apps_dir" ]; then
  log_error "noda-apps 目录不存在: $apps_dir"
  log_error "请检查 Jenkinsfile Pre-flight stage 的 checkout 配置"
  return 1
fi

# 检查 package.json 和 lint/test 脚本
if [ ! -f "$apps_dir/package.json" ]; then
  log_error "noda-apps/package.json 不存在: $apps_dir/package.json"
  return 1
fi

if ! grep -q '"lint"' "$apps_dir/package.json"; then
  log_error "noda-apps/package.json 缺少 lint 脚本"
  log_error "请在 package.json 的 scripts 中添加: \"lint\": \"eslint .\""
  return 1
fi

if ! grep -q '"test"' "$apps_dir/package.json"; then
  log_error "noda-apps/package.json 缺少 test 脚本"
  log_error "请在 package.json 的 scripts 中添加: \"test\": \"vitest run\""
  return 1
fi

log_success "所有前置检查通过"
```

注意：
- pipeline_preflight 需要接受一个可选参数 APPS_DIR（默认 $WORKSPACE/noda-apps）
- pnpm 可用性检查已在 Plan 01 的 pipeline_preflight 中存在（`command -v pnpm`），这里不再重复，只添加版本输出
- pipeline_test 函数保持 Plan 01 的简洁实现（仅 `pnpm install --frozen-lockfile`），不添加 [lint]/[test] 阶段标签
- lint 和 test 的执行由 Jenkinsfile Test stage 中的独立 sh 步骤直接调用（`sh 'pnpm lint'` / `sh 'pnpm test'`），这样 Jenkins Stage View 可以区分 install、lint、test 三步的失败
  </action>
  <verify>
    <automated>bash -c 'source scripts/pipeline-stages.sh 2>/dev/null; declare -f pipeline_preflight | grep -q "command -v node" && declare -f pipeline_preflight | grep -q "package.json" && declare -f pipeline_preflight | grep -q "lint" && echo "CHECKS PASSED"'</automated>
  </verify>
  <done>
    1. pipeline_preflight 检查 Node.js、pnpm 版本输出、noda-apps 目录、package.json lint/test 脚本完整性
    2. pipeline_test 仅处理 pnpm install，不包含 lint/test 阶段标签（避免死代码）
    3. Pre-flight 阶段可在 Test 阶段之前发现所有环境问题（Docker/nginx/network/Node.js/pnpm/noda-apps），避免无意义的 lint/test 执行
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 验证 Pipeline 配置完整性和语法正确性</name>
  <files>jenkins/Jenkinsfile, scripts/pipeline-stages.sh</files>
  <action>
在服务器上人工验证 Phase 23 所有产出。Claude 已自动化：pipeline-stages.sh 语法检查、Jenkinsfile 8 阶段结构、03-pipeline-job.groovy SCM 模式。人工需要验证：Jenkins UI 作业配置、手动触发构建。
  </action>
  <what-built>
    Plan 01 和 Plan 02 完成了以下工作：
    1. `scripts/pipeline-stages.sh` — 9 个 pipeline_* 阶段函数 + 3 个复用函数，pipeline_preflight 包含完整环境检查（Docker/nginx/network/Node.js/pnpm/noda-apps/package.json）
    2. `jenkins/Jenkinsfile` — 8 阶段 Declarative Pipeline（Pre-flight 单一真相源调用 pipeline_preflight，Test 三步独立 sh 步骤，手动触发 + 失败日志归档）
    3. `scripts/jenkins/init.groovy.d/03-pipeline-job.groovy` — SCM 模式作业配置
  </what-built>
  <how-to-verify>
    在服务器上执行以下验证步骤：

    **Step 1: 本地语法检查**
    ```bash
    # pipeline-stages.sh 语法检查
    bash -n scripts/pipeline-stages.sh && echo "pipeline-stages.sh: OK"

    # 确认 source 加载无副作用
    bash -c 'source scripts/pipeline-stages.sh; echo "SOURCE OK"'

    # 确认 8 个 stage 都存在
    grep "stage('"'"'" jenkins/Jenkinsfile | head -10

    # 确认 Pre-flight 无内联 Docker 检查（单一真相源）
    # 以下命令应无输出（无内联检查）
    grep -n "docker info" jenkins/Jenkinsfile

    # 确认 SCM 模式配置
    grep "CpsScmFlowDefinition" scripts/jenkins/init.groovy.d/03-pipeline-job.groovy

    # 确认 Test stage 有三个独立 sh 步骤
    grep -A 10 "stage('Test')" jenkins/Jenkinsfile | grep "pnpm"
    ```

    **Step 2: Jenkins 作业更新**（需要 Jenkins 已运行，Phase 19 已完成）
    ```bash
    # 方式 A：重新运行 init.groovy.d 脚本（需 Jenkins restart）
    # 方式 B：在 Jenkins UI 中手动更新（推荐）
    # 访问 http://<server>:8888/job/noda-apps-deploy/configure
    # Pipeline -> Definition 改为 "Pipeline script from SCM"
    # SCM: Git, Repository URL: git@github.com:dianwenwang/noda-infra.git
    # Credentials: 选择 noda-infra-git-credentials
    # Branch: */main
    # Script Path: jenkins/Jenkinsfile
    # Lightweight checkout: 勾选
    ```

    **Step 3: 触发一次构建验证**
    ```bash
    # 在 Jenkins UI 中点击 "Build Now"
    # 预期：Pre-flight 阶段 checkout noda-apps，Build 阶段构建镜像，
    # Test 阶段执行 install/lint/test（三个独立步骤），后续阶段部署蓝绿容器
    # 首次可能因凭据未配置而失败 — 这是预期的
    ```

    **Step 4: 验证手动触发（非自动触发）**
    - 确认 Jenkins 作业配置中无 "Build Triggers" 或 "Poll SCM" 勾选
    - 确认只能通过 "Build Now" 手动触发 (PIPE-04)
  </how-to-verify>
  <verify>
    <automated>bash -c 'bash -n scripts/pipeline-stages.sh && bash -c "source scripts/pipeline-stages.sh 2>/dev/null; echo OK" && grep -c "stage(" jenkins/Jenkinsfile | grep -q "8" && echo "PRE-VERIFY PASSED"'</automated>
  </verify>
  <done>人工确认：Jenkins 作业配置为 SCM 模式 + Jenkinsfile 路径正确 + 手动触发执行 Pipeline + 无自动触发配置 + Pre-flight 无内联检查 + Test 三步独立</done>
  <resume-signal>输入 "approved" 确认所有验证通过，或描述需要修复的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins -> noda-apps 代码 | checkout 外部仓库代码，可能包含恶意依赖 |
| pnpm install -> npm registry | 下载第三方依赖，供应链攻击风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | noda-apps 代码注入 | accept | noda-apps 是自有仓库，SSH key 认证确保来源可信 |
| T-23-07 | Information Disclosure | pnpm install 泄露信息 | accept | npm registry 通信为 HTTPS，无敏感数据泄露路径 |
</threat_model>

<verification>
1. `bash -n scripts/pipeline-stages.sh` 语法检查通过
2. `bash -c 'source scripts/pipeline-stages.sh; echo OK'` source 加载无副作用
3. `grep 'command -v node' scripts/pipeline-stages.sh` 匹配 Node.js 检查
4. `grep 'pnpm --version' scripts/pipeline-stages.sh` 匹配 pnpm 版本输出
5. `grep 'package.json' scripts/pipeline-stages.sh` 匹配 package.json 检查
6. `grep 'pipeline_preflight' jenkins/Jenkinsfile` 匹配（单一真相源调用）
7. `grep -c 'pnpm' jenkins/Jenkinsfile` 返回 3+（install/lint/test 三步独立）
8. Jenkins 作业配置为 SCM 模式（手动在 Jenkins UI 验证）
9. Pipeline 手动触发成功执行（手动在 Jenkins UI 验证）
</verification>

<success_criteria>
1. pipeline_preflight 检查 Node.js + pnpm 版本 + noda-apps 目录 + package.json 脚本完整性
2. pipeline_test 仅处理 pnpm install，不包含 lint/test 逻辑（避免死代码）
3. Jenkinsfile Test stage 中 install/lint/test 为独立 sh 步骤（Stage View 可区分）
4. Jenkinsfile Pre-flight stage 不含内联检查（单一真相源：所有检查在 pipeline_preflight 中）
5. 人工验证确认 Pipeline 配置完整，手动触发可执行 (PIPE-04)
</success_criteria>

<output>
After completion, create `.planning/phases/23-pipeline-integration/23-02-SUMMARY.md`
</output>
