---
phase: 29-jenkins-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 29-01
  - 29-02
files_modified:
  - scripts/deploy/deploy-infrastructure-prod.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "deploy-infrastructure-prod.sh 不再包含 nginx 和 noda-ops 的部署逻辑"
    - "脚本保留 postgres 部署逻辑作为手动回退"
    - "脚本顶部注释说明此脚本为 Jenkins 不可用时的紧急回退方案"
  artifacts:
    - path: "scripts/deploy/deploy-infrastructure-prod.sh"
      provides: "精简后的手动部署回退脚本"
      contains: "紧急回退"
  key_links:
    - from: "scripts/deploy/deploy-infrastructure-prod.sh"
      to: "jenkins/Jenkinsfile.infra"
      via: "注释引用"
      pattern: "Jenkinsfile\\.infra"
---

<objective>
精简 deploy-infrastructure-prod.sh，移除已被 Pipeline 覆盖的 nginx/noda-ops 部署逻辑，保留 postgres 手动回退。

Purpose: 消除手动脚本和 Pipeline 之间的重复，避免维护两套部署逻辑。
Output: 精简的 deploy-infrastructure-prod.sh，仅保留 postgres 部署 + 健康检查。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-jenkins-pipeline/29-CONTEXT.md
@.planning/phases/29-jenkins-pipeline/29-01-SUMMARY.md
@.planning/phases/29-jenkins-pipeline/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 精简 deploy-infrastructure-prod.sh，移除 nginx/noda-ops 部署逻辑</name>
  <files>scripts/deploy/deploy-infrastructure-prod.sh</files>
  <read_first>
    scripts/deploy/deploy-infrastructure-prod.sh (完整文件，当前 360 行)
  </read_first>
  <acceptance_criteria>
    - `grep -c "noda-ops" scripts/deploy/deploy-infrastructure-prod.sh` 输出 0 (在 START_SERVICES 和 EXPECTED_CONTAINERS 中不出现)
    - `grep -c "nginx" scripts/deploy/deploy-infrastructure-prod.sh` 输出 <= 2 (仅在注释中出现)
    - `grep "START_SERVICES=" scripts/deploy/deploy-infrastructure-prod.sh | grep -c "postgres"` 输出 >= 1
    - `grep -c "Jenkinsfile.infra" scripts/deploy/deploy-infrastructure-prod.sh` 输出 >= 1
    - `bash -n scripts/deploy/deploy-infrastructure-prod.sh` 退出码 0
    - `grep "EXPECTED_CONTAINERS" scripts/deploy/deploy-infrastructure-prod.sh | grep -c "postgres-dev"` 输出 0 (Phase 27 已移除 dev 服务)
  </acceptance_criteria>
  <action>
修改 scripts/deploy/deploy-infrastructure-prod.sh（per D-10）。

**修改 1: 更新文件头部注释（行 1-11）**
将现有注释块替换为:
```bash
#!/bin/bash
# ============================================
# 手动回退部署脚本（生产环境）
# ============================================
# NOTE: 此脚本作为 Jenkins Pipeline 不可用时的紧急回退方案保留。
# 正常部署请使用 Jenkins Pipeline:
#   - keycloak/nginx/noda-ops: Jenkinsfile.infra (参数化服务选择)
#   - findclass-ssr: Jenkinsfile (蓝绿部署)
#   - noda-site: Jenkinsfile.noda-site (蓝绿部署)
#
# 此脚本仅部署 postgres（其他服务已迁移到 Pipeline 管理）。
# Keycloak 和 findclass-ssr 使用蓝绿 docker run 管理。
# ============================================
```

**修改 2: 更新 EXPECTED_CONTAINERS 数组**
移除 `noda-infra-nginx` 和 `noda-ops`，移除 `noda-infra-postgres-dev`（Phase 27 已清理）:
```bash
EXPECTED_CONTAINERS=(
  "noda-infra-postgres-prod"
  # nginx, noda-ops 已迁移到 Jenkinsfile.infra Pipeline 管理
  # keycloak 和 findclass-ssr 已迁移到蓝绿 docker run 管理
)
```

**修改 3: 更新 START_SERVICES**
移除 nginx, noda-ops, postgres-dev:
```bash
START_SERVICES="postgres"
```

**修改 4: 更新 COMPOSE_FILES**
移除 dev overlay（Phase 27 已清理 dev 服务）:
```bash
COMPOSE_FILES="-f docker/docker-compose.yml -f docker/docker-compose.prod.yml"
```

**修改 5: 移除 save_image_tags 中对 nginx/noda-ops 的处理**
save_image_tags 函数保持不变（它会遍历 EXPECTED_CONTAINERS，移除后自动跳过）。

**修改 6: 更新 rollback_images 中的 container_to_service 映射**
移除 nginx 和 noda-ops 映射:
```bash
container_to_service() {
  case "$1" in
    noda-infra-postgres-prod) echo "postgres" ;;
    *) echo "" ;;
  esac
}
```

**修改 7: 更新步骤 4/7 "重启容器"**
改为仅启动 postgres:
```bash
log_info "启动 PostgreSQL..."
docker compose $COMPOSE_FILES up -d $START_SERVICES
```

**修改 8: 移除步骤 6/7 "配置 Keycloak"**
删除整个步骤 6 代码块（行 305-312），更新步骤编号。

**修改 9: 更新步骤编号**
- 步骤 1/7: 验证环境 -> 步骤 1/5
- 步骤 2/7: 保存镜像标签 -> 步骤 2/5
- 步骤 3/7: 部署前备份 -> 步骤 3/5
- 步骤 4/7: 重启容器 -> 步骤 4/5
- 步骤 5/7: 等待服务健康 -> 步骤 5/5
- 步骤 6/7: 删除
- 步骤 7/7: 删除（合并到步骤 5/5）

**修改 10: 更新部署完成消息**
```bash
log_success "=========================================="
log_success "基础设施部署完成！"
log_success "=========================================="
log_info "✓ PostgreSQL (Prod): 运行中"
log_info "✓ Keycloak: 蓝绿管理（通过 Jenkinsfile.infra 或 manage-containers.sh）"
log_info "✓ Nginx: compose 管理（通过 Jenkinsfile.infra 或 deploy-infrastructure-prod.sh）"
log_info "✓ Noda-Ops: compose 管理（通过 Jenkinsfile.infra 或 deploy-infrastructure-prod.sh）"
log_info "✓ Findclass-SSR: 蓝绿管理（通过 Jenkinsfile 或 blue-green-deploy.sh）"
```
  </action>
  <verify>
    <automated>bash -n scripts/deploy/deploy-infrastructure-prod.sh && echo "syntax OK"</automated>
  </verify>
  <done>deploy-infrastructure-prod.sh 精简为仅部署 postgres，nginx/noda-ops 引用指向 Jenkinsfile.infra，语法检查通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 手动脚本 -> Docker Compose | 仅操作 postgres 服务 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Denial of Service | 手动部署 | accept | 仅在 Jenkins 不可用时使用，操作者需 SSH 到服务器 |

## 需求覆盖说明

本 Plan 的 requirements 字段为空，因为 D-10（deploy-infrastructure-prod.sh 精简）是维护性变更，不直接对应 PIPELINE-01~07 中的任何需求。PIPELINE-01~07 已全部由 Plan 01 和 Plan 02 覆盖。
</threat_model>

<verification>
1. `bash -n scripts/deploy/deploy-infrastructure-prod.sh` 语法通过
2. `grep "START_SERVICES=" scripts/deploy/deploy-infrastructure-prod.sh` 只包含 postgres
3. `grep "EXPECTED_CONTAINERS" -A 5 scripts/deploy/deploy-infrastructure-prod.sh` 不包含 nginx/noda-ops
4. `grep "Jenkinsfile.infra" scripts/deploy/deploy-infrastructure-prod.sh` 引用 Pipeline
</verification>

<success_criteria>
- deploy-infrastructure-prod.sh 仅部署 postgres
- nginx/noda-ops 从 EXPECTED_CONTAINERS 和 START_SERVICES 中移除
- 脚本顶部注释引用 Jenkinsfile.infra
- 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/29-jenkins-pipeline/29-03-SUMMARY.md`
</output>
