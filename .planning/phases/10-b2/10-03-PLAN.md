---
phase: 10-b2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/lib/restore.sh
  - scripts/backup/lib/test-verify.sh
autonomous: true
requirements:
  - BFIX-03

must_haves:
  truths:
    - "download_backup() 能正确下载 B2 日期子目录中的备份文件"
    - "download_latest_backup() 从 list_b2_backups() 输出中正确提取完整路径和纯文件名"
    - "rclone --include 使用 **/ 模式匹配子目录中的文件"
  artifacts:
    - path: "scripts/backup/lib/restore.sh"
      provides: "download_backup() 路径修复"
      contains: 'basename'
    - path: "scripts/backup/lib/test-verify.sh"
      provides: "download_latest_backup() 路径修复"
      contains: 'basename'
  key_links:
    - from: "scripts/backup/lib/test-verify.sh::download_latest_backup()"
      to: "scripts/backup/lib/restore.sh::download_backup()"
      via: "传递完整路径或纯文件名"
      pattern: "download_backup"
    - from: "scripts/backup/lib/restore.sh::download_backup()"
      to: "rclone copy --include"
      via: "--include 模式匹配子目录中的文件"
      pattern: "rclone copy.*include"
---

<objective>
修复 B2 下载路径解析错误（BFIX-03）

Purpose: B2 存储使用 `YYYY/MM/DD/` 日期子目录结构，`rclone ls` 输出含路径前缀（如 `2026/04/07/keycloak_db_20260407_030000.dump`），但 `download_backup()` 和 `download_latest_backup()` 未正确处理这个路径前缀，导致 `--include` 匹配失败，文件无法下载。修复后，下载函数能正确处理含子目录路径的文件。

Output: 修正后的 `restore.sh` 和 `test-verify.sh`，下载功能能正确处理 B2 日期子目录。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-b2/10-CONTEXT.md
@.planning/phases/10-b2/10-RESEARCH.md

<interfaces>
<!-- B2 路径结构 -->
```
b2remote:noda-backups/backups/postgres/
  └── 2026/
      └── 04/
          └── 07/
              ├── keycloak_db_20260407_030000.dump
              ├── globals_20260407_030000.sql
              └── metadata_20260407_030000.json
```

<!-- list_b2_backups() 输出格式（rclone ls） -->
```
12345 2026/04/07/keycloak_db_20260407_030000.dump
678 2026/04/07/globals_20260407_030000.sql
234 2026/04/07/metadata_20260407_030000.json
```

<!-- download_backup() 当前签名 -->
```bash
download_backup() {
  local backup_filename=$1     # 当前接收纯文件名，但可能收到含路径的字符串
  local local_dir=${2:-$(mktemp -d)}
  # ...
  # 第 103 行验证：backup_filename 必须匹配 ^[^_]+_[0-9]{8}_[0-9]{6}\.(sql|dump)$
  # 第 123 行：--include "$backup_filename"
  # 第 130-135 行：查找下载的文件
}
```

<!-- download_latest_backup() 当前逻辑（第 119-137 行） -->
```bash
# 第 119 行
backups=$(list_b2_backups | grep "$db_name" | sort -r | head -1)
# 第 128 行
filename=$(echo "$backups" | awk '{print $2}')
# filename 此时为 "2026/04/07/keycloak_db_20260407_030000.dump"（含路径）
# 第 137 行
backup_file=$(download_backup "$filename" "$TEST_BACKUP_DIR" 2>/dev/null)
```

<!-- rclone --include 行为 -->
```
rclone copy "b2remote:bucket/path/" dest --include "file.dump"
  → 只匹配 dest 直接子级中的 file.dump，不匹配 2026/04/07/file.dump

rclone copy "b2remote:bucket/path/" dest --include "**/file.dump"
  → 递归匹配所有层级中的 file.dump
  → 保留目录结构：dest/2026/04/07/file.dump
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 download_backup 和 download_latest_backup 的路径处理</name>
  <files>scripts/backup/lib/restore.sh, scripts/backup/lib/test-verify.sh</files>
  <read_first>
    - scripts/backup/lib/restore.sh（重点 download_backup 函数，第 95-152 行）
    - scripts/backup/lib/test-verify.sh（重点 download_latest_backup 函数，第 107-157 行）
    - scripts/backup/lib/cloud.sh（确认 list_b2_backups 函数的 rclone ls 行为）
  </read_first>
  <action>
    **修改 1：`scripts/backup/lib/restore.sh` 的 `download_backup()` 函数（第 95-152 行）**

    将 `download_backup()` 函数修改为接受"可能含路径的文件名"，自动提取纯文件名，并使用 `**/` 通配符匹配子目录。

    **具体修改点：**

    a) 第 96-103 行 — 参数处理和文件名验证：
    ```bash
    download_backup() {
      local backup_path=$1     # 可能是 "2026/04/07/db.dump" 或纯 "db.dump"
      local local_dir=${2:-$(mktemp -d)}

      # 提取纯文件名（去除可能的路径前缀）
      local backup_filename
      backup_filename=$(basename "$backup_path")
    ```

    b) 第 103 行 — 验证纯文件名（不是完整路径）：
    ```bash
      # 验证文件名格式（使用纯文件名）
      if [[ ! $backup_filename =~ ^[^_]+_[0-9]{8}_[0-9]{6}\.(sql|dump)$ ]]; then
        log_error "无效的备份文件名格式: $backup_filename" >&2
        return 1
      fi
    ```

    c) 第 120-124 行 — rclone copy 使用 `**/` 通配符匹配子目录：
    ```bash
      if rclone copy "b2remote:${b2_bucket_name}/${b2_path}" \
        "$local_dir" \
        --config "$rclone_config" \
        --include "**/$backup_filename" \
        --progress >&2; then
    ```

    d) 第 128-135 行 — 文件查找逻辑保持不变（find 递归查找已经能处理子目录）：
    不需要修改，因为现有的 `find "$local_dir" -name "$backup_filename"` 已经能找到子目录中的文件。

    **修改 2：`scripts/backup/lib/test-verify.sh` 的 `download_latest_backup()` 函数（第 119-137 行）**

    a) 第 128-130 行 — 提取完整路径（而非纯文件名），传给 download_backup：
    ```bash
      # 提取完整相对路径（含子目录前缀）
      local backup_path
      backup_path=$(echo "$backups" | awk '{print $2}')

      log_info "找到备份文件: $backup_path"
    ```

    b) 第 137 行 — 传递完整路径给 download_backup（不再传纯文件名）：
    ```bash
        local backup_file
        backup_file=$(download_backup "$backup_path" "$TEST_BACKUP_DIR" 2>/dev/null)
    ```

    **关键变更总结：**
    - `download_backup()` 参数从"纯文件名"变为"可能含路径的字符串"
    - 内部使用 `basename` 提取纯文件名用于验证和查找
    - `--include` 使用 `"**/$backup_filename"` 模式匹配子目录
    - `download_latest_backup()` 传递完整路径给 `download_backup()`
    - `download_backup()` 的 stdout 仍返回完整的本地文件路径
  </action>
  <acceptance_criteria>
    - `scripts/backup/lib/restore.sh` 的 `download_backup()` 函数包含 `basename "$backup_path"` 或 `basename "$1"`
    - `scripts/backup/lib/restore.sh` 的 `download_backup()` 函数包含 `--include "**/$backup_filename"`
    - `scripts/backup/lib/restore.sh` 的 `download_backup()` 参数名从 `backup_filename` 改为 `backup_path`（或含 `basename` 提取逻辑）
    - `scripts/backup/lib/test-verify.sh` 的 `download_latest_backup()` 将完整路径（含子目录）传给 `download_backup()`
    - `scripts/backup/lib/test-verify.sh` 的 `download_latest_backup()` 不再仅提取纯文件名
    - `bash -n scripts/backup/lib/restore.sh` 语法检查通过（退出码 0）
    - `bash -n scripts/backup/lib/test-verify.sh` 语法检查通过（退出码 0）
    - `scripts/backup/lib/restore.sh` 仍包含文件名验证正则 `^[^_]+_[0-9]{8}_[0-9]{6}\.(sql|dump)$`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/backup/lib/restore.sh && bash -n scripts/backup/lib/test-verify.sh && grep -q 'basename' scripts/backup/lib/restore.sh && grep -q '\*\*/' scripts/backup/lib/restore.sh && grep -q 'backup_path' scripts/backup/lib/test-verify.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>download_backup() 能处理含子目录路径的文件名，使用 **/ 通配符匹配 B2 日期子目录中的文件；download_latest_backup() 传递完整路径给 download_backup()；两个文件语法检查通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 脚本 → B2 云存储 | rclone 使用临时配置文件认证（setup_rclone_config） |
| 文件名输入 → rclone --include | backup_filename 来自 rclone ls 输出，经正则验证后传入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | 文件名注入 rclone --include | mitigate | 正则验证 `^[^_]+_[0-9]{8}_[0-9]{6}\.(sql\|dump)$` 确保只含安全字符 |
| T-10-06 | Information Disclosure | B2 凭证泄露 | accept | 使用临时配置文件，操作后立即清理 |
</threat_model>

<verification>
1. `bash -n scripts/backup/lib/restore.sh` — 语法检查通过
2. `bash -n scripts/backup/lib/test-verify.sh` — 语法检查通过
3. `download_backup()` 使用 `**/` 通配符匹配子目录
4. `download_latest_backup()` 传递完整路径给 `download_backup()`
5. 文件名正则验证仍然有效
</verification>

<success_criteria>
- download_backup() 能正确下载 B2 日期子目录 `YYYY/MM/DD/` 中的备份文件
- download_latest_backup() 能从 list_b2_backups() 输出中提取完整路径
- rclone --include 使用 **/ 模式匹配子目录中的文件
- 两个脚本语法检查通过
- 文件名验证逻辑不受影响
</success_criteria>

<output>
After completion, create `.planning/phases/10-b2/10-03-SUMMARY.md`
</output>
