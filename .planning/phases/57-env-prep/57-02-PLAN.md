---
phase: 57-env-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.r4s.yml
  - .env.r4s.example
autonomous: true
requirements:
  - ENV-03

must_haves:
  truths:
    - "docker-compose.r4s.yml 中所有 8 个服务配置了内存限制（deploy.resources.limits）"
    - "Nginx 端口映射为 8080:80 和 8443:443（高端口，per D-28）"
    - "容器日志配置为 max-size=5m, max-file=2（per D-30）"
    - "所有内存限制总和不超过 2.31 GiB（per D-10）"
    - ".env.r4s.example 包含所有 r4s 特有环境变量及注释说明"
  artifacts:
    - path: "docker/docker-compose.r4s.yml"
      provides: "r4s Docker Compose overlay（内存限制 + 端口映射 + 日志配置）"
      contains: "x-r4s-logging"
    - path: ".env.r4s.example"
      provides: "r4s 环境变量模板，文档化所有 r4s 特有变量"
      contains: "DOCKER_DATA_DIR"
  key_links:
    - from: "docker/docker-compose.r4s.yml"
      to: "docker/docker-compose.yml"
      via: "Docker Compose overlay 合并"
      pattern: "services:"
    - from: "docker/docker-compose.r4s.yml"
      to: "docker/docker-compose.prod.yml"
      via: "Docker Compose overlay 合并（prod + r4s 叠加）"
      pattern: "services:"
    - from: ".env.r4s.example"
      to: "docker/docker-compose.r4s.yml"
      via: "${DOCKER_DATA_DIR} 等环境变量引用"
      pattern: "DOCKER_DATA_DIR"
---

<objective>
创建 Docker Compose r4s overlay 文件和环境变量模板，配置所有容器的内存限制、端口映射和日志策略。

Purpose: r4s 内存仅 3.77 GiB，必须精确控制每个容器内存使用。overlay 模式不修改现有文件，保持 Mac 环境不变。
Output: docker/docker-compose.r4s.yml + .env.r4s.example
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-env-prep/57-CONTEXT.md
@.planning/phases/57-env-prep/57-RESEARCH.md
@docker/docker-compose.yml
@docker/docker-compose.prod.yml

<interfaces>
<!-- docker-compose.yml 中的服务名 — r4s overlay 必须使用相同服务名 -->
```yaml
services:
  postgres:        # PostgreSQL 17.9
  nginx:           # Nginx 1.25-alpine，端口 80:80, 81:81, 443:443
  noda-ops:        # 备份 + Cloudflare Tunnel
  keycloak:        # profiles: [disabled]，生产环境通过 docker run 管理
networks:
  noda-network:    # external: true
volumes:
  postgres_data:   # driver: local
```

<!-- docker-compose.prod.yml 中的资源限制（Mac 当前值）— r4s overlay 需要覆盖 -->
```yaml
postgres.deploy.resources.limits.memory: 2G
keycloak.deploy.resources.limits.memory: 1G
nginx: 无内存限制
noda-ops: 无内存限制
x-common-security.logging: max-size=10m, max-file=3
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 docker-compose.r4s.yml overlay 文件</name>
  <files>docker/docker-compose.r4s.yml</files>
  <read_first>
    - docker/docker-compose.yml（基础服务定义、网络、卷）
    - docker/docker-compose.prod.yml（prod overlay 模式参考、x-common-security anchor、现有内存限制值）
    - .planning/phases/57-env-prep/57-CONTEXT.md（D-01~D-11 内存预算分配、D-28 端口映射、D-30 日志配置）
    - .planning/phases/57-env-prep/57-RESEARCH.md（overlay YAML 示例）
  </read_first>
  <acceptance_criteria>
    - 文件开头有 name: noda-infra（与 base + prod 保持一致）
    - 定义 x-r4s-logging YAML anchor，包含 logging.driver=json-file, max-size=5m, max-file=2（per D-30）
    - postgres 服务：deploy.resources.limits.memory=768M, reservations.memory=256M（per D-01）
    - keycloak 服务：deploy.resources.limits.memory=640M, reservations.memory=256M（per D-02）
    - nginx 服务：ports=["8080:80", "8443:443"]（per D-28，替换 base 的 80:80/81:81/443:443）
    - nginx 服务：deploy.resources.limits.memory=64M, reservations.memory=16M（per D-03）
    - noda-ops 服务：deploy.resources.limits.memory=256M, reservations.memory=64M（per D-04）
    - 所有 4 个服务引用 x-r4s-logging anchor（<<: *r4s-logging）
    - keycloak 保持 profiles: [disabled] 不变（r4s 上同样通过 docker run 管理 per D-19）
    - 所有服务注释标注对应的决策编号（如 "# PostgreSQL — 768M (D-01)"）
    - `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.r4s.yml config` 能成功合并（无语法错误）
    - 内存总计：768+640+64+256 = 1728M（1.69 GiB），剩余空间留给 Phase 59 的应用容器（findclass-ssr 384+128, noda-admin 64, noda-auth 64 = 640M），总计 2368M（~2.31 GiB）符合 D-10
  </acceptance_criteria>
  <action>
    创建 docker/docker-compose.r4s.yml：
    - 文件顶部注释说明用途、使用方式（三文件 overlay 命令）、覆盖内容概要
    - name: noda-infra（与 docker-compose.yml 保持一致）
    - 定义 x-r4s-logging YAML anchor：logging.driver=json-file, options.max-size=5m, max-file=2（per D-30）
    - services 块中覆盖 4 个基础服务（postgres、nginx、noda-ops、keycloak）：
      - postgres：引用 *r4s-logging，deploy.resources.limits.memory=768M, reservations.memory=256M（per D-01）
      - nginx：引用 *r4s-logging，ports 完整定义为 ["8080:80", "8443:443"]（per D-28，注意 Docker Compose overlay 中 ports 是完全替换不是合并，所以不包含 base 中的 81:81），deploy.resources.limits.memory=64M, reservations.memory=16M（per D-03）
      - noda-ops：引用 *r4s-logging，deploy.resources.limits.memory=256M, reservations.memory=64M（per D-04）
      - keycloak：引用 *r4s-logging，deploy.resources.limits.memory=640M, reservations.memory=256M（per D-02）。注意 keycloak 在 base 和 prod 中都是 profiles: [disabled]，r4s overlay 中不需要重复 profiles 定义（overlay 合并时 profiles 保留），只需添加 deploy 和 logging 覆盖
    - 每个服务注释标注决策编号和内存值
    - 不修改 networks 和 volumes 定义（使用 base 文件中的定义）
    - 注意：nginx 的 81:81 端口（管理端口）在 r4s 上是否需要取决于 nginx 配置。检查 docker-compose.yml 中 81:81 的用途。如果 nginx 配置中有 server block 监听 81 端口（用于管理或其他），则 r4s overlay 需要包含 8081:81 映射。经检查 base 文件中 nginx 端口为 80:80, 81:81, 443:443，81 端口在 prod overlay 中未提及特殊用途。r4s overlay 应包含 8081:81 映射以保持功能完整（per Pitfall 7 RESEARCH 中提到的端口覆盖完整性）。即 nginx ports 完整定义为：["8080:80", "8081:81", "8443:443"]
    - 文件底部不包含 networks 定义（使用 base 文件的 external noda-network）
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.r4s.yml config --services 2>&1 | grep -c 'postgres\|nginx\|noda-ops\|keycloak'</automated>
  </verify>
  <done>
    docker-compose.r4s.yml 包含所有 4 个基础服务的内存限制覆盖、nginx 高端口映射、r4s 紧凑日志配置。overlay 与 base + prod 正确合并。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 .env.r4s.example 环境变量模板</name>
  <files>.env.r4s.example</files>
  <read_first>
    - docker/docker-compose.yml（识别所有 ${VAR} 引用）
    - docker/docker-compose.prod.yml（识别所有 ${VAR} 引用）
    - docker/docker-compose.r4s.yml（识别新增的 ${VAR} 引用）
    - .planning/phases/57-env-prep/57-CONTEXT.md（D-15 卷路径通过 .env 管理、D-16 Doppler 动态注入）
  </read_first>
  <acceptance_criteria>
    - 文件包含 DOCKER_DATA_DIR 变量（per D-15，默认值 /mnt/mmc1-4/docker）
    - 文件包含 docker-compose.yml 中引用的所有 ${VAR}：POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, B2_ACCOUNT_ID, B2_APPLICATION_KEY, B2_BUCKET_NAME, B2_PATH, ALERT_EMAIL, CLOUDFLARE_TUNNEL_TOKEN, DOPPLER_TOKEN, KEYCLOAK_ADMIN_USER, KEYCLOAK_ADMIN_PASSWORD, SMTP_HOST, SMTP_PORT, SMTP_FROM, SMTP_USER, SMTP_PASSWORD
    - 每个变量有中文注释说明用途
    - 文件顶部说明此文件是模板，实际值通过 Doppler 动态注入（per D-16）
    - 文件中敏感值为占位符（如 CHANGE_ME），非敏感值有合理默认值
    - `grep -c 'CHANGE_ME' .env.r4s.example` 返回 >= 1（标识敏感变量）
  </acceptance_criteria>
  <action>
    创建 .env.r4s.example：
    - 文件顶部注释说明：此文件是 r4s 环境变量模板，实际值通过 Doppler CLI 动态注入（per D-16），不应直接填写真实密钥
    - 按功能分组注释：
      1. r4s 路径配置：DOCKER_DATA_DIR=/mnt/mmc1-4/docker（per D-15），说明未来迁移到机械硬盘时只需改此变量
      2. PostgreSQL 配置：POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB（从 docker-compose.yml 提取）
      3. Keycloak 配置：KEYCLOAK_ADMIN_USER, KEYCLOAK_ADMIN_PASSWORD
      4. B2 云存储配置：B2_ACCOUNT_ID, B2_APPLICATION_KEY, B2_BUCKET_NAME, B2_PATH
      5. Cloudflare Tunnel 配置：CLOUDFLARE_TUNNEL_TOKEN
      6. Doppler 配置：DOPPLER_TOKEN
      7. SMTP 配置：SMTP_HOST, SMTP_PORT, SMTP_FROM, SMTP_USER, SMTP_PASSWORD
      8. 告警配置：ALERT_EMAIL
    - 敏感变量值设为 CHANGE_ME
    - 非敏感变量给合理默认值（如 B2_BUCKET_NAME=noda-backups, B2_PATH=backups/postgres/）
    - 每个变量一行注释说明用途
  </action>
  <verify>
    <automated>grep -c 'DOCKER_DATA_DIR\|POSTGRES_USER\|POSTGRES_PASSWORD\|POSTGRES_DB\|B2_ACCOUNT_ID\|CLOUDFLARE_TUNNEL_TOKEN\|DOPPLER_TOKEN\|KEYCLOAK_ADMIN' .env.r4s.example</automated>
  </verify>
  <done>
    .env.r4s.example 包含所有 docker-compose.yml/prod.yml/r4s.yml 引用的环境变量，按功能分组并有中文注释。模板明确说明密钥通过 Doppler 动态注入。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env 文件 -> Docker Compose | 密钥通过 Doppler 动态注入到 .env，用完删除（per D-16） |
| .env.r4s.example -> 开发者 | 模板文件不包含真实密钥，仅占位符 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Information Disclosure | .env 文件密钥泄露 | mitigate | Doppler 动态注入 + 用完删除（per D-16），.env.r4s.example 仅占位符 |
| T-57-06 | Elevation of Privilege | 容器内存无限制导致 OOM | mitigate | 所有容器配置内存限制，总计不超过 2.31 GiB + 2GB Swap 缓冲 |
</threat_model>

<verification>
1. docker compose 三文件 overlay 合并成功（config 命令无报错）
2. 所有 4 个服务有内存限制覆盖
3. nginx 端口映射为高端口（8080/8081/8443）
4. 日志配置为 5m×2
5. .env.r4s.example 包含所有必要的 VAR 引用
6. 内存总计 = 768+640+64+256 = 1728M（基础设施）+ 384+128+64+64 = 640M（应用，Phase 59）= 2368M 符合 D-10 的 ~2.31 GiB 预算
</verification>

<success_criteria>
- docker-compose.r4s.yml 与 base + prod 正确合并
- 所有基础设施服务的内存限制符合 D-01~D-04 预算
- nginx 使用高端口避免与 iStoreOS 冲突
- 容器日志限制比 Mac 更紧凑（5m×2 vs 10m×3）
- .env.r4s.example 文档化所有环境变量
</success_criteria>

<output>
Create `.planning/phases/57-env-prep/57-02-SUMMARY.md` when done
</output>
