---
phase: 49-findclass-ssr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-findclass-ssr/49-RESEARCH.md
  - .planning/phases/49-findclass-ssr/49-DECISION.md
autonomous: true
requirements:
  - SSR-01
  - SSR-02

must_haves:
  truths:
    - "6 个 Python 脚本（crawl-skykiwi.py, llm_filter.py, llm_extract.py, llm_quality_gate.py, db_import.py, retrofix_data_quality.py）的调用链路、调用方、运行频率完整记录"
    - "2 处 spawn('python3', ...) 调用点（crawl-scheduler.ts L253, re-tag-regions.ts L52-53）已确认并记录"
    - "4 个 API 端点（/api/crawl/trigger, /api/crawl/status, /api/crawl/history, /api/crawl/stats）与 Python 的关系已明确"
    - "最终决策文档存在，明确选择方案 A（完全移除 Python/Chromium）"
    - "Phase 50 可执行的移除清单已产出"
  artifacts:
    - path: ".planning/phases/49-findclass-ssr/49-DECISION.md"
      provides: "最终决策文档 — 方案选择、影响范围、Phase 50 移除清单"
      contains: "方案 A"
    - path: ".planning/phases/49-findclass-ssr/49-RESEARCH.md"
      provides: "完整审计文档（已完成，本 plan 验证其完整性）"
      contains: "StealthySession"
  key_links:
    - from: "49-RESEARCH.md"
      to: "49-DECISION.md"
      via: "审计结论驱动决策"
      pattern: "StealthySession.*不在.*定时"
---

<objective>
验证审计完整性并产出最终决策文档

Purpose: RESEARCH.md 已完成完整审计，用户已确认方案 A（完全移除 Python/Chromium）。本 plan 验证审计覆盖 SSR-01 的所有要求，并产出 SSR-02 所需的决策文档，为 Phase 50 执行移除提供精确清单。

Output: 49-DECISION.md 决策文档，包含方案 A 完整影响分析、精确移除清单、Phase 50 执行指引。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-findclass-ssr/49-CONTEXT.md
@.planning/phases/49-findclass-ssr/49-RESEARCH.md
@deploy/Dockerfile.findclass-ssr
@docker/docker-compose.app.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证审计完整性（SSR-01）</name>
  <files>.planning/phases/49-findclass-ssr/49-RESEARCH.md</files>
  <read_first>
    .planning/phases/49-findclass-ssr/49-RESEARCH.md
    .planning/phases/49-findclass-ssr/49-CONTEXT.md
  </read_first>
  <action>
验证 49-RESEARCH.md 审计覆盖 SSR-01 的全部要求。逐项检查：

1. **Python 脚本清单完整性** — 确认以下 6 个脚本均有独立章节记录：
   - crawl-skykiwi.py（主入口，L246-264 区域）
   - llm_extract.py（L266-275 区域）
   - llm_filter.py（L277-286 区域）
   - llm_quality_gate.py（L288-297 区域）
   - db_import.py（L299-308 区域）
   - retrofix_data_quality.py（L310-319 区域）

2. **每个脚本必须包含**：功能描述、调用方、是否需要 Chromium、是否需要 LLM API、是否有 API 端点直接调用、代码位置

3. **Node.js 调用点汇总**（L323-328 区域）— 确认 2 处 spawn('python3', ...) 调用：
   - crawl-scheduler.ts L253
   - re-tag-regions.ts L52-53

4. **API 端点与 Python 关系**（L330-336 区域）— 确认 4 个端点：
   - POST /api/crawl/trigger → 间接调用 Python（通过 spawn）
   - GET /api/crawl/status → 不调用 Python
   - GET /api/crawl/history → 不调用 Python
   - GET /api/crawl/stats → 不调用 Python

5. **StealthySession 使用分析**（L257-263 区域）— 确认：
   - 日常 crawl_board() 使用 Fetcher.get()（纯 HTTP，不需要 Chromium）
   - --re-analyze 模式使用 StealthySession（需要 Chromium）
   - 代码注释 L499 "使用 Fetcher 直连"

如果发现任何遗漏项，在 49-RESEARCH.md 中补充缺失内容。如果审计完整，记录验证通过结论到 Task 2 的决策文档中。
  </action>
  <verify>
    <automated>grep -c "crawl-skykiwi.py\|llm_extract.py\|llm_filter.py\|llm_quality_gate.py\|db_import.py\|retrofix_data_quality.py" .planning/phases/49-findclass-ssr/49-RESEARCH.md | grep -q "^[6-9]" && grep -q "StealthySession" .planning/phases/49-findclass-ssr/49-RESEARCH.md && grep -q "spawn.*python3" .planning/phases/49-findclass-ssr/49-RESEARCH.md</automated>
  </verify>
  <done>
49-RESEARCH.md 包含全部 6 个 Python 脚本的完整审计（功能/调用方/Chromium 依赖/API 端点关系），2 处 spawn 调用点已记录，4 个 API 端点与 Python 关系已明确，StealthySession 使用分析完整。
  </done>
</task>

<task type="auto">
  <name>Task 2: 产出最终决策文档（SSR-02）</name>
  <files>.planning/phases/49-findclass-ssr/49-DECISION.md</files>
  <read_first>
    .planning/phases/49-findclass-ssr/49-RESEARCH.md
    .planning/phases/49-findclass-ssr/49-CONTEXT.md
    deploy/Dockerfile.findclass-ssr
    docker/docker-compose.app.yml
  </read_first>
  <action>
创建 49-DECISION.md 决策文档。此文档是 Phase 50 执行移除的输入。

文档结构：

# Phase 49 决策文档：Python/Chromium 完全移除

## 决策结论

**选定方案：方案 A — 完全移除 Python/Chromium/patchright**

决策依据（引用 CONTEXT.md 决策 ID）：
- D-06 标准：StealthySession 不在日常定时抓取流程中使用 — **确认满足**（代码证据：crawl_board() L494-599 使用 Fetcher.get()）
- D-07 标准：代码证据直接判断 — **确认满足**
- D-08 指令：StealthySession 未使用 → 完全移除 — **适用**
- 用户明确确认：可以放弃定时爬虫和 LLM 处理能力

## 影响范围

### 功能影响

| 功能 | 影响 | 处理方式 |
|------|------|----------|
| 定时爬虫（crawl-scheduler.ts cron） | 废弃 | Phase 50 移除调度逻辑 |
| POST /api/crawl/trigger | 废弃 | Phase 50 返回 501 或移除端点 |
| GET /api/crawl/status|history|stats | 只读端点 | Phase 50 评估是否保留（无 Python 依赖，可能仍可返回数据库数据） |
| --re-analyze 模式 | 废弃 | 无替代方案 |
| re-tag-regions.ts 维护脚本 | 废弃 | 无替代方案 |
| LLM 过滤/提取/质量检查 | 废弃 | 无替代方案 |
| db_import.py | 废弃 | crawl_executions 表数据保留但不再新增 |

### Dockerfile 变更清单（Phase 50 执行）

基于 deploy/Dockerfile.findclass-ssr 精确行号：

| 行号 | 当前内容 | Phase 50 操作 |
|------|----------|---------------|
| L67 注释 | `# Stage 2: 运行镜像（Debian slim，兼容 Python manylinux wheels）` | 更新注释，移除 Python 相关说明 |
| L71-76 | `RUN apt-get install python3 python3-pip python3-venv chromium` | **删除整段** — Python/Chromium 安装 |
| L84-86 | `ENV PLAYWRIGHT_BROWSERS_PATH=0` / `ENV CHROMIUM_PATH=/usr/bin/chromium` | **删除** — 不再需要 |
| L121-130 | Python 爬虫运行时（requirements.txt + pip install + patchright install + scripts COPY） | **删除整段** |
| L133 | `RUN chown -R nodejs:nodejs /app` | 保留，但后续 Phase 51 可能改用 COPY --chown |

### docker-compose.app.yml 变更清单

| 配置项 | 当前值 | Phase 50 操作 |
|--------|--------|---------------|
| `tmpfs: /app/scripts/logs` | Python 爬虫日志目录 | **删除此 tmpfs 条目** |
| `ANTHROPIC_AUTH_TOKEN` | Python LLM 脚本使用 | **删除环境变量** |
| `ANTHROPIC_BASE_URL` | Python LLM 脚本使用 | **删除环境变量** |
| `ANTHROPIC_API_KEY` | Python LLM 脚本使用 | **删除环境变量** |
| memory limit `1G` | 包含 Python 运行时 | Phase 50 可考虑降低至 512M（移除 ~3GB 运行时后内存占用显著减少） |

### 预期效果

| 指标 | 当前 | 移除后 | 改善 |
|------|------|--------|------|
| 镜像体积 | 5.02GB | ~1.5-2.0GB | 节省 ~3GB |
| 攻击面 | Python + Chromium + patchright | 纯 Node.js | 显著减少 |
| 内存占用 | ~512MB-1G | ~256-512MB | 减少约 50% |
| 构建时间 | 包含 pip install + patchright install | 纯 Node.js | 减少约 2-3 分钟 |

## Phase 50 执行指引

### 移除步骤（按依赖顺序）

1. **Dockerfile 清理** — 删除 Python/Chromium 安装、pip 依赖、scripts COPY
2. **docker-compose.app.yml 清理** — 删除 ANTHROPIC_* 环境变量、/app/scripts/logs tmpfs
3. **crawl-scheduler.ts 改造** — 移除 cron 调度逻辑和 spawn 调用（需在 noda-apps 仓库操作）
4. **crawl API 端点处理** — trigger 返回 501 或移除，status/history/stats 评估保留
5. **re-tag-regions.ts 处理** — 废弃或标记为不可用
6. **构建验证** — docker build + 健康检查 + 镜像体积确认

### 不移除的内容

- crawl_executions 表（历史数据保留）
- crawl API 只读端点（如不依赖 Python 可保留）
- DATABASE_URL 环境变量（Node.js API 仍需连接数据库）

## 假设确认

| # | 假设 | 确认状态 |
|---|------|----------|
| A1 | crawl-scheduler.ts 是唯一自动触发 Python 的调度器 | 已确认（RESEARCH.md 调用链路审计） |
| A2 | re-tag-regions.ts 很少使用，可以废弃 | 已确认（用户决策：放弃定时爬虫） |
| A3 | Python/Chromium 占约 3GB | 已确认（RESEARCH.md 体积分析） |
| A4 | 未来不需要在 findclass-ssr 容器内运行 Python | 已确认（用户选择方案 A） |
  </action>
  <verify>
    <automated>test -f .planning/phases/49-findclass-ssr/49-DECISION.md && grep -q "方案 A" .planning/phases/49-findclass-ssr/49-DECISION.md && grep -q "D-06" .planning/phases/49-findclass-ssr/49-DECISION.md && grep -q "D-08" .planning/phases/49-findclass-ssr/49-DECISION.md && grep -q "Phase 50" .planning/phases/49-findclass-ssr/49-DECISION.md</automated>
  </verify>
  <done>
49-DECISION.md 存在，包含：方案 A 决策结论、D-06/D-07/D-08 依据引用、功能影响表、Dockerfile 精确变更清单（行号级别）、docker-compose.app.yml 变更清单、Phase 50 执行指引（6 步骤）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本阶段为纯审计+决策文档，不涉及代码变更、网络请求、认证授权。无信任边界需要分析。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | N/A | 审计文档 | accept | 本阶段无代码变更，无安全威胁面。移除 Python/Chromium 本身是安全加固行为。 |

**Note:** 移除 Python/Chromium 消除了以下安全风险：Chromium 漏洞利用面、Python pip 供应链攻击、patchright 浏览器指纹泄露。这是安全净正面变更。
</threat_model>

<verification>
1. 49-RESEARCH.md 包含全部 6 个 Python 脚本审计
2. 49-DECISION.md 包含方案 A 决策和 Phase 50 移除清单
3. 决策引用 CONTEXT.md D-06/D-07/D-08 决策 ID
</verification>

<success_criteria>
- SSR-01: 49-RESEARCH.md 包含全部 6 个 Python 脚本的调用链路审计，每项包含功能/调用方/Chromium 依赖/API 端点关系
- SSR-02: 49-DECISION.md 明确选择方案 A（完全移除），含精确的 Dockerfile/docker-compose 变更清单和 Phase 50 执行指引
</success_criteria>

<output>
After completion, create `.planning/phases/49-findclass-ssr/49-01-SUMMARY.md`
</output>
