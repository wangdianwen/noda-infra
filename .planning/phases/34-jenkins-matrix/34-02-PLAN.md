---
phase: 34-jenkins-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - scripts/setup-docker-permissions.sh
autonomous: true
requirements:
  - PERM-05

must_haves:
  truths:
    - "setup-docker-permissions.sh apply 一键配置 Phase 31-34 所有权限（socket + 文件 + sudoers + auditd + Jenkins 矩阵）"
    - "setup-docker-permissions.sh verify 输出全部 PASS 的权限检查结果，遇到第一个 FAIL 立即退出"
    - "setup-docker-permissions.sh rollback 可恢复到权限收敛前的状态，执行前需输入 YES 确认"
    - "编排器只调用现有脚本子命令，不重复实现逻辑"
    - "apply 按 Phase 31→32→33→34 顺序执行，rollback 按反序执行"
  artifacts:
    - path: "scripts/setup-docker-permissions.sh"
      provides: "统一权限管理编排器脚本"
      exports: ["apply", "verify", "rollback"]
      min_lines: 200
  key_links:
    - from: "scripts/setup-docker-permissions.sh"
      to: "scripts/apply-file-permissions.sh"
      via: "bash apply-file-permissions.sh apply/verify"
      pattern: "apply-file-permissions\\.sh"
    - from: "scripts/setup-docker-permissions.sh"
      to: "scripts/install-sudoers-whitelist.sh"
      via: "bash install-sudoers-whitelist.sh install/verify/uninstall"
      pattern: "install-sudoers-whitelist\\.sh"
    - from: "scripts/setup-docker-permissions.sh"
      to: "scripts/install-auditd-rules.sh"
      via: "bash install-auditd-rules.sh install/verify/uninstall"
      pattern: "install-auditd-rules\\.sh"
    - from: "scripts/setup-docker-permissions.sh"
      to: "scripts/install-sudo-log.sh"
      via: "bash install-sudo-log.sh install/verify/uninstall"
      pattern: "install-sudo-log\\.sh"
    - from: "scripts/setup-docker-permissions.sh"
      to: "scripts/setup-jenkins.sh"
      via: "bash setup-jenkins.sh apply-matrix-auth/verify-matrix-auth"
      pattern: "setup-jenkins\\.sh.*matrix"
    - from: "scripts/setup-docker-permissions.sh"
      to: "scripts/undo-permissions.sh"
      via: "bash undo-permissions.sh undo"
      pattern: "undo-permissions\\.sh"
---

<objective>
创建 setup-docker-permissions.sh 统一编排器脚本，整合 Phase 31-34 所有权限配置的 apply/verify/rollback 操作。

Purpose: 一站式管理所有权限配置（PERM-05），管理员可通过单条命令完成权限配置、验证、回滚
Output: scripts/setup-docker-permissions.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/34-jenkins-matrix/34-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-jenkins-matrix/34-CONTEXT.md
@.planning/phases/34-jenkins-matrix/34-RESEARCH.md

<interfaces>
<!-- 现有脚本子命令接口 — 编排器通过这些接口调用 -->

Phase 31 脚本:
- scripts/apply-file-permissions.sh — 子命令: apply / verify / hook
- scripts/undo-permissions.sh — 子命令: backup / undo

Phase 32 脚本:
- scripts/install-sudoers-whitelist.sh — 子命令: install / verify / uninstall
- scripts/break-glass.sh — 不参与编排（独立紧急入口）

Phase 33 脚本:
- scripts/install-auditd-rules.sh — 子命令: install / verify / uninstall
- scripts/install-sudo-log.sh — 子命令: install / verify / uninstall

Phase 34 脚本:
- scripts/setup-jenkins.sh — 子命令: apply-matrix-auth / verify-matrix-auth（Plan 01 新增）

通用:
- scripts/lib/log.sh — log_info(), log_success(), log_error(), log_warn()
- 所有脚本使用 `set -euo pipefail` 模式
- 所有脚本返回 0 表示成功，非 0 表示失败
- macOS 平台各脚本自行处理跳过逻辑（编排器无需特殊处理）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 setup-docker-permissions.sh 编排器脚本 — apply + verify + rollback 三个子命令</name>
  <files>scripts/setup-docker-permissions.sh</files>
  <read_first>
    - scripts/apply-file-permissions.sh（Phase 31 apply/verify 子命令模式）
    - scripts/install-sudoers-whitelist.sh（Phase 32 install/verify/uninstall 模式）
    - scripts/install-auditd-rules.sh（Phase 33 install/verify/uninstall 模式）
    - scripts/install-sudo-log.sh（Phase 33 install/verify/uninstall 模式）
    - scripts/undo-permissions.sh（Phase 31 backup/undo 模式）
    - scripts/lib/log.sh（日志函数库）
  </read_first>
  <acceptance_criteria>
    - 文件 scripts/setup-docker-permissions.sh 存在且可执行（chmod +x）
    - 以 `#!/bin/bash` + `set -euo pipefail` 开头
    - source scripts/lib/log.sh
    - 包含 detect_platform() 函数
    - 包含 cmd_apply() 函数，按 Phase 31→32→33→34 顺序调用
    - 包含 cmd_verify() 函数，汇总 Phase 31-34 所有检查
    - 包含 cmd_rollback() 函数，按反序 34→33→32→31 回滚
    - cmd_rollback 包含交互确认（输入 YES）
    - verify 输出格式为 `[PASS/FAIL] 检查项描述`
    - verify 使用快速失败模式（set +e 失败后 exit 1）
    - apply 中 Phase 34 调用 `bash "$SCRIPT_DIR/setup-jenkins.sh" apply-matrix-auth`
    - rollback 中 Phase 34 调用 `bash "$SCRIPT_DIR/setup-jenkins.sh" apply-matrix-auth`（通过 Groovy 脚本恢复为 FullControlOnceLoggedInAuthorizationStrategy，RESEARCH.md 示例 3）
    - rollback 中 Phase 33 调用 install-sudo-log.sh uninstall + install-auditd-rules.sh uninstall
    - rollback 中 Phase 32 调用 install-sudoers-whitelist.sh uninstall
    - rollback 中 Phase 31 调用 undo-permissions.sh undo
    - case 语句分发 apply / verify / rollback / help
    - usage() 显示完整帮助信息
    - root 权限检查（EUID -ne 0 时退出）
  </acceptance_criteria>
  <action>
创建 scripts/setup-docker-permissions.sh 文件，内容遵循以下结构：

### 头部和常量
```bash
#!/bin/bash
set -euo pipefail
```
- SCRIPT_DIR + PROJECT_ROOT 定义（复用所有脚本的相同模式）
- source log.sh
- detect_platform()（复用标准模式）
- root 权限检查：`if [[ $EUID -ne 0 ]]; then log_error "需要 root 权限执行"; exit 1; fi`

### cmd_apply() 函数（per D-05, D-06, D-07）
编排器调用顺序（按 Phase 顺序）：
1. Phase 31: `bash "$SCRIPT_DIR/apply-file-permissions.sh" apply`
2. Phase 32: `bash "$SCRIPT_DIR/install-sudoers-whitelist.sh" install`
3. Phase 33:
   - `bash "$SCRIPT_DIR/install-auditd-rules.sh" install`
   - `bash "$SCRIPT_DIR/install-sudo-log.sh" install`
4. Phase 34: `bash "$SCRIPT_DIR/setup-jenkins.sh" apply-matrix-auth`

每个子脚本调用前后添加 `log_info` 标注当前 Phase。子脚本失败时整个 apply 失败退出（set -e）。

### cmd_verify() 函数（per D-11, D-12, D-13）
对每个 Phase 的检查：
1. Phase 31: `bash "$SCRIPT_DIR/apply-file-permissions.sh" verify` — exit code 判断 PASS/FAIL
2. Phase 32: `bash "$SCRIPT_DIR/install-sudoers-whitelist.sh" verify` — 同上
3. Phase 33:
   - `bash "$SCRIPT_DIR/install-auditd-rules.sh" verify`
   - `bash "$SCRIPT_DIR/install-sudo-log.sh" verify`
4. Phase 34: `bash "$SCRIPT_DIR/setup-jenkins.sh" verify-matrix-auth`

实现模式（per D-12 快速失败）：
```bash
cmd_verify() {
    local failed=0

    # 使用 set +e 允许子脚本失败而不退出
    verify_item "Phase 31: Docker socket + 文件权限" \
        bash "$SCRIPT_DIR/apply-file-permissions.sh" verify

    verify_item "Phase 32: sudoers 白名单" \
        bash "$SCRIPT_DIR/install-sudoers-whitelist.sh" verify

    verify_item "Phase 33: auditd 规则" \
        bash "$SCRIPT_DIR/install-auditd-rules.sh" verify

    verify_item "Phase 33: sudo 日志" \
        bash "$SCRIPT_DIR/install-sudo-log.sh" verify

    verify_item "Phase 34: Jenkins 权限矩阵" \
        bash "$SCRIPT_DIR/setup-jenkins.sh" verify-matrix-auth

    if [[ $failed -gt 0 ]]; then
        echo "[FAIL] ${failed} 项检查失败"
        return 1
    fi
    echo "[PASS] 所有 Phase 31-34 配置验证通过"
}

verify_item() {
    local desc="$1"
    shift
    set +e
    if "$@" > /dev/null 2>&1; then
        echo "[PASS] ${desc}"
    else
        echo "[FAIL] ${desc}"
        failed=$((failed + 1))
        # D-12: 快速失败模式 — 遇到第一个 FAIL 立即退出
        return 1
    fi
    set -e
}
```

注意：verify 输出 [PASS/FAIL] 前缀（per D-11），子脚本自身的输出被静默（`> /dev/null 2>&1`），编排器通过 exit code 判断状态。

### cmd_rollback() 函数（per D-08, D-09, D-10）
1. **交互确认**（per D-09）：
   - 显示将要回滚的配置列表（Phase 34→33→32→31）
   - 提示 `输入 YES 确认回滚: `
   - 读取输入，非 "YES" 则退出
2. **反序回滚**（per D-10）：
   - Phase 34: 需要执行 Groovy 脚本恢复权限策略。创建内联 Groovy 将策略改回 FullControlOnceLoggedInAuthorizationStrategy 并删除 developer 用户（参考 RESEARCH.md 示例 3）。通过 setup-jenkins.sh 的 jenkins-cli.jar 模式执行。最简实现：编排器直接写入临时 Groovy 文件并通过 `sudo -u jenkins java -jar $cli_jar -s http://localhost:8888/ groovy < $tmpfile` 执行。
   - Phase 33:
     - `bash "$SCRIPT_DIR/install-sudo-log.sh" uninstall`
     - `bash "$SCRIPT_DIR/install-auditd-rules.sh" uninstall`
   - Phase 32: `bash "$SCRIPT_DIR/install-sudoers-whitelist.sh" uninstall`
   - Phase 31: `bash "$SCRIPT_DIR/undo-permissions.sh" undo`
3. **确认列表内容**（per D-09）：
   ```
   将要回滚以下配置：
     Phase 34: Jenkins 权限矩阵 → FullControlOnceLoggedInAuthorizationStrategy，删除 developer 用户
     Phase 33: 卸载 auditd 规则 + sudo 日志配置
     Phase 32: 卸载 sudoers 白名单规则
     Phase 31: 恢复 Docker socket 属组为 root:docker，移除 systemd override
   ```

### usage() 和 case 分发
usage() 显示：
- 脚本用途说明
- 子命令列表：apply / verify / rollback / help
- 每个子命令的简要说明
- 示例命令

case 分发：
```bash
case "${1:-}" in
    apply)    cmd_apply ;;
    verify)   cmd_verify ;;
    rollback) cmd_rollback ;;
    help|--help|-h) usage ;;
    *) usage && exit 1 ;;
esac
```

### 文件权限
创建完成后设置 `chmod +x scripts/setup-docker-permissions.sh`
  </action>
  <verify>
    <automated>test -f /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && test -x /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "cmd_apply\b" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "cmd_verify\b" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "cmd_rollback\b" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "apply-file-permissions" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "install-sudoers-whitelist" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "install-auditd-rules" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "install-sudo-log" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "apply-matrix-auth" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && grep -c "undo-permissions" /Users/dianwenwang/Project/noda-infra/scripts/setup-docker-permissions.sh && echo "PASS: setup-docker-permissions.sh 验证通过"</automated>
  </verify>
  <done>
    - scripts/setup-docker-permissions.sh 文件存在且可执行
    - apply 子命令按 Phase 31→32→33→34 顺序调用现有脚本
    - verify 子命令汇总所有 Phase 检查，输出 [PASS/FAIL] 格式
    - verify 实现快速失败模式（第一个 FAIL 即退出）
    - rollback 子命令按反序 34→33→32→31 回滚
    - rollback 执行前强制交互确认（输入 YES）
    - rollback 恢复 Jenkins 权限为 FullControlOnceLoggedInAuthorizationStrategy
    - 编排器不重复实现任何配置逻辑（只调用子脚本子命令）
    - root 权限检查
    - usage() 帮助信息完整
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 编排器 → 各子脚本 | 编排器以 root 权限运行，调用各子脚本的子命令 |
| rollback → Jenkins | rollback 通过 CLI 执行 Groovy 脚本修改运行时权限策略 |
| 交互确认 → 用户 | rollback 依赖用户输入 YES 确认，防止误操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering | 编排器脚本被未授权修改 | mitigate | 文件在 Git 版本控制中；文件权限 750 root:root |
| T-34-06 | Denial of Service | rollback 误操作导致所有权限配置丢失 | mitigate | D-09 交互确认 + 显示回滚列表，管理员可审查 |
| T-34-07 | Elevation | rollback 恢复 FullControlOnceLoggedIn 后任何登录用户获得全权限 | accept | 这是设计意图（回滚到 v1.6 前状态），只有管理员执行 rollback |
</threat_model>

<verification>
1. setup-docker-permissions.sh 文件存在且可执行
2. bash scripts/setup-docker-permissions.sh help 输出包含 apply / verify / rollback 子命令说明
3. grep 确认编排器引用了所有 7 个现有脚本（apply-file-permissions, install-sudoers-whitelist, install-auditd-rules, install-sudo-log, setup-jenkins, undo-permissions, break-glass）
4. apply 子命令调用顺序为 Phase 31→32→33→34
5. rollback 子命令调用顺序为 Phase 34→33→32→31
6. rollback 包含交互确认逻辑
7. verify 输出 [PASS/FAIL] 格式
</verification>

<success_criteria>
- setup-docker-permissions.sh apply 可一键配置所有权限
- setup-docker-permissions.sh verify 输出全部 PASS 的权限检查结果
- setup-docker-permissions.sh rollback 可恢复到权限收敛前的状态
- 编排器只调用现有脚本子命令，不重复实现逻辑
</success_criteria>

<output>
After completion, create `.planning/phases/34-jenkins-matrix/34-02-SUMMARY.md`
</output>
