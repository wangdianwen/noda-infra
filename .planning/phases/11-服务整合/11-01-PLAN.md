---
phase: 11-服务整合
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/docker-compose.yml
  - docker/docker-compose.app.yml
  - scripts/deploy/deploy-findclass-zero-deps.sh
autonomous: true
requirements: [GROUP-01]

must_haves:
  truths:
    - "docker compose config 对所有 compose 文件输出正确的 Dockerfile 路径"
    - "所有 compose 文件中 findclass-ssr 的 dockerfile 指向同一份 noda-infra/deploy/Dockerfile.findclass-ssr"
    - "废弃部署脚本中的 Dockerfile 引用被修复或标记废弃"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "基础 compose 配置，Dockerfile 路径统一"
      contains: "dockerfile: ../noda-infra/deploy/Dockerfile.findclass-ssr"
    - path: "docker/docker-compose.app.yml"
      provides: "应用 compose 配置，Dockerfile 路径统一"
      contains: "dockerfile: ../noda-infra/deploy/Dockerfile.findclass-ssr"
    - path: "scripts/deploy/deploy-findclass-zero-deps.sh"
      provides: "废弃脚本路径引用修复"
  key_links:
    - from: "docker/docker-compose.yml"
      to: "deploy/Dockerfile.findclass-ssr"
      via: "build.dockerfile 相对路径（相对于 context ../../noda-apps）"
      pattern: "dockerfile.*Dockerfile\\.findclass-ssr"
    - from: "docker/docker-compose.app.yml"
      to: "deploy/Dockerfile.findclass-ssr"
      via: "build.dockerfile 相对路径（相对于 context ../../noda-apps）"
      pattern: "dockerfile.*Dockerfile\\.findclass-ssr"
---

<objective>
统一所有 Docker Compose 文件中 findclass-ssr 的 Dockerfile 路径引用，消除路径不一致问题。

Purpose: 当前 docker-compose.yml 和 docker-compose.app.yml 指向不同仓库中内容不同的 Dockerfile，生产部署行为不可预测。统一后确保构建来源唯一、可追溯（per D-01, D-02, D-03）。
Output: 两个 compose 文件路径统一，废弃脚本引用修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-服务整合/11-CONTEXT.md
@.planning/phases/11-服务整合/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一 findclass-ssr Dockerfile 路径引用</name>
  <files>docker/docker-compose.yml, docker/docker-compose.app.yml</files>
  <read_first>
    docker/docker-compose.yml
    docker/docker-compose.app.yml
  </read_first>
  <acceptance_criteria>
    - `docker/docker-compose.yml` 中 findclass-ssr 服务的 dockerfile 值为 `../noda-infra/deploy/Dockerfile.findclass-ssr`
    - `docker/docker-compose.app.yml` 中 findclass-ssr 服务的 dockerfile 值为 `../noda-infra/deploy/Dockerfile.findclass-ssr`
    - 两个文件中 findclass-ssr 的 build.context 保持 `../../noda-apps` 不变
    - `cd docker && docker compose -f docker-compose.yml config 2>&1 | grep dockerfile` 输出包含 Dockerfile.findclass-ssr 且路径正确
    - `cd docker && docker compose -f docker-compose.app.yml config 2>&1 | grep dockerfile` 输出包含 Dockerfile.findclass-ssr 且路径正确
    - 两个 compose 文件的 docker compose config 命令均无错误退出
  </acceptance_criteria>
  <action>
  **背景（per D-01, D-02, D-03）：**
  当前两个 compose 文件对同一 Dockerfile 使用了不同路径，且指向不同仓库中内容不同的文件。根据 D-01 决策，Dockerfile 以 `noda-infra/deploy/Dockerfile.findclass-ssr` 为准。

  **具体修改：**

  1. 修改 `docker/docker-compose.yml` 第 98 行：
     - 当前值：`dockerfile: ./infra/docker/Dockerfile.findclass-ssr`
     - 新值：`dockerfile: ../noda-infra/deploy/Dockerfile.findclass-ssr`
     - 注意：dockerfile 路径是相对于 context（即 `../../noda-apps`）的，从 `/Users/dianwenwang/Project/noda-apps` 出发，`../noda-infra/deploy/Dockerfile.findclass-ssr` 解析为 `/Users/dianwenwang/Project/noda-infra/deploy/Dockerfile.findclass-ssr`
     - context 保持 `../../noda-apps` 不变

  2. `docker/docker-compose.app.yml` 第 20 行已经是 `dockerfile: ../noda-infra/deploy/Dockerfile.findclass-ssr`，无需修改。验证其 context 为 `../../noda-apps`。

  3. 验证：对两个文件分别执行 `cd docker && docker compose -f <file> config`，确认输出中 dockerfile 路径解析正确，无错误。
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra/docker && docker compose -f docker-compose.yml config >/dev/null 2>&1 && docker compose -f docker-compose.app.yml config >/dev/null 2>&1 && echo "CONFIG_OK"</automated>
  </verify>
  <done>两个 compose 文件中 findclass-ssr 的 dockerfile 路径统一为 `../noda-infra/deploy/Dockerfile.findclass-ssr`，docker compose config 命令对两个文件均无错误退出。</done>
</task>

<task type="auto">
  <name>Task 2: 修复废弃部署脚本中的 Dockerfile 路径引用</name>
  <files>scripts/deploy/deploy-findclass-zero-deps.sh</files>
  <read_first>
    scripts/deploy/deploy-findclass-zero-deps.sh
  </read_first>
  <acceptance_criteria>
    - `scripts/deploy/deploy-findclass-zero-deps.sh` 文件前 5 行包含 `DEPRECATED` 标记注释
    - 脚本中不再引用不存在的 `$PROJECT_ROOT/docker/Dockerfile.findclass` 路径
    - 脚本中不再引用 `$PROJECT_ROOT/docker/Dockerfile.api` 路径
  </acceptance_criteria>
  <action>
  **背景：**
  `deploy-findclass-zero-deps.sh` 是旧架构遗留脚本，引用了不存在的 `$PROJECT_ROOT/docker/Dockerfile.findclass`（第 89 行）和 `$PROJECT_ROOT/docker/Dockerfile.api`（第 97 行）。当前部署已由 `deploy-infrastructure-prod.sh` 和 `deploy-apps-prod.sh` 替代。

  **具体修改：**

  在脚本第 1 行 shebang 之后（第 2 行），添加废弃标记注释：
  ```bash
  # DEPRECATED: 此脚本已过时，请使用 scripts/deploy/deploy-infrastructure-prod.sh 或 scripts/deploy/deploy-apps-prod.sh
  ```

  将 `build_images` 函数中的两个 docker build 调用替换为错误提示：
  - 将第 86-109 行的 `build_images` 函数体替换为：
    ```bash
      log_error "此脚本已废弃，请使用 deploy-infrastructure-prod.sh 或 deploy-apps-prod.sh"
      exit 1
    ```
  这确保任何尝试运行此脚本的人都会收到明确的废弃提示，而不是在构建失败时才发现问题。
  </action>
  <verify>
    <automated>grep -c "DEPRECATED" /Users/dianwenwang/Project/noda-infra/scripts/deploy/deploy-findclass-zero-deps.sh | grep -q "^[1-9]" && ! grep -q "Dockerfile.findclass" /Users/dianwenwang/Project/noda-infra/scripts/deploy/deploy-findclass-zero-deps.sh && ! grep -q "Dockerfile.api" /Users/dianwenwang/Project/noda-infra/scripts/deploy/deploy-findclass-zero-deps.sh && echo "SCRIPT_OK"</automated>
  </verify>
  <done>deploy-findclass-zero-deps.sh 已标记废弃，不再引用不存在的 Dockerfile 路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本阶段无安全影响 — 仅修改 Docker Compose 配置中的路径引用。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | — | — | accept | 无安全影响 — 仅修改路径引用和标签，不涉及认证、授权、加密或网络变更 |
</threat_model>

<verification>
1. `cd docker && docker compose -f docker-compose.yml config >/dev/null 2>&1` — 无错误
2. `cd docker && docker compose -f docker-compose.app.yml config >/dev/null 2>&1` — 无错误
3. 两个文件中 grep dockerfile 输出均为 `../noda-infra/deploy/Dockerfile.findclass-ssr`
4. 废弃脚本包含 DEPRECATED 标记，不再引用不存在的路径
</verification>

<success_criteria>
- docker-compose.yml 和 docker-compose.app.yml 中 findclass-ssr 的 dockerfile 路径完全一致
- docker compose config 对所有 compose 文件无错误退出
- 废弃脚本已标记且不引用不存在路径
</success_criteria>

<output>
After completion, create `.planning/phases/11-服务整合/11-01-SUMMARY.md`
</output>
