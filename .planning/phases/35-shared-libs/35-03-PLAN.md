---
phase: 35-shared-libs
plan: 03
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - scripts/lib/image-cleanup.sh
  - scripts/blue-green-deploy.sh
  - scripts/keycloak-blue-green-deploy.sh
  - scripts/pipeline-stages.sh
autonomous: true
requirements:
  - LIB-03

must_haves:
  truths:
    - "image-cleanup.sh 包含 3 个独立清理函数，每个消费者选择自己需要的函数"
    - "cleanup_by_tag_count() 保留最近 N 个带标签的镜像"
    - "cleanup_by_date_threshold() 删除超过 N 天的旧镜像和 dangling"
    - "cleanup_dangling() 仅清理无标签的 dangling images"
    - "3 个消费者脚本通过 source image-cleanup.sh 调用所需函数"
  artifacts:
    - path: "scripts/lib/image-cleanup.sh"
      provides: "3 个镜像清理函数"
      contains: "_NODA_IMAGE_CLEANUP_LOADED"
      exports: ["cleanup_by_tag_count", "cleanup_by_date_threshold", "cleanup_dangling"]
      min_lines: 80
  key_links:
    - from: "scripts/blue-green-deploy.sh"
      to: "scripts/lib/image-cleanup.sh"
      via: "source"
      pattern: "source.*image-cleanup\\.sh"
    - from: "scripts/pipeline-stages.sh"
      to: "scripts/lib/image-cleanup.sh"
      via: "source"
      pattern: "source.*image-cleanup\\.sh"
    - from: "scripts/keycloak-blue-green-deploy.sh"
      to: "scripts/lib/image-cleanup.sh"
      via: "source"
      pattern: "source.*image-cleanup\\.sh"
---

<objective>
提取镜像清理函数到共享库 scripts/lib/image-cleanup.sh

Purpose: 消除 3 个文件中的镜像清理重复代码。三种清理策略本质不同：标签保留（blue-green-deploy.sh）、日期阈值（pipeline-stages.sh）、dangling 清理（keycloak-blue-green-deploy.sh）。per D-01/D-02/D-03，保持 3 个独立函数而非统一入口。

Output: scripts/lib/image-cleanup.sh 新文件（含 3 个函数）+ 3 个消费者脚本迁移
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-shared-libs/35-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-shared-libs/35-01-SUMMARY.md

<interfaces>
<!-- Source Guard 模式参考 -->
From scripts/backup/lib/config.sh:
```bash
if [[ -n "${_NODA_CONFIG_LOADED:-}" ]]; then
  return 0
fi
_NODA_CONFIG_LOADED=1
```

<!-- 当前 3 种清理策略的完整实现 -->

策略 1 - 标签保留 (scripts/blue-green-deploy.sh):
```bash
cleanup_old_images() {
  local keep_count="${1:-5}"
  local images
  images=$(docker images findclass-ssr --format '{{.Tag}} {{.CreatedAt}}' \
    | grep -v '^latest ' \
    | sort -t' ' -k2 -r \
    | awk '{print $1}')
  local total
  total=$(echo "$images" | grep -c . || true)
  if [ "$total" -le "$keep_count" ]; then
    log_info "镜像清理: ${total} 个标签镜像 <= 保留 ${keep_count}，无需清理"
    return 0
  fi
  local to_delete
  to_delete=$(echo "$images" | tail -n +$((keep_count + 1)))
  log_info "镜像清理: ${total} 个标签镜像，保留 ${keep_count}，删除 $((total - keep_count)) 个"
  for tag in $to_delete; do
    log_info "  删除 findclass-ssr:${tag}"
    docker rmi "findclass-ssr:${tag}" 2>/dev/null || true
  done
  log_success "旧镜像清理完成"
}
```

策略 2 - 日期阈值 (scripts/pipeline-stages.sh):
```bash
cleanup_old_images() {
  local retention_days="${IMAGE_RETENTION_DAYS:-${1:-7}}"
  # macOS/Linux 兼容的 cutoff_epoch 计算
  # 清理 SHA 标签镜像（docker inspect 获取创建时间）
  # 清理 dangling images
  # 使用 SERVICE_NAME 变量确定镜像名
}
```

策略 3 - dangling 清理 (scripts/keycloak-blue-green-deploy.sh):
```bash
cleanup_old_keycloak_images() {
  local deleted=0
  local dangling_ids
  dangling_ids=$(docker images -f "dangling=true" --format '{{.ID}}' 2>/dev/null || true)
  for img_id in $dangling_ids; do
    docker rmi "$img_id" 2>/dev/null || true
    deleted=$((deleted + 1))
  done
  if [ "$deleted" -gt 0 ]; then
    log_success "镜像清理完成: 删除 ${deleted} 个 dangling 镜像"
  else
    log_info "镜像清理: 无需清理"
  fi
}
```

<!-- Plan 01 完成后的消费者文件结构（source 语句顺序） -->
Plan 01 已添加 `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"` 到 blue-green-deploy.sh、keycloak-blue-green-deploy.sh、pipeline-stages.sh。Plan 03 的 source 语句应添加在 deploy-check.sh 之后。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 image-cleanup.sh 共享库并迁移 3 个消费者脚本</name>
  <files>
    scripts/lib/image-cleanup.sh
    scripts/blue-green-deploy.sh
    scripts/keycloak-blue-green-deploy.sh
    scripts/pipeline-stages.sh
  </files>
  <read_first>
    scripts/blue-green-deploy.sh
    scripts/keycloak-blue-green-deploy.sh
    scripts/pipeline-stages.sh
    scripts/backup/lib/config.sh
  </read_first>
  <action>
1. 创建 scripts/lib/image-cleanup.sh，包含以下内容：

```bash
#!/bin/bash
# ============================================
# 镜像清理共享库
# ============================================
# 提供 3 个独立的镜像清理函数（per D-01/D-02/D-03）
# 依赖：log.sh
# ============================================

# Source Guard
if [[ -n "${_NODA_IMAGE_CLEANUP_LOADED:-}" ]]; then
  return 0
fi
_NODA_IMAGE_CLEANUP_LOADED=1

# cleanup_by_tag_count - 保留最近 N 个带标签的镜像，删除更早的
# 参数:
#   $1: 镜像名（如 findclass-ssr）
#   $2: 保留数量（默认 5）
# 返回：无（删除旧镜像）
cleanup_by_tag_count() {
  local image_name="$1"
  local keep_count="${2:-5}"

  # 列出所有非 latest 标签的镜像，按创建时间排序（最新在前）
  local images
  images=$(docker images "$image_name" --format '{{.Tag}} {{.CreatedAt}}' \
    | grep -v '^latest ' \
    | sort -t' ' -k2 -r \
    | awk '{print $1}')

  local total
  total=$(echo "$images" | grep -c . || true)

  if [ "$total" -le "$keep_count" ]; then
    log_info "镜像清理: ${total} 个标签镜像 <= 保留 ${keep_count}，无需清理"
    return 0
  fi

  local to_delete
  to_delete=$(echo "$images" | tail -n +$((keep_count + 1)))

  log_info "镜像清理: ${total} 个标签镜像，保留 ${keep_count}，删除 $((total - keep_count)) 个"

  for tag in $to_delete; do
    log_info "  删除 ${image_name}:${tag}"
    docker rmi "${image_name}:${tag}" 2>/dev/null || true
  done

  log_success "旧镜像清理完成"
}

# cleanup_by_date_threshold - 删除超过指定天数的旧镜像和 dangling images
# 参数:
#   $1: 镜像名（如 findclass-ssr 或 keycloak）
#   $2: 保留天数（默认 7）
# 返回：无（删除旧镜像）
cleanup_by_date_threshold() {
  local image_name="$1"
  local retention_days="${2:-7}"

  log_info "镜像清理: 删除超过 ${retention_days} 天的旧镜像..."

  # macOS 兼容：BSD date 使用 -v-${retention_days}d
  local cutoff_epoch
  if date -v-1d >/dev/null 2>&1; then
    cutoff_epoch=$(date -v-"${retention_days}"d +%s)
  else
    cutoff_epoch=$(date -d "${retention_days} days ago" +%s)
  fi

  # 1. 清理带 Git SHA 标签的旧镜像（排除 latest）
  local sha_tags
  sha_tags=$(docker images "$image_name" --format '{{.Tag}}' \
    | grep -v '^latest$' \
    | grep -v '^<none>' || true)

  local deleted=0
  for tag in $sha_tags; do
    # 使用 docker inspect 获取 ISO 8601 创建时间
    local created_iso
    created_iso=$(docker inspect --format '{{.Created}}' "${image_name}:${tag}" 2>/dev/null || echo "")

    if [ -z "$created_iso" ]; then
      continue
    fi

    # 将 ISO 8601 转为 epoch（macOS 兼容）
    local image_epoch
    if date -j -f "%Y-%m-%dT%H:%M:%S" "" >/dev/null 2>&1; then
      # macOS: 截取 ISO 8601 到秒级精度
      local created_short
      created_short=$(echo "$created_iso" | sed 's/\..*//' | sed 's/Z$//')
      image_epoch=$(date -j -f "%Y-%m-%dT%H:%M:%S" "$created_short" +%s 2>/dev/null || echo "0")
    else
      image_epoch=$(date -d "$created_iso" +%s 2>/dev/null || echo "0")
    fi

    if [ "$image_epoch" -eq 0 ]; then
      continue
    fi

    if [ "$image_epoch" -lt "$cutoff_epoch" ]; then
      # macOS 兼容的日期显示
      local image_date
      if date -r 0 >/dev/null 2>&1; then
        image_date=$(date -r "$image_epoch" +"%Y-%m-%d")
      else
        image_date=$(date -d "@$image_epoch" +"%Y-%m-%d")
      fi
      log_info "  删除 ${image_name}:${tag} ($image_date)"
      docker rmi "${image_name}:${tag}" 2>/dev/null || true
      deleted=$((deleted + 1))
    fi
  done

  # 2. 清理 dangling images
  local dangling_ids
  dangling_ids=$(docker images -f "dangling=true" --format '{{.ID}}' 2>/dev/null || true)
  for img_id in $dangling_ids; do
    docker rmi "$img_id" 2>/dev/null || true
    deleted=$((deleted + 1))
  done

  if [ "$deleted" -gt 0 ]; then
    log_success "镜像清理完成: 删除 ${deleted} 个镜像"
  else
    log_info "镜像清理: 无需清理"
  fi
}

# cleanup_dangling - 清理无标签的 dangling images
# 参数：无
# 返回：无（删除 dangling 镜像）
cleanup_dangling() {
  local deleted=0

  local dangling_ids
  dangling_ids=$(docker images -f "dangling=true" --format '{{.ID}}' 2>/dev/null || true)
  for img_id in $dangling_ids; do
    docker rmi "$img_id" 2>/dev/null || true
    deleted=$((deleted + 1))
  done

  if [ "$deleted" -gt 0 ]; then
    log_success "镜像清理完成: 删除 ${deleted} 个 dangling 镜像"
  else
    log_info "镜像清理: 无需清理"
  fi
}
```

2. 修改 scripts/blue-green-deploy.sh：
   - 在 `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/image-cleanup.sh"`
   - 删除整个 cleanup_old_images 函数定义（从注释 `# 函数: cleanup_old_images` 到函数右大括号，约第 125-160 行，Plan 01 完成后行号可能有偏移，以实际文件内容为准）
   - 修改调用处：
     将 `cleanup_old_images 5`
     改为 `cleanup_by_tag_count "findclass-ssr" 5`

3. 修改 scripts/keycloak-blue-green-deploy.sh：
   - 在 `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/image-cleanup.sh"`
   - 删除整个 cleanup_old_keycloak_images 函数定义（从注释 `# 函数: cleanup_old_keycloak_images` 到函数右大括号，约第 148-168 行，Plan 01 完成后行号可能有偏移）
   - 修改调用处：
     将 `cleanup_old_keycloak_images`
     改为 `cleanup_dangling`

4. 修改 scripts/pipeline-stages.sh：
   - 在 `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/image-cleanup.sh"`
   - 删除整个 cleanup_old_images 函数定义（从注释 `# 函数: cleanup_old_images` 到函数右大括号，约第 209-290 行，Plan 01 完成后行号可能有偏移）
   - 修改第一处调用（pipeline_cleanup 函数中，约第 598 行）：
     将 `cleanup_old_images`
     改为 `cleanup_by_date_threshold "${SERVICE_NAME:-findclass-ssr}" "${IMAGE_RETENTION_DAYS:-7}"`
   - 修改第二处调用（pipeline_full_cleanup 函数中的 keycloak case，约第 1046 行）：
     将 `cleanup_old_images`
     改为 `cleanup_dangling`

注意事项：
- Plan 01（Wave 1）已完成，deploy-check.sh 的 source 语句已存在于 3 个消费者文件中
- 本 Plan 的 image-cleanup.sh source 语句添加在 deploy-check.sh 之后
- per D-01，3 个独立函数，不做统一入口
- per D-02，函数命名为策略描述风格
- cleanup_by_tag_count 参数化镜像名（原硬编码 findclass-ssr），便于未来复用
- cleanup_by_date_threshold 参数化镜像名和天数
- 先读取文件确认实际行号再删除，不要依赖预估行号
  </action>
  <acceptance_criteria>
    - scripts/lib/image-cleanup.sh 文件存在
    - grep -c '_NODA_IMAGE_CLEANUP_LOADED' scripts/lib/image-cleanup.sh 返回 2
    - grep -c 'cleanup_by_tag_count()' scripts/lib/image-cleanup.sh 返回 1
    - grep -c 'cleanup_by_date_threshold()' scripts/lib/image-cleanup.sh 返回 1
    - grep -c 'cleanup_dangling()' scripts/lib/image-cleanup.sh 返回 1
    - grep -c 'source.*image-cleanup\\.sh' scripts/blue-green-deploy.sh 返回 1
    - grep -c 'source.*image-cleanup\\.sh' scripts/keycloak-blue-green-deploy.sh 返回 1
    - grep -c 'source.*image-cleanup\\.sh' scripts/pipeline-stages.sh 返回 1
    - grep 'cleanup_old_images()' scripts/blue-green-deploy.sh 无匹配（函数定义已删除）
    - grep 'cleanup_old_keycloak_images()' scripts/keycloak-blue-green-deploy.sh 无匹配
    - grep 'cleanup_old_images()' scripts/pipeline-stages.sh 无匹配
    - grep 'cleanup_by_tag_count.*findclass-ssr' scripts/blue-green-deploy.sh 有匹配
    - grep 'cleanup_dangling' scripts/keycloak-blue-green-deploy.sh 有匹配
    - grep 'cleanup_by_date_threshold' scripts/pipeline-stages.sh 有匹配
    - 所有文件通过 bash -n 语法检查
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/lib/image-cleanup.sh && bash -n scripts/blue-green-deploy.sh && bash -n scripts/keycloak-blue-green-deploy.sh && bash -n scripts/pipeline-stages.sh && echo "语法检查通过"</automated>
  </verify>
  <done>image-cleanup.sh 包含 3 个独立清理函数，3 个消费者脚本删除内联定义并通过 source 调用对应函数</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → image-cleanup.sh | source 引入，信任调用方已加载 log.sh |
| image-cleanup.sh → docker rmi | 删除镜像操作，仅删除符合条件的镜像 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | T (Tampering) | docker rmi 删除镜像 | mitigate | keep_count 和 retention_days 限制删除范围，不删除 latest 标签 |
| T-35-05 | D (Denial of Service) | docker rmi 大量镜像 | accept | 清理操作仅在部署成功后执行，频率低 |

</threat_model>

<verification>
1. `bash -n scripts/lib/image-cleanup.sh` — 新文件语法正确
2. `bash -n scripts/blue-green-deploy.sh && bash -n scripts/keycloak-blue-green-deploy.sh && bash -n scripts/pipeline-stages.sh` — 消费者语法正确
3. 确认 image-cleanup.sh 包含 3 个函数定义
4. 确认 3 个消费者文件不再包含旧的 cleanup 函数定义
5. 确认消费者调用处使用正确的新函数名
</verification>

<success_criteria>
- scripts/lib/image-cleanup.sh 存在，包含 Source Guard + 3 个清理函数
- 3 个消费者脚本均 source image-cleanup.sh
- blue-green-deploy.sh 使用 cleanup_by_tag_count("findclass-ssr", 5)
- keycloak-blue-green-deploy.sh 使用 cleanup_dangling()
- pipeline-stages.sh 使用 cleanup_by_date_threshold 和 cleanup_dangling
- 所有文件通过 bash -n 语法检查
</success_criteria>

<output>
After completion, create `.planning/phases/35-shared-libs/35-03-SUMMARY.md`
</output>
