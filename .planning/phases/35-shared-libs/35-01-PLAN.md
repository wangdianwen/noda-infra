---
phase: 35-shared-libs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/deploy-check.sh
  - scripts/blue-green-deploy.sh
  - scripts/keycloak-blue-green-deploy.sh
  - scripts/rollback-findclass.sh
  - scripts/pipeline-stages.sh
autonomous: true
requirements:
  - LIB-01

must_haves:
  truths:
    - "http_health_check() 和 e2e_verify() 只在 deploy-check.sh 中定义一次"
    - "4 个消费者脚本通过 source deploy-check.sh 调用这两个函数"
    - "http_health_check 使用位置参数接收 container/port/health_path/max_retries/interval"
    - "e2e_verify 使用位置参数接收 target_env/port/health_path/max_retries/interval"
    - "所有共享库文件包含 Source Guard 防止重复加载"
  artifacts:
    - path: "scripts/lib/deploy-check.sh"
      provides: "http_health_check() + e2e_verify() 共享函数"
      contains: "_NODA_DEPLOY_CHECK_LOADED"
      min_lines: 40
  key_links:
    - from: "scripts/blue-green-deploy.sh"
      to: "scripts/lib/deploy-check.sh"
      via: "source"
      pattern: "source.*deploy-check\\.sh"
    - from: "scripts/pipeline-stages.sh"
      to: "scripts/lib/deploy-check.sh"
      via: "source"
      pattern: "source.*deploy-check\\.sh"
---

<objective>
提取 http_health_check() 和 e2e_verify() 到共享库 scripts/lib/deploy-check.sh

Purpose: 消除 4 个文件中 http_health_check + e2e_verify 的重复定义（每处约 50-80 行），统一参数化接口。目前 blue-green-deploy.sh 硬编码 localhost:3001/api/health，keycloak 版硬编码 SERVICE_PORT/HEALTH_PATH 环境变量，rollback-findclass.sh 又有独立的一份。提取后所有差异通过位置参数传递。

Output: scripts/lib/deploy-check.sh 新文件 + 4 个消费者脚本删除内联函数并添加 source 语句
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-shared-libs/35-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Source Guard 模式参考 -->
From scripts/backup/lib/config.sh:
```bash
if [[ -n "${_NODA_CONFIG_LOADED:-}" ]]; then
  return 0
fi
_NODA_CONFIG_LOADED=1
```

<!-- 依赖的外部函数 -->
From scripts/manage-containers.sh:
```bash
get_container_name() {
  local env="$1"
  echo "${SERVICE_NAME}-${env}"
}
```

<!-- 调用方常量定义 -->
From scripts/blue-green-deploy.sh:
```
HEALTH_CHECK_MAX_RETRIES=30, HEALTH_CHECK_INTERVAL=4, E2E_MAX_RETRIES=5, E2E_INTERVAL=2
NGINX_CONTAINER 变量来自 manage-containers.sh
```

From scripts/keycloak-blue-green-deploy.sh:
```
HEALTH_CHECK_MAX_RETRIES=45, HEALTH_CHECK_INTERVAL=4, E2E_MAX_RETRIES=5, E2E_INTERVAL=2
SERVICE_PORT=8080, HEALTH_PATH=/realms/master
NGINX_CONTAINER 变量来自 manage-containers.sh
```

From scripts/rollback-findclass.sh:
```
默认值硬编码在函数内: max_retries=10, interval=3 (http), max_retries=5, interval=2 (e2e)
NGINX_CONTAINER 变量来自 manage-containers.sh
```

From scripts/pipeline-stages.sh:
```
HEALTH_CHECK_MAX_RETRIES=30, HEALTH_CHECK_INTERVAL=4, E2E_MAX_RETRIES=5, E2E_INTERVAL=2
SERVICE_PORT 默认 3001, HEALTH_PATH 默认 /api/health
NGINX_CONTAINER 变量来自 manage-containers.sh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 deploy-check.sh 共享库并迁移 blue-green-deploy.sh 和 keycloak-blue-green-deploy.sh</name>
  <files>
    scripts/lib/deploy-check.sh
    scripts/blue-green-deploy.sh
    scripts/keycloak-blue-green-deploy.sh
  </files>
  <read_first>
    scripts/lib/deploy-check.sh
    scripts/blue-green-deploy.sh
    scripts/keycloak-blue-green-deploy.sh
    scripts/backup/lib/config.sh
    scripts/manage-containers.sh
  </read_first>
  <action>
1. 创建 scripts/lib/deploy-check.sh，包含以下内容：

```bash
#!/bin/bash
# ============================================
# 部署检查共享库
# ============================================
# 提供 HTTP 健康检查和 E2E 验证函数
# 依赖：log.sh, manage-containers.sh (get_container_name)
# ============================================

# Source Guard
if [[ -n "${_NODA_DEPLOY_CHECK_LOADED:-}" ]]; then
  return 0
fi
_NODA_DEPLOY_CHECK_LOADED=1

# http_health_check - 通过 nginx 容器 wget 检查目标容器 HTTP 端点
# 参数:
#   $1: 容器名
#   $2: 服务端口
#   $3: 健康检查路径
#   $4: 最大重试次数
#   $5: 重试间隔秒数
# 返回：0=健康，1=失败
http_health_check() {
  local container="$1"
  local service_port="$2"
  local health_path="$3"
  local max_retries="$4"
  local interval="$5"
  local attempt=0

  log_info "HTTP 健康检查: $container (最多 ${max_retries} 次, 间隔 ${interval}s)"

  while [ $attempt -lt $max_retries ]; do
    attempt=$((attempt + 1))

    if docker exec "$NGINX_CONTAINER" wget --quiet --tries=1 --spider "http://${container}:${service_port}${health_path}" 2>/dev/null; then
      log_success "$container — HTTP 健康检查通过 (第 ${attempt}/${max_retries} 次)"
      return 0
    fi

    if [ $attempt -lt $max_retries ]; then
      sleep "$interval"
    fi
  done

  log_error "$container — HTTP 健康检查失败 (${max_retries} 次尝试)"
  log_info "最近容器日志:"
  docker logs "$container" --tail 20 2>&1 | sed 's/^/  /'
  return 1
}

# e2e_verify - 通过 nginx 容器 curl/wget 验证完整请求链路
# 参数:
#   $1: 目标环境 (blue 或 green)
#   $2: 服务端口
#   $3: 健康检查路径
#   $4: 最大重试次数
#   $5: 重试间隔秒数
# 返回：0=验证通过，1=验证失败
e2e_verify() {
  local target_env="$1"
  local service_port="$2"
  local health_path="$3"
  local max_retries="$4"
  local interval="$5"
  local container_name
  container_name=$(get_container_name "$target_env")

  log_info "E2E 验证: nginx -> $container_name (最多 ${max_retries} 次)"

  # 检测 nginx 容器是否有 curl
  local use_curl=true
  if ! docker exec "$NGINX_CONTAINER" which curl >/dev/null 2>&1; then
    log_info "nginx 容器无 curl，使用 wget 备选方案"
    use_curl=false
  fi

  local attempt=0
  while [ $attempt -lt $max_retries ]; do
    attempt=$((attempt + 1))

    local result=1

    if [ "$use_curl" = true ]; then
      local http_code
      http_code=$(docker exec "$NGINX_CONTAINER" \
        curl -s -o /dev/null -w "%{http_code}" \
        "http://${container_name}:${service_port}${health_path}" 2>/dev/null || echo "000")

      if [ "$http_code" = "200" ]; then
        result=0
      fi
    else
      if docker exec "$NGINX_CONTAINER" \
        wget --quiet --tries=1 --spider \
        "http://${container_name}:${service_port}${health_path}" 2>/dev/null; then
        result=0
      fi
    fi

    if [ $result -eq 0 ]; then
      log_success "E2E 验证通过 (第 ${attempt}/${max_retries} 次)"
      return 0
    fi

    if [ $attempt -lt $max_retries ]; then
      sleep "$interval"
    fi
  done

  log_error "E2E 验证失败 (${max_retries} 次尝试)"
  return 1
}
```

2. 修改 scripts/blue-green-deploy.sh：
   - 在 `source "$PROJECT_ROOT/scripts/manage-containers.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"`
   - 删除整个 http_health_check 函数定义（约第 27-61 行）
   - 删除整个 e2e_verify 函数定义（约第 63-123 行）
   - 修改调用处（约第 245 行）：
     将 `http_health_check "$target_container" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"`
     改为 `http_health_check "$target_container" "3001" "/api/health" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"`
   - 修改调用处（约第 273 行）：
     将 `e2e_verify "$target_env" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"`
     改为 `e2e_verify "$target_env" "3001" "/api/health" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"`

3. 修改 scripts/keycloak-blue-green-deploy.sh：
   - 在 `source "$PROJECT_ROOT/scripts/manage-containers.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"`
   - 删除整个 http_health_check 函数定义（约第 60-90 行）
   - 删除整个 e2e_verify 函数定义（约第 92-146 行）
   - 修改调用处（约第 245 行）：
     将 `http_health_check "$target_container" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"`
     改为 `http_health_check "$target_container" "$SERVICE_PORT" "$HEALTH_PATH" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"`
   - 修改调用处（约第 273 行）：
     将 `e2e_verify "$target_env" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"`
     改为 `e2e_verify "$target_env" "$SERVICE_PORT" "$HEALTH_PATH" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"`

注意事项：
- per D-04/D-05，所有配置通过位置参数传递，不使用环境变量隐式传递
- deploy-check.sh 不 source log.sh 和 manage-containers.sh（调用方已 source，通过 Source Guard 防止重复加载后仍可用）
- blue-green-deploy.sh 中的 http_health_check 使用 docker exec 到容器内部 wget（而非 nginx 容器），统一改为通过 nginx 容器检查（与 pipeline-stages.sh 和 keycloak 版一致，更通用）
  </action>
  <acceptance_criteria>
    - scripts/lib/deploy-check.sh 文件存在
    - grep -c '_NODA_DEPLOY_CHECK_LOADED' scripts/lib/deploy-check.sh 返回 2（定义行和检查行）
    - grep -c 'http_health_check()' scripts/lib/deploy-check.sh 返回 1（仅此一处定义）
    - grep -c 'e2e_verify()' scripts/lib/deploy-check.sh 返回 1（仅此一处定义）
    - grep -c 'source.*deploy-check.sh' scripts/blue-green-deploy.sh 返回 1
    - grep -c 'source.*deploy-check.sh' scripts/keycloak-blue-green-deploy.sh 返回 1
    - grep -c 'http_health_check()' scripts/blue-green-deploy.sh 返回 0（函数定义已删除，调用处不包含括号）
    - grep -c 'e2e_verify()' scripts/blue-green-deploy.sh 返回 0
    - grep -c 'http_health_check()' scripts/keycloak-blue-green-deploy.sh 返回 0
    - grep -c 'e2e_verify()' scripts/keycloak-blue-green-deploy.sh 返回 0
    - blue-green-deploy.sh 的调用处包含 5 个参数: http_health_check "$target_container" "3001" "/api/health" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"
    - keycloak-blue-green-deploy.sh 的调用处使用 $SERVICE_PORT 和 $HEALTH_PATH 变量
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/lib/deploy-check.sh && bash -n scripts/blue-green-deploy.sh && bash -n scripts/keycloak-blue-green-deploy.sh && echo "语法检查通过"</automated>
  </verify>
  <done>deploy-check.sh 包含 http_health_check + e2e_verify 两个函数，blue-green-deploy.sh 和 keycloak-blue-green-deploy.sh 删除了内联函数定义，通过 source 调用共享库</done>
</task>

<task type="auto">
  <name>Task 2: 迁移 rollback-findclass.sh 和 pipeline-stages.sh 使用 deploy-check.sh 共享库</name>
  <files>
    scripts/rollback-findclass.sh
    scripts/pipeline-stages.sh
  </files>
  <read_first>
    scripts/rollback-findclass.sh
    scripts/pipeline-stages.sh
    scripts/lib/deploy-check.sh
  </read_first>
  <action>
1. 修改 scripts/rollback-findclass.sh：
   - 在 `source "$PROJECT_ROOT/scripts/manage-containers.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"`
   - 删除整个 http_health_check 函数定义（约第 18-53 行，包含注释和函数体）
   - 删除整个 e2e_verify 函数定义（约第 55-120 行，包含注释和函数体）
   - 修改调用处（约第 160 行）：
     将 `http_health_check "$rollback_container" 10 3`
     改为 `http_health_check "$rollback_container" "3001" "/api/health" "10" "3"`
   - 修改调用处（约第 182 行）：
     将 `e2e_verify "$rollback_env"`
     改为 `e2e_verify "$rollback_env" "3001" "/api/health" "5" "2"`
   - 注意：rollback-findclass.sh 原来使用 docker exec 到容器内部 wget，统一改为通过 nginx 容器检查

2. 修改 scripts/pipeline-stages.sh：
   - 在 `source "$PROJECT_ROOT/scripts/manage-containers.sh"` 之后添加：
     `source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"`
   - 删除整个 http_health_check 函数定义（约第 109-144 行）
   - 删除整个 e2e_verify 函数定义（约第 146-207 行）
   - 修改 pipeline_health_check 函数（约第 497-502 行）中的调用：
     将 `http_health_check "$target_container" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"`
     改为 `http_health_check "$target_container" "${SERVICE_PORT:-3001}" "${HEALTH_PATH:-/api/health}" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"`
   - 修改 pipeline_verify 函数（约第 527-530 行）中的调用：
     将 `e2e_verify "$target_env" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"`
     改为 `e2e_verify "$target_env" "${SERVICE_PORT:-3001}" "${HEALTH_PATH:-/api/health}" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"`

注意事项：
- pipeline-stages.sh 使用 ${SERVICE_PORT:-3001} 和 ${HEALTH_PATH:-/api/health} 保持参数化兼容
- rollback-findclass.sh 回滚场景的参数 10/3 和 5/2 保留其原始值（更激进的重试策略）
  </action>
  <acceptance_criteria>
    - grep -c 'source.*deploy-check.sh' scripts/rollback-findclass.sh 返回 1
    - grep -c 'source.*deploy-check.sh' scripts/pipeline-stages.sh 返回 1
    - grep -c 'http_health_check()' scripts/rollback-findclass.sh 返回 0
    - grep -c 'e2e_verify()' scripts/rollback-findclass.sh 返回 0
    - grep -c 'http_health_check()' scripts/pipeline-stages.sh 返回 0
    - grep -c 'e2e_verify()' scripts/pipeline-stages.sh 返回 0
    - grep 'http_health_check.*rollback_container.*3001.*api/health' scripts/rollback-findclass.sh 有匹配
    - grep 'http_health_check.*SERVICE_PORT:-3001' scripts/pipeline-stages.sh 有匹配
    - grep 'e2e_verify.*rollback_env.*3001.*api/health' scripts/rollback-findclass.sh 有匹配
    - grep 'e2e_verify.*SERVICE_PORT:-3001' scripts/pipeline-stages.sh 有匹配
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/rollback-findclass.sh && bash -n scripts/pipeline-stages.sh && echo "语法检查通过"</automated>
  </verify>
  <done>rollback-findclass.sh 和 pipeline-stages.sh 删除内联的 http_health_check + e2e_verify 函数，改为 source deploy-check.sh，保留各文件的差异化参数</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调用方 → deploy-check.sh | source 引入，信任调用方已加载 log.sh 和 manage-containers.sh |
| deploy-check.sh → docker exec | 执行 docker 命令检查容器健康状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | T (Tampering) | deploy-check.sh 函数参数 | accept | 函数仅在部署流程中被可信脚本调用，无外部输入 |
| T-35-02 | D (DoS) | docker exec 健康检查 | accept | 已有 max_retries + interval 限制，不会无限循环 |
</threat_model>

<verification>
1. `bash -n scripts/lib/deploy-check.sh` — 新文件语法正确
2. `bash -n scripts/blue-green-deploy.sh && bash -n scripts/keycloak-blue-green-deploy.sh && bash -n scripts/rollback-findclass.sh && bash -n scripts/pipeline-stages.sh` — 所有消费者语法正确
3. 确认 4 个消费者文件中不再有 http_health_check() 或 e2e_verify() 的函数定义
4. 确认 deploy-check.sh 中 http_health_check + e2e_verify 各仅定义一次
</verification>

<success_criteria>
- scripts/lib/deploy-check.sh 存在，包含 Source Guard + http_health_check() + e2e_verify()
- 4 个消费者脚本均 source deploy-check.sh
- 4 个消费者脚本均不包含 http_health_check() 或 e2e_verify() 的函数定义
- 各消费者的调用参数保留原有的差异化值（端口/路径/重试次数/间隔）
- 所有文件通过 bash -n 语法检查
</success_criteria>

<output>
After completion, create `.planning/phases/35-shared-libs/35-01-SUMMARY.md`
</output>
