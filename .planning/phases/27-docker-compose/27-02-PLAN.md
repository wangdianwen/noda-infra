---
phase: 27-docker-compose
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy/deploy-infrastructure-prod.sh
  - scripts/setup-postgres-local.sh
autonomous: true
requirements:
  - CLEANUP-04

must_haves:
  truths:
    - "deploy-infrastructure-prod.sh 的 EXPECTED_CONTAINERS 不包含 noda-infra-postgres-dev"
    - "deploy-infrastructure-prod.sh 的 START_SERVICES 不包含 postgres-dev"
    - "deploy-infrastructure-prod.sh 的 COMPOSE_FILES 不引用 docker-compose.dev.yml"
    - "deploy-infrastructure-prod.sh 的完成信息不包含 PostgreSQL (Dev)"
    - "setup-postgres-local.sh 的 migrate-data 在 postgres-dev 容器不存在时输出友好提示"
  artifacts:
    - path: "scripts/deploy/deploy-infrastructure-prod.sh"
      provides: "生产部署脚本，已移除 dev 服务引用"
      contains: "COMPOSE_FILES"
    - path: "scripts/setup-postgres-local.sh"
      provides: "本地 PG 管理脚本，migrate-data 已标记废弃"
      contains: "cmd_migrate_data"
  key_links:
    - from: "scripts/deploy/deploy-infrastructure-prod.sh"
      to: "docker/docker-compose.yml"
      via: "COMPOSE_FILES -f 参数"
      pattern: "COMPOSE_FILES.*docker-compose\\.yml.*docker-compose\\.prod\\.yml"
---

<objective>
更新部署脚本和本地 PG 脚本，移除 dev 容器引用并添加兼容性处理

Purpose: Docker Compose 文件已清理 dev 服务定义，部署脚本的容器列表和服务列表必须同步更新。migrate-data 子命令需兼容 postgres-dev 容器已移除的情况。

Output: 更新后的 deploy-infrastructure-prod.sh 和 setup-postgres-local.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-docker-compose/27-CONTEXT.md
@.planning/phases/27-docker-compose/27-RESEARCH.md

<interfaces>
<!-- deploy-infrastructure-prod.sh 关键变量位置 -->

COMPOSE_FILES（L42）:
  当前值: "-f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.dev.yml"
  目标值: "-f docker/docker-compose.yml -f docker/docker-compose.prod.yml"

EXPECTED_CONTAINERS（L44-51）:
  当前数组: ["noda-infra-postgres-prod", "noda-infra-postgres-dev", "noda-infra-keycloak-prod", "noda-infra-nginx", "noda-ops", "findclass-ssr"]
  目标数组: ["noda-infra-postgres-prod", "noda-infra-keycloak-prod", "noda-infra-nginx", "noda-ops", "findclass-ssr"]

START_SERVICES（L54）:
  当前值: "postgres keycloak nginx noda-ops postgres-dev"
  目标值: "postgres keycloak nginx noda-ops"

完成信息（L353-354）:
  当前值: 包含 "PostgreSQL (Dev): 运行中"
  目标值: 移除此行

文件顶部注释（L8-9）:
  当前值: "包括：PostgreSQL (Prod/Dev), Keycloak, Nginx, Noda-Ops, Findclass-SSR"
  目标值: "包括：PostgreSQL, Keycloak, Nginx, Noda-Ops, Findclass-SSR"

步骤 4 日志（L273）:
  当前值: "启动 PostgreSQL, Keycloak, Nginx, Noda-Ops, PostgreSQL-Dev, Findclass-SSR..."
  目标值: "启动 PostgreSQL, Keycloak, Nginx, Noda-Ops, Findclass-SSR..."

rollback_images container_to_service 映射（L99-107）:
  已跳过 postgres-dev（返回空字符串），无需修改

<!-- setup-postgres-local.sh migrate-data 函数 -->

cmd_migrate_data()（L340-466）:
  容器名: noda-infra-postgres-dev（L357）
  容器未运行处理（L358-369）:
    当前: 交互式询问是否跳过，或提示启动命令
    目标: 检测容器是否已被移除（compose 文件中不再定义），输出废弃提示

  需要修改的判断逻辑:
    将现有的 "容器未运行" 判断改为区分 "容器未运行但定义存在" vs "容器定义已被移除"
    因为 dev.yml 已移除 postgres-dev，docker compose up postgres-dev 不再有效
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 deploy-infrastructure-prod.sh 移除 dev 服务引用</name>
  <files>scripts/deploy/deploy-infrastructure-prod.sh</files>
  <read_first>
    - scripts/deploy/deploy-infrastructure-prod.sh
  </read_first>
  <action>
按 D-04 决策更新部署脚本，共 5 处修改：

1. **L42 COMPOSE_FILES 变量**:
   - 旧值: `COMPOSE_FILES="-f docker/docker-compose.yml -f docker/docker-compose.prod.yml -f docker/docker-compose.dev.yml"`
   - 新值: `COMPOSE_FILES="-f docker/docker-compose.yml -f docker/docker-compose.prod.yml"`
   - 生产部署不应包含 dev overlay（与 CLAUDE.md 部署规则一致："需使用 -f base -f prod 双文件"）

2. **L44-51 EXPECTED_CONTAINERS 数组**:
   - 移除 `"noda-infra-postgres-dev"` 行

3. **L54 START_SERVICES 变量**:
   - 旧值: `START_SERVICES="postgres keycloak nginx noda-ops postgres-dev"`
   - 新值: `START_SERVICES="postgres keycloak nginx noda-ops"`

4. **L273 步骤 4 日志消息**:
   - 旧值: `log_info "启动 PostgreSQL, Keycloak, Nginx, Noda-Ops, PostgreSQL-Dev, Findclass-SSR..."`
   - 新值: `log_info "启动 PostgreSQL, Keycloak, Nginx, Noda-Ops, Findclass-SSR..."`

5. **L353-354 完成信息**:
   - 移除 `log_info "✓ PostgreSQL (Dev): 运行中"` 行

6. **L8-9 文件顶部注释**:
   - 旧值: `包括：PostgreSQL (Prod/Dev), Keycloak, Nginx, Noda-Ops, Findclass-SSR`
   - 新值: `包括：PostgreSQL, Keycloak, Nginx, Noda-Ops, Findclass-SSR`

注意：container_to_service() 映射函数无需修改（已通过 `*` 通配符跳过未映射容器）
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && grep -c "postgres-dev" scripts/deploy/deploy-infrastructure-prod.sh && echo "---" && grep "COMPOSE_FILES=" scripts/deploy/deploy-infrastructure-prod.sh && echo "---" && grep "START_SERVICES=" scripts/deploy/deploy-infrastructure-prod.sh && echo "---" && grep "EXPECTED_CONTAINERS" -A6 scripts/deploy/deploy-infrastructure-prod.sh && echo "---" && grep "PostgreSQL (Dev)" scripts/deploy/deploy-infrastructure-prod.sh</automated>
    验证预期：
    - postgres-dev 匹配数为 0
    - COMPOSE_FILES 仅含 base + prod 两个 -f 参数
    - START_SERVICES 不含 postgres-dev
    - EXPECTED_CONTAINERS 不含 postgres-dev
    - PostgreSQL (Dev) 无匹配
  </verify>
  <acceptance_criteria>
    - grep -c "postgres-dev" scripts/deploy/deploy-infrastructure-prod.sh 返回 0
    - grep "COMPOSE_FILES=" scripts/deploy/deploy-infrastructure-prod.sh 匹配 `-f docker/docker-compose.yml -f docker/docker-compose.prod.yml`（不含 dev.yml）
    - grep "START_SERVICES=" scripts/deploy/deploy-infrastructure-prod.sh 匹配 "postgres keycloak nginx noda-ops"（不含 postgres-dev）
    - grep -c "noda-infra-postgres-dev" scripts/deploy/deploy-infrastructure-prod.sh 返回 0
    - grep -c "PostgreSQL (Dev)" scripts/deploy/deploy-infrastructure-prod.sh 返回 0
  </acceptance_criteria>
  <done>部署脚本中所有 dev 容器引用已移除，COMPOSE_FILES 改为双文件模式，完成信息不再提及 Dev PostgreSQL</done>
</task>

<task type="auto">
  <name>Task 2: 更新 setup-postgres-local.sh migrate-data 兼容性</name>
  <files>scripts/setup-postgres-local.sh</files>
  <read_first>
    - scripts/setup-postgres-local.sh
    - scripts/setup-postgres-local.sh L340-466（cmd_migrate_data 函数）
  </read_first>
  <action>
按 D-05 决策更新 migrate-data 函数：

1. **修改前置检查 2 的容器未运行处理逻辑**（L356-369 区域）:

   替换现有的交互式跳过逻辑为：

   ```bash
   # 前置检查 2: Docker postgres-dev 容器是否仍存在
   log_info "前置检查: Docker postgres-dev 容器状态"
   local docker_container="noda-infra-postgres-dev"

   if ! docker ps -a --format "{{.Names}}" | grep -q "$docker_container"; then
     # 容器定义已被移除（Phase 27 清理后 dev.yml 不再定义 postgres-dev）
     log_warn "postgres-dev 容器已不存在（Phase 27 清理后已移除）"
     log_info "开发数据应已在本地 PostgreSQL 中，无需从 Docker 迁移"
     log_info "如需清理残留 Docker volume: docker volume rm noda-infra_postgres_dev_data"
     log_success "跳过迁移（容器已被移除）"
     return 0
   fi

   if ! docker ps --format "{{.Names}}" | grep -q "$docker_container"; then
     # 容器存在但未运行（可能是旧部署残留）
     log_warn "Docker 容器 ${docker_container} 存在但未运行"
     log_info "尝试启动容器以完成迁移..."
     docker compose -f docker/docker-compose.yml up -d postgres-dev 2>/dev/null || {
       log_warn "无法启动容器，开发数据可能已丢失"
       log_info "建议使用本地 PostgreSQL 重新创建开发数据: bash $0 init-db"
       return 0
     }
     # 等待容器健康
     sleep 5
   fi
   log_success "Docker 容器 ${docker_container} 可用"
   ```

2. **更新 usage() 中的 migrate-data 描述**（L69）:
   - 旧值: `migrate-data  从 Docker postgres-dev 容器迁移数据到本地 PostgreSQL`
   - 新值: `migrate-data  [已废弃] 从 Docker postgres-dev 迁移数据（容器已在 Phase 27 移除）`

关键设计决策：
- 使用 `docker ps -a` 检查所有容器（包括停止的），而非仅 `docker ps`
- 容器完全不存在时直接返回 0（不需要交互）
- 容器存在但未运行时尝试启动（兼容旧部署残留场景）
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && grep "Phase 27" scripts/setup-postgres-local.sh && echo "---" && grep "已废弃" scripts/setup-postgres-local.sh && echo "---" && bash -n scripts/setup-postgres-local.sh && echo "PASS: syntax OK"</automated>
    验证预期：
    - Phase 27 提及存在（废弃提示）
    - 已废弃标记存在
    - 语法检查通过
  </verify>
  <acceptance_criteria>
    - grep "Phase 27" scripts/setup-postgres-local.sh 有匹配（废弃提示引用清理阶段）
    - grep "已废弃" scripts/setup-postgres-local.sh 有匹配（usage 中标记废弃）
    - bash -n scripts/setup-postgres-local.sh 返回 0（语法正确）
    - grep "docker volume rm" scripts/setup-postgres-local.sh 有匹配（提示用户清理 volume）
    - grep "return 0" scripts/setup-postgres-local.sh 中 migrate-data 函数内有 return 0（容器不存在时正常退出）
  </acceptance_criteria>
  <done>migrate-data 子命令在 postgres-dev 容器不存在时输出友好提示并正常退出；usage 中标记为已废弃；脚本语法正确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 部署脚本 → Docker daemon | deploy-infrastructure-prod.sh 以 jenkins/docker 组用户身份执行 docker compose 命令 |
| 部署脚本 → 生产服务 | COMPOSE_FILES 变更影响生产部署行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | S (Spoofing) | COMPOSE_FILES 变更 | mitigate | 移除 dev.yml 引用后生产部署仅使用 base+prod，减少非预期覆盖风险；验证 docker compose config 无错误 |
| T-27-04 | D (Denial of Service) | EXPECTED_CONTAINERS 缩减 | accept | 移除 dev 容器后健康检查循环更短，不影响生产服务可用性 |
</threat_model>

<verification>
1. deploy-infrastructure-prod.sh 中 grep "postgres-dev" 匹配数为 0
2. COMPOSE_FILES 仅包含 `-f docker/docker-compose.yml -f docker/docker-compose.prod.yml`
3. setup-postgres-local.sh 语法检查通过（bash -n）
4. docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config 无错误（生产部署配置有效）
</verification>

<success_criteria>
1. 部署脚本中无 postgres-dev/keycloak-dev 引用
2. COMPOSE_FILES 改为双文件模式（base + prod）
3. migrate-data 子命令在容器不存在时优雅退出
4. 两个脚本语法检查均通过
</success_criteria>

<output>
After completion, create `.planning/phases/27-docker-compose/27-02-SUMMARY.md`
</output>
