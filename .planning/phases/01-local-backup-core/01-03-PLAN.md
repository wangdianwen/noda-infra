---
phase: 01-local-backup-core
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [scripts/backup/lib/verify.sh, scripts/backup/backup-postgres.sh]
autonomous: true
requirements: [BACKUP-05, VERIFY-01]
must_haves:
  truths:
    - "每个备份文件生成后立即使用 pg_restore --list 验证可读性"
    - "每个备份文件生成 SHA-256 校验和并记录到元数据文件"
    - "主脚本可以执行完整的备份流程（健康检查 → 备份 → 验证 → 清理）"
    - "主脚本支持 --list-databases、--dry-run、--test 参数"
    - "主脚本使用 PID 文件锁定防止并发执行"
    - "--test 模式可以创建测试数据库并验证完整备份和恢复流程（D-43）"
    - "验证失败时返回退出码 5（MONITOR-05）"
  artifacts:
    - path: "scripts/backup/lib/verify.sh"
      provides: "备份验证函数（pg_restore --list、SHA-256 校验和）"
      min_lines: 80
    - path: "scripts/backup/backup-postgres.sh"
      provides: "主脚本（参数解析、流程编排、锁定机制、测试模式）"
      min_lines: 250
    - path: "scripts/backup/templates/RESTORE.md"
      provides: "恢复文档模板"
      min_lines: 100
  key_links:
    - from: "scripts/backup/lib/verify.sh"
      to: "备份文件"
      via: "pg_restore --list 验证"
      pattern: "pg_restore.*--list"
    - from: "scripts/backup/lib/verify.sh"
      to: "备份文件"
      via: "sha256sum 计算校验和"
      pattern: "sha256sum"
    - from: "scripts/backup/backup-postgres.sh"
      to: "所有库文件"
      via: "source 命令加载"
      pattern: "source.*lib/\\.sh"
    - from: "scripts/backup/backup-postgres.sh"
      to: "PID 文件"
      via: "锁定机制防止并发"
      pattern: "\\$PID_FILE"
    - from: "scripts/backup/backup-postgres.sh"
      to: "测试脚本"
      via: "--test 模式调用测试脚本（D-43）"
      pattern: "test_restore\\.sh"
---

<objective>
实现备份验证功能和主脚本集成，提供完整的备份流程（健康检查 → 备份 → 验证 → 清理）和命令行参数支持，完整实现 D-43 测试模式。

目的：确保每个备份文件在创建后立即验证可读性和完整性，主脚本提供友好的命令行接口和防止并发执行的锁定机制，测试模式可以验证完整的备份和恢复流程。
输出：验证库文件、主脚本文件、恢复文档模板
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-local-backup-core/01-CONTEXT.md
@.planning/phases/01-local-backup-core/01-RESEARCH.md
@.planning/phases/01-local-backup-core/01-01-PLAN.md
@.planning/phases/01-local-backup-core/01-02-PLAN.md
@.planning/phases/01-local-backup-core/01-00-PLAN.md
@scripts/verify/quick-verify.sh
@services/postgres/init/01-create-databases.sql

## 关键决策引用

**从 CONTEXT.md 继承的锁定决策：**
- D-06: 每次备份后立即验证（`pg_restore --list` + SHA-256 校验和）
- D-07: 遇到错误立即失败，不继续备份其他数据库
- D-08: 严格遵循 MONITOR-05 错误退出码：0=成功、1=连接失败、2=备份失败、3=上传失败、4=清理失败、5=验证失败
- D-10: 备份目录结构按日期分层（如 2026/04/06/）
- D-16: 备份失败时清理所有已创建的备份文件
- D-17: 实现本地清理策略，按天数保留旧备份（通过环境变量配置天数）
- D-18: 脚本名称：`backup-postgres.sh`
- D-19: 使用 Bash 编写，主脚本 + 库文件结构
- D-20: 默认无参数直接备份所有数据库
- D-21: 提供 `--list-databases` 参数列出所有可备份的数据库
- D-22: 提供 `--dry-run` 模式模拟备份执行
- D-23: 提供锁定机制防止并发执行（使用 PID 文件）
- D-24: 自动清理过期的锁定文件
- D-25: 显示详细进度信息（当前数据库、完成百分比、预计剩余时间）
- D-29: 生成 JSON 格式元数据文件记录每次备份
- D-30: 记录备份历史（JSON 格式）到备份目录
- D-37: 库文件按功能划分（db.sh、log.sh、util.sh 等）
- D-38: 脚本中包含详细注释和文档头
- D-43: 提供 `--test` 模式，创建测试数据库并验证完整备份和恢复流程
- D-45: 在备份目录创建详细的恢复文档（RESTORE.md）
- D-46: 提供详细的错误消息和解决建议

**从 RESEARCH.md 继承的技术标准：**
- 标准技术栈：PostgreSQL 17.9、pg_restore --list、sha256sum、Bash 4.0+
- 架构模式：主脚本编排、库文件功能模块化
- 退出码：EXIT_SUCCESS=0, EXIT_CONNECTION_FAILED=1, EXIT_BACKUP_FAILED=2, EXIT_VERIFICATION_FAILED=5

**现有代码模式：**
- quick-verify.sh 使用 `docker exec` 在容器内执行命令
- 输出格式使用符号（✅、❌、⚠️）提高可读性
- 错误处理使用 `set -euo pipefail`

**从 Wave 0 继承：**
- 测试脚本已就绪（test_restore.sh 可用于 --test 模式）
- 测试数据库创建脚本已就绪（create_test_db.sh）
</context>

<tasks>

<task type="auto">
  <name>任务 1：创建验证库文件（lib/verify.sh）</name>
  <files>scripts/backup/lib/verify.sh</files>
  <read_first>
    - .planning/phases/01-local-backup-core/01-RESEARCH.md（了解 pg_restore --list 使用方法）
    - .planning/phases/01-local-backup-core/01-02-PLAN.md（了解备份文件格式和位置）
    - scripts/backup/lib/util.sh（了解校验和函数接口）
  </read_first>
  <action>
创建 `scripts/backup/lib/verify.sh` 验证库文件，实现以下功能：

**函数清单：**
1. `verify_backup_readable(backup_file)` - 使用 pg_restore --list 验证备份可读性
2. `verify_backup_checksum(backup_file, expected_checksum)` - 验证备份文件校验和
3. `verify_backup(backup_file, metadata_file)` - 综合验证函数（可读性 + 校验和）
4. `generate_metadata(backup_file, db_name, timestamp)` - 生成备份元数据（JSON 格式）
5. `verify_all_backups(backup_dir, metadata_file)` - 验证所有备份文件

**实现细节：**

**verify_backup_readable():**
```bash
verify_backup_readable() {
  local backup_file=$1
  docker exec noda-infra-postgres-1 pg_restore --list "$backup_file" > /dev/null 2>&1
}
```

**verify_backup_checksum():**
```bash
verify_backup_checksum() {
  local backup_file=$1
  local expected_checksum=$2
  local actual_checksum=$(calculate_checksum "$backup_file")
  [ "$actual_checksum" = "$expected_checksum" ]
}
```

**generate_metadata():**
```bash
generate_metadata() {
  local backup_file=$1
  local db_name=$2
  local timestamp=$3
  local checksum=$(calculate_checksum "$backup_file")
  local file_size=$(stat -f%z "$backup_file" 2>/dev/null || stat -c%s "$backup_file" 2>/dev/null)

  cat > "$metadata_file" <<EOF
{
  "database": "$db_name",
  "timestamp": "$timestamp",
  "file": "$(basename "$backup_file")",
  "size": $file_size,
  "checksum": "$checksum",
  "created_at": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")",
  "format": "custom",
  "compression": "zlib"
}
EOF
}
```

**verify_all_backups():**
- 读取元数据文件
- 遍历所有备份条目
- 对每个备份调用 verify_backup()
- 任何验证失败返回退出码 5

**错误处理：**
- 定义退出码常量：
  ```bash
  EXIT_VERIFICATION_FAILED=5
  ```
- 验证失败时输出详细错误信息（D-46）

**文件头注释：**
```bash
#!/bin/bash
# ============================================
# Noda 数据库备份系统 - 验证库
# ============================================
# 功能：备份验证（pg_restore --list、SHA-256 校验和）
# 依赖：log.sh, util.sh
# ============================================
```

**依赖加载：**
```bash
source "$(dirname "${BASH_SOURCE[0]}")/log.sh"
source "$(dirname "${BASH_SOURCE[0]}")/util.sh"
```

**使用 set -euo pipefail** 确保错误传播
  </action>
  <verify>
    <automated>bash -n scripts/backup/lib/verify.sh && grep -q "verify_backup_readable\|verify_backup_checksum\|verify_backup\|generate_metadata\|verify_all_backups" scripts/backup/lib/verify.sh</automated>
  </verify>
  <done>
    - [ ] verify.sh 文件创建成功
    - [ ] 语法检查通过（bash -n）
    - [ ] 包含所有必需函数
    - [ ] verify_backup_readable 使用 pg_restore --list
    - [ ] verify_backup_checksum 使用 sha256sum
    - [ ] generate_metadata 生成有效 JSON
    - [ ] 验证失败返回退出码 5
    - [ ] 依赖 log.sh 和 util.sh
  </done>
</task>

<task type="auto">
  <name>任务 2：创建主脚本（backup-postgres.sh）完整实现 D-43 测试模式</name>
  <files>scripts/backup/backup-postgres.sh</files>
  <read_first>
    - scripts/verify/quick-verify.sh（了解现有脚本模式）
    - .planning/phases/01-local-backup-core/01-CONTEXT.md（了解 D-18 到 D-26、D-43 脚本功能决策）
    - .planning/phases/01-local-backup-core/01-01-PLAN.md（了解配置和健康检查接口）
    - .planning/phases/01-local-backup-core/01-02-PLAN.md（了解备份和日志接口）
    - .planning/phases/01-local-backup-core/01-00-PLAN.md（了解测试脚本接口）
    - scripts/backup/lib/verify.sh（了解验证接口）
  </read_first>
  <action>
创建 `scripts/backup/backup-postgres.sh` 主脚本文件，实现以下功能：

**脚本结构：**
```bash
#!/bin/bash
set -euo pipefail

# ============================================
# Noda 数据库备份系统 - 主脚本
# ============================================
# 功能：完整的备份流程（健康检查 → 备份 → 验证 → 清理）
# 用法：./backup-postgres.sh [选项]
# 选项：
#   --list-databases    列出所有可备份的数据库
#   --dry-run          模拟备份执行（不实际备份）
#   --test             测试模式（创建测试数据库并验证，D-43）
#   --help             显示帮助信息
# ============================================

# 脚本目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 加载库文件
source "$SCRIPT_DIR/lib/config.sh"
source "$SCRIPT_DIR/lib/health.sh"
source "$SCRIPT_DIR/lib/log.sh"
source "$SCRIPT_DIR/lib/util.sh"
source "$SCRIPT_DIR/lib/db.sh"
source "$SCRIPT_DIR/lib/verify.sh"

# 全局变量
PID_FILE="/tmp/backup-postgres.pid"
LOCK_TIMEOUT=3600  # 1小时
DRY_RUN=false
TEST_MODE=false

# 命令行参数解析
parse_arguments() {
  while [[ $# -gt 0 ]]; do
    case $1 in
      --list-databases)
        list_databases
        exit 0
        ;;
      --dry-run)
        DRY_RUN=true
        shift
        ;;
      --test)
        TEST_MODE=true
        shift
        ;;
      --help)
        show_help
        exit 0
        ;;
      *)
        log_error "未知参数: $1"
        show_help
        exit 1
        ;;
    esac
  done
}

# 显示帮助信息
show_help() {
  cat <<EOF
用法: $(basename "$0") [选项]

选项:
  --list-databases    列出所有可备份的数据库
  --dry-run          模拟备份执行（不实际备份）
  --test             测试模式（创建测试数据库并验证完整流程）
  --help             显示帮助信息

示例:
  $(basename "$0")                 # 备份所有数据库
  $(basename "$0") --list-databases  # 列出数据库
  $(basename "$0") --dry-run       # 模拟备份
  $(basename "$0") --test          # 测试模式（D-43）
EOF
}

# 列出数据库
list_databases() {
  log_info "可备份的数据库列表："
  local databases=$(discover_databases)
  local count=0
  for db in $databases; do
    echo "  • $db"
    ((count++))
  done
  log_info "总计: $count 个数据库"
}

# 获取锁
acquire_lock() {
  if [ -f "$PID_FILE" ]; then
    local pid=$(cat "$PID_FILE")
    local pid_age=$(($(date +%s) - $(stat -f%m "$PID_FILE" 2>/dev/null || stat -c%Y "$PID_FILE" 2>/dev/null)))
    if [ $pid_age -gt $LOCK_TIMEOUT ]; then
      log_warn "检测到过期的锁文件（PID: $pid，已锁定 ${pid_age} 秒），自动清理"
      release_lock
    else
      log_error "另一个备份实例正在运行（PID: $pid）"
      exit 1
    fi
  fi
  echo $$ > "$PID_FILE"
  log_info "获取锁成功（PID: $$）"
}

# 释放锁
release_lock() {
  if [ -f "$PID_FILE" ]; then
    rm -f "$PID_FILE"
    log_info "释放锁成功"
  fi
}

# 清理旧备份
cleanup_old_backups() {
  local backup_dir=$1
  local retention_days=$(get_retention_days)
  log_info "清理 $retention_days 天前的旧备份..."

  # 查找并删除旧备份
  find "$backup_dir" -type f -name "*.dump" -mtime +$retention_days -delete
  find "$backup_dir" -type f -name "globals_*.sql" -mtime +$retention_days -delete

  log_success "旧备份清理完成"
}

# 测试模式（D-43 完整实现）
run_test_mode() {
  log_info "=========================================="
  log_info "测试模式：完整备份和恢复流程验证"
  log_info "=========================================="

  local test_script="$SCRIPT_DIR/tests/test_restore.sh"

  # 检查测试脚本是否存在
  if [ ! -f "$test_script" ]; then
    log_error "测试脚本不存在: $test_script"
    log_error "请确保已运行 Wave 0 计划创建测试基础设施"
    return 1
  fi

  # 执行测试脚本
  log_info "执行测试脚本: $test_script"
  if bash "$test_script"; then
    log_success "=========================================="
    log_success "测试模式完成！所有测试通过。"
    log_success "=========================================="
    return 0
  else
    log_error "=========================================="
    log_error "测试模式失败！请检查测试日志。"
    log_error "=========================================="
    return 1
  fi
}

# 主流程
main() {
  parse_arguments "$@"
  load_config
  validate_config

  log_info "=========================================="
  log_info "Noda 数据库备份系统"
  log_info "=========================================="

  acquire_lock

  # 测试模式（D-43）
  if [ "$TEST_MODE" = true ]; then
    run_test_mode
    local test_result=$?
    release_lock
    exit $test_result
  fi

  # 健康检查
  log_info "步骤 1/5: 健康检查"
  check_prerequisites
  log_success "健康检查通过"

  # 备份
  log_info "步骤 2/5: 备份数据库"
  local timestamp=$(get_timestamp)
  local date_path=$(get_date_path)
  local backup_dir="$(get_backup_dir)/$date_path"
  mkdir -p "$backup_dir"

  if [ "$DRY_RUN" = true ]; then
    log_warn "模拟模式：跳过实际备份"
    log_info "备份目录: $backup_dir"
    log_info "时间戳: $timestamp"
    release_lock
    exit 0
  fi

  backup_all_databases "$backup_dir" "$timestamp"
  log_success "数据库备份完成"

  # 验证
  log_info "步骤 3/5: 验证备份"
  local metadata_file="$backup_dir/metadata_$timestamp.json"
  verify_all_backups "$backup_dir" "$metadata_file"
  log_success "备份验证完成"

  # 清理旧备份
  log_info "步骤 4/5: 清理旧备份"
  cleanup_old_backups "$backup_dir"
  log_success "旧备份清理完成"

  # 完成
  log_info "步骤 5/5: 备份完成"
  log_success "=========================================="
  log_success "备份成功完成！"
  log_success "备份目录: $backup_dir"
  log_success "元数据文件: $metadata_file"
  log_success "=========================================="

  release_lock
  exit 0
}

# 捕获退出信号，确保释放锁
trap release_lock EXIT INT TERM

# 执行主流程
main "$@"
```

**关键实现要点：**
- 使用 `set -euo pipefail` 确保错误传播
- 使用 `trap` 捕获退出信号，确保锁被释放
- 支持 --list-databases、--dry-run、--test 参数
- **完整实现 D-43 测试模式**：调用 test_restore.sh 脚本验证完整备份和恢复流程
- 使用 PID 文件锁定机制
- 自动清理过期的锁文件（D-24）
- 显示详细进度信息（D-25）
- 生成 JSON 格式元数据文件（D-29、D-30）
- 清理旧备份（D-17）

**测试模式实现细节（D-43）：**
- 创建 `run_test_mode()` 函数
- 检查测试脚本是否存在（test_restore.sh）
- 调用测试脚本执行完整的备份和恢复流程验证
- 测试失败时返回非零退出码
- 提供清晰的测试结果输出

**文件权限：**
创建后设置可执行权限：`chmod +x scripts/backup/backup-postgres.sh`
  </action>
  <verify>
    <automated>bash -n scripts/backup/backup-postgres.sh && grep -q "parse_arguments\|acquire_lock\|release_lock\|run_test_mode\|main" scripts/backup/backup-postgres.sh && test -x scripts/backup/backup-postgres.sh</automated>
  </verify>
  <done>
    - [ ] backup-postgres.sh 文件创建成功
    - [ ] 语法检查通过（bash -n）
    - [ ] 文件具有可执行权限
    - [ ] 包含所有必需函数
    - [ ] 支持 --list-databases 参数
    - [ ] 支持 --dry-run 参数
    - [ ] **完整实现 --test 参数（D-43）**
    - [ ] **run_test_mode() 函数调用 test_restore.sh**
    - [ ] 实现 PID 文件锁定机制
    - [ ] 自动清理过期的锁文件
    - [ ] 使用 trap 捕获退出信号
    - [ ] 清理旧备份功能正常
    - [ ] 生成 JSON 元数据文件
    - [ ] 所有库文件正确加载
  </done>
</task>

<task type="auto">
  <name>任务 3：创建恢复文档模板（templates/RESTORE.md）</name>
  <files>scripts/backup/templates/RESTORE.md</files>
  <read_first>
    - .planning/phases/01-local-backup-core/01-CONTEXT.md（了解 D-45 恢复文档决策）
    - .planning/phases/01-local-backup-core/01-RESEARCH.md（了解 pg_restore 使用方法）
  </read_first>
  <action>
创建 `scripts/backup/templates/RESTORE.md` 恢复文档模板文件：

**文档结构：**
```markdown
# 数据库恢复指南

## 备份文件位置

备份文件存储在以下目录：
- 容器内：`/var/lib/postgresql/backup/YYYY/MM/DD/`
- 宿主机：`/var/lib/docker/volumes/noda-infra_postgres_data/_data/backup/YYYY/MM/DD/`

## 备份文件命名

- 数据库备份：`{db_name}_{YYYYMMDD_HHmmss}.dump`
- 全局对象：`globals_{YYYYMMDD_HHmmSS}.sql`
- 元数据：`metadata_{YYYYMMDD_HHmmSS}.json`

## 恢复步骤

### 1. 查看可用备份

\`\`\`bash
# 列出最近的备份
ls -lth /var/lib/docker/volumes/noda-infra_postgres_data/_data/backup/2026/04/06/

# 查看元数据
cat /var/lib/docker/volumes/noda-infra_postgres_data/_data/backup/2026/04/06/metadata_20260406_143000.json | jq
\`\`\`

### 2. 恢复全局对象（可选）

\`\`\`bash
# 恢复角色和表空间定义
docker exec -i noda-infra-postgres-1 psql -U postgres -d postgres < \
  /var/lib/docker/volumes/noda-infra_postgres_data/_data/backup/2026/04/06/globals_20260406_143000.sql
\`\`\`

### 3. 恢复单个数据库

\`\`\`bash
# 恢复到原数据库（会覆盖现有数据）
docker exec noda-infra-postgres-1 pg_restore -U postgres -d keycloak_db \
  /var/lib/postgresql/backup/2026/04/06/keycloak_db_20260406_143000.dump

# 恢复到新数据库（用于测试）
docker exec noda-infra-postgres-1 psql -U postgres -d postgres -c "CREATE DATABASE keycloak_db_test;"
docker exec noda-infra-postgres-1 pg_restore -U postgres -d keycloak_db_test \
  /var/lib/postgresql/backup/2026/04/06/keycloak_db_20260406_143000.dump
\`\`\`

### 4. 验证恢复结果

\`\`\`bash
# 检查数据库是否恢复成功
docker exec noda-infra-postgres-1 psql -U postgres -d keycloak_db -c "\dt"

# 检查表数量
docker exec noda-infra-postgres-1 psql -U postgres -d keycloak_db -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public';"
\`\`\`

## 测试模式验证（D-43）

使用主脚本的测试模式验证完整备份和恢复流程：

\`\`\`bash
# 运行测试模式
bash scripts/backup/backup-postgres.sh --test

# 测试模式会自动：
# 1. 创建测试数据库
# 2. 执行备份
# 3. 恢复到新数据库
# 4. 验证数据完整性
# 5. 清理测试资源
\`\`\`

## 常见问题

### Q1: 恢复时提示数据库已存在？
A: 使用 `DROP DATABASE` 删除现有数据库，或恢复到新的数据库名称。

### Q2: 恢复时提示角色不存在？
A: 先恢复全局对象（globals_*.sql 文件）。

### Q3: 如何验证备份文件是否完整？
A: 使用 `pg_restore --list` 查看备份内容：
\`\`\`bash
docker exec noda-infra-postgres-1 pg_restore --list /var/lib/postgresql/backup/2026/04/06/keycloak_db_20260406_143000.dump
\`\`\`

### Q4: 如何使用测试模式验证备份系统？
A: 运行 `bash scripts/backup/backup-postgres.sh --test`，它会自动创建测试数据库并验证完整流程。

## 注意事项

- ⚠️ 恢复到生产数据库前，先恢复到测试数据库验证
- ⚠️ 恢复会覆盖目标数据库的所有数据，不可逆
- ⚠️ 确保磁盘空间充足（数据库大小 × 2）
- ⚠️ 恢复期间数据库可能不可用
- ✅ 使用 `--test` 模式可以在不影响生产的情况下验证备份系统

## 联系支持

如果遇到问题，请联系运维团队并提供以下信息：
- 备份文件路径
- 错误消息
- PostgreSQL 版本：17.9
```

**关键要点：**
- 提供完整的恢复步骤
- 包含命令示例
- **添加测试模式验证说明（D-43）**
- 说明常见问题和解决方案
- 强调恢复前的注意事项
- 提供联系支持的信息
  </action>
  <verify>
    <automated>test -f scripts/backup/templates/RESTORE.md && grep -q "恢复步骤\|pg_restore\|测试模式\|常见问题" scripts/backup/templates/RESTORE.md</automated>
  </verify>
  <done>
    - [ ] RESTORE.md 文件创建成功
    - [ ] 包含完整的恢复步骤
    - [ ] 包含命令示例
    - [ ] **包含测试模式验证说明（D-43）**
    - [ ] 包含常见问题和解决方案
    - [ ] 包含注意事项
    - [ ] 格式清晰易读
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 → 主脚本 | 命令行参数输入和权限检查 |
| 主脚本 → 库文件 | 函数调用和数据传递 |
| 验证函数 → 备份文件 | 读取和验证备份文件 |
| 主脚本 → 测试脚本 | --test 模式调用测试脚本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | 备份文件篡改 | mitigate | SHA-256 校验和验证备份完整性，pg_restore --list 验证可读性 |
| T-03-02 | Denial of Service | 锁文件未释放 | mitigate | 使用 trap 捕获退出信号，自动清理过期的锁文件（超时 1 小时） |
| T-03-03 | Information Disclosure | 元数据文件泄露 | mitigate | 元数据文件包含校验和但不包含敏感数据，权限 600 |
| T-03-04 | Spoofing | 测试模式欺骗 | accept | 测试模式仅用于开发，测试数据库独立于生产数据库 |
| T-03-05 | Elevation of Privilege | 脚本权限 | accept | 脚本假设运行在有权限访问 Docker 和 PostgreSQL 的环境 |

## Security Notes

- **ASVS V5（输入验证）**：命令行参数验证，不接受任意数据库名或路径
- **ASVS V6（加密）**：元数据文件权限 600，校验和验证完整性
- **错误处理**：验证失败返回退出码 5（MONITOR-05 标准）
- **测试隔离**：测试模式使用独立测试数据库，不影响生产环境（D-43）
</threat_model>

<verification>
## 阶段验证检查

### 功能验证
1. **验证功能验证**：
   - [ ] verify_backup_readable 使用 pg_restore --list 验证
   - [ ] verify_backup_checksum 使用 sha256sum 验证
   - [ ] generate_metadata 生成有效 JSON
   - [ ] 验证失败返回退出码 5

2. **主脚本验证**：
   - [ ] --list-databases 列出所有数据库
   - [ ] --dry-run 模拟备份不实际执行
   - [ ] **--test 模式调用 test_restore.sh 并验证完整流程（D-43）**
   - [ ] PID 文件锁定机制正常工作
   - [ ] 自动清理过期的锁文件
   - [ ] trap 捕获退出信号释放锁
   - [ ] 清理旧备份功能正常

3. **文档验证**：
   - [ ] RESTORE.md 提供完整的恢复步骤
   - [ ] **包含测试模式验证说明（D-43）**
   - [ ] 包含命令示例
   - [ ] 包含常见问题和解决方案

### 集成验证
- [ ] 主脚本正确加载所有库文件
- [ ] 完整备份流程执行成功
- [ ] **测试模式执行成功（D-43）**
- [ ] 错误处理正确（返回标准退出码）
- [ ] 日志输出格式正确

### 安全验证
- [ ] 元数据文件权限为 600
- [ ] 验证失败返回退出码 5
- [ ] 锁文件机制防止并发执行
- [ ] **测试模式使用独立数据库，不影响生产（D-43）**
</verification>

<success_criteria>
## 完成标准

### 必须达成
1. ✅ 验证库文件（verify.sh）提供备份验证功能
2. ✅ 主脚本（backup-postgres.sh）实现完整的备份流程
3. ✅ 主脚本支持 --list-databases、--dry-run、--test 参数
4. ✅ **完整实现 D-43 测试模式（调用 test_restore.sh）**
5. ✅ 主脚本使用 PID 文件锁定防止并发执行
6. ✅ 主脚本自动清理过期的锁文件
7. ✅ 备份后立即验证（pg_restore --list + SHA-256）
8. ✅ 验证失败返回退出码 5
9. ✅ 生成 JSON 格式元数据文件
10. ✅ 清理旧备份功能正常
11. ✅ 恢复文档（RESTORE.md）提供完整的恢复指南
12. ✅ **恢复文档包含测试模式验证说明（D-43）**

### 可选达成
- 🤖 元数据文件包含更多字段（压缩级别、表数量等）
- 🤖 测试模式支持更多测试场景
</success_criteria>

<output>
完成此计划后，创建 `.planning/phases/01-local-backup-core/01-03-SUMMARY.md` 摘要文件，记录：
- 实际创建的文件和函数
- 遇到的问题和解决方案
- 偏离计划的地方和原因
- 验证结果和测试覆盖
- **D-43 测试模式的完整实现**
</output>
