---
phase: 61-backup-network-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BACKUP-01, BACKUP-02, BACKUP-03, BACKUP-04]

must_haves:
  truths:
    - "r4s noda-ops 容器中 pg_dump 备份正常执行，生成 .dump 文件"
    - "B2 上传成功，备份文件在 B2 bucket 中可见"
    - "Doppler 密钥备份正常执行，age 加密后上传 B2"
    - "周验证测试正常执行（dry-run 或完整模式）"
  artifacts:
    - path: "r4s:/tmp/postgres_backups/"
      provides: "pg_dump 备份文件"
    - path: "r4s:/var/log/noda-backup/backup.log"
      provides: "备份执行日志"
    - path: "r4s:/var/log/noda-backup/doppler-backup.log"
      provides: "Doppler 备份日志"
    - path: "B2:noda-backups/backups/postgres/"
      provides: "云端备份文件"
  key_links:
    - from: "noda-ops 容器"
      to: "noda-infra-postgres-prod:5432"
      via: "pg_dump 连接"
      pattern: "pg_dump.*postgres"
    - from: "noda-ops 容器"
      to: "B2 cloud storage"
      via: "rclone copy"
      pattern: "rclone copy.*b2remote"
    - from: "noda-ops 容器"
      to: "Doppler API"
      via: "doppler secrets download"
      pattern: "doppler.*download"
---

<objective>
验证 r4s noda-ops 容器中 4 个备份 cronjob 功能正常。

Purpose: 确认 r4s 上所有备份功能可用，数据安全有保障，才能清理 Mac 旧服务。
Output: 4 个 cronjob 验证通过 + 备份文件存在于 r4s 本地和 B2 云端。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-backup-network-migration/61-CONTEXT.md
@.planning/phases/61-backup-network-migration/61-RESEARCH.md
@.planning/phases/60-ci-cd/60-CONTEXT.md

<interfaces>
<!-- noda-ops 容器内脚本路径和接口 -->

Crontab 定义的 5 个 cronjob（deploy/crontab）：
- 03:00 /app/backup/backup-postgres.sh → 主备份（pg_dump + B2 上传 + 验证）
- 03:00 Sun /app/backup/test-verify-weekly.sh → 周验证测试
- */6h /app/backup/lib/metrics.sh cleanup → 历史清理
- 04:00 find /tmp/postgres_backups -mtime +7 -delete → 旧文件清理
- 03:30 /app/backup/backup-doppler-secrets.sh → Doppler 密钥备份

备份脚本参数（deploy/crontab + scripts/backup/backup-postgres.sh）：
- backup-postgres.sh: 支持 --dry-run（模拟）, --list-databases, --test
- backup-doppler-secrets.sh: --project noda --config prd, 支持 --dry-run
- test-verify-weekly.sh: 支持 --databases "db1 db2"

Docker 容器名（docker/docker-compose.yml）：
- noda-ops 容器: container_name = noda-ops
- PostgreSQL 容器: container_name = noda-infra-postgres-prod
- Nginx 容器: container_name = noda-infra-nginx

rclone 配置（deploy/entrypoint-ops.sh）：
- 配置文件: /home/nodaops/.config/rclone/rclone.conf
- Remote 名: b2remote
- Bucket: $B2_BUCKET_NAME
- B2 上传路径: b2remote:noda-backups/backups/postgres/

备份文件目录：
- 本地: /tmp/postgres_backups/
- 日志: /var/log/noda-backup/

SSH 模式（Phase 60 D-12）：
- 命令格式: ssh root@r4s 'docker exec noda-ops <command>'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 验证 pg_dump 备份和 B2 上传</name>
  <files>无文件修改（纯验证操作）</files>
  <read_first>
  scripts/backup/backup-postgres.sh
  scripts/backup/lib/cloud.sh
  scripts/backup/lib/config.sh
  deploy/entrypoint-ops.sh
  </read_first>
  <action>
按照 D-05 的串行顺序，第一步验证 pg_dump 备份和 B2 上传。

**步骤 1.1 — 前置检查：**
- SSH 到 r4s 检查 noda-ops 容器运行状态: `ssh root@r4s 'docker ps --filter name=noda-ops --format "{{.Names}} {{.Status}}"'`
- 检查 noda-ops 健康状态: `ssh root@r4s 'docker inspect --format "{{.State.Health.Status}}" noda-ops'`
- 检查容器环境变量是否包含备份所需变量（POSTGRES_HOST, B2_ACCOUNT_ID, B2_APPLICATION_KEY, B2_BUCKET_NAME）: `ssh root@r4s 'docker exec noda-ops env | grep -E "POSTGRES_HOST|B2_"` — 预期输出包含这 4 个变量
- 检查 rclone 配置文件是否存在: `ssh root@r4s 'docker exec noda-ops cat /home/nodaops/.config/rclone/rclone.conf'` — 预期包含 [b2remote] section
- 检查 supervisord 中 cron 进程状态: `ssh root@r4s 'docker exec noda-ops supervisorctl status'` — 预期 cron RUNNING

**步骤 1.2 — Dry-run 验证（快速确认配置正确，per D-04）：**
- 执行: `ssh root@r4s 'docker exec noda-ops /app/backup/backup-postgres.sh --dry-run'`
- 预期输出包含 "模拟模式" 和 "健康检查通过"，退出码 0
- 如果 dry-run 失败，检查错误信息，重点关注：POSTGRES_HOST 连接失败、环境变量缺失、rclone 配置问题

**步骤 1.3 — 列出可备份数据库：**
- 执行: `ssh root@r4s 'docker exec noda-ops /app/backup/backup-postgres.sh --list-databases'`
- 预期输出包含至少 noda_prod、keycloak_db、noda_preprod 数据库

**步骤 1.4 — 完整备份执行（per D-06 SSH docker exec 模式）：**
- 执行: `ssh root@r4s 'docker exec noda-ops /app/backup/backup-postgres.sh'`
- 该脚本执行 7 步流程：健康检查 → 备份 → 验证 → B2 上传 → 清理旧备份 → 清理历史 → 记录指标
- 预期退出码 0
- 超时设置：备份可能耗时较长（PG 数据库 + B2 上传），SSH 命令超时建议 600 秒

**步骤 1.5 — 验证备份结果：**
- 检查本地备份文件: `ssh root@r4s 'docker exec noda-ops ls -la /tmp/postgres_backups/'` — 预期存在包含今天日期的子目录和 .dump 文件
- 检查备份日志: `ssh root@r4s 'docker exec noda-ops tail -30 /var/log/noda-backup/backup.log'` — 预期包含 "备份成功完成"
- 检查 B2 云端文件: `ssh root@r4s 'docker exec noda-ops rclone ls b2remote:noda-backups/backups/postgres/ --config /home/nodaops/.config/rclone/rclone.conf'` — 预期列出刚上传的备份文件（per D-08）

**失败回退：**
- 如果 pg_dump 连接失败：检查 r4s 上 noda-infra-postgres-prod 容器是否运行，检查 Docker 内部网络 noda-network
- 如果 B2 上传失败：检查 rclone 配置中 B2_ACCOUNT_ID 和 B2_APPLICATION_KEY 是否正确，检查 r4s 网络是否能访问 B2 API
- 如果脚本本身报错：查看 /var/log/noda-backup/backup.log 获取详细错误信息
  </action>
  <verify>
    <automated>ssh root@r4s 'docker exec noda-ops sh -c "ls /tmp/postgres_backups/*/`date +%Y`*/*.dump 2>/dev/null | wc -l"' — 预期输出 >= 1（至少 1 个 .dump 文件存在）</automated>
  </verify>
  <done>
  1. pg_dump 备份在 r4s noda-ops 容器内成功执行，退出码 0
  2. /tmp/postgres_backups/ 目录中存在今天日期的备份子目录和 .dump 文件
  3. B2 云端 noda-backups/backups/postgres/ 路径下可见刚上传的备份文件
  4. backup.log 包含 "备份成功完成" 消息
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 验证 Doppler 密钥备份和周验证测试</name>
  <files>无文件修改（纯验证操作）</files>
  <read_first>
  scripts/backup/backup-doppler-secrets.sh
  scripts/backup/test-verify-weekly.sh
  scripts/backup/lib/verify.sh
  </read_first>
  <action>
按照 D-05 串行顺序，在 pg_dump + B2 验证通过后，继续验证 Doppler 密钥备份和周验证测试。

**步骤 2.1 — Doppler 密钥备份验证（per BACKUP-03）：**

前置检查：
- 检查 DOPPLER_TOKEN 环境变量: `ssh root@r4s 'docker exec noda-ops env | grep DOPPLER_TOKEN'` — 预期非空
- 检查 doppler CLI 可用: `ssh root@r4s 'docker exec noda-ops doppler --version'` — 预期输出版本号
- 检查 age 加密工具可用: `ssh root@r4s 'docker exec noda-ops age --version'` — 预期输出版本号

Dry-run 验证：
- 执行: `ssh root@r4s 'docker exec noda-ops /app/backup/backup-doppler-secrets.sh --dry-run'`
- 预期输出包含密钥下载和加密流程描述，退出码 0

完整执行：
- 执行: `ssh root@r4s 'docker exec noda-ops /app/backup/backup-doppler-secrets.sh --project noda --config prd'`
- 该脚本流程：下载 Doppler 密钥 → age 公钥加密 → 上传 B2
- 预期退出码 0
- 超时设置：300 秒

验证结果：
- 检查日志: `ssh root@r4s 'docker exec noda-ops tail -20 /var/log/noda-backup/doppler-backup.log'` — 预期包含备份成功消息
- 检查 B2 上传: `ssh root@r4s 'docker exec noda-ops rclone ls b2remote:noda-backups/secrets/ --config /home/nodaops/.config/rclone/rclone.conf'` — 预期列出加密的密钥文件

**步骤 2.2 — 周验证测试（per BACKUP-04）：**

前置检查：
- 检查 test-verify-weekly.sh 脚本存在: `ssh root@r4s 'docker exec noda-ops ls -la /app/backup/test-verify-weekly.sh'`

执行周验证（使用 --databases 限制测试范围，减少耗时）：
- 执行: `ssh root@r4s 'docker exec noda-ops /app/backup/test-verify-weekly.sh --databases "keycloak_db"'`
- 该脚本流程：从 B2 下载最新备份 → 恢复到临时数据库 → 验证数据完整性
- 预期退出码 0
- 超时设置：600 秒（下载 + 恢复 + 验证耗时较长）

验证结果：
- 检查日志: `ssh root@r4s 'docker exec noda-ops tail -20 /var/log/noda-backup/test.log'` — 预期包含验证通过消息

**失败回退：**
- Doppler token 无效：检查 docker-compose.yml 中 DOPPLER_TOKEN 环境变量是否正确注入
- age 加密失败：检查 AGE_PUBLIC_KEY 环境变量是否配置
- 周验证恢复失败：可能是 pg_restore 与 pg_dump 版本不匹配，检查 postgresql17-client 版本
- B2 下载失败：检查 r4s 网络连接到 B2 的可达性
  </action>
  <verify>
    <automated>ssh root@r4s 'docker exec noda-ops sh -c "tail -5 /var/log/noda-backup/doppler-backup.log | grep -c -i success"' — 预期输出 >= 1</automated>
  </verify>
  <done>
  1. Doppler 密钥备份成功执行，退出码 0
  2. 加密后的密钥文件已上传到 B2 的 secrets/ 路径
  3. doppler-backup.log 包含成功消息
  4. 周验证测试成功执行（至少 keycloak_db 验证通过）
  5. test.log 包含验证通过消息
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH 连接 | Mac Jenkins/Claude 通过 SSH 连接 r4s，需 SSH 密钥认证 |
| noda-ops → PostgreSQL | 容器间 Docker 内部网络，无 TLS |
| noda-ops → B2 Cloud | 公网 HTTPS，凭证在环境变量中 |
| noda-ops → Doppler API | 公网 HTTPS，token 在环境变量中 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Information Disclosure | B2 凭证泄露 | mitigate | 凭证通过 docker-compose 环境变量注入，不落盘在代码中 |
| T-61-02 | Information Disclosure | Doppler token 泄露 | mitigate | token 在容器环境变量中，容器 read-only + tmpfs |
| T-61-03 | Information Disclosure | 备份数据泄露 | mitigate | B2 bucket 访问控制 + Doppler 密钥 age 公钥加密 |
| T-61-SC | Tampering | SSH 远程命令执行 | mitigate | SSH 密钥认证 + 非交互式部署密钥 |
</threat_model>

<verification>
1. `ssh root@r4s 'docker exec noda-ops ls /tmp/postgres_backups/'` — 存在备份子目录
2. `ssh root@r4s 'docker exec noda-ops rclone lsd b2remote:noda-backups/ --config /home/nodaops/.config/rclone/rclone.conf'` — B2 bucket 可列目录
3. `ssh root@r4s 'docker exec noda-ops tail -5 /var/log/noda-backup/doppler-backup.log'` — Doppler 备份成功
4. `ssh root@r4s 'docker exec noda-ops tail -5 /var/log/noda-backup/test.log'` — 周验证通过
</verification>

<success_criteria>
1. pg_dump 备份在 r4s noda-ops 中成功执行，.dump 文件存在于 /tmp/postgres_backups/（BACKUP-01）
2. B2 上传成功，rclone ls 能列出云端备份文件（BACKUP-02）
3. Doppler 密钥备份成功执行，加密文件上传到 B2（BACKUP-03）
4. 周验证测试成功执行，keycloak_db 恢复验证通过（BACKUP-04）
</success_criteria>

<output>
Create `.planning/phases/61-backup-network-migration/61-01-SUMMARY.md` when done
</output>
