---
phase: 33-audit-logging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/jenkins/init.groovy.d/05-audit-trail.groovy
  - config/logrotate/jenkins-audit-trail
autonomous: true
requirements:
  - AUDIT-03

must_haves:
  truths:
    - "Jenkins Audit Trail 插件安装并记录 Pipeline 触发事件"
    - "Audit Trail 日志存储在 JENKINS_HOME/audit-trail/ 目录"
    - "日志保留 14 天，单文件最大 50MB（per D-01）"
    - "logrotate 管理 Audit Trail 日志轮转"
  artifacts:
    - path: "scripts/jenkins/init.groovy.d/05-audit-trail.groovy"
      provides: "Audit Trail 插件安装 + File Logger 配置 Groovy 脚本"
      contains: "audit-trail"
    - path: "config/logrotate/jenkins-audit-trail"
      provides: "Jenkins Audit Trail 日志轮转配置"
      contains: "rotate 14"
  key_links:
    - from: "scripts/jenkins/init.groovy.d/05-audit-trail.groovy"
      to: "Jenkins 插件管理器"
      via: "pm.getPlugin / uc.getPlugin / plugin.deploy"
      pattern: "getPlugin.*audit-trail"
    - from: "config/logrotate/jenkins-audit-trail"
      to: "/etc/logrotate.d/"
      via: "安装脚本复制到系统 logrotate 目录"
      pattern: "logrotate"
---

<objective>
创建 Jenkins Audit Trail 插件安装 Groovy 脚本和对应的 logrotate 轮转配置，实现 Jenkins Pipeline 触发事件的审计记录。

Purpose: 实现 AUDIT-03（Jenkins Audit Trail 记录 Pipeline 触发事件），仅记录最小粒度事件（per D-02）。
Output: 05-audit-trail.groovy + config/logrotate/jenkins-audit-trail
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-audit-logging/33-CONTEXT.md
@.planning/phases/33-audit-logging/33-RESEARCH.md

<interfaces>
<!-- 现有插件安装 Groovy 脚本接口 -->

From scripts/jenkins/init.groovy.d/02-plugins.groovy:
```groovy
import jenkins.model.*
import hudson.PluginWrapper

def instance = Jenkins.getInstance()
def pm = instance.getPluginManager()
def uc = instance.getUpdateCenter()

uc.updateAllSites()

def plugins = [...]  // 插件列表

plugins.each { pluginId ->
    if (!pm.getPlugin(pluginId)) {
        println "Installing plugin: ${pluginId}"
        def plugin = uc.getPlugin(pluginId)
        if (plugin) {
            def result = plugin.deploy(true)
            allInstalled = false
        } else {
            println "WARNING: Plugin ${pluginId} not found in Update Center"
        }
    } else {
        println "Plugin already installed: ${pluginId}"
    }
}
instance.save()
```

From scripts/setup-jenkins.sh 步骤 7:
```bash
# 步骤 7/10: 写入 init.groovy.d 脚本
if [ -d "$GROOVY_SRC_DIR" ] && ls "$GROOVY_SRC_DIR"/*.groovy > /dev/null 2>&1; then
    sudo mkdir -p "$JENKINS_HOME_LINUX/init.groovy.d"
    sudo cp "$GROOVY_SRC_DIR"/*.groovy "$JENKINS_HOME_LINUX/init.groovy.d/"
    sudo chown -R jenkins:jenkins "$JENKINS_HOME_LINUX/init.groovy.d"
fi
```

关键信息：
- JENKINS_HOME_LINUX = "/var/lib/jenkins"
- Groovy 脚本按字母顺序执行：01-security → 02-plugins → ... → 05-audit-trail
- setup-jenkins.sh 步骤 7 的 cp *.groovy 会自动复制新的 05-audit-trail.groovy
- Jenkins 首次安装时 init.groovy.d 脚本执行后会被清理（步骤 10）

Audit Trail 插件配置说明（来自 RESEARCH.md）：
- 插件 ID: audit-trail
- 版本: 436.vc0d1e79fc5a_3
- File Logger 配置：插件安装后需通过 UI 或 Groovy Console 配置
- 由于 Audit Trail 插件的 File Logger Java API 不稳定，推荐安装后通过 UI 手动配置 File Logger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Audit Trail 插件安装 Groovy 脚本和 logrotate 配置</name>
  <files>scripts/jenkins/init.groovy.d/05-audit-trail.groovy, config/logrotate/jenkins-audit-trail</files>
  <read_first>
    scripts/jenkins/init.groovy.d/02-plugins.groovy
  </read_first>
  <behavior>
    - Groovy 脚本: 安装 audit-trail 插件（幂等），如果 Jenkins 已安装则跳过
    - logrotate 配置: daily rotate 14, maxsize 50M, 0640 jenkins:jenkins
  </behavior>
  <action>
**1. 创建 `scripts/jenkins/init.groovy.d/05-audit-trail.groovy`**

严格遵循 `02-plugins.groovy` 的模式（import 语句、instance 获取、Update Center 初始化、幂等安装检查）。

```groovy
// Jenkins Audit Trail 插件安装
// 功能：安装 Audit Trail 插件，记录 Pipeline 触发事件（per AUDIT-03, D-02）
//
// 执行时机：02-plugins.groovy 之后执行（字母顺序 05 > 02）
// 幂等性：已安装插件跳过，不重复安装
// 配置：File Logger 需要在 Jenkins 启动后通过 UI 手动配置
//       Jenkins 管理 → Audit Trail → 添加 File Logger → 路径: /var/lib/jenkins/audit-trail/audit-trail.log
import jenkins.model.*
import hudson.PluginWrapper

def instance = Jenkins.getInstance()
def pm = instance.getPluginManager()
def uc = instance.getUpdateCenter()

// 初始化 Update Center
uc.updateAllSites()

// Audit Trail 插件（per AUDIT-03, D-02 仅记录 Pipeline 触发事件）
def pluginId = 'audit-trail'

if (!pm.getPlugin(pluginId)) {
    println "Installing plugin: ${pluginId}"
    def plugin = uc.getPlugin(pluginId)
    if (plugin) {
        plugin.deploy(true)
        println "Audit Trail plugin installed. Jenkins will restart to complete installation."
    } else {
        println "WARNING: Plugin ${pluginId} not found in Update Center"
    }
} else {
    println "Plugin already installed: ${pluginId}"
}

instance.save()
```

**2. 创建 `config/logrotate/jenkins-audit-trail`**

先创建 `config/logrotate/` 目录（`mkdir -p config/logrotate/`），然后写入 logrotate 配置文件。

```bash
# /etc/logrotate.d/jenkins-audit-trail
# Jenkins Audit Trail 日志轮转配置（per AUDIT-03, D-01）
# 保留 14 天，单文件最大 50MB
/var/lib/jenkins/audit-trail/*.log {
    daily
    rotate 14
    maxsize 50M
    compress
    delaycompress
    missingok
    notifempty
    create 0640 jenkins jenkins
}
```

参数说明（per D-01）：
- `daily` — 每天轮转一次
- `rotate 14` — 保留 14 个轮转文件（约 14 天）
- `maxsize 50M` — 单文件超过 50MB 时提前轮转
- `compress` + `delaycompress` — 压缩旧日志节省空间
- `create 0640 jenkins jenkins` — 新日志文件权限和属主
- `missingok` — 日志文件不存在时不报错
- `notifempty` — 空文件不轮转
  </action>
  <verify>
    <automated>test -f scripts/jenkins/init.groovy.d/05-audit-trail.groovy && test -f config/logrotate/jenkins-audit-trail && echo "文件存在检查通过"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/jenkins/init.groovy.d/05-audit-trail.groovy` 文件存在
    - `grep "audit-trail" scripts/jenkins/init.groovy.d/05-audit-trail.groovy` 匹配插件 ID
    - `grep "getPlugin" scripts/jenkins/init.groovy.d/05-audit-trail.groovy` 包含幂等安装检查
    - `grep "deploy" scripts/jenkins/init.groovy.d/05-audit-trail.groovy` 包含插件部署调用
    - `config/logrotate/jenkins-audit-trail` 文件存在
    - `grep "rotate 14" config/logrotate/jenkins-audit-trail` 匹配 14 天保留
    - `grep "maxsize 50M" config/logrotate/jenkins-audit-trail` 匹配 50MB 限制
    - `grep "jenkins:jenkins\|jenkins jenkins" config/logrotate/jenkins-audit-trail` 匹配文件属主
    - `grep "/var/lib/jenkins/audit-trail" config/logrotate/jenkins-audit-trail` 匹配日志路径
  </acceptance_criteria>
  <done>
    - 05-audit-trail.groovy 创建完成，遵循 02-plugins.groovy 模式
    - 插件 ID 为 audit-trail，安装逻辑幂等（已安装跳过）
    - config/logrotate/jenkins-audit-trail 配置 daily + rotate 14 + maxsize 50M
    - logrotate 路径指向 /var/lib/jenkins/audit-trail/*.log（per D-03）
    - setup-jenkins.sh 步骤 7 的 cp *.groovy 会自动包含新脚本，无需修改 setup-jenkins.sh
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins 插件生态 | 插件从 Jenkins Update Center 下载，信任 Jenkins 官方仓库 |
| Jenkins 日志目录 | /var/lib/jenkins/audit-trail/ 由 jenkins 用户写入 |
| logrotate 操作 | logrotate 以 root 运行，管理 jenkins 用户文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Tampering | Audit Trail 日志文件 | mitigate | 日志目录属主 jenkins:jenkins 0640，普通用户无写权限 |
| T-33-06 | Denial of Service | Audit Trail 日志磁盘占用 | mitigate | logrotate daily + rotate 14 + maxsize 50M（per D-01） |
| T-33-07 | Information Disclosure | Audit Trail 日志内容 | accept | 日志包含 Pipeline 触发信息（用户名、Job 名），属主 jenkins:jenkins 0640 限制读取 |
</threat_model>

<verification>
**验证 Groovy 脚本语法：**
1. `grep "import jenkins" scripts/jenkins/init.groovy.d/05-audit-trail.groovy` — 确认正确的 import
2. `grep "audit-trail" scripts/jenkins/init.groovy.d/05-audit-trail.groovy` — 确认插件 ID

**验证 logrotate 配置：**
1. `cat config/logrotate/jenkins-audit-trail` — 确认配置内容
2. 在生产服务器上复制后可用 `logrotate -d /etc/logrotate.d/jenkins-audit-trail` 调试模式验证

**生产服务器端到端验证（需要 Jenkins 运行 + 插件安装后）：**
1. Jenkins 管理 → Audit Trail → 确认插件已安装
2. 手动添加 File Logger → 路径 `/var/lib/jenkins/audit-trail/audit-trail.log`
3. 触发一次 Pipeline 构建
4. `sudo tail -5 /var/lib/jenkins/audit-trail/audit-trail.log` — 确认有触发记录
</verification>

<success_criteria>
- 05-audit-trail.groovy 遵循 02-plugins.groovy 模式，幂等安装 audit-trail 插件
- config/logrotate/jenkins-audit-trail 配置 daily + rotate 14 + maxsize 50M
- logrotate 路径指向 /var/lib/jenkins/audit-trail/*.log
- 无需修改 setup-jenkins.sh（cp *.groovy 自动包含）
</success_criteria>

<output>
After completion, create `.planning/phases/33-audit-logging/33-02-SUMMARY.md`
</output>
