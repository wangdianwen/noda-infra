---
phase: 06-fix-variable-conflicts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/verify-phase6.sh
autonomous: true
requirements: [技术债务修复]

must_haves:
  truths:
    - "verify-phase6.sh 可以检测所有已知的变量冲突问题"
    - "验证脚本确认 constants.sh 是唯一的 EXIT_* 定义来源"
    - "验证脚本确认所有库文件有正确的 constants.sh 加载机制"
    - "验证脚本确认无裸 LIB_DIR 变量和 .bak 残留"
  artifacts:
    - path: "scripts/backup/verify-phase6.sh"
      provides: "Phase 6 验证脚本（纯验证，不修改任何代码）"
      min_lines: 30
  key_links:
    - from: "verify-phase6.sh"
      to: "scripts/backup/lib/*.sh"
      via: "grep 和 bash -n 检查"
      pattern: "grep.*lib/.*\\.sh"
---

<objective>
验证 Phase 1-5 中已完成的变量冲突修复，创建自动化验证脚本记录修复状态（per D-01: 不涉及新的代码实现）。

Purpose: 严格遵循 D-01 "Phase 6 不涉及新的代码实现，而是文档化和验证已有修复"，创建验证脚本确认所有变量冲突修复的有效性。验证脚本仅做只读检查，不修改任何库文件。
Output: verify-phase6.sh 验证脚本 + 验证结果报告
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-variable-conflicts/06-CONTEXT.md
@.planning/phases/06-fix-variable-conflicts/06-RESEARCH.md

<interfaces>
<!-- 关键类型和契约 -->

scripts/backup/lib/constants.sh 导出的 readonly 变量:
```bash
readonly EXIT_SUCCESS=0
readonly EXIT_CONNECTION_FAILED=1
readonly EXIT_BACKUP_FAILED=2
readonly EXIT_CLOUD_UPLOAD_FAILED=3
readonly EXIT_CLEANUP_FAILED=4
readonly EXIT_VERIFICATION_FAILED=5
readonly EXIT_DISK_SPACE_INSUFFICIENT=6
readonly EXIT_RESTORE_FAILED=7
readonly EXIT_INVALID_ARGS=8
readonly EXIT_TIMEOUT=9
readonly EXIT_DOWNLOAD_FAILED=11
readonly EXIT_RESTORE_TEST_FAILED=12
readonly EXIT_VERIFY_TEST_FAILED=13
readonly EXIT_CLEANUP_TEST_FAILED=14
```

条件加载模式（alert.sh 和 metrics.sh 已使用）:
```bash
LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -z "${EXIT_SUCCESS+x}" ]]; then
  source "$LIB_DIR/constants.sh"
fi
```

局部变量目录定位模式（db.sh、verify.sh、cloud.sh 等已使用）:
```bash
_DB_LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$_DB_LIB_DIR/log.sh"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 verify-phase6.sh 纯验证脚本（per D-01, D-03, D-08, D-09, D-10）</name>
  <files>
    scripts/backup/verify-phase6.sh
  </files>
  <read_first>
    scripts/backup/lib/constants.sh
    scripts/backup/lib/db.sh
    scripts/backup/lib/verify.sh
    scripts/backup/lib/cloud.sh
    scripts/backup/lib/health.sh
    scripts/backup/lib/alert.sh
    scripts/backup/lib/metrics.sh
    scripts/backup/lib/test-verify.sh
    scripts/backup/lib/restore.sh
    scripts/backup/backup-postgres.sh
  </read_first>
  <action>
    **创建 scripts/backup/verify-phase6.sh 纯验证脚本（per D-01: 不涉及新的代码实现）**

    此脚本仅执行只读检查，不修改任何库文件。创建独立的验证脚本，执行以下检查项。脚本应使用 `set -euo pipefail`，输出带状态符号的检查结果。

    ```bash
    #!/bin/bash
    # Phase 6 验证脚本 - 修复变量冲突（纯验证，不修改代码）
    set -euo pipefail

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    LIB_DIR="$SCRIPT_DIR/lib"
    FAILED=0
    WARNINGS=0

    echo "=========================================="
    echo "Phase 6 验证：修复变量冲突（只读检查）"
    echo "=========================================="
    echo ""

    # 检查 1: constants.sh 存在且定义了所有退出码（per D-04）
    echo -n "检查 1: constants.sh 存在并定义退出码... "
    if [[ -f "$LIB_DIR/constants.sh" ]] && grep -q '^readonly EXIT_SUCCESS=0' "$LIB_DIR/constants.sh"; then
      echo "通过"
    else
      echo "失败"
      FAILED=$((FAILED + 1))
    fi

    # 检查 2: 无重复 EXIT_* 定义（constants.sh 除外）（per D-05, D-09）
    echo -n "检查 2: 无重复 readonly EXIT_* 定义... "
    DUPES=$(grep -rn '^readonly EXIT_' "$LIB_DIR"/*.sh 2>/dev/null | grep -v constants.sh || true)
    if [[ -z "$DUPES" ]]; then
      echo "通过"
    else
      echo "失败"
      echo "  发现重复定义: $DUPES"
      FAILED=$((FAILED + 1))
    fi

    # 检查 3: 所有使用 EXIT_* 的库文件有条件加载或主脚本加载 constants.sh（per D-08）
    echo -n "检查 3: 所有 EXIT_* 使用者有防御性加载... "
    MISSING_GUARD=0
    for file in db.sh verify.sh cloud.sh health.sh test-verify.sh alert.sh metrics.sh; do
      if [[ -f "$LIB_DIR/$file" ]]; then
        if grep -q 'return \$EXIT_\|exit \$EXIT_\|exit \$EXIT_' "$LIB_DIR/$file" 2>/dev/null; then
          if ! grep -q 'EXIT_SUCCESS+x\|source.*constants.sh' "$LIB_DIR/$file"; then
            echo "失败"
            echo "  $file 使用 EXIT_* 但无显式加载 constants.sh（依赖主脚本加载）"
            MISSING_GUARD=$((MISSING_GUARD + 1))
          fi
        fi
      fi
    done
    if [[ $MISSING_GUARD -eq 0 ]]; then
      echo "通过"
    else
      # 这是 WARNING 而非 FAILURE：隐式依赖在当前加载顺序下可以工作
      echo "  [WARNING] $MISSING_GUARD 个文件使用 EXIT_* 但依赖主脚本隐式加载"
      WARNINGS=$((WARNINGS + 1))
    fi

    # 检查 4: 所有库文件使用 _*_LIB_DIR 前缀（无裸 LIB_DIR）（per D-06）
    echo -n "检查 4: 无裸 LIB_DIR 变量名... "
    BARE_LIB_DIR=$(grep -rn '^\(LIB_DIR=\|readonly LIB_DIR\)' "$LIB_DIR"/*.sh 2>/dev/null || true)
    if [[ -z "$BARE_LIB_DIR" ]]; then
      echo "通过"
    else
      echo "警告"
      echo "  发现裸 LIB_DIR: $BARE_LIB_DIR"
      echo "  [WARNING] alert.sh 和 metrics.sh 使用裸 LIB_DIR，其他库使用 _*_LIB_DIR 前缀"
      WARNINGS=$((WARNINGS + 1))
    fi

    # 检查 5: 无 .bak 残留文件
    echo -n "检查 5: 无 .bak 残留文件... "
    BAK_FILES=$(find "$LIB_DIR" -name '*.bak' -o -name '*.bak2' 2>/dev/null || true)
    if [[ -z "$BAK_FILES" ]]; then
      echo "通过"
    else
      echo "警告"
      echo "  发现残留文件: $BAK_FILES"
      WARNINGS=$((WARNINGS + 1))
    fi

    # 检查 6: 所有库文件语法检查通过
    echo -n "检查 6: 所有库文件语法检查... "
    SYNTAX_FAILED=0
    for file in "$LIB_DIR"/*.sh; do
      if ! bash -n "$file" 2>/dev/null; then
        echo "失败"
        echo "  语法错误: $file"
        SYNTAX_FAILED=$((SYNTAX_FAILED + 1))
      fi
    done
    if [[ $SYNTAX_FAILED -eq 0 ]]; then
      echo "通过"
    else
      FAILED=$((FAILED + 1))
    fi

    # 检查 7: 主脚本语法检查通过
    echo -n "检查 7: 主脚本语法检查... "
    if bash -n "$SCRIPT_DIR/backup-postgres.sh" 2>/dev/null; then
      echo "通过"
    else
      echo "失败"
      FAILED=$((FAILED + 1))
    fi

    # 检查 8: 主脚本正确加载 constants.sh（per D-07）
    echo -n "检查 8: 主脚本加载 constants.sh... "
    if grep -q 'source.*constants.sh' "$SCRIPT_DIR/backup-postgres.sh" 2>/dev/null; then
      echo "通过"
    else
      echo "失败"
      FAILED=$((FAILED + 1))
    fi

    # 汇总
    echo ""
    echo "=========================================="
    if [[ $FAILED -eq 0 && $WARNINGS -eq 0 ]]; then
      echo "所有检查通过（0 failed, 0 warnings）"
      echo "=========================================="
      exit 0
    elif [[ $FAILED -eq 0 ]]; then
      echo "核心检查通过，$WARNINGS 项警告（非阻塞）"
      echo "=========================================="
      echo ""
      echo "警告项目可通过 06-02 可选修复计划解决"
      exit 0
    else
      echo "$FAILED 项检查失败, $WARNINGS 项警告"
      echo "=========================================="
      exit 1
    fi
    ```

    设置执行权限：`chmod +x scripts/backup/verify-phase6.sh`
  </action>
  <acceptance_criteria>
    - `test -f scripts/backup/verify-phase6.sh` 文件存在
    - `test -x scripts/backup/verify-phase6.sh` 文件可执行
    - `wc -l scripts/backup/verify-phase6.sh` 至少 30 行
    - 脚本不包含任何写入库文件的命令（无 sed、无重定向到 lib/*.sh）
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/backup/verify-phase6.sh && test -x scripts/backup/verify-phase6.sh && bash scripts/backup/verify-phase6.sh && echo "PHASE6_VERIFY_PASS"</automated>
  </verify>
  <done>verify-phase6.sh 创建并可执行，所有核心检查通过（可能有非阻塞警告），退出码 0</done>
</task>

<task type="auto">
  <name>Task 2: 运行验证并记录结果（per D-02, D-10）</name>
  <files>
    scripts/backup/verify-phase6.sh
  </files>
  <action>
    **运行验证脚本确认修复状态（per D-02: 确认修复的完整性, D-10: 运行完整测试套件）**

    执行验证脚本：
    ```bash
    bash scripts/backup/verify-phase6.sh
    ```

    **预期结果分析（基于 RESEARCH.md 发现）：**
    - 检查 1 (constants.sh): 预期通过（D-04 已确认）
    - 检查 2 (无重复 EXIT_*): 预期通过（D-05 已确认）
    - 检查 3 (防御性加载): 可能显示 WARNING -- db.sh、verify.sh、cloud.sh、health.sh、test-verify.sh 使用 EXIT_* 但依赖主脚本隐式加载（非失败，因为这些文件在当前加载顺序下正常工作）
    - 检查 4 (LIB_DIR 命名): 可能显示 WARNING -- alert.sh 和 metrics.sh 使用裸 LIB_DIR（非失败，因为两个文件同目录，目前不会冲突）
    - 检查 5 (.bak 残留): 可能显示 WARNING -- db.sh.bak 和 db.sh.bak2 可能仍存在
    - 检查 6-8 (语法): 预期通过

    **重要：** 此任务仅记录验证结果，不修改任何代码。WARNING 项说明存在技术债务，可在 06-02 可选修复计划中处理。

    如果验证脚本报错（有 FAILED 项），在 SUMMARY 中详细记录失败原因。
  </action>
  <acceptance_criteria>
    - `bash scripts/backup/verify-phase6.sh` 退出码为 0（核心检查通过，WARNING 不影响退出码）
    - 脚本输出包含各检查项的结果
    - 验证结果将被记录在 06-01-SUMMARY.md 中
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/backup/verify-phase6.sh && echo "PHASE6_VERIFY_EXECUTED"</automated>
  </verify>
  <done>验证脚本执行完成，核心检查全部通过，WARNING 项已记录用于 06-02 可选修复</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本阶段为纯验证（只读检查），不修改任何代码，不引入新的信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| 无 | - | - | - | 本计划为纯验证，不涉及安全变更 |

注：.bak 残留文件和裸 LIB_DIR 命名问题属于 06-02 可选修复范围。
</threat_model>

<verification>
1. `bash scripts/backup/verify-phase6.sh` -- 核心检查通过（退出码 0）
2. 验证脚本是只读的 -- `grep -c 'sed\|>\|>>' scripts/backup/verify-phase6.sh` 不包含写入 lib/*.sh 的操作
3. `test -x scripts/backup/verify-phase6.sh` -- 可执行
</verification>

<success_criteria>
1. verify-phase6.sh 创建并可执行
2. 验证脚本运行后核心检查全部通过（退出码 0）
3. 无代码文件被修改（严格遵循 D-01）
4. WARNING 项已记录用于后续可选修复
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-variable-conflicts/06-01-SUMMARY.md`
</output>
