---
phase: 06-fix-variable-conflicts
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/backup/lib/db.sh
  - scripts/backup/lib/verify.sh
  - scripts/backup/lib/cloud.sh
  - scripts/backup/lib/health.sh
  - scripts/backup/lib/test-verify.sh
  - scripts/backup/lib/alert.sh
  - scripts/backup/lib/metrics.sh
  - scripts/backup/test-verify-weekly.sh
autonomous: true
requirements: [技术债务修复]

must_haves:
  truths:
    - "所有使用 EXIT_* 常量的库文件都有防御性条件加载 constants.sh"
    - "所有库文件使用带前缀的 _*_LIB_DIR 局部变量（无裸 LIB_DIR）"
    - "lib 目录中无 .bak 残留文件"
    - "test-verify-weekly.sh 的 print_summary 调用正确（无括号）"
  artifacts:
    - path: "scripts/backup/lib/alert.sh"
      provides: "告警库（修复为 _ALERT_LIB_DIR）"
    - path: "scripts/backup/lib/metrics.sh"
      provides: "指标库（修复为 _METRICS_LIB_DIR）"
  key_links:
    - from: "scripts/backup/lib/db.sh"
      to: "scripts/backup/lib/constants.sh"
      via: "条件 source EXIT_SUCCESS+x 检查"
      pattern: "EXIT_SUCCESS\\+x.*constants\\.sh"
    - from: "scripts/backup/lib/alert.sh"
      to: "scripts/backup/lib/constants.sh"
      via: "条件 source（使用 _ALERT_LIB_DIR）"
      pattern: "_ALERT_LIB_DIR.*constants\\.sh"
---

<objective>
修复 RESEARCH.md 发现的代码不一致性问题：为隐式依赖库添加防御性条件加载、统一 LIB_DIR 命名、修复 print_summary bug、清理 .bak 残留。

Purpose: 解决 06-01 验证中发现的 WARNING 项，使所有库文件的变量管理完全统一，消除隐式依赖和命名冲突风险。这些是 RESEARCH.md 发现的真实代码不一致性，修复后可提高系统健壮性。
Output: 修复后的库文件 + verify-phase6.sh 重新通过（0 warnings）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-variable-conflicts/06-CONTEXT.md
@.planning/phases/06-fix-variable-conflicts/06-RESEARCH.md
@.planning/phases/06-fix-variable-conflicts/06-01-SUMMARY.md

<interfaces>
<!-- 关键类型和契约 -->

scripts/backup/lib/constants.sh 导出的 readonly 变量:
```bash
readonly EXIT_SUCCESS=0
readonly EXIT_CONNECTION_FAILED=1
readonly EXIT_BACKUP_FAILED=2
readonly EXIT_CLOUD_UPLOAD_FAILED=3
readonly EXIT_CLEANUP_FAILED=4
readonly EXIT_VERIFICATION_FAILED=5
readonly EXIT_DISK_SPACE_INSUFFICIENT=6
readonly EXIT_RESTORE_FAILED=7
readonly EXIT_INVALID_ARGS=8
readonly EXIT_TIMEOUT=9
readonly EXIT_DOWNLOAD_FAILED=11
readonly EXIT_RESTORE_TEST_FAILED=12
readonly EXIT_VERIFY_TEST_FAILED=13
readonly EXIT_CLEANUP_TEST_FAILED=14
```

条件加载模式（已验证 alert.sh 和 metrics.sh 使用）:
```bash
LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -z "${EXIT_SUCCESS+x}" ]]; then
  source "$LIB_DIR/constants.sh"
fi
```

局部变量目录定位模式（db.sh、verify.sh、cloud.sh 等使用）:
```bash
_DB_LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$_DB_LIB_DIR/log.sh"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 5 个隐式依赖库添加防御性条件加载 + 统一 LIB_DIR 命名</name>
  <files>
    scripts/backup/lib/db.sh
    scripts/backup/lib/verify.sh
    scripts/backup/lib/cloud.sh
    scripts/backup/lib/health.sh
    scripts/backup/lib/test-verify.sh
    scripts/backup/lib/alert.sh
    scripts/backup/lib/metrics.sh
  </files>
  <read_first>
    scripts/backup/lib/constants.sh
    scripts/backup/lib/db.sh
    scripts/backup/lib/verify.sh
    scripts/backup/lib/cloud.sh
    scripts/backup/lib/health.sh
    scripts/backup/lib/test-verify.sh
    scripts/backup/lib/alert.sh
    scripts/backup/lib/metrics.sh
    scripts/backup/backup-postgres.sh
  </read_first>
  <action>
    **第一部分：为 5 个缺少 constants.sh 条件加载的库文件添加防御性加载（解决检查 3 WARNING）**

    以下库文件使用 EXIT_* 常量但不显式加载 constants.sh，需要添加条件加载：

    1. **scripts/backup/lib/db.sh** -- 在 `_DB_LIB_DIR=...` 之后，第一个 `source` 之前，插入：
       ```bash
       # 条件 source constants.sh（避免重复定义 readonly 变量）
       if [[ -z "${EXIT_SUCCESS+x}" ]]; then
         source "$_DB_LIB_DIR/constants.sh"
       fi
       ```
       同时更新文件头部注释的依赖列表，加入 `constants.sh`。

    2. **scripts/backup/lib/verify.sh** -- 在 `_VERIFY_LIB_DIR=...` 之后，第一个 `source` 之前，插入相同的条件加载代码（使用 `$_VERIFY_LIB_DIR/constants.sh`）。
       更新文件头部注释：加入 `constants.sh`。

    3. **scripts/backup/lib/cloud.sh** -- 在 `_CLOUD_LIB_DIR=...` 之后，第一个 `source` 之前，插入相同的条件加载代码（使用 `$_CLOUD_LIB_DIR/constants.sh`）。
       更新文件头部注释：加入 `constants.sh`。

    4. **scripts/backup/lib/health.sh** -- 在 `_HEALTH_LIB_DIR=...` 之后（`fi` 之后），第一个 `if ! type` 之前，插入相同的条件加载代码（使用 `$_HEALTH_LIB_DIR/constants.sh`）。
       更新文件头部注释：加入 `constants.sh`。

    5. **scripts/backup/lib/test-verify.sh** -- 在 `_TEST_VERIFY_LIB_DIR=...` 之后，第一个 `source` 之前，插入相同的条件加载代码（使用 `$_TEST_VERIFY_LIB_DIR/constants.sh`）。
       更新文件头部注释：加入 `constants.sh`。

    **第二部分：修复 alert.sh 和 metrics.sh 的裸 LIB_DIR 变量名（解决检查 4 WARNING）**

    alert.sh 和 metrics.sh 使用裸变量名 `LIB_DIR`，其他 7 个库文件使用带前缀的 `_*_LIB_DIR`。需要统一为前缀模式。

    1. **scripts/backup/lib/alert.sh**:
       - 将 `LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` 改为 `_ALERT_LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
       - 将文件中所有 `$LIB_DIR` 引用替换为 `$_ALERT_LIB_DIR`
       - 验证替换：`grep -n 'LIB_DIR' scripts/backup/lib/alert.sh` 应只显示 `_ALERT_LIB_DIR`

    2. **scripts/backup/lib/metrics.sh**:
       - 将 `LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` 改为 `_METRICS_LIB_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
       - 将文件中所有 `$LIB_DIR` 引用替换为 `$_METRICS_LIB_DIR`
       - 注意 metrics.sh 中的 `source "$LIB_DIR/alert.sh"` 应变为 `source "$_METRICS_LIB_DIR/alert.sh"`
       - 验证替换：`grep -n 'LIB_DIR' scripts/backup/lib/metrics.sh` 应只显示 `_METRICS_LIB_DIR`
  </action>
  <acceptance_criteria>
    - `grep -c 'EXIT_SUCCESS+x' scripts/backup/lib/db.sh scripts/backup/lib/verify.sh scripts/backup/lib/cloud.sh scripts/backup/lib/health.sh scripts/backup/lib/test-verify.sh | grep -c ':1$'` 返回 5
    - `grep -n '^LIB_DIR=' scripts/backup/lib/alert.sh scripts/backup/lib/metrics.sh` 返回空
    - `grep -n '_ALERT_LIB_DIR=' scripts/backup/lib/alert.sh` 返回包含 1 行
    - `grep -n '_METRICS_LIB_DIR=' scripts/backup/lib/metrics.sh` 返回包含 1 行
    - `grep -c '\$LIB_DIR' scripts/backup/lib/alert.sh` 返回 0
    - `grep -c '\$LIB_DIR' scripts/backup/lib/metrics.sh` 返回 0
    - 所有 7 个修改的文件 `bash -n` 语法检查通过
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'EXIT_SUCCESS+x' scripts/backup/lib/db.sh scripts/backup/lib/verify.sh scripts/backup/lib/cloud.sh scripts/backup/lib/health.sh scripts/backup/lib/test-verify.sh | grep -c ':1$' && grep -n '^LIB_DIR=' scripts/backup/lib/alert.sh scripts/backup/lib/metrics.sh; test $? -ne 0 && bash -n scripts/backup/lib/alert.sh && bash -n scripts/backup/lib/metrics.sh && echo "TASK1_PASS"</automated>
  </verify>
  <done>7 个库文件修复完成：5 个添加防御性条件加载，2 个统一为前缀 LIB_DIR 命名，语法检查全部通过</done>
</task>

<task type="auto">
  <name>Task 2: 修复 print_summary bug + 清理 .bak 残留 + 重新运行验证</name>
  <files>
    scripts/backup/test-verify-weekly.sh
    scripts/backup/lib/db.sh.bak
    scripts/backup/lib/db.sh.bak2
    scripts/backup/verify-phase6.sh
  </files>
  <read_first>
    scripts/backup/test-verify-weekly.sh
    scripts/backup/verify-phase6.sh
  </read_first>
  <action>
    **第一部分：修复 test-verify-weekly.sh 的 print_summary() 语法错误**

    scripts/backup/test-verify-weekly.sh 中 `print_summary()` 带括号会被 Bash 解析为函数定义而非函数调用。需要：
    - 找到 `print_summary()` 调用位置（应为函数调用处，不是函数定义处）
    - 将 `print_summary()` 改为 `print_summary`（移除括号）
    - 确认修改的是函数调用而非函数定义（函数定义的 `print_summary()` 通常后跟 `{`）

    **第二部分：清理残留 .bak 文件（解决检查 5 WARNING）**

    删除以下残留备份文件：
    ```bash
    rm scripts/backup/lib/db.sh.bak
    rm scripts/backup/lib/db.sh.bak2
    ```
    如果文件不存在则跳过（不报错）。

    **第三部分：重新运行验证脚本确认所有修复**

    ```bash
    bash scripts/backup/verify-phase6.sh
    ```

    预期结果：所有 8 项检查通过，0 failed，0 warnings。
  </action>
  <acceptance_criteria>
    - `bash -n scripts/backup/test-verify-weekly.sh` 语法检查通过
    - test-verify-weekly.sh 中函数调用处为 `print_summary`（无括号）
    - `ls scripts/backup/lib/db.sh.bak scripts/backup/lib/db.sh.bak2 2>&1` 返回错误（文件已删除）
    - `bash scripts/backup/verify-phase6.sh` 退出码为 0，0 failed，0 warnings
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/backup/test-verify-weekly.sh && test ! -f scripts/backup/lib/db.sh.bak && test ! -f scripts/backup/lib/db.sh.bak2 && bash scripts/backup/verify-phase6.sh && echo "TASK2_ALL_PASS"</automated>
  </verify>
  <done>print_summary 调用修复、.bak 文件清理完成、验证脚本全部通过（0 failed, 0 warnings）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 本阶段为技术债务修复，不引入新的信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | .bak 残留文件 | mitigate | 删除 db.sh.bak 和 db.sh.bak2，防止泄露修复前的代码历史 |
| T-06-02 | Tampering | db.sh 硬编码连接参数 | accept | 属于 Phase 2 范围的重构，本阶段聚焦变量冲突修复 |

注：db.sh 硬编码连接参数问题 (T-06-02) 被接受而非修复，因为它涉及更广泛的重构（引入 getter 函数替代硬编码），超出本阶段范围。
</threat_model>

<verification>
1. `bash scripts/backup/verify-phase6.sh` -- 所有 8 项检查通过（0 failed, 0 warnings）
2. `grep -rn '^readonly EXIT_' scripts/backup/lib/*.sh | grep -v constants.sh` -- 返回空
3. `grep -c 'EXIT_SUCCESS+x' scripts/backup/lib/db.sh scripts/backup/lib/verify.sh scripts/backup/lib/cloud.sh scripts/backup/lib/health.sh scripts/backup/lib/test-verify.sh | grep -c ':1$'` -- 返回 5
4. `grep -n '^LIB_DIR=' scripts/backup/lib/alert.sh scripts/backup/lib/metrics.sh` -- 返回空
5. `test ! -f scripts/backup/lib/db.sh.bak && test ! -f scripts/backup/lib/db.sh.bak2` -- 残留文件已删除
6. `bash -n scripts/backup/lib/*.sh` -- 所有库文件语法正确
</verification>

<success_criteria>
1. 所有使用 EXIT_* 常量的 7 个库文件都有防御性条件加载 constants.sh
2. 所有 9 个库文件使用带前缀的 _*_LIB_DIR 局部变量（无裸 LIB_DIR）
3. lib/ 目录中无 .bak 残留文件
4. test-verify-weekly.sh 的 print_summary 调用不带括号
5. verify-phase6.sh 执行通过（0 failed, 0 warnings）
6. 所有修改的文件 bash -n 语法检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-variable-conflicts/06-02-SUMMARY.md`
</output>
