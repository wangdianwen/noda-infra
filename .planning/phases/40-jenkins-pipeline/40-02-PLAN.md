---
phase: 40-jenkins-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - jenkins/Jenkinsfile.findclass-ssr
  - jenkins/Jenkinsfile.infra
  - jenkins/Jenkinsfile.keycloak
autonomous: true
requirements:
  - PIPE-02
  - PIPE-04

must_haves:
  truths:
    - "3 个 Jenkinsfile 的 environment 块包含 DOPPLER_TOKEN = credentials('doppler-service-token')"
    - "Jenkins 构建日志中 DOPPLER_TOKEN 的值被自动遮蔽为 ****"
    - "VITE_* 构建参数仍通过 Dockerfile ARG 硬编码，不受 Doppler 影响（per D-12）"
  artifacts:
    - path: "jenkins/Jenkinsfile.findclass-ssr"
      provides: "findclass-ssr 蓝绿部署 Pipeline，DOPPLER_TOKEN 注入"
      contains: "DOPPLER_TOKEN = credentials"
    - path: "jenkins/Jenkinsfile.infra"
      provides: "基础设施服务 Pipeline，DOPPLER_TOKEN 注入"
      contains: "DOPPLER_TOKEN = credentials"
    - path: "jenkins/Jenkinsfile.keycloak"
      provides: "Keycloak 蓝绿部署 Pipeline，DOPPLER_TOKEN 注入"
      contains: "DOPPLER_TOKEN = credentials"
  key_links:
    - from: "Jenkinsfile.*"
      to: "Jenkins Credentials Store"
      via: "credentials('doppler-service-token')"
      pattern: "credentials\\('doppler-service-token'\\)"
    - from: "Jenkinsfile.* environment"
      to: "pipeline-stages.sh load_secrets()"
      via: "DOPPLER_TOKEN 环境变量"
      pattern: "DOPPLER_TOKEN"
---

<objective>
在 3 个 Jenkinsfile 的 environment 块中添加 DOPPLER_TOKEN = credentials('doppler-service-token')（per D-06, D-07），使 Pipeline 执行时自动注入 Doppler Service Token。

Purpose: Jenkins credentials() 方法自动遮蔽日志中的 token 值（per PIPE-02），同时让 pipeline-stages.sh 的 load_secrets() 能通过 DOPPLER_TOKEN 环境变量检测到 Doppler 模式。

Output: 3 个 Jenkinsfile 添加 DOPPLER_TOKEN 环境变量
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-jenkins-pipeline/40-CONTEXT.md
@.planning/phases/40-jenkins-pipeline/40-RESEARCH.md

<interfaces>
<!-- Jenkinsfile.findclass-ssr 当前 environment 块 -->
From jenkins/Jenkinsfile.findclass-ssr (第 11-18 行):
```groovy
environment {
    PROJECT_ROOT = "${WORKSPACE}"
    ACTIVE_ENV = sh(
        script: 'cat /opt/noda/active-env 2>/dev/null || echo blue',
        returnStdout: true
    ).trim()
    TARGET_ENV = "${env.ACTIVE_ENV == 'blue' ? 'green' : 'blue'}"
}
```

<!-- Jenkinsfile.infra 当前 environment 块 -->
From jenkins/Jenkinsfile.infra (第 11-14 行):
```groovy
environment {
    PROJECT_ROOT = "${WORKSPACE}"
    COMPOSE_BASE = "-f docker/docker-compose.yml -f docker/docker-compose.prod.yml"
    BACKUP_HOST_DIR = "${WORKSPACE}/docker/volumes/backup"
}
```

<!-- Jenkinsfile.keycloak 当前 environment 块 -->
From jenkins/Jenkinsfile.keycloak (第 11-40 行):
```groovy
environment {
    PROJECT_ROOT = "${WORKSPACE}"
    SERVICE_NAME = "keycloak"
    SERVICE_PORT = "8080"
    UPSTREAM_NAME = "keycloak_backend"
    HEALTH_PATH = "/realms/master"
    // ... 更多变量
    ACTIVE_ENV = sh(
        script: 'cat /opt/noda/active-env-keycloak 2>/dev/null || echo blue',
        returnStdout: true
    ).trim()
    TARGET_ENV = "${env.ACTIVE_ENV == 'blue' ? 'green' : 'blue'}"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 个 Jenkinsfile 添加 DOPPLER_TOKEN 环境变量</name>
  <files>jenkins/Jenkinsfile.findclass-ssr, jenkins/Jenkinsfile.infra, jenkins/Jenkinsfile.keycloak</files>
  <read_first>
    - jenkins/Jenkinsfile.findclass-ssr（完整文件，定位 environment 块位置）
    - jenkins/Jenkinsfile.infra（完整文件，定位 environment 块位置）
    - jenkins/Jenkinsfile.keycloak（完整文件，定位 environment 块位置）
  </read_first>
  <action>
在每个 Jenkinsfile 的 `environment { }` 块中，在最后一个变量定义之后、闭合 `}` 之前，添加一行（per D-06, D-07）：

```groovy
        // Doppler Service Token（Phase 40，per D-06/D-07）
        DOPPLER_TOKEN = credentials('doppler-service-token')
```

具体位置：
1. **Jenkinsfile.findclass-ssr**: 在 `TARGET_ENV = "${env.ACTIVE_ENV == 'blue' ? 'green' : 'blue'}"` 之后添加
2. **Jenkinsfile.infra**: 在 `BACKUP_HOST_DIR = "${WORKSPACE}/docker/volumes/backup"` 之后添加
3. **Jenkinsfile.keycloak**: 在 `TARGET_ENV = "${env.ACTIVE_ENV == 'blue' ? 'green' : 'blue'}"` 之后添加

注意事项：
- credentialsId 必须是 `doppler-service-token`（per D-07，Phase 39 已在 Jenkins Credentials Store 创建）
- 不要修改任何 stage 定义或 sh 步骤
- 不要修改 VITE_* 相关的任何代码（per D-12）
- 不要修改 noda-site 的 Jenkinsfile（per D-01）
- 注释中包含 Phase 标记以便未来追溯
  </action>
  <acceptance_criteria>
    - `grep "DOPPLER_TOKEN = credentials" jenkins/Jenkinsfile.findclass-ssr` 输出包含 `credentials('doppler-service-token')`
    - `grep "DOPPLER_TOKEN = credentials" jenkins/Jenkinsfile.infra` 输出包含 `credentials('doppler-service-token')`
    - `grep "DOPPLER_TOKEN = credentials" jenkins/Jenkinsfile.keycloak` 输出包含 `credentials('doppler-service-token')`
    - `grep -c "DOPPLER_TOKEN" jenkins/Jenkinsfile.noda-site` 返回 0（noda-site 不改，per D-01）
    - `grep "VITE_KEYCLOAK_URL=https://auth.noda.co.nz" jenkins/Jenkinsfile.findclass-ssr` 无匹配（VITE_* 在 pipeline-stages.sh 中硬编码，不在 Jenkinsfile 中，但如果搜索到也不应被修改）
    - 3 个文件各自的 `environment { }` 块中原有变量不变
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DOPPLER_TOKEN = credentials('doppler-service-token')" jenkins/Jenkinsfile.findclass-ssr jenkins/Jenkinsfile.infra jenkins/Jenkinsfile.keycloak | grep -v ':0$'</automated>
  </verify>
  <done>3 个 Jenkinsfile 的 environment 块都包含 DOPPLER_TOKEN = credentials('doppler-service-token')。Jenkins 运行时 DOPPLER_TOKEN 自动注入到所有 stage 的环境中，pipeline-stages.sh 的 load_secrets() 可检测到并走 Doppler 路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins Credentials Store -> Pipeline environment | 凭据从加密存储注入到构建环境 |
| Pipeline environment -> Jenkins 日志 | credentials() 自动遮蔽，防止 token 泄露到日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Information Disclosure | DOPPLER_TOKEN 在 Jenkins 日志中 | mitigate | Jenkins credentials() 方法自动将值替换为 **** |
| T-40-05 | Elevation of Privilege | credentialsId 'doppler-service-token' 不存在 | accept | Pipeline 启动即失败，无安全风险。Phase 39 已创建该 credential |
</threat_model>

<verification>
- 3 个 Jenkinsfile 都包含 `DOPPLER_TOKEN = credentials('doppler-service-token')`
- noda-site 的 Jenkinsfile 不包含此行
- 原有 environment 变量不变
</verification>

<success_criteria>
1. jenkins/Jenkinsfile.findclass-ssr 的 environment 块包含 DOPPLER_TOKEN = credentials('doppler-service-token')
2. jenkins/Jenkinsfile.infra 的 environment 块包含 DOPPLER_TOKEN = credentials('doppler-service-token')
3. jenkins/Jenkinsfile.keycloak 的 environment 块包含 DOPPLER_TOKEN = credentials('doppler-service-token')
4. jenkins/Jenkinsfile.noda-site 未被修改（per D-01）
</success_criteria>

<output>
After completion, create `.planning/phases/40-jenkins-pipeline/40-02-SUMMARY.md`
</output>
