---
phase: 40-jenkins-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - scripts/blue-green-deploy.sh
  - scripts/deploy/deploy-infrastructure-prod.sh
  - scripts/deploy/deploy-apps-prod.sh
autonomous: true
requirements:
  - PIPE-01
  - PIPE-03

must_haves:
  truths:
    - "blue-green-deploy.sh source scripts/lib/secrets.sh 并调用 load_secrets()"
    - "deploy-infrastructure-prod.sh source scripts/lib/secrets.sh 并调用 load_secrets()"
    - "deploy-apps-prod.sh source scripts/lib/secrets.sh 并调用 load_secrets()"
    - "手动脚本在 DOPPLER_TOKEN 未设置时回退到 docker/.env"
    - "手动脚本在 DOPPLER_TOKEN 已设置但 Doppler 不可用时给出明确错误提示"
  artifacts:
    - path: "scripts/blue-green-deploy.sh"
      provides: "蓝绿部署核心脚本，Doppler 双模式支持"
      contains: "load_secrets"
    - path: "scripts/deploy/deploy-infrastructure-prod.sh"
      provides: "手动基础设施部署脚本，Doppler 双模式支持"
      contains: "load_secrets"
    - path: "scripts/deploy/deploy-apps-prod.sh"
      provides: "手动应用部署脚本，Doppler 双模式支持"
      contains: "load_secrets"
  key_links:
    - from: "scripts/blue-green-deploy.sh"
      to: "scripts/lib/secrets.sh"
      via: "source"
      pattern: "source.*scripts/lib/secrets\\.sh"
    - from: "scripts/deploy/deploy-infrastructure-prod.sh"
      to: "scripts/lib/secrets.sh"
      via: "source"
      pattern: "source.*scripts/lib/secrets\\.sh"
    - from: "scripts/deploy/deploy-apps-prod.sh"
      to: "scripts/lib/secrets.sh"
      via: "source"
      pattern: "source.*scripts/lib/secrets\\.sh"
---

<objective>
改造 3 个手动部署脚本使用 scripts/lib/secrets.sh 的 load_secrets() 函数，实现 Doppler 双模式密钥获取（per D-08, D-09, D-10）。

Purpose: 手动脚本是 Jenkins Pipeline 不可用时的紧急回退方案，必须支持与 Pipeline 相同的 Doppler 密钥获取方式。用户需要预先设置 DOPPLER_TOKEN 环境变量才能使用 Doppler 模式，否则回退到 docker/.env。

Output: 3 个手动脚本改造完成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-jenkins-pipeline/40-CONTEXT.md
@.planning/phases/40-jenkins-pipeline/40-RESEARCH.md
@.planning/phases/40-jenkins-pipeline/40-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的 load_secrets() 函数签名 -->
From scripts/lib/secrets.sh:
```bash
load_secrets()
# 无参数
# 依赖环境变量: DOPPLER_TOKEN（由调用者或 Jenkins withCredentials 注入）
# 依赖环境变量: PROJECT_ROOT（由调用者设置）
# 副作用: 将密钥导出到当前 shell 环境（set -a; eval/source; set +a）
# 返回: 0=成功，1=失败
```

<!-- blue-green-deploy.sh 当前密钥加载（第 22-27 行）-->
From scripts/blue-green-deploy.sh:
```bash
if [ -f "$PROJECT_ROOT/docker/.env" ]; then
    set -a
    source "$PROJECT_ROOT/docker/.env"
    set +a
fi
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: blue-green-deploy.sh 使用 load_secrets()</name>
  <files>scripts/blue-green-deploy.sh</files>
  <read_first>
    - scripts/blue-green-deploy.sh（完整文件，定位第 22-27 行的 .env 加载逻辑和 source 语句顺序）
    - scripts/lib/secrets.sh（Plan 01 创建的 load_secrets 函数）
  </read_first>
  <action>
修改 scripts/blue-green-deploy.sh：

1. 在第 21 行 `source "$PROJECT_ROOT/scripts/lib/health.sh"` 之后添加：
   ```bash
   source "$PROJECT_ROOT/scripts/lib/secrets.sh"
   ```

2. 删除第 22-27 行的 .env 加载逻辑（从 `# 加载 .env` 注释到 `fi`），替换为：
   ```bash
   # 加载密钥（Doppler 双模式，per D-03/D-08/D-10）
   load_secrets
   ```

注意：
- 不要修改 `PROJECT_ROOT` 的定义（第 17 行）
- 不要修改 `main` 函数中的任何逻辑
- load_secrets() 必须在 source manage-containers.sh 之前调用，因为 manage-containers.sh 的 envsubst 需要环境变量
- 保留所有其他 source 语句不变
  </action>
  <acceptance_criteria>
    - `grep 'source.*scripts/lib/secrets.sh' scripts/blue-green-deploy.sh` 输出非空
    - `grep 'load_secrets' scripts/blue-green-deploy.sh` 输出包含 load_secrets 调用
    - `grep -c 'source.*docker/.env' scripts/blue-green-deploy.sh` 返回 0（旧的 source .env 已删除）
    - `grep -c 'set -a' scripts/blue-green-deploy.sh` 确认不再有直接 set -a + source .env
    - `bash -n scripts/blue-green-deploy.sh` 返回 0（语法正确）
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/blue-green-deploy.sh && echo "syntax OK"</automated>
  </verify>
  <done>blue-green-deploy.sh 使用 load_secrets() 替代直接 source docker/.env，支持 Doppler 双模式</done>
</task>

<task type="auto">
  <name>Task 2: deploy-infrastructure-prod.sh 和 deploy-apps-prod.sh 使用 load_secrets()</name>
  <files>scripts/deploy/deploy-infrastructure-prod.sh, scripts/deploy/deploy-apps-prod.sh</files>
  <read_first>
    - scripts/deploy/deploy-infrastructure-prod.sh（完整文件，定位 source 语句和密钥使用位置）
    - scripts/deploy/deploy-apps-prod.sh（完整文件，定位 source 语句和密钥使用位置）
    - scripts/lib/secrets.sh（Plan 01 创建的 load_secrets 函数）
  </read_first>
  <action>
修改两个脚本：

**scripts/deploy/deploy-infrastructure-prod.sh:**

1. 在第 20 行 `source "$PROJECT_ROOT/scripts/lib/health.sh"` 之后添加：
   ```bash
   source "$PROJECT_ROOT/scripts/lib/secrets.sh"
   ```

2. 检查文件中是否有 `source docker/.env` 或类似的密钥加载逻辑。根据当前代码分析，deploy-infrastructure-prod.sh 当前没有显式的 source docker/.env -- 它依赖 `docker compose` 的 `${VAR}` 替换（compose 自动读取同目录 .env 文件）。

   但是，此脚本的 `COMPOSE_FILES` 变量引用了 `-f docker/docker-compose.yml -f docker/docker-compose.prod.yml`，docker compose 会自动从 `docker/.env` 读取变量。为了让 Doppler 模式工作，需要手动将密钥注入到环境中，这样 compose 的 `${VAR}` 替换才能工作。

   在 source secrets.sh 之后、步骤 1/5 之前添加：
   ```bash
   # 加载密钥（Doppler 双模式，per D-08/D-09/D-10）
   # Docker Compose 不会自动读取 .env（因为 compose 文件在 docker/ 子目录）
   # 需要显式加载密钥到环境中
   load_secrets
   ```

3. 同时删除第 209-212 行对 `config/secrets.sops.yaml` 的检查（这是旧 SOPS 逻辑，Phase 41 会清理 SOPS 相关代码，但这个检查会在 Doppler 模式下报错因为 sops 文件可能不存在）：
   将：
   ```bash
   if [ ! -f "config/secrets.sops.yaml" ]; then
       log_error "加密配置文件不存在: config/secrets.sops.yaml"
       exit 1
   fi
   ```
   替换为：
   ```bash
   # Doppler 密钥已在上方通过 load_secrets() 加载（per Phase 40）
   # config/secrets.sops.yaml 检查已移除（SOPS 将在 Phase 41 清理）
   ```

**scripts/deploy/deploy-apps-prod.sh:**

1. 在第 21 行 `source "$PROJECT_ROOT/scripts/lib/health.sh"` 之后添加：
   ```bash
   source "$PROJECT_ROOT/scripts/lib/secrets.sh"
   ```

2. 检查此脚本是否有密钥加载逻辑。当前 deploy-apps-prod.sh 没有显式的 source .env（它构建 docker-compose.app.yml 中的服务，compose 可能从 docker/.env 自动读取）。但由于 COMPOSE_FILE 指向 `docker/docker-compose.app.yml`，compose 会从 `docker/.env` 读取变量。

   为了 Doppler 模式兼容，在 source secrets.sh 之后添加：
   ```bash
   # 加载密钥（Doppler 双模式，per D-08/D-09/D-10）
   load_secrets
   ```
  </action>
  <acceptance_criteria>
    - `grep 'source.*scripts/lib/secrets.sh' scripts/deploy/deploy-infrastructure-prod.sh` 输出非空
    - `grep 'source.*scripts/lib/secrets.sh' scripts/deploy/deploy-apps-prod.sh` 输出非空
    - `grep 'load_secrets' scripts/deploy/deploy-infrastructure-prod.sh` 输出包含 load_secrets 调用
    - `grep 'load_secrets' scripts/deploy/deploy-apps-prod.sh` 输出包含 load_secrets 调用
    - `bash -n scripts/deploy/deploy-infrastructure-prod.sh` 返回 0（语法正确）
    - `bash -n scripts/deploy/deploy-apps-prod.sh` 返回 0（语法正确）
    - `grep 'secrets.sops.yaml' scripts/deploy/deploy-infrastructure-prod.sh` 不包含 `log_error` 或 `exit 1`（SOPS 检查已移除或弱化）
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/deploy/deploy-infrastructure-prod.sh && bash -n scripts/deploy/deploy-apps-prod.sh && echo "syntax OK"</automated>
  </verify>
  <done>3 个手动脚本都使用 load_secrets() 获取密钥，支持 Doppler 双模式。用户可通过 export DOPPLER_TOKEN=xxx 使用 Doppler，不设则回退 docker/.env。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 shell -> 脚本环境 | 用户手动设置 DOPPLER_TOKEN 环境变量 |
| Doppler API -> 脚本环境 | 外部服务密钥注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Information Disclosure | DOPPLER_TOKEN 在 ps 辅助输出中可见 | accept | 手动部署场景下用户已有服务器 shell 访问权限，token 暴露风险低于 Jenkins 日志场景 |
| T-40-07 | Denial of Service | Doppler 不可用 | mitigate | 双模式回退到 docker/.env（per D-10），手动脚本可正常工作 |
</threat_model>

<verification>
- `bash -n scripts/blue-green-deploy.sh` 语法正确
- `bash -n scripts/deploy/deploy-infrastructure-prod.sh` 语法正确
- `bash -n scripts/deploy/deploy-apps-prod.sh` 语法正确
- 3 个脚本都 source scripts/lib/secrets.sh 并调用 load_secrets()
- 旧的 source docker/.env 逻辑已删除
</verification>

<success_criteria>
1. scripts/blue-green-deploy.sh 使用 load_secrets() 替代直接 source docker/.env
2. scripts/deploy/deploy-infrastructure-prod.sh 使用 load_secrets() 并移除 SOPS 检查
3. scripts/deploy/deploy-apps-prod.sh 使用 load_secrets()
4. 3 个脚本 bash 语法检查通过
5. 手动脚本不 source pipeline-stages.sh（保持独立性），而是直接 source scripts/lib/secrets.sh
</success_criteria>

<output>
After completion, create `.planning/phases/40-jenkins-pipeline/40-03-SUMMARY.md`
</output>
