---
phase: 31-docker-socket
plan: 03
type: execute
wave: 1
depends_on: [31-01, 31-02]
files_modified:
  - scripts/apply-file-permissions.sh
  - scripts/undo-permissions.sh
  - scripts/setup-jenkins.sh
autonomous: true
gap_closure: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04, JENKINS-01, JENKINS-02]

must_haves:
  truths:
    - "apply-file-permissions.sh apply 在 macOS 上执行成功（无 crash，Linux-only 操作跳过并 warning）"
    - "apply-file-permissions.sh verify 在 macOS 上输出可行项 PASS，Linux-only 项标记 N/A，整体返回成功"
    - "undo-permissions.sh backup 在 macOS 上执行成功，备份文件包含可行项"
    - "undo-permissions.sh undo 在 macOS 上执行成功，回滚可行项"
    - "setup-jenkins.sh 各子命令在 macOS 上不 crash（Linux-only 步骤跳过并 warning）"
    - "三个脚本在 Linux 环境下行为不变（所有 Linux 路径仍然正常执行）"
  artifacts:
    - path: "scripts/apply-file-permissions.sh"
      provides: "跨平台权限应用（macOS 适配 + Linux 原有逻辑）"
      contains: "detect_platform"
    - path: "scripts/undo-permissions.sh"
      provides: "跨平台权限回滚（macOS 适配 + Linux 原有逻辑）"
      contains: "detect_platform"
    - path: "scripts/setup-jenkins.sh"
      provides: "跨平台 Jenkins 管理（macOS 适配 + Linux 原有逻辑）"
      contains: "detect_platform"
  key_links:
    - from: "apply-file-permissions.sh"
      to: "detect_platform()"
      via: "每个子命令入口调用"
      pattern: "detect_platform"
    - from: "undo-permissions.sh"
      to: "detect_platform()"
      via: "backup/undo 子命令调用"
      pattern: "detect_platform"
    - from: "setup-jenkins.sh"
      to: "detect_platform()"
      via: "install/status/uninstall 子命令调用"
      pattern: "detect_platform"
---

<objective>
修复 Phase 31 三个脚本的 macOS 兼容性问题。

Purpose: UAT 发现所有脚本假设 Linux 环境（systemd + jenkins 用户/组），但实际部署环境是 macOS（launchd + dianwenwang 用户 + Docker Desktop）。脚本在 macOS 上执行会 crash（chown jenkins: illegal group name、systemctl: command not found 等），导致 Phase 31 权限收敛完全无法在当前环境执行。

Output: 三个脚本（apply-file-permissions.sh、undo-permissions.sh、setup-jenkins.sh）均支持 macOS/Linux 双平台，macOS 上 Linux-only 操作优雅跳过，Linux 上行为完全不变。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-docker-socket/31-UAT.md
@.planning/phases/31-docker-socket/31-01-SUMMARY.md
@.planning/phases/31-docker-socket/31-02-SUMMARY.md

<interfaces>
<!-- 当前环境信息 -->
操作系统: macOS Darwin 25.4.0
Jenkins: Homebrew 安装，brew services 管理，以 dianwenwang 用户运行
Docker: Docker Desktop for Mac，socket 在 /var/run/docker.sock (symlink to /Users/dianwenwang/.docker/run/docker.sock)
Socket 权限: root:daemon（Docker Desktop 管理，host 无法通过 chown 持久修改）
无 jenkins 用户/组: macOS 上不存在 jenkins 系统用户
无 systemd: macOS 使用 launchd

<!-- scripts/lib/log.sh 日志接口 -->
log_info "消息"    — 黄色信息
log_success "消息" — 绿色成功
log_error "消息"   — 红色错误（stderr）
log_warn "消息"    — 黄色警告
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 detect_platform() + 重构 apply-file-permissions.sh 为跨平台兼容</name>
  <files>scripts/apply-file-permissions.sh</files>
  <read_first>scripts/apply-file-permissions.sh, scripts/lib/log.sh</read_first>
  <behavior>
    - macOS 上 `sudo bash scripts/apply-file-permissions.sh apply` 执行成功（exit 0）
    - macOS 上 `sudo bash scripts/apply-file-permissions.sh verify` 输出 N/A 项并返回成功
    - Linux 上 apply/verify 行为与修改前完全一致
    - shellcheck -S error 通过
  </behavior>
  <action>
在 scripts/apply-file-permissions.sh 顶部（source log.sh 之后）添加 `detect_platform()` 函数：

```bash
# 检测运行平台
detect_platform() {
    local os
    os="$(uname)"
    if [[ "$os" == "Darwin" ]]; then
        echo "macos"
    else
        echo "linux"
    fi
}
```

在脚本顶部（detect_platform 之后）添加全局变量 `PLATFORM="$(detect_platform)"`。

重构 cmd_apply() 子命令，添加平台条件分支：

1. **步骤 1（创建 /opt/noda/）**: macOS 上改为 `sudo chown root:staff /opt/noda`（staff 是 macOS 默认组），或直接使用当前用户 `sudo chown "$(whoami):staff /opt/noda"`。日志消息添加平台标识。
2. **步骤 2（文件权限锁定）**: macOS 上改为 `sudo chown "$(whoami):staff "$script"` + `sudo chmod 750 "$script"`。日志说明 macOS 上使用当前用户。
3. **步骤 3（systemd override）**: macOS 上完全跳过此步骤，输出 `log_warn "macOS 环境：跳过 systemd override（Docker Desktop 管理 socket 权限）"`。只有 `[[ "$PLATFORM" == "linux" ]]` 时才创建 override 文件。
4. **步骤 4（daemon-reload）**: 已有 `if command -v systemctl` 保护，无需改动。
5. **步骤 5（重启 Docker）**: 已有 `if command -v systemctl` 保护，无需改动。macOS 上可额外提示 `log_info "macOS 上 Docker Desktop 通过应用重启管理 Docker 服务"`。
6. **步骤 6（gpasswd -d jenkins docker）**: macOS 上跳过，输出 `log_warn "macOS 环境：跳过 jenkins 组操作（无 jenkins 用户）"`。只有 `[[ "$PLATFORM" == "linux" ]]` 时执行。
7. **步骤 7（重启 Jenkins）**: macOS 上改为 `brew services restart jenkins 2>/dev/null || log_warn "macOS: Jenkins 未通过 Homebrew 管理"`。保留 Linux 的 systemctl 路径。
8. **步骤 8（hook）**: cmd_hook 不依赖平台，无需改动。但 hook 内容中的 `chown root:jenkins` 在 macOS 上也需要适配——改为 `if [[ "$(uname)" == "Darwin" ]]; then chown "$(whoami):staff" ... else chown root:jenkins ... fi`。

重构 cmd_verify() 子命令，添加平台条件分支：

1. **检查 1-2（socket 属组/权限）**: macOS 上 socket 由 Docker Desktop 管理（root:daemon），无法也无需修改。改为：`if [[ "$PLATFORM" == "macos" ]]; then log_warn "Docker socket 属组: N/A（macOS Docker Desktop 管理）" ; else` ... 原有检查 ... `fi`。
2. **检查 3（systemd override）**: macOS 上标记 N/A（已有部分逻辑，确认完善）。
3. **检查 4（锁定脚本权限）**: macOS 上检查 `750:$(whoami):staff` 而非 `750:root:jenkins`。使用条件判断：`local expected_perms; if [[ "$PLATFORM" == "macos" ]]; then expected_perms="750:$(whoami):staff"; else expected_perms="750:root:jenkins"; fi`。
4. **检查 5（post-merge hook）**: 通用，无需改动。
5. **检查 6（jenkins 用户 docker 访问）**: macOS 上改为检查当前用户的 Docker 可访问性：`if [[ "$PLATFORM" == "macos" ]]; then if docker info >/dev/null 2>&1; then log_success "当前用户可以执行 docker 命令（macOS Docker Desktop）"; else log_error "当前用户无法执行 docker 命令"; all_ok=false; fi`。
6. **检查 7（/opt/noda/ 权限）**: macOS 上检查 `770:$(whoami):staff`。
7. **最终返回**: macOS 上只要有可行项 PASS 且无 FAIL（N/A 不算 FAIL）就返回 0。

重构 cmd_hook() 中的 hook 模板内容，使其跨平台：

hook 脚本中的 LOCKED_SCRIPTS 循环改为：
```bash
if [[ "$(uname)" == "Darwin" ]]; then
    OWNER_GROUP="$(whoami):staff"
else
    OWNER_GROUP="root:jenkins"
fi
for script in "${LOCKED_SCRIPTS[@]}"; do
    if [ -f "$script" ]; then
        chown "$OWNER_GROUP" "$script" 2>/dev/null || \
            sudo chown "$OWNER_GROUP" "$script" 2>/dev/null || true
        chmod 750 "$script" 2>/dev/null || \
            sudo chmod 750 "$script" 2>/dev/null || true
    fi
done
```

**关键约束：**
- `set -euo pipefail` 必须保留，所有 macOS 跳过的操作不能触发 errexit
- 所有 Linux-only 操作使用 `if [[ "$PLATFORM" == "linux" ]]; then ... fi` 条件包裹
- 不删除任何 Linux 逻辑，只添加 macOS 分支
- 修改后 `shellcheck -S error scripts/apply-file-permissions.sh` 必须通过
  </action>
  <verify>
    <automated>shellcheck -S error scripts/apply-file-permissions.sh && echo "PASS: shellcheck" || echo "FAIL: shellcheck"</automated>
    additional: |
      # 验证 detect_platform 函数存在
      grep -q "detect_platform" scripts/apply-file-permissions.sh && echo "PASS: detect_platform exists" || echo "FAIL"
      # 验证 macOS 分支覆盖所有关键位置
      grep -c "macos" scripts/apply-file-permissions.sh | grep -q "[3-9]" && echo "PASS: macos branches present" || echo "FAIL: insufficient macos branches"
      # 验证 Linux 路径仍保留
      grep -q "root:jenkins" scripts/apply-file-permissions.sh && echo "PASS: linux path retained" || echo "FAIL: linux path lost"
      # macOS 实际执行测试
      bash scripts/apply-file-permissions.sh verify 2>&1 | grep -qiE "(PASS|N/A|成功)" && echo "PASS: verify runs on macOS" || echo "FAIL: verify crashes on macOS"
  </verify>
  <done>
    - detect_platform() 函数存在于 apply-file-permissions.sh
    - cmd_apply 在 macOS 上跳过 systemd/jenkins 组操作，不 crash
    - cmd_verify 在 macOS 上输出 N/A 替代 FAIL，返回成功
    - cmd_hook 生成的 hook 脚本包含 macOS 分支
    - shellcheck 无 error
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 undo-permissions.sh 为跨平台兼容</name>
  <files>scripts/undo-permissions.sh</files>
  <read_first>scripts/undo-permissions.sh, scripts/apply-file-permissions.sh</read_first>
  <behavior>
    - macOS 上 `bash scripts/undo-permissions.sh backup` 执行成功
    - macOS 上 `bash scripts/undo-permissions.sh undo` 执行成功（Linux-only 步骤跳过）
    - Linux 上 backup/undo 行为不变
    - shellcheck -S error 通过
  </behavior>
  <action>
在 scripts/undo-permissions.sh 顶部（source log.sh 之后，BACKUP_FILE 常量之前）添加与 apply-file-permissions.sh 相同的 `detect_platform()` 函数和全局变量 `PLATFORM="$(detect_platform)"`。

重构 backup_current_state()：

1. **systemd override 备份**: 当前已有 `if [[ "$(uname)" != "Darwin" ]]` 保护，但需要确保 `set -euo pipefail` 不会因 `sudo cat` 在 macOS 上失败而退出。使用已有的条件分支即可，但确认 `local override_dir` 在 Darwin 分支中不会未定义导致 errexit。将 Darwin 分支改为只输出 N/A 注释，不执行任何 Linux 命令。
2. **jenkins 组信息**: macOS 上 jenkins 用户不存在（当前已有 `if id jenkins` 保护），确认 `id jenkins` 返回非零不会触发 errexit。添加 `|| true` 保护。
3. **socket 属组/模式**: 已有 Darwin 分支，确认完善。

重构 undo_permissions()，添加平台条件分支：

1. **步骤 1（恢复 socket 属组 root:docker）**: macOS 上 Docker Desktop 管理 socket，无法也不应修改。添加条件：`if [[ "$PLATFORM" == "linux" ]]; then sudo chown root:docker /var/run/docker.sock; sudo chmod 660 /var/run/docker.sock; log_success "..."; else log_warn "macOS 环境：跳过 socket 属组恢复（Docker Desktop 管理）"; fi`。
2. **步骤 2（移除 systemd override）**: `if [[ "$PLATFORM" == "linux" ]]; then ... fi`，macOS 上跳过并 warning。
3. **步骤 3（重启 Docker）**: `if [[ "$PLATFORM" == "linux" ]]; then sudo systemctl restart docker; fi`，macOS 上提示用户手动重启 Docker Desktop。
4. **步骤 4（恢复脚本权限 755）**: 通用操作，macOS 和 Linux 都执行。macOS 上 `sudo chown "$(whoami):staff"` 替代 `sudo chown "$(whoami):$(whoami)"`。
5. **步骤 5（jenkins 加入 docker 组）**: `if [[ "$PLATFORM" == "linux" ]]; then sudo usermod -aG docker jenkins; fi`，macOS 上跳过并 warning。
6. **步骤 6（重启 Jenkins）**: macOS 上 `brew services restart jenkins 2>/dev/null || log_warn "macOS: Jenkins 未通过 Homebrew 管理"`。

更新 usage() 中的回滚操作说明，添加平台差异提示。

**关键约束：**
- `set -euo pipefail` 必须保留
- 不删除任何 Linux 逻辑
- shellcheck 必须通过
  </action>
  <verify>
    <automated>shellcheck -S error scripts/undo-permissions.sh && echo "PASS: shellcheck" || echo "FAIL: shellcheck"</automated>
    additional: |
      # 验证 detect_platform 函数存在
      grep -q "detect_platform" scripts/undo-permissions.sh && echo "PASS: detect_platform exists" || echo "FAIL"
      # 验证 macOS 分支存在
      grep -q "macos" scripts/undo-permissions.sh && echo "PASS: macos branches present" || echo "FAIL"
      # 验证 Linux 路径保留
      grep -q "root:docker" scripts/undo-permissions.sh && echo "PASS: linux path retained" || echo "FAIL"
      # macOS 实际执行测试
      bash scripts/undo-permissions.sh backup 2>&1 && echo "PASS: backup runs on macOS" || echo "FAIL: backup crashes on macOS"
  </verify>
  <done>
    - detect_platform() 函数存在于 undo-permissions.sh
    - backup 在 macOS 上成功备份可行项（Linux-only 项标记 N/A）
    - undo 在 macOS 上跳过 systemd/jenkins 操作，不 crash
    - shellcheck 无 error
  </done>
</task>

<task type="auto">
  <name>Task 3: 重构 setup-jenkins.sh 为跨平台兼容</name>
  <files>scripts/setup-jenkins.sh</files>
  <read_first>scripts/setup-jenkins.sh</read_first>
  <behavior>
    - macOS 上 `bash scripts/setup-jenkins.sh status` 不 crash，输出环境信息
    - macOS 上 `bash scripts/setup-jenkins.sh install` 报错说明不支持 macOS（因为 macOS Jenkins 安装用 Homebrew）
    - Linux 上所有子命令行为不变
    - shellcheck -S error 通过
  </behavior>
  <action>
在 scripts/setup-jenkins.sh 顶部（source log.sh 之后，常量之前）添加 `detect_platform()` 函数和 `PLATFORM="$(detect_platform)"`。

重构 cmd_install()：

1. **步骤 1（检查已安装）**: macOS 上检查 Homebrew Jenkins 而非 dpkg。`if [[ "$PLATFORM" == "macos" ]]; then if brew list jenkins 2>/dev/null; then log_error "Jenkins 已安装"; exit 1; fi; else` ... dpkg 检查 ... `fi`。
2. **步骤 2-5（Java + apt 安装）**: macOS 上这些步骤全部不适用。在步骤 2 开头添加平台检查：`if [[ "$PLATFORM" == "macos" ]]; then log_error "macOS 上请使用 'brew install jenkins' 安装 Jenkins。setup-jenkins.sh install 仅支持 Linux。"; exit 1; fi`。这是最简洁的处理——macOS 有 Homebrew 安装流程，不需要这个脚本的 install 功能。
3. **步骤 6-10**: macOS 上不会执行到（已在步骤 2 退出），无需改动。但步骤 8（Docker 权限）中的 systemd override 需要条件保护，以防将来 macOS 上也想执行到。添加 `if [[ "$PLATFORM" == "linux" ]]; then` 包裹 systemd 操作。

重构 cmd_status()：

1. **检查 1（Jenkins 包）**: macOS 上改为 `brew list jenkins 2>/dev/null` 检查。
2. **检查 2（服务状态）**: macOS 上改为 `brew services list 2>/dev/null | grep jenkins`。
3. **检查 3（端口）**: 通用（curl localhost:8888），无需改动。
4. **检查 4（Docker 权限）**: macOS 上改为检查当前用户 Docker 可用性：`if [[ "$PLATFORM" == "macos" ]]; then if docker info >/dev/null 2>&1; then log_success "Docker 可用（macOS Docker Desktop）"; else log_error "Docker 不可用"; all_ok=false; fi; else` ... 原有 jenkins 用户检查 ... `fi`。
5. **检查 4 补充（systemd override）**: macOS 上标记 N/A。
6. **检查 5（版本）**: macOS 上从 `brew info jenkins` 获取版本。

重构 cmd_uninstall()：

在开头添加平台检查：macOS 上提示使用 `brew uninstall jenkins`，然后 exit。uninstall 的完整流程（systemctl stop/disable, apt remove, userdel 等）全部是 Linux 专属。简化为：`if [[ "$PLATFORM" == "macos" ]]; then log_error "macOS 上请使用 'brew uninstall jenkins' 卸载 Jenkins"; exit 1; fi`。

其他子命令（show-password、restart、upgrade、reset-password）：
- **show-password**: macOS 上 Jenkins home 可能在 `/opt/homebrew/var/jenkins` 或 `~/Library/Application Support/Jenkins`。添加 macOS 路径检测。
- **restart**: macOS 上改为 `brew services restart jenkins`。
- **upgrade**: macOS 上改为 `brew upgrade jenkins`。
- **reset-password**: 密码重置逻辑依赖 jenkins-cli.jar 和 sudo -u jenkins，macOS 上改为当前用户执行。

**关键约束：**
- `set -euo pipefail` 保留
- install 和 uninstall 在 macOS 上明确拒绝（提示使用 Homebrew），不做半吊子适配
- status 在 macOS 上提供有用的环境信息
- restart/upgrade/show-password 在 macOS 上适配 Homebrew 路径
- Linux 逻辑完全不变
  </action>
  <verify>
    <automated>shellcheck -S error scripts/setup-jenkins.sh && echo "PASS: shellcheck" || echo "FAIL: shellcheck"</automated>
    additional: |
      # 验证 detect_platform 函数存在
      grep -q "detect_platform" scripts/setup-jenkins.sh && echo "PASS: detect_platform exists" || echo "FAIL"
      # 验证 macOS 分支
      grep -q "macos" scripts/setup-jenkins.sh && echo "PASS: macos branches present" || echo "FAIL"
      # 验证 install 在 macOS 上拒绝
      grep -q "brew install jenkins" scripts/setup-jenkins.sh && echo "PASS: install rejects on macOS" || echo "FAIL"
      # 验证 restart 有 macOS 分支
      grep -q "brew services restart" scripts/setup-jenkins.sh && echo "PASS: restart has macOS path" || echo "FAIL"
      # macOS 实际执行测试
      bash scripts/setup-jenkins.sh status 2>&1 && echo "PASS: status runs on macOS" || echo "FAIL: status crashes on macOS"
  </verify>
  <done>
    - detect_platform() 函数存在于 setup-jenkins.sh
    - status 子命令在 macOS 上输出环境信息不 crash
    - install/uninstall 在 macOS 上明确拒绝并提示 Homebrew
    - restart/upgrade 在 macOS 上使用 brew services
    - Linux 上所有子命令行为不变
    - shellcheck 无 error
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 脚本输入 → shell 执行 | detect_platform() 输出用于条件判断，uname 输出不可信但仅用于字符串比较 |
| macOS 文件权限 | macOS 上使用 whoami:staff 替代 root:jenkins，权限模型弱于 Linux（无独立 jenkins 用户隔离） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | E (Elevation) | apply-file-permissions.sh macOS 分支 | accept | macOS Docker Desktop 的安全模型本身不通过 socket group 控制访问，脚本无法改变此架构。macOS 开发环境安全要求低于 Linux 生产服务器 |
| T-31-03-02 | S (Spoofing) | detect_platform() 返回值 | accept | uname 是系统调用，不可被环境变量欺骗（需 root 权限修改内核参数） |
</threat_model>

<verification>
```bash
# 1. 所有脚本通过 shellcheck
shellcheck -S error scripts/apply-file-permissions.sh scripts/undo-permissions.sh scripts/setup-jenkins.sh

# 2. macOS 实际执行测试
bash scripts/apply-file-permissions.sh verify 2>&1
bash scripts/undo-permissions.sh backup 2>&1
bash scripts/setup-jenkins.sh status 2>&1

# 3. 验证 detect_platform 一致性（三个脚本都包含）
grep -l "detect_platform" scripts/apply-file-permissions.sh scripts/undo-permissions.sh scripts/setup-jenkins.sh | wc -l | grep -q "3"

# 4. 验证 Linux 路径保留（关键字符串仍存在）
grep -q "root:jenkins" scripts/apply-file-permissions.sh
grep -q "root:docker" scripts/undo-permissions.sh
grep -q "systemctl" scripts/setup-jenkins.sh
```
</verification>

<success_criteria>
1. `shellcheck -S error` 三个脚本全部通过（0 error）
2. `bash scripts/apply-file-permissions.sh verify` 在 macOS 上 exit 0（N/A 项不导致失败）
3. `bash scripts/undo-permissions.sh backup` 在 macOS 上 exit 0
4. `bash scripts/setup-jenkins.sh status` 在 macOS 上 exit 0
5. 三个脚本均包含 `detect_platform` 函数
6. 所有 Linux 路径（root:jenkins、systemctl、gpasswd）在代码中仍存在
</success_criteria>

<output>
After completion, create `.planning/phases/31-docker-socket/31-03-SUMMARY.md`
</output>
