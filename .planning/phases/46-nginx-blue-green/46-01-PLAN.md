---
phase: 46-nginx-blue-green
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/nginx/nginx.conf
  - scripts/pipeline-stages.sh
autonomous: true
requirements:
  - DNS-01
  - DNS-02

must_haves:
  truths:
    - "nginx.conf 包含 resolver 127.0.0.11 指令，nginx 使用 Docker 内置 DNS 解析容器名称"
    - "pipeline_deploy_nginx() 在 docker compose up --force-recreate 后等待 5 秒再执行 nginx -s reload"
    - "reload 失败时 pipeline_deploy_nginx() 返回非零退出码，Pipeline 失败处理机制接管"
  artifacts:
    - path: "config/nginx/nginx.conf"
      provides: "Docker DNS resolver 配置"
      contains: "resolver 127.0.0.11"
    - path: "scripts/pipeline-stages.sh"
      provides: "部署后 DNS 刷新步骤"
      contains: "nginx -s reload"
  key_links:
    - from: "scripts/pipeline-stages.sh"
      to: "config/nginx/nginx.conf"
      via: "docker exec nginx -s reload 触发 DNS 重新解析"
      pattern: "resolver.*127\\.0\\.0\\.11"
---

<objective>
修复 nginx infra Pipeline --force-recreate 后 DNS 解析失败导致的容器重启循环。

Purpose: nginx upstream 块使用 Docker DNS 容器名称（如 findclass-ssr-blue:3001）。--force-recreate 重建 nginx 容器后，Docker DNS 可能未就绪导致解析失败、502 错误、restart 循环。添加 resolver 指令 + 部署后 reload 解决此问题。

Output: 修改后的 nginx.conf（添加 resolver）和 pipeline-stages.sh（添加 sleep + reload 步骤）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-nginx-blue-green/46-CONTEXT.md
@.planning/phases/46-nginx-blue-green/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: nginx.conf 添加 Docker DNS resolver 指令（per D-01）</name>
  <files>config/nginx/nginx.conf</files>
  <read_first>
    - config/nginx/nginx.conf（当前完整内容，确认插入位置）
  </read_first>
  <action>
在 `config/nginx/nginx.conf` 的 `http { }` 块中，在 `default_type application/octet-stream;` 这行之后，添加 2 行：

```nginx
    # Docker 内置 DNS 解析器（per D-01）
    resolver 127.0.0.11 valid=30s;
    resolver_timeout 5s;
```

完整的 http 块开头应为：
```nginx
http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Docker 内置 DNS 解析器（per D-01）
    resolver 127.0.0.11 valid=30s;
    resolver_timeout 5s;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
    ...
```

不要修改文件中的任何其他内容。
  </action>
  <verify>
    <automated>grep -c 'resolver 127.0.0.11 valid=30s' config/nginx/nginx.conf && grep -c 'resolver_timeout 5s' config/nginx/nginx.conf</automated>
  </verify>
  <done>nginx.conf 的 http 块包含 `resolver 127.0.0.11 valid=30s;` 和 `resolver_timeout 5s;` 两行，位于 default_type 之后、log_format 之前</done>
  <acceptance_criteria>
    - `config/nginx/nginx.conf` 包含 `resolver 127.0.0.11 valid=30s;`
    - `config/nginx/nginx.conf` 包含 `resolver_timeout 5s;`
    - 两个指令都在 `http { }` 块内
    - `resolver` 行出现在 `default_type` 之后
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: pipeline_deploy_nginx() 添加部署后 DNS 刷新步骤（per D-02）</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    - scripts/pipeline-stages.sh（line 655-670，pipeline_deploy_nginx() 函数当前实现）
    - scripts/manage-containers.sh（line 281-289，reload_nginx() 已有模式参考）
  </read_first>
  <action>
修改 `scripts/pipeline-stages.sh` 中的 `pipeline_deploy_nginx()` 函数。

在 `docker compose ... up -d --force-recreate --no-deps nginx` 这行之后、`log_success "Nginx 重建完成"` 之前，插入以下代码块：

```bash
    # 等待 Docker DNS 就绪（per D-02）
    log_info "等待 Docker DNS 就绪..."
    sleep 5

    # 触发 DNS 重新解析（per D-02）
    log_info "触发 nginx DNS 重新解析..."
    if ! docker exec noda-infra-nginx nginx -s reload; then
        log_error "nginx reload 失败，DNS 可能未就绪"
        return 1
    fi
    log_success "nginx DNS 刷新完成"
```

修改后的完整函数应为：
```bash
pipeline_deploy_nginx()
{
    log_info "Nginx 重建部署（docker compose recreate）"

    # 保存当前镜像 digest（用于回滚）
    INFRA_ROLLBACK_IMAGE=$(docker inspect --format='{{.Image}}' noda-infra-nginx 2>/dev/null || echo "")
    export INFRA_ROLLBACK_IMAGE
    if [ -n "$INFRA_ROLLBACK_IMAGE" ]; then
        log_info "保存当前镜像: ${INFRA_ROLLBACK_IMAGE:0:12}..."
    fi

    docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml \
        up -d --force-recreate --no-deps nginx

    # 等待 Docker DNS 就绪（per D-02）
    log_info "等待 Docker DNS 就绪..."
    sleep 5

    # 触发 DNS 重新解析（per D-02）
    log_info "触发 nginx DNS 重新解析..."
    if ! docker exec noda-infra-nginx nginx -s reload; then
        log_error "nginx reload 失败，DNS 可能未就绪"
        return 1
    fi
    log_success "nginx DNS 刷新完成"

    log_success "Nginx 重建完成"
}
```

注意：
- 不修改 `pipeline_deploy_noda_ops()` 函数（per CONTEXT Claude's Discretion，noda-ops 不在 nginx upstream 中被引用，不需要 reload）
- 使用 `return 1` 而非 `exit 1`（Pipeline 函数必须返回非零而非终止进程）
- sleep 5 秒是保守值（Docker DNS 通常 1-2 秒就绪，但 5 秒提供了安全余量）
  </action>
  <verify>
    <automated>grep -c 'sleep 5' scripts/pipeline-stages.sh && grep -c 'docker exec noda-infra-nginx nginx -s reload' scripts/pipeline-stages.sh && grep -c 'log_error "nginx reload 失败' scripts/pipeline-stages.sh</automated>
  </verify>
  <done>pipeline_deploy_nginx() 在 docker compose up --force-recreate 后包含：sleep 5 秒等待 + docker exec nginx -s reload + 失败返回 1</done>
  <acceptance_criteria>
    - `scripts/pipeline-stages.sh` 中 `pipeline_deploy_nginx()` 函数体包含 `sleep 5`
    - `scripts/pipeline-stages.sh` 中 `pipeline_deploy_nginx()` 函数体包含 `docker exec noda-infra-nginx nginx -s reload`
    - `scripts/pipeline-stages.sh` 中 `pipeline_deploy_nginx()` 函数体包含 reload 失败时的 `return 1`
    - reload 步骤在 `docker compose ... up` 之后、`log_success "Nginx 重建完成"` 之前
    - `pipeline_deploy_noda_ops()` 函数未被修改
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins → docker exec | Jenkins 用户通过 docker exec 发送 nginx reload 信号，需确保 nginx 容器正在运行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | E (Elevation) | docker exec reload | accept | reload 是信号发送，不涉及权限提升；Jenkins 用户已在 docker 组中，权限已收敛（Phase 31） |
| T-46-02 | S (Spoofing) | resolver 127.0.0.11 | accept | Docker DNS 127.0.0.11 是容器内部 DNS，不暴露到外部网络，无法被外部欺骗 |

本 phase 修改无安全影响：resolver 指向 Docker 内部 DNS，reload 是标准运维操作，无新增网络暴露面。
</threat_model>

<verification>
1. `grep 'resolver 127.0.0.11 valid=30s' config/nginx/nginx.conf` — 确认 resolver 指令存在
2. `grep 'resolver_timeout 5s' config/nginx/nginx.conf` — 确认 timeout 配置存在
3. `grep -A 20 'pipeline_deploy_nginx()' scripts/pipeline-stages.sh | grep 'sleep 5'` — 确认 sleep 步骤存在
4. `grep -A 20 'pipeline_deploy_nginx()' scripts/pipeline-stages.sh | grep 'nginx -s reload'` — 确认 reload 步骤存在
5. 端到端验证需要手动触发 Jenkins infra Pipeline 部署 nginx（不在本 plan 自动化范围内）
</verification>

<success_criteria>
1. config/nginx/nginx.conf http 块包含 `resolver 127.0.0.11 valid=30s;` 和 `resolver_timeout 5s;`
2. pipeline_deploy_nginx() 在 docker compose up --force-recreate 后执行 sleep 5 + nginx -s reload
3. reload 失败时函数返回非零（return 1）
4. pipeline_deploy_noda_ops() 未被修改
5. 两个文件修改均通过 git diff 验证
</success_criteria>

<output>
After completion, create `.planning/phases/46-nginx-blue-green/46-01-SUMMARY.md`
</output>
