---
phase: 38-quality-assurance
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - "scripts/**/*.sh（约 60 个文件，排除 deploy-findclass-zero-deps.sh）"
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "所有 60 个活跃 .sh 文件通过 shfmt 格式化后风格一致"
    - "格式化后 ShellCheck 仍然 0 error"
    - "格式化后所有脚本 bash -n 语法检查通过"
    - "DEPRECATED 脚本（deploy-findclass-zero-deps.sh）未被修改"
  artifacts:
    - path: "scripts/**/*.sh"
      provides: "格式化后的 shell 脚本"
      verify: "shfmt -d scripts/ 输出为空（无差异）"
  key_links:
    - from: ".editorconfig"
      to: "scripts/**/*.sh"
      via: "shfmt -w 读取 .editorconfig 配置格式化所有脚本"
      pattern: "shfmt -w"
    - from: ".shellcheckrc"
      to: "scripts/**/*.sh"
      via: "shellcheck 读取 .shellcheckrc 验证格式化后脚本"
      pattern: "shellcheck scripts/"
---

<objective>
使用 shfmt 批量格式化所有活跃 shell 脚本，并通过 ShellCheck 和 bash -n 双重验证。

Purpose: 完成本阶段核心目标 -- 所有 .sh 文件代码风格一致（4 空格缩进、case 缩进、函数大括号另起行），且 ShellCheck 维持 0 error。

Output: 60 个 .sh 文件格式化完成，通过 shellcheck + bash -n 验证
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-quality-assurance/38-CONTEXT.md
@.planning/phases/38-quality-assurance/38-RESEARCH.md
@.planning/phases/38-quality-assurance/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shfmt 批量格式化所有活跃脚本（per D-03, D-05）</name>
  <files>scripts/**/*.sh（约 60 个文件）</files>
  <read_first>
    - .editorconfig（Plan 01 创建的配置文件，确认 shfmt 配置参数）
    - .shellcheckrc（Plan 01 创建的配置文件）
  </read_first>
  <action>
按照以下步骤依次执行：

**步骤 1 — 格式化前快照**
记录当前状态用于对比：
```bash
echo "=== 格式化前 ==="
shfmt -d scripts/ 2>&1 | wc -l
echo "预期: 大量差异行"
```

**步骤 2 — 检查 .editorconfig ignore 是否对 DEPRECATED 脚本生效**
```bash
# 验证 shfmt 不会格式化 DEPRECATED 文件
shfmt -d scripts/deploy/deploy-findclass-zero-deps.sh 2>&1
# 如果输出为空，说明 ignore 规则生效
# 如果有输出，说明 ignore 未生效，需改用 find 排除
```

如果 .editorconfig ignore 未生效（A3 假设风险），改用 find 方式：
```bash
find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -w {} +
```

如果 ignore 生效，直接使用：
```bash
shfmt -w scripts/
```

**步骤 3 — 执行格式化**
使用上述确认后的方法执行格式化。`shfmt -w` 会原地修改文件。

**步骤 4 — 验证格式化结果**
1. `shfmt -d scripts/ 2>&1 | wc -l` — 应输出 0（无剩余差异），但需排除 DEPRECATED 文件：
   ```bash
   # 如果用 find 排除方式
   find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \; 2>&1 | wc -l
   # 如果 .editorconfig ignore 生效
   shfmt -l scripts/ | wc -l  # 列出仍需格式化的文件，应为 0
   ```

2. `shellcheck -S error scripts/**/*.sh` — 仍然 0 error（exit code 0）

3. `find scripts/ -name '*.sh' -exec bash -n {} \;` — 所有 61 个文件语法检查通过

4. 验证 DEPRECATED 文件未被修改：
   ```bash
   git diff --name-only scripts/deploy/deploy-findclass-zero-deps.sh
   # 应无输出（未被修改）
   ```

**步骤 5 — 审查变更**
```bash
git diff --stat scripts/
```
检查变更统计是否合理：
- 大约 57 个文件的函数大括号位置会改变（`name() {` → `name()` + `{` 另起行）
- 6 个文件的缩进从 2 空格变为 4 空格
- 其他格式差异（空格、引号规范化等）
  </action>
  <verify>
    <automated>find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \; 2>&1 | wc -l && shellcheck -S error scripts/**/*.sh && find scripts/ -name '*.sh' -exec bash -n {} \;</automated>
  </verify>
  <acceptance_criteria>
    - `find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \;` 无任何输出（所有活跃文件格式一致）
    - `shellcheck -S error scripts/**/*.sh` exit code 为 0（0 error）
    - `find scripts/ -name '*.sh' -exec bash -n {} \;` 无任何错误输出（61 个文件全部通过语法检查）
    - `git diff --name-only scripts/deploy/deploy-findclass-zero-deps.sh` 无输出（DEPRECATED 文件未被修改）
  </acceptance_criteria>
  <done>60 个活跃 .sh 文件格式一致，ShellCheck 0 error，bash -n 全通过，DEPRECATED 文件未变更</done>
</task>

<task type="auto">
  <name>Task 2: 最终验证 + 提交格式化变更</name>
  <files>无新文件（验证 + 提交）</files>
  <read_first>
    - .editorconfig
    - .shellcheckrc
  </read_first>
  <action>
**步骤 1 — 运行完整验证套件**
```bash
# 1. ShellCheck 零 error
shellcheck -S error scripts/**/*.sh
echo "ShellCheck error check: $?"

# 2. ShellCheck warning 级别检查（确认抑制规则生效）
shellcheck -S warning scripts/**/*.sh 2>&1 | grep -c "SC2034\|SC2155"
# 预期: 0（已被 .shellcheckrc 抑制）

# 3. shfmt 格式一致性
find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \; 2>&1 | wc -l
# 预期: 0

# 4. bash -n 语法检查
find scripts/ -name '*.sh' -exec bash -n {} \;
echo "bash -n check: $?"
# 预期: 0

# 5. DEPRECATED 文件未修改
git diff --stat scripts/deploy/deploy-findclass-zero-deps.sh
# 预期: 无输出
```

**步骤 2 — 审查 git diff**
```bash
git diff --stat
```
确认变更范围合理：
- .editorconfig（Plan 01 已提交，可能不在 diff 中）
- .shellcheckrc（Plan 01 已提交，可能不在 diff 中）
- scripts/ 下约 57-60 个 .sh 文件

**步骤 3 — 提交**
如果 Plan 01 和 Plan 02 在同一执行周期内，合并提交：
```bash
git add .editorconfig .shellcheckrc scripts/
git commit -m "style(38): add .editorconfig + .shellcheckrc + shfmt format all scripts"
```

如果 Plan 01 已单独提交，仅提交格式化变更：
```bash
git add scripts/
git commit -m "style(38): shfmt format all shell scripts (4-space indent, function-next-line)"
```
  </action>
  <verify>
    <automated>shellcheck -S error scripts/**/*.sh && find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \; 2>&1 | wc -l && find scripts/ -name '*.sh' -exec bash -n {} \;</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck -S error scripts/**/*.sh` exit code 0
    - `shellcheck -S warning scripts/**/*.sh 2>&1 | grep -c "SC2034\|SC2155"` 输出 0
    - `find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \;` 无输出
    - `find scripts/ -name '*.sh' -exec bash -n {} \;` 无错误输出
    - git log 最新提交包含 "style(38)" 消息
  </acceptance_criteria>
  <done>所有验证通过，格式化变更已提交</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 格式化工具 → 源码 | shfmt 基于 AST 格式化，不应改变逻辑，但 heredoc/字符串边界需验证 |
| 配置文件 → 工具行为 | .editorconfig 和 .shellcheckrc 控制工具行为，配置正确性是信任基础 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Tampering | shfmt 格式化破坏 heredoc | mitigate | 格式化后 bash -n 逐文件验证（61 个文件） |
| T-38-04 | Tampering | shfmt 格式化破坏字符串内容 | mitigate | shfmt 基于 AST 不会修改字符串内容；bash -n 验证兜底 |
| T-38-05 | Repudiation | backup 系统脚本被意外破坏 | mitigate | bash -n 验证 + git diff 人工审查 backup/ 目录变更 |
</threat_model>

<verification>
1. `shellcheck -S error scripts/**/*.sh` — 零 error
2. `find scripts/ -name '*.sh' ! -name 'deploy-findclass-zero-deps.sh' -exec shfmt -d {} \;` — 无差异
3. `find scripts/ -name '*.sh' -exec bash -n {} \;` — 全部通过
4. `git diff --name-only scripts/deploy/deploy-findclass-zero-deps.sh` — DEPRECATED 未修改
5. `shellcheck -S warning scripts/**/*.sh 2>&1 | grep -c "SC2034\|SC2155"` — 已抑制（0 匹配）
</verification>

<success_criteria>
- 60 个活跃 .sh 文件 shfmt 格式一致（shfmt -d 无差异）
- ShellCheck 0 error（维持不变）
- 61 个文件 bash -n 语法检查全通过
- DEPRECATED 脚本未被修改
- SC2034 和 SC2155 warning 被 .shellcheckrc 抑制
- 变更已提交
</success_criteria>

<output>
完成后创建 `.planning/phases/38-quality-assurance/38-02-SUMMARY.md`
</output>
