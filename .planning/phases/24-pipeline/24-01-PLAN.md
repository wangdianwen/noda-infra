---
phase: 24-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pipeline-stages.sh
autonomous: true
requirements: [ENH-01, ENH-02, ENH-03]

must_haves:
  truths:
    - "Pipeline Pre-flight 阶段检查备份文件时效性，超过 12 小时阻止部署"
    - "部署成功后 pipeline_purge_cdn 可调用 Cloudflare API 清除 CDN 缓存"
    - "Pipeline Cleanup 阶段自动清理超过 7 天的旧 Docker 镜像"
    - "CDN 清除失败或凭据缺失不阻止部署流程"
  artifacts:
    - path: "scripts/pipeline-stages.sh"
      provides: "check_backup_freshness + pipeline_purge_cdn + 重写 cleanup_old_images"
      contains: "check_backup_freshness()"
    - path: "scripts/pipeline-stages.sh"
      provides: "pipeline_purge_cdn 函数"
      contains: "pipeline_purge_cdn()"
    - path: "scripts/pipeline-stages.sh"
      provides: "时间阈值镜像清理"
      contains: "IMAGE_RETENTION_DAYS"
  key_links:
    - from: "scripts/pipeline-stages.sh#pipeline_preflight"
      to: "check_backup_freshness"
      via: "函数调用（Pre-flight 末尾）"
      pattern: "check_backup_freshness"
    - from: "scripts/pipeline-stages.sh#pipeline_cleanup"
      to: "cleanup_old_images"
      via: "函数调用（无参数，使用 IMAGE_RETENTION_DAYS）"
      pattern: "cleanup_old_images"
---

<objective>
在 pipeline-stages.sh 中实现三项增强函数：备份时效性检查、CDN 缓存清除、时间阈值镜像清理。

Purpose: 为 Pipeline 增加部署安全保障（备份检查）和运维自动化（CDN 清除 + 镜像清理），所有逻辑封装在 bash 函数中，Jenkinsfile 在 Plan 02 中集成调用。

Output: pipeline-stages.sh 包含三个新/重写函数，pipeline_preflight 末尾集成备份检查，pipeline_cleanup 使用新的镜像清理逻辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-pipeline/24-CONTEXT.md
@.planning/phases/24-pipeline/24-RESEARCH.md

@scripts/pipeline-stages.sh
@scripts/lib/log.sh
@scripts/backup/lib/config.sh
@scripts/backup/lib/util.sh

<interfaces>
<!-- 现有接口：executor 需要了解的函数和变量 -->

From scripts/lib/log.sh:
```bash
log_info()     # 黄色 ℹ️ 前缀
log_success()  # 绿色 ✅ 前缀
log_error()    # 红色 ❌ 前缀，输出到 stderr
log_warn()     # 黄色 ⚠️ 前缀
```

From scripts/manage-containers.sh:
```bash
NGINX_CONTAINER="noda-infra-nginx"
NETWORK_NAME="noda-network"
get_container_name()  # 参数: blue|green, 返回容器名
```

From scripts/pipeline-stages.sh (现有结构):
```bash
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"  # 项目根目录
HEALTH_CHECK_MAX_RETRIES=30
HEALTH_CHECK_INTERVAL=4

# 现有函数（按文件顺序）:
http_health_check()    # 容器内 HTTP 健康检查
e2e_verify()           # nginx -> 容器 E2E 验证
cleanup_old_images()   # 第 134-163 行，保留最近 N 个（将被重写）
pipeline_preflight()   # 第 169-234 行，前置检查（末尾插入备份检查）
pipeline_build()       # 构建镜像
pipeline_test()        # 安装依赖
pipeline_deploy()      # 部署容器
pipeline_health_check()# 健康检查
pipeline_switch()      # 切换流量
pipeline_verify()      # E2E 验证
pipeline_cleanup()     # 第 312-315 行，调用 cleanup_old_images 5（需改为无参数）
pipeline_failure_cleanup() # 失败清理
```

备份文件路径模式:
```
docker/volumes/backup/YYYY/MM/DD/{dbname}_{YYYYMMDD}_{HHMMSS}.{dump|sql}
```
示例: docker/volumes/backup/2026/04/11/noda_prod_20260411_210754.dump
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 check_backup_freshness 备份时效性检查函数</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    scripts/pipeline-stages.sh（第 169-234 行 pipeline_preflight 函数）
    scripts/lib/log.sh（日志函数接口）
    scripts/backup/lib/util.sh（get_date_path 返回 YYYY/MM/DD 格式）
    scripts/backup/lib/config.sh（BACKUP_HOST_DIR 默认路径）
  </read_first>
  <action>
在 `scripts/pipeline-stages.sh` 的常量区域（第 26 行 `COMPOSE_FILE=...` 之后）新增常量：

```bash
BACKUP_HOST_DIR="${BACKUP_HOST_DIR:-$PROJECT_ROOT/docker/volumes/backup}"
BACKUP_MAX_AGE_HOURS="${BACKUP_MAX_AGE_HOURS:-12}"
IMAGE_RETENTION_DAYS="${IMAGE_RETENTION_DAYS:-7}"
```

在 `cleanup_old_images()` 函数（第 128 行注释区域）之前新增 `check_backup_freshness()` 函数：

```bash
# ============================================
# 函数: check_backup_freshness
# ============================================
# 检查数据库备份文件是否在指定小时内
# 策略：先检查当天/昨天日期子目录，再回退全目录搜索
# 返回：0=备份新鲜，1=备份过期或不存在
# 环境变量：
#   BACKUP_HOST_DIR - 备份目录（默认 $PROJECT_ROOT/docker/volumes/backup）
#   BACKUP_MAX_AGE_HOURS - 最大允许年龄小时数（默认 12）
check_backup_freshness() {
  local backup_dir="${BACKUP_HOST_DIR:-$PROJECT_ROOT/docker/volumes/backup}"
  local max_age_hours="${BACKUP_MAX_AGE_HOURS:-12}"

  # 策略：先检查当天目录，再检查前一天（D-04）
  local today today_minus1
  today=$(date +"%Y/%m/%d")
  today_minus1=$(date -d "yesterday" +"%Y/%m/%d")

  local newest_file=""
  for search_dir in "$backup_dir/$today" "$backup_dir/$today_minus1"; do
    if [ -d "$search_dir" ]; then
      newest_file=$(find "$search_dir" -type f \( -name "*.dump" -o -name "*.sql" \) -printf '%T@ %p\n' \
        2>/dev/null | sort -rn | head -1 | cut -d' ' -f2-)
      [ -n "$newest_file" ] && break
    fi
  done

  # 回退：全目录搜索最新备份文件
  if [ -z "$newest_file" ]; then
    newest_file=$(find "$backup_dir" -type f \( -name "*.dump" -o -name "*.sql" \) -printf '%T@ %p\n' \
      2>/dev/null | sort -rn | head -1 | cut -d' ' -f2-)
  fi

  if [ -z "$newest_file" ]; then
    log_error "未找到任何备份文件（查找路径: $backup_dir）"
    return 1
  fi

  # 计算文件年龄（秒 -> 小时），使用 GNU stat（Linux 生产环境）
  local file_epoch now_epoch age_seconds age_hours
  file_epoch=$(stat -c%Y "$newest_file")
  now_epoch=$(date +%s)
  age_seconds=$((now_epoch - file_epoch))
  age_hours=$((age_seconds / 3600))

  if [ "$age_hours" -ge "$max_age_hours" ]; then
    log_error "备份已过期 ${age_hours} 小时（阈值: ${max_age_hours} 小时）"
    log_error "最新备份: $newest_file"
    return 1
  fi

  log_info "备份检查通过: 最新备份 ${age_hours} 小时前（阈值: ${max_age_hours} 小时）"
  return 0
}
```

然后在 `pipeline_preflight()` 函数末尾（第 233 行 `log_success "前置检查全部通过"` 之前）插入备份检查调用（per D-19，集成到现有 Pre-flight 阶段，不新增阶段）：

```bash
  # 备份时效性检查（D-01, D-19: 放在所有其他检查之后）
  check_backup_freshness || return 1
```

设计要点：
- 备份检查在 pipeline_preflight 的**最末尾**（所有 Docker/nginx/network/Node.js/pnpm 检查之后），确保基础设施正常后才检查备份（per RESEARCH Pitfall 6）
- 使用 `|| return 1` 确保超 12 小时时阻止部署（per D-03）
- `find -printf '%T@ %p\n'` 利用 GNU find 特有功能按修改时间排序（per D-02/D-04）
- `stat -c%Y` 使用 GNU stat 获取 epoch 时间戳（仅 Linux 生产环境，per RESEARCH Pitfall 1）
  </action>
  <verify>
    <automated>bash -n scripts/pipeline-stages.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pipeline-stages.sh` 包含字符串 `check_backup_freshness()`
    - `scripts/pipeline-stages.sh` 包含常量 `BACKUP_HOST_DIR`
    - `scripts/pipeline-stages.sh` 包含常量 `BACKUP_MAX_AGE_HOURS`
    - `scripts/pipeline-stages.sh` 中 `pipeline_preflight` 函数包含 `check_backup_freshness || return 1` 调用
    - `scripts/pipeline-stages.sh` 中备份检查调用位于 `log_success "前置检查全部通过"` 之前
    - `bash -n scripts/pipeline-stages.sh` 退出码为 0
  </acceptance_criteria>
  <done>check_backup_freshness 函数实现完毕，pipeline_preflight 末尾集成调用，bash 语法检查通过</done>
</task>

<task type="auto">
  <name>Task 2: 实现 pipeline_purge_cdn CDN 缓存清除函数</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    scripts/pipeline-stages.sh（全文件，特别是 pipeline_cleanup 和 source guard 之间的位置）
    scripts/lib/log.sh（日志函数接口）
  </read_first>
  <action>
在 `scripts/pipeline-stages.sh` 的 `pipeline_verify()` 函数（约第 305-310 行）之后、`pipeline_cleanup()` 函数之前，新增 `pipeline_purge_cdn()` 函数（per D-08：在 Verify 后、Cleanup 前执行）：

```bash
# pipeline_purge_cdn - 调用 Cloudflare API 清除 CDN 缓存
# 环境变量（由 Jenkins withCredentials 注入）：
#   CF_API_TOKEN - Cloudflare API Token
#   CF_ZONE_ID   - Cloudflare Zone ID
# 返回：0=成功或跳过（永远不阻止部署，per D-09）
pipeline_purge_cdn() {
  # 凭据缺失时跳过（D-11）
  if [ -z "${CF_API_TOKEN:-}" ] || [ -z "${CF_ZONE_ID:-}" ]; then
    log_warn "Cloudflare 凭据未配置，跳过 CDN 缓存清除"
    return 0
  fi

  log_info "清除 CDN 缓存 (zone: $CF_ZONE_ID)..."

  local http_code
  http_code=$(curl -s -o /dev/null -w "%{http_code}" \
    -X POST "https://api.cloudflare.com/client/v4/zones/${CF_ZONE_ID}/purge_cache" \
    -H "Authorization: Bearer ${CF_API_TOKEN}" \
    -H "Content-Type: application/json" \
    --data '{"purge_everything":true}' \
    --connect-timeout 10 \
    --max-time 30 2>/dev/null) || true

  if [ "$http_code" = "200" ]; then
    log_success "CDN 缓存清除完成"
  else
    # D-09: 失败不阻止部署
    log_error "CDN 缓存清除失败 (HTTP ${http_code:-timeout})，不影响部署"
  fi

  return 0
}
```

设计要点：
- 函数始终返回 0（per D-09），CDN 清除失败不影响部署
- 凭据缺失时跳过并警告（per D-11），支持 Jenkins 凭据未配置的场景
- 使用 `purge_everything: true`（per D-10），简单可靠
- curl 使用 `--connect-timeout 10 --max-time 30` 防止长时间阻塞
- 不在日志中输出 CF_API_TOKEN 值（安全：Information Disclosure 防护）
- `2>/dev/null` 和 `|| true` 确保 curl 失败不会触发 `set -e`
  </action>
  <verify>
    <automated>bash -n scripts/pipeline-stages.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pipeline-stages.sh` 包含字符串 `pipeline_purge_cdn()`
    - `scripts/pipeline-stages.sh` 包含 `CF_API_TOKEN` 和 `CF_ZONE_ID` 环境变量检查
    - `scripts/pipeline-stages.sh` 包含 `purge_everything` JSON payload
    - `scripts/pipeline-stages.sh` 中 `pipeline_purge_cdn` 函数内 `return 0` 出现在函数末尾（非阻塞）
    - `scripts/pipeline-stages.sh` 中 `pipeline_purge_cdn` 位于 `pipeline_verify` 之后、`pipeline_cleanup` 之前
    - `bash -n scripts/pipeline-stages.sh` 退出码为 0
  </acceptance_criteria>
  <done>pipeline_purge_cdn 函数实现完毕，凭据缺失时跳过，API 失败不阻止部署，bash 语法检查通过</done>
</task>

<task type="auto">
  <name>Task 3: 重写 cleanup_old_images 为时间阈值清理 + 更新 pipeline_cleanup 调用</name>
  <files>scripts/pipeline-stages.sh</files>
  <read_first>
    scripts/pipeline-stages.sh（第 128-163 行现有 cleanup_old_images 函数 + 第 312-315 行 pipeline_cleanup 函数）
  </read_first>
  <action>
将 `scripts/pipeline-stages.sh` 中现有的 `cleanup_old_images()` 函数（第 128-163 行）完全替换为以下时间阈值版本（per D-12）：

```bash
# ============================================
# 函数: cleanup_old_images
# ============================================
# 删除超过指定天数的旧镜像和 dangling images（per D-12）
# 原逻辑：保留最近 N 个 → 新逻辑：删除超过 7 天的
# 参数：
#   $1: 保留天数（可选，默认使用 IMAGE_RETENTION_DAYS 变量）
# 环境变量：
#   IMAGE_RETENTION_DAYS - 镜像保留天数（默认 7，per D-13）
cleanup_old_images() {
  local retention_days="${IMAGE_RETENTION_DAYS:-${1:-7}}"

  log_info "镜像清理: 删除超过 ${retention_days} 天的旧镜像..."

  local cutoff_epoch
  cutoff_epoch=$(date -d "${retention_days} days ago" +%s)

  # 1. 清理带 Git SHA 标签的旧镜像（排除 latest，per D-14）
  local sha_tags
  sha_tags=$(docker images findclass-ssr --format '{{.Tag}}' \
    | grep -v '^latest$' \
    | grep -v '^<none>' || true)

  local deleted=0
  for tag in $sha_tags; do
    # 使用 docker inspect 获取 ISO 8601 创建时间（per RESEARCH: docker images CreatedAt 格式不稳定）
    local created_iso
    created_iso=$(docker inspect --format '{{.Created}}' "findclass-ssr:${tag}" 2>/dev/null || echo "")

    if [ -z "$created_iso" ]; then
      continue
    fi

    # 将 ISO 8601 转为 epoch
    local image_epoch
    image_epoch=$(date -d "$created_iso" +%s 2>/dev/null || echo "0")

    if [ "$image_epoch" -eq 0 ]; then
      continue
    fi

    if [ "$image_epoch" -lt "$cutoff_epoch" ]; then
      log_info "  删除 findclass-ssr:${tag} ($(date -d "@$image_epoch" +"%Y-%m-%d"))"
      docker rmi "findclass-ssr:${tag}" 2>/dev/null || true
      deleted=$((deleted + 1))
    fi
  done

  # 2. 清理 dangling images（per D-15）
  local dangling_ids
  dangling_ids=$(docker images -f "dangling=true" --format '{{.ID}}' 2>/dev/null || true)
  for img_id in $dangling_ids; do
    docker rmi "$img_id" 2>/dev/null || true
    deleted=$((deleted + 1))
  done

  if [ "$deleted" -gt 0 ]; then
    log_success "镜像清理完成: 删除 ${deleted} 个镜像"
  else
    log_info "镜像清理: 无需清理"
  fi
}
```

然后更新 `pipeline_cleanup()` 函数（约第 312-315 行），将 `cleanup_old_images 5` 改为 `cleanup_old_images`（per D-16：无参数，使用 IMAGE_RETENTION_DAYS 默认值）：

```bash
# pipeline_cleanup - 清理旧镜像
pipeline_cleanup() {
  cleanup_old_images
}
```

设计要点：
- 使用 `docker inspect --format '{{.Created}}'` 获取 ISO 8601 时间（per RESEARCH: docker images CreatedAt 因 locale 变化不可靠）
- `date -d "$created_iso" +%s` 将 ISO 8601 转为 epoch 进行数值比较
- 仅清理 `findclass-ssr` 仓库的 SHA 标签镜像（grep 排除 latest 和 none，per D-14）
- dangling images 全部清理（docker rmi 逐个删除，不使用 docker image prune，per RESEARCH Pitfall 5 避免影响其他服务）
- `2>/dev/null || true` 确保 docker rmi 失败不中断清理循环
  </action>
  <verify>
    <automated>bash -n scripts/pipeline-stages.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/pipeline-stages.sh` 中 `cleanup_old_images` 函数包含 `IMAGE_RETENTION_DAYS` 变量引用
    - `scripts/pipeline-stages.sh` 中 `cleanup_old_images` 函数包含 `docker inspect --format '{{.Created}}'`
    - `scripts/pipeline-stages.sh` 中 `cleanup_old_images` 函数包含 `cutoff_epoch` 变量（时间阈值计算）
    - `scripts/pipeline-stages.sh` 中 `cleanup_old_images` 函数包含 `grep -v '^latest$'`（排除 latest 标签）
    - `scripts/pipeline-stages.sh` 中 `cleanup_old_images` 函数包含 dangling images 清理逻辑（`dangling=true`）
    - `scripts/pipeline-stages.sh` 中 `pipeline_cleanup` 函数调用 `cleanup_old_images`（无参数）
    - `scripts/pipeline-stages.sh` 中不再包含旧逻辑 `cleanup_old_images 5`
    - `bash -n scripts/pipeline-stages.sh` 退出码为 0
  </acceptance_criteria>
  <done>cleanup_old_images 重写为时间阈值版本，pipeline_cleanup 调用更新，bash 语法检查通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins Pipeline -> Cloudflare API | API Token 通过 HTTPS + Bearer 认证传输，Pipeline 不可信侧不应泄露凭据 |
| Pipeline -> 备份文件系统 | 读取备份文件 mtime，路径通过固定默认值约束，不接受外部输入 |
| Pipeline -> Docker daemon | 清理旧镜像操作，已有 docker 组权限约束 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | pipeline_purge_cdn | mitigate | CF_API_TOKEN 不输出到日志（log_info/log_error 中不引用 token 值）；Jenkins withCredentials 自动遮蔽敏感值；使用单引号 sh 块防止 Groovy 插值 |
| T-24-02 | Tampering | check_backup_freshness | accept | BACKUP_HOST_DIR 使用固定默认值，不接受命令行参数注入；仅 stat 读取 mtime 不修改文件 |
| T-24-03 | Denial of Service | cleanup_old_images | accept | docker rmi 逐个删除且 `|| true`，即使失败也不影响 Pipeline；dangling images 清理不影响正在运行的容器 |
| T-24-04 | Information Disclosure | Cloudflare API response | accept | curl 输出 `-o /dev/null` 丢弃响应体（可能包含 zone 配置信息）；仅捕获 HTTP 状态码 |
</threat_model>

<verification>
1. `bash -n scripts/pipeline-stages.sh` — 语法检查通过
2. `grep -c 'check_backup_freshness' scripts/pipeline-stages.sh` — 至少出现 3 次（函数定义 + 常量引用 + pipeline_preflight 调用）
3. `grep -c 'pipeline_purge_cdn' scripts/pipeline-stages.sh` — 至少出现 1 次（函数定义）
4. `grep -c 'IMAGE_RETENTION_DAYS' scripts/pipeline-stages.sh` — 至少出现 2 次（常量定义 + cleanup_old_images 引用）
5. `grep 'cleanup_old_images 5' scripts/pipeline-stages.sh` — 应返回空（旧调用已移除）
</verification>

<success_criteria>
1. `bash -n scripts/pipeline-stages.sh` 退出码为 0
2. pipeline-stages.sh 包含三个新/重写函数：check_backup_freshness、pipeline_purge_cdn、cleanup_old_images（时间阈值版本）
3. pipeline_preflight 末尾调用 check_backup_freshness（阻止型）
4. pipeline_purge_cdn 始终返回 0（非阻止型）
5. pipeline_cleanup 调用 cleanup_old_images 无参数（使用 IMAGE_RETENTION_DAYS 默认值 7）
6. 无硬编码敏感信息（CF_API_TOKEN 值不出现在代码中）
</success_criteria>

<output>
After completion, create `.planning/phases/24-pipeline/24-01-SUMMARY.md`
</output>
