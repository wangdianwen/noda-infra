---
phase: 28-keycloak
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - scripts/keycloak-blue-green-deploy.sh
  - scripts/deploy/deploy-infrastructure-prod.sh
autonomous: true
requirements:
  - KCBLUE-04
  - KCBLUE-03

must_haves:
  truths:
    - "keycloak-blue-green-deploy.sh 可执行完整的 Keycloak 蓝绿部署流程（pull -> stop old -> start new -> health check -> switch -> verify -> cleanup）"
    - "deploy-infrastructure-prod.sh 不再尝试通过 docker compose 启动 keycloak 服务"
    - "Keycloak 从 compose 迁移到 docker run 蓝绿管理的流程可通过 init 子命令完成"
  artifacts:
    - path: "scripts/keycloak-blue-green-deploy.sh"
      provides: "Keycloak 蓝绿部署脚本"
      min_lines: 80
      exports: ["main"]
    - path: "scripts/deploy/deploy-infrastructure-prod.sh"
      provides: "移除 keycloak 从 START_SERVICES"
  key_links:
    - from: "scripts/keycloak-blue-green-deploy.sh"
      to: "scripts/manage-containers.sh"
      via: "source 加载 run_container/update_upstream/wait_container_healthy 等函数"
      pattern: "source.*manage-containers"
    - from: "scripts/keycloak-blue-green-deploy.sh"
      to: "docker/env-keycloak.env"
      via: "manage-containers.sh 的 ENV_TEMPLATE 变量"
      pattern: "SERVICE_NAME=keycloak"
---

<objective>
创建 Keycloak 蓝绿部署脚本（keycloak-blue-green-deploy.sh），并更新 deploy-infrastructure-prod.sh 排除 keycloak。

Purpose: Keycloak 需要独立的部署脚本处理官方镜像拉取（无构建步骤）、Keycloak 特有健康检查（/health/ready、更长启动超时）和部署流程。同时需要将 keycloak 从 compose 部署脚本中移除，避免与蓝绿 docker run 管理冲突。
Output: keycloak-blue-green-deploy.sh 脚本、更新后的 deploy-infrastructure-prod.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-keycloak/28-CONTEXT.md
@.planning/phases/28-keycloak/28-RESEARCH.md
@.planning/phases/28-keycloak/28-01-SUMMARY.md

<interfaces>
<!-- manage-containers.sh 环境变量覆盖（Plan 01 新增） -->
CONTAINER_MEMORY     # 默认 512m, Keycloak 用 1g
CONTAINER_MEMORY_RESERVATION  # 默认 128m, Keycloak 用 512m
CONTAINER_READONLY   # 默认 true, Keycloak 用 false
EXTRA_DOCKER_ARGS    # 额外 docker run 参数
ENVSUBST_VARS        # envsubst 替换的变量列表
SERVICE_GROUP        # 默认 apps, Keycloak 用 infra

<!-- manage-containers.sh 可用函数 -->
source scripts/manage-containers.sh  # 加载后可调用：
# get_active_env, get_inactive_env, set_active_env
# get_container_name, is_container_running
# run_container, update_upstream, reload_nginx
# wait_container_healthy (from health.sh)

<!-- Keycloak 蓝绿参数 -->
SERVICE_NAME=keycloak
SERVICE_PORT=8080
UPSTREAM_NAME=keycloak_backend
HEALTH_PATH=/health/ready
ACTIVE_ENV_FILE=/opt/noda/active-env-keycloak
UPSTREAM_CONF=$PROJECT_ROOT/config/nginx/snippets/upstream-keycloak.conf
IMAGE_NAME=quay.io/keycloak/keycloak:26.2.3
CONTAINER_MEMORY=1g
CONTAINER_MEMORY_RESERVATION=512m
CONTAINER_READONLY=false
SERVICE_GROUP=infra
ENVSUBST_VARS='${POSTGRES_USER} ${POSTGRES_PASSWORD} ${KEYCLOAK_ADMIN_USER} ${KEYCLOAK_ADMIN_PASSWORD} ${SMTP_HOST} ${SMTP_PORT} ${SMTP_FROM} ${SMTP_USER} ${SMTP_PASSWORD}'

<!-- deploy-infrastructure-prod.sh 当前 START_SERVICES -->
START_SERVICES="postgres keycloak nginx noda-ops"
<!-- 需要改为 -->
START_SERVICES="postgres nginx noda-ops"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 keycloak-blue-green-deploy.sh 部署脚本</name>
  <files>scripts/keycloak-blue-green-deploy.sh</files>
  <read_first>
    scripts/blue-green-deploy.sh
    scripts/manage-containers.sh
    scripts/pipeline-stages.sh
    docker/docker-compose.prod.yml (L57-109, Keycloak 配置)
    docker/env-findclass-ssr.env
  </read_first>
  <action>
创建 `scripts/keycloak-blue-green-deploy.sh`，参照 `scripts/blue-green-deploy.sh` 但适配 Keycloak 特性。

Keycloak 与 findclass-ssr 的关键差异：
1. 无构建步骤 — 使用官方镜像 `quay.io/keycloak/keycloak:26.2.3`
2. 无测试步骤 — 不从源码构建
3. 健康检查路径不同 — `/health/ready`（per D-01）
4. 启动时间更长 — start_period 至少 90s（RESEARCH Pitfall 3）
5. 需要主题卷挂载 — `/opt/keycloak/themes/noda:ro`
6. 需要 data tmpfs — `/opt/keycloak/data`
7. 更高内存 — 1G limit / 512M reservation

脚本结构：
```bash
#!/bin/bash
set -euo pipefail

# ============================================
# Keycloak 蓝绿部署脚本
# ============================================
# 功能：Keycloak 官方镜像的零停机蓝绿部署
# 步骤：Pull Image -> Stop Old -> Start New -> Health Check -> Switch -> E2E Verify -> Cleanup
# 用途：手动部署或由 Jenkinsfile.keycloak 调用
# 依赖：scripts/manage-containers.sh（参数化复用）
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

source "$PROJECT_ROOT/scripts/lib/log.sh"
source "$PROJECT_ROOT/scripts/lib/health.sh"
source "$PROJECT_ROOT/scripts/manage-containers.sh"

# ============================================
# Keycloak 蓝绿参数（覆盖 manage-containers.sh 默认值）
# ============================================
export SERVICE_NAME="${SERVICE_NAME:-keycloak}"
export SERVICE_PORT="${SERVICE_PORT:-8080}"
export UPSTREAM_NAME="${UPSTREAM_NAME:-keycloak_backend}"
export HEALTH_PATH="${HEALTH_PATH:-/health/ready}"
export ACTIVE_ENV_FILE="${ACTIVE_ENV_FILE:-/opt/noda/active-env-keycloak}"
export UPSTREAM_CONF="${UPSTREAM_CONF:-$PROJECT_ROOT/config/nginx/snippets/upstream-keycloak.conf}"
export SERVICE_GROUP="${SERVICE_GROUP:-infra}"

# Keycloak 容器安全参数（覆盖 run_container 默认值）
export CONTAINER_MEMORY="${CONTAINER_MEMORY:-1g}"
export CONTAINER_MEMORY_RESERVATION="${CONTAINER_MEMORY_RESERVATION:-512m}"
export CONTAINER_READONLY="${CONTAINER_READONLY:-false}"

# Keycloak 额外 docker run 参数
export EXTRA_DOCKER_ARGS="${EXTRA_DOCKER_ARGS:--v $PROJECT_ROOT/docker/services/keycloak/themes:/opt/keycloak/themes/noda:ro --tmpfs /opt/keycloak/data}"

# envsubst 变量列表（Keycloak 需要替换的变量比 findclass-ssr 多）
export ENVSUBST_VARS='${POSTGRES_USER} ${POSTGRES_PASSWORD} ${KEYCLOAK_ADMIN_USER} ${KEYCLOAK_ADMIN_PASSWORD} ${SMTP_HOST} ${SMTP_PORT} ${SMTP_FROM} ${SMTP_USER} ${SMTP_PASSWORD}'

# 镜像配置
KEYCLOAK_IMAGE="${KEYCLOAK_IMAGE:-quay.io/keycloak/keycloak:26.2.3}"

# 健康检查参数（Keycloak 启动较慢）
HEALTH_CHECK_MAX_RETRIES=45
HEALTH_CHECK_INTERVAL=4
E2E_MAX_RETRIES=5
E2E_INTERVAL=2

# ============================================
# 函数: http_health_check
# ============================================
# Keycloak 专用 HTTP 健康检查（per D-01）
# 使用 /health/ready 端点
http_health_check() {
  local container="$1"
  local max_retries="${2:-$HEALTH_CHECK_MAX_RETRIES}"
  local interval="${3:-$HEALTH_CHECK_INTERVAL}"
  local attempt=0

  log_info "Keycloak HTTP 健康检查: $container (最多 ${max_retries} 次, 间隔 ${interval}s)"

  while [ $attempt -lt $max_retries ]; do
    attempt=$((attempt + 1))

    if docker exec "$container" wget --quiet --tries=1 --spider "http://localhost:${SERVICE_PORT}${HEALTH_PATH}" 2>/dev/null; then
      log_success "$container — Keycloak 健康检查通过 (第 ${attempt}/${max_retries} 次)"
      return 0
    fi

    if [ $attempt -lt $max_retries ]; then
      sleep "$interval"
    fi
  done

  log_error "$container — Keycloak 健康检查失败 (${max_retries} 次尝试)"
  log_info "最近容器日志:"
  docker logs "$container" --tail 20 2>&1 | sed 's/^/  /'
  return 1
}

# ============================================
# 函数: e2e_verify
# ============================================
# 通过 nginx 容器验证 Keycloak 可达性
e2e_verify() {
  local target_env="$1"
  local max_retries="${2:-$E2E_MAX_RETRIES}"
  local interval="${3:-$E2E_INTERVAL}"
  local container_name
  container_name=$(get_container_name "$target_env")

  log_info "E2E 验证: nginx -> $container_name (最多 ${max_retries} 次)"

  local use_curl=true
  if ! docker exec "$NGINX_CONTAINER" which curl >/dev/null 2>&1; then
    log_info "nginx 容器无 curl，使用 wget 备选方案"
    use_curl=false
  fi

  local attempt=0
  while [ $attempt -lt $max_retries ]; do
    attempt=$((attempt + 1))

    local result=1

    if [ "$use_curl" = true ]; then
      local http_code
      http_code=$(docker exec "$NGINX_CONTAINER" \
        curl -s -o /dev/null -w "%{http_code}" \
        "http://${container_name}:${SERVICE_PORT}${HEALTH_PATH}" 2>/dev/null || echo "000")

      if [ "$http_code" = "200" ]; then
        result=0
      fi
    else
      if docker exec "$NGINX_CONTAINER" \
        wget --quiet --tries=1 --spider \
        "http://${container_name}:${SERVICE_PORT}${HEALTH_PATH}" 2>/dev/null; then
        result=0
      fi
    fi

    if [ $result -eq 0 ]; then
      log_success "E2E 验证通过 (第 ${attempt}/${max_retries} 次)"
      return 0
    fi

    if [ $attempt -lt $max_retries ]; then
      sleep "$interval"
    fi
  done

  log_error "E2E 验证失败 (${max_retries} 次尝试)"
  return 1
}

# ============================================
# 函数: cleanup_old_keycloak_images
# ============================================
# 清理无标签的 Keycloak 镜像（官方镜像不使用 Git SHA 标签）
cleanup_old_keycloak_images() {
  local deleted=0

  # 清理 dangling images
  local dangling_ids
  dangling_ids=$(docker images -f "dangling=true" --format '{{.ID}}' 2>/dev/null || true)
  for img_id in $dangling_ids; do
    docker rmi "$img_id" 2>/dev/null || true
    deleted=$((deleted + 1))
  done

  if [ "$deleted" -gt 0 ]; then
    log_success "镜像清理完成: 删除 ${deleted} 个 dangling 镜像"
  else
    log_info "镜像清理: 无需清理"
  fi
}

# ============================================
# 主函数
# ============================================
main() {
  log_info "=========================================="
  log_info "Keycloak 蓝绿部署开始"
  log_info "=========================================="

  # 前置检查
  log_info "前置检查..."

  if ! docker info >/dev/null 2>&1; then
    log_error "Docker daemon 不可用"
    exit 1
  fi

  if [ "$(is_container_running "$NGINX_CONTAINER")" != "true" ]; then
    log_error "nginx 容器 ($NGINX_CONTAINER) 未运行"
    exit 1
  fi

  if ! docker network inspect "$NETWORK_NAME" >/dev/null 2>&1; then
    log_error "Docker 网络 $NETWORK_NAME 不存在"
    exit 1
  fi

  # 读取活跃环境
  local active_env target_env
  active_env=$(get_active_env)
  target_env=$(get_inactive_env)
  local target_container
  target_container=$(get_container_name "$target_env")

  log_info "活跃环境: $active_env"
  log_info "目标环境: $target_env"
  log_info "目标容器: $target_container"
  log_info "镜像: $KEYCLOAK_IMAGE"

  # 步骤 1/7: 拉取镜像
  log_info "=========================================="
  log_info "步骤 1/7: 拉取 Keycloak 镜像"
  log_info "=========================================="

  docker pull "$KEYCLOAK_IMAGE"
  log_success "镜像拉取完成: $KEYCLOAK_IMAGE"

  # 步骤 2/7: 停止旧目标容器
  log_info "=========================================="
  log_info "步骤 2/7: 停止旧目标容器 + 启动新容器"
  log_info "=========================================="

  if [ "$(is_container_running "$target_container")" = "true" ]; then
    log_info "停止旧目标容器: $target_container"
    docker stop -t 30 "$target_container"
    docker rm "$target_container"
    log_success "旧目标容器已停止"
  fi

  run_container "$target_env" "$KEYCLOAK_IMAGE"

  # 步骤 3/7: HTTP 健康检查
  log_info "=========================================="
  log_info "步骤 3/7: HTTP 健康检查"
  log_info "=========================================="

  if ! http_health_check "$target_container" "$HEALTH_CHECK_MAX_RETRIES" "$HEALTH_CHECK_INTERVAL"; then
    log_error "健康检查失败，保持当前环境: $active_env"
    log_warn "新容器 $target_container 仍在运行，可手动检查后停止"
    exit 1
  fi

  # 步骤 4/7: 切换流量
  log_info "=========================================="
  log_info "步骤 4/7: 切换流量 $active_env -> $target_env"
  log_info "=========================================="

  update_upstream "$target_env"

  if ! docker exec "$NGINX_CONTAINER" nginx -t; then
    log_error "nginx 配置验证失败，回滚 upstream"
    update_upstream "$active_env"
    exit 1
  fi

  reload_nginx
  set_active_env "$target_env"
  log_success "流量切换完成: $active_env -> $target_env"

  # 步骤 5/7: E2E 验证
  log_info "=========================================="
  log_info "步骤 5/7: E2E 验证"
  log_info "=========================================="

  if ! e2e_verify "$target_env" "$E2E_MAX_RETRIES" "$E2E_INTERVAL"; then
    log_error "E2E 验证失败，执行自动回滚"
    update_upstream "$active_env"
    docker exec "$NGINX_CONTAINER" nginx -t
    reload_nginx
    set_active_env "$active_env"
    log_warn "已回滚到 $active_env，新容器 $target_container 仍在运行"
    exit 1
  fi

  # 步骤 6/7: 清理旧镜像
  log_info "=========================================="
  log_info "步骤 6/7: 清理旧镜像"
  log_info "=========================================="

  cleanup_old_keycloak_images

  # 完成
  log_success "=========================================="
  log_success "Keycloak 蓝绿部署完成"
  log_success "=========================================="
  log_success "$active_env -> $target_env (镜像: $KEYCLOAK_IMAGE)"
}

main "$@"
```

关键设计决策：
- 健康检查使用 HTTP `/health/ready` 端点（per D-01），而非 TCP 检查
- start_period 在 run_container 中保持 60s，但 HEALTH_CHECK_MAX_RETRIES=45（45*4=180s 总超时），为 Keycloak 的慢启动留足时间
- 主题卷路径使用 `$PROJECT_ROOT/docker/services/keycloak/themes`（与 compose 配置一致）
- 无 Build/Test/SHA 标签步骤（Keycloak 使用官方镜像）
- 镜像版本可通过 `KEYCLOAK_IMAGE` 环境变量覆盖
- export 所有 SERVICE_* 变量确保 source manage-containers.sh 时能正确覆盖默认值

脚本需要 `chmod +x scripts/keycloak-blue-green-deploy.sh`
  </action>
  <verify>
    <automated>test -f scripts/keycloak-blue-green-deploy.sh && test -x scripts/keycloak-blue-green-deploy.sh && grep -q "KEYCLOAK_IMAGE" scripts/keycloak-blue-green-deploy.sh && grep -q "HEALTH_PATH=/health/ready" scripts/keycloak-blue-green-deploy.sh && grep -q "CONTAINER_MEMORY" scripts/keycloak-blue-green-deploy.sh</automated>
  </verify>
  <done>keycloak-blue-green-deploy.sh 存在且可执行，包含完整的 7 步部署流程（Pull -> Stop Old -> Start New -> Health Check -> Switch -> E2E Verify -> Cleanup），使用 Keycloak 特有参数（1G 内存、/health/ready、官方镜像）</done>
  <acceptance_criteria>
    - 文件存在且可执行: `test -x scripts/keycloak-blue-green-deploy.sh`
    - source manage-containers.sh: `grep -q "source.*manage-containers" scripts/keycloak-blue-green-deploy.sh`
    - Keycloak 镜像配置: `grep -q "KEYCLOAK_IMAGE" scripts/keycloak-blue-green-deploy.sh`
    - 健康检查路径: `grep -q "/health/ready" scripts/keycloak-blue-green-deploy.sh`
    - 1G 内存限制: `grep -q "1g" scripts/keycloak-blue-green-deploy.sh`
    - 主题卷挂载: `grep -q "themes" scripts/keycloak-blue-green-deploy.sh`
    - 蓝绿状态文件: `grep -q "active-env-keycloak" scripts/keycloak-blue-green-deploy.sh`
    - 自动回滚: `grep -q "自动回滚" scripts/keycloak-blue-green-deploy.sh`
    - export 环境变量: `grep -q "export SERVICE_NAME" scripts/keycloak-blue-green-deploy.sh`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 deploy-infrastructure-prod.sh 排除 keycloak</name>
  <files>scripts/deploy/deploy-infrastructure-prod.sh</files>
  <read_first>
    scripts/deploy/deploy-infrastructure-prod.sh
  </read_first>
  <action>
修改 `scripts/deploy/deploy-infrastructure-prod.sh`，将 keycloak 从 START_SERVICES 中移除，并在 EXPECTED_CONTAINERS 中保留 keycloak 的健康检查（因为 compose 仍然定义了 keycloak 服务）。

具体修改：

**1. START_SERVICES（L53）— 移除 keycloak**

当前：
```bash
START_SERVICES="postgres keycloak nginx noda-ops"
```

改为：
```bash
# keycloak 已迁移到蓝绿 docker run 管理，不再通过 compose 启动
START_SERVICES="postgres nginx noda-ops"
```

**2. EXPECTED_CONTAINERS 数组（L44-50）— 保留 keycloak 容器名但添加注释**

当前：
```bash
EXPECTED_CONTAINERS=(
  "noda-infra-postgres-prod"
  "noda-infra-keycloak-prod"
  "noda-infra-nginx"
  "noda-ops"
  "findclass-ssr"
)
```

改为：
```bash
EXPECTED_CONTAINERS=(
  "noda-infra-postgres-prod"
  "noda-infra-nginx"
  "noda-ops"
  # keycloak 和 findclass-ssr 已迁移到蓝绿 docker run 管理
  # 不再通过此脚本的健康检查验证
)
```

注意：移除 `noda-infra-keycloak-prod` 和 `findclass-ssr`，因为两者都已迁移到蓝绿 docker run 管理。findclass-ssr 已经通过 Jenkins Pipeline 管理。

**3. 步骤 4（L264-275）— 更新启动命令注释**

当前注释：
```bash
log_info "启动 PostgreSQL, Keycloak, Nginx, Noda-Ops, Findclass-SSR..."
```

改为：
```bash
log_info "启动 PostgreSQL, Nginx, Noda-Ops..."
```

当前命令：
```bash
docker compose $COMPOSE_FILES up -d $START_SERVICES findclass-ssr
```

改为：
```bash
docker compose $COMPOSE_FILES up -d $START_SERVICES
```

**4. 步骤 6 Keycloak 配置（L303-315）— 添加跳过注释**

在步骤 6 的 Keycloak 配置前后添加注释说明蓝绿模式下不再需要：

```bash
# 注意: Keycloak 配置已由蓝绿部署流程管理
# 如需重新配置 Keycloak realm/client，请手动执行 setup-keycloak-full.sh
log_info "步骤 6/7: 配置 Keycloak"
log_info "Keycloak 已迁移到蓝绿部署模式，跳过 compose 级别的配置"
# 保留 setup-keycloak-full.sh 引用作为手动配置入口
```

**5. 部署完成输出（L348-361）— 更新状态说明**

在最终输出中更新 Keycloak 的状态描述：

```bash
log_info "Keycloak: 蓝绿管理（通过 manage-containers.sh 或 Jenkins Pipeline 部署）"
```
  </action>
  <verify>
    <automated>grep "START_SERVICES" scripts/deploy/deploy-infrastructure-prod.sh | grep -qv "keycloak" && grep "EXPECTED_CONTAINERS" scripts/deploy/deploy-infrastructure-prod.sh -A 6 | grep -qv "noda-infra-keycloak-prod"</automated>
  </verify>
  <done>deploy-infrastructure-prod.sh 的 START_SERVICES 不包含 keycloak，EXPECTED_CONTAINERS 不包含 noda-infra-keycloak-prod，部署流程不再尝试通过 compose 启动 Keycloak</done>
  <acceptance_criteria>
    - START_SERVICES 不含 keycloak: `grep "START_SERVICES=" scripts/deploy/deploy-infrastructure-prod.sh | grep -qv "keycloak"`
    - EXPECTED_CONTAINERS 不含 keycloak: `grep -A 10 "EXPECTED_CONTAINERS" scripts/deploy/deploy-infrastructure-prod.sh | grep -qv "noda-infra-keycloak-prod"`
    - up -d 不含 findclass-ssr: `grep "up -d" scripts/deploy/deploy-infrastructure-prod.sh | grep -qv "findclass-ssr"`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jenkins/手动 -> keycloak-blue-green-deploy.sh | 部署操作入口 |
| keycloak-blue-green-deploy.sh -> docker run | 创建新容器 |
| keycloak-blue-green-deploy.sh -> nginx upstream | 流量切换 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Spoofing | Keycloak 官方镜像 | mitigate | 使用明确版本标签 quay.io/keycloak/keycloak:26.2.3，不使用 latest |
| T-28-05 | Denial of Service | 蓝绿切换 | accept | 切换期间用户会话丢失（JVM 内存），在维护窗口执行 |
| T-28-06 | Elevation of Privilege | EXTRA_DOCKER_ARGS | mitigate | 脚本中硬编码 EXTRA_DOCKER_ARGS 默认值，不接受外部输入覆盖卷挂载路径 |
</threat_model>

<verification>
1. keycloak-blue-green-deploy.sh 存在且可执行
2. 脚本包含完整的 7 步部署流程
3. deploy-infrastructure-prod.sh 不包含 keycloak 在 START_SERVICES 中
4. `bash -n scripts/keycloak-blue-green-deploy.sh` 无语法错误
</verification>

<success_criteria>
- keycloak-blue-green-deploy.sh 可执行完整的 Keycloak 蓝绿部署流程（Pull -> Stop Old -> Start New -> Health Check -> Switch -> Verify -> Cleanup）
- deploy-infrastructure-prod.sh 不再尝试通过 docker compose 启动 keycloak
- 脚本使用 Keycloak 特有参数（1G 内存、/health/ready、官方镜像、主题卷挂载）
</success_criteria>

<output>
After completion, create `.planning/phases/28-keycloak/28-02-SUMMARY.md`
</output>
