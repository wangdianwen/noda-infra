---
phase: 37-cleanup-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup/lib/health.sh
  - scripts/backup/lib/db-health.sh
  - scripts/backup/backup-postgres.sh
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "scripts/backup/lib/ 目录下文件名为 db-health.sh，不再有 health.sh"
    - "backup-postgres.sh source 路径指向 db-health.sh，脚本可正常加载"
    - "db-health.sh 内容包含 PostgreSQL 连接检查和磁盘空间检查函数"
  artifacts:
    - path: "scripts/backup/lib/db-health.sh"
      provides: "数据库健康检查库（PG 连接 + 磁盘空间）"
      exports: ["check_postgres_connection", "check_disk_space", "check_prerequisites"]
      min_lines: 300
    - path: "scripts/backup/lib/health.sh"
      provides: "旧文件，必须不存在"
      must_not_exist: true
    - path: "scripts/backup/backup-postgres.sh"
      provides: "备份主脚本，source 路径已更新"
      contains: "db-health.sh"
  key_links:
    - from: "scripts/backup/backup-postgres.sh"
      to: "scripts/backup/lib/db-health.sh"
      via: "source 引用"
      pattern: "source.*db-health\\.sh"
      must_be_present: true
    - from: "scripts/backup/backup-postgres.sh"
      to: "scripts/backup/lib/health.sh"
      via: "旧 source 引用"
      pattern: "source.*lib/health\\.sh"
      must_be_absent: true
---

<objective>
重命名 scripts/backup/lib/health.sh 为 scripts/backup/lib/db-health.sh，更新唯一消费者 backup-postgres.sh 的 source 路径，消除与 scripts/lib/health.sh 的命名混淆。

Purpose: 当前 scripts/backup/lib/health.sh（数据库连接+磁盘检查）与 scripts/lib/health.sh（Docker 容器健康检查）同名但功能完全不同，导致开发者混淆。重命名为 db-health.sh 精确反映其功能（per D-01）。
Output: scripts/backup/lib/db-health.sh 存在，scripts/backup/lib/health.sh 不存在，backup-postgres.sh source 路径已更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-cleanup-rename/37-CONTEXT.md
</context>

<interfaces>
<!-- 关键区分：两个 health.sh 的不同职责 -->
<!-- scripts/lib/health.sh: Docker 容器健康检查轮询，本 phase 不涉及 -->
<!-- scripts/backup/lib/db-health.sh (原 health.sh): PostgreSQL 连接 + 磁盘空间检查 -->

From scripts/backup/backup-postgres.sh (唯一消费者):
```bash
# 第 24 行 — 当前引用：
source "$SCRIPT_DIR/lib/health.sh"
# 需改为：
source "$SCRIPT_DIR/lib/db-health.sh"
```

From scripts/backup/lib/health.sh — 关键函数签名（重命名后文件内容不变）:
```bash
check_postgres_connection()    # 检查 PG 连接状态
get_database_size()            # 获取单个数据库大小
get_total_database_size()      # 获取所有用户数据库总大小
check_disk_space()             # 检查磁盘空间（容器 + 宿主机）
check_prerequisites()          # 综合检查（连接 + 磁盘）
list_databases()               # 列出可备份的数据库
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重命名 health.sh 为 db-health.sh 并更新引用</name>
  <files>
    scripts/backup/lib/health.sh
    scripts/backup/lib/db-health.sh
    scripts/backup/backup-postgres.sh
  </files>
  <read_first>
    scripts/backup/lib/health.sh
    scripts/backup/backup-postgres.sh
  </read_first>
  <acceptance_criteria>
    - `test -f scripts/backup/lib/db-health.sh` 文件存在
    - `test ! -f scripts/backup/lib/health.sh` 旧文件不存在
    - `bash -n scripts/backup/lib/db-health.sh` 语法检查通过（退出码 0）
    - `bash -n scripts/backup/backup-postgres.sh` 语法检查通过（退出码 0）
    - `grep 'db-health\\.sh' scripts/backup/backup-postgres.sh` 找到 source 行
    - `grep 'lib/health\\.sh' scripts/backup/backup-postgres.sh` 零匹配（不包含旧路径）
    - `scripts/backup/lib/db-health.sh` 第 3 行注释包含"数据库健康检查库"（自注释更新，Claude's Discretion）
    - `scripts/backup/lib/db-health.sh` 仍然导出所有 6 个函数：check_postgres_connection, get_database_size, get_total_database_size, check_disk_space, check_prerequisites, list_databases
  </acceptance_criteria>
  <action>
**步骤 A — 使用 git mv 重命名文件：**

```bash
git mv scripts/backup/lib/health.sh scripts/backup/lib/db-health.sh
```

使用 `git mv` 保留 git 历史追踪。

**步骤 B — 更新 db-health.sh 文件头部注释（Claude's Discretion — 更新自注释以反映新文件名）：**

在 `scripts/backup/lib/db-health.sh` 中更新文件头注释（第 3 行）：
- 原: `# Noda 数据库备份系统 - 健康检查库`
- 改: `# Noda 数据库备份系统 - 数据库健康检查库`

更新依赖注释（第 6 行）：
- 原: `# 功能：备份前前置检查（连接状态 + 磁盘空间）`
- 改: `# 功能：备份前数据库健康检查（PG 连接状态 + 磁盘空间检查）`

**步骤 C — 更新 backup-postgres.sh 的 source 路径：**

在 `scripts/backup/backup-postgres.sh` 第 24 行：
- 原: `source "$SCRIPT_DIR/lib/health.sh"`
- 改: `source "$SCRIPT_DIR/lib/db-health.sh"`

这是唯一需要更新的源码引用（per D-02，docs/ 和 .planning/ 中的历史引用保留不动）。

**步骤 D — 验证无其他源码文件引用旧路径：**

在 scripts/ 目录搜索 `backup/lib/health.sh`，确认 backup-postgres.sh 是唯一消费者且已更新。
  </action>
  <verify>
    <automated>test -f scripts/backup/lib/db-health.sh && echo "db-health.sh exists" || echo "FAIL: db-health.sh missing"; test ! -f scripts/backup/lib/health.sh && echo "health.sh removed" || echo "FAIL: health.sh still exists"; bash -n scripts/backup/lib/db-health.sh && echo "db-health.sh syntax OK"; bash -n scripts/backup/backup-postgres.sh && echo "backup-postgres.sh syntax OK"; grep -q 'db-health\.sh' scripts/backup/backup-postgres.sh && echo "source path updated" || echo "FAIL: source path not updated"; ! grep -q 'lib/health\.sh' scripts/backup/backup-postgres.sh && echo "old path removed" || echo "FAIL: old path still present"; grep -q '数据库健康检查库' scripts/backup/lib/db-health.sh && echo "header comment updated" || echo "WARN: header comment not updated"</automated>
  </verify>
  <done>
    - scripts/backup/lib/health.sh 已重命名为 scripts/backup/lib/db-health.sh（per D-01）
    - db-health.sh 文件头部注释已更新为"数据库健康检查库"
    - backup-postgres.sh 第 24 行 source 路径已从 lib/health.sh 改为 lib/db-health.sh
    - 两个文件语法检查通过
    - scripts/ 源码目录中无其他文件引用旧的 backup/lib/health.sh 路径
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新信任边界 | 本计划仅重命名文件和更新 source 路径，不改变任何逻辑或安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Denial of Service | backup-postgres.sh source 路径更新失败导致备份中断 | mitigate | `bash -n` 语法检查 + grep 验证 source 路径正确性（acceptance_criteria 中已包含） |
</threat_model>

<verification>
1. `test -f scripts/backup/lib/db-health.sh && test ! -f scripts/backup/lib/health.sh` — 重命名完成
2. `bash -n scripts/backup/lib/db-health.sh && bash -n scripts/backup/backup-postgres.sh` — 语法通过
3. `grep 'db-health\.sh' scripts/backup/backup-postgres.sh` — source 路径已更新
4. `! grep -q 'lib/health\.sh' scripts/backup/backup-postgres.sh` — 旧路径已移除
5. `grep -rn 'backup/lib/health\.sh' scripts/` — scripts/ 中无其他文件引用旧路径
</verification>

<success_criteria>
- scripts/backup/lib/db-health.sh 存在，scripts/backup/lib/health.sh 不存在
- backup-postgres.sh source 路径指向 db-health.sh
- 两个文件语法检查通过
- db-health.sh 仍然包含全部 6 个导出函数
</success_criteria>

<output>
After completion, create `.planning/phases/37-cleanup-rename/37-02-SUMMARY.md`
</output>
