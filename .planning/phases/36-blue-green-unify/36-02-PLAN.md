---
phase: 36-blue-green-unify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/rollback-deploy.sh
  - scripts/rollback-findclass.sh
  - scripts/rollback-keycloak.sh
autonomous: true
requirements:
  - BLUE-02

must_haves:
  truths:
    - "统一回滚脚本通过 SERVICE_PORT 和 HEALTH_PATH 环境变量参数化，无硬编码端口/路径"
    - "rollback-findclass.sh 保留为向后兼容 wrapper，调用统一回滚脚本"
    - "rollback-keycloak.sh 作为 keycloak 回滚 wrapper，设置正确参数后调用统一回滚脚本"
    - "findclass-ssr 回滚行为与重构前一致（验证目标容器健康 -> 切换流量 -> E2E 验证）"
  artifacts:
    - path: "scripts/rollback-deploy.sh"
      provides: "统一参数化蓝绿回滚脚本"
      contains: "SERVICE_PORT"
    - path: "scripts/rollback-findclass.sh"
      provides: "findclass-ssr 回滚 wrapper"
      contains: "exec"
    - path: "scripts/rollback-keycloak.sh"
      provides: "keycloak 回滚 wrapper"
      contains: "exec"
  key_links:
    - from: "scripts/rollback-findclass.sh"
      to: "scripts/rollback-deploy.sh"
      via: "exec 调用"
      pattern: "exec.*rollback-deploy\\.sh"
    - from: "scripts/rollback-keycloak.sh"
      to: "scripts/rollback-deploy.sh"
      via: "exec 调用"
      pattern: "exec.*rollback-deploy\\.sh"
---

<objective>
将 rollback-findclass.sh 通用化并创建 keycloak 回滚 wrapper

Purpose: 回滚能力覆盖 findclass-ssr 和 keycloak 两个服务（per D-03, D-04），消除硬编码端口 "3001" 和路径 "/api/health"

Output: 统一回滚脚本 rollback-deploy.sh + findclass wrapper + keycloak wrapper
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-blue-green-unify/36-CONTEXT.md
@.planning/phases/36-blue-green-unify/36-RESEARCH.md

<interfaces>
<!-- manage-containers.sh 提供的核心函数 -->

From scripts/manage-containers.sh:
```bash
# 环境变量
SERVICE_NAME="${SERVICE_NAME:-findclass-ssr}"
SERVICE_PORT="${SERVICE_PORT:-3001}"
HEALTH_PATH="${HEALTH_PATH:-/api/health}"
ACTIVE_ENV_FILE="${ACTIVE_ENV_FILE:-/opt/noda/active-env}"
UPSTREAM_CONF="${UPSTREAM_CONF:-$PROJECT_ROOT/config/nginx/snippets/upstream-findclass.conf}"
NGINX_CONTAINER="noda-infra-nginx"

# 函数
get_active_env()           # 返回 blue/green
get_container_name(env)    # 返回 {SERVICE_NAME}-{env}
is_container_running(name) # 返回 true/false
update_upstream(env)       # 更新 nginx upstream 配置
reload_nginx()             # nginx -s reload
set_active_env(env)        # 原子写入活跃环境状态
```

From scripts/lib/deploy-check.sh:
```bash
http_health_check(container, port, path, retries, interval)  # 0=通过, 1=失败
e2e_verify(target_env, port, path, retries, interval)         # 0=通过, 1=失败
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建统一回滚脚本 rollback-deploy.sh</name>
  <files>scripts/rollback-deploy.sh</files>
  <read_first>
    - scripts/rollback-findclass.sh（当前 findclass-ssr 回滚脚本，逻辑参考）
    - scripts/manage-containers.sh（复用的函数签名和默认值）
    - scripts/lib/deploy-check.sh（http_health_check 和 e2e_verify 签名）
  </read_first>
  <action>
**新建** `scripts/rollback-deploy.sh` 作为统一参数化回滚脚本。

基于当前 `rollback-findclass.sh` 的逻辑，做以下修改：

1. **消除硬编码值**（per D-03）：
   - 第 62 行 `http_health_check "$rollback_container" "3001" "/api/health" "10" "3"` 改为：
     ```bash
     http_health_check "$rollback_container" "${SERVICE_PORT}" "${HEALTH_PATH}" "${ROLLBACK_HEALTH_RETRIES:-10}" "${ROLLBACK_HEALTH_INTERVAL:-3}"
     ```
   - 第 85 行 `e2e_verify "$rollback_env" "3001" "/api/health" "5" "2"` 改为：
     ```bash
     e2e_verify "$rollback_env" "${SERVICE_PORT}" "${HEALTH_PATH}" "${ROLLBACK_E2E_RETRIES:-5}" "${ROLLBACK_E2E_INTERVAL:-2}"
     ```

2. **完整脚本结构**：
```bash
#!/bin/bash
set -euo pipefail

# ============================================
# 统一蓝绿回滚脚本
# ============================================
# 功能：一键将流量切回上一个活跃环境的容器
# 通过环境变量参数化支持 findclass-ssr、keycloak 等多服务回滚
# 复用 manage-containers.sh 的 SERVICE_NAME/SERVICE_PORT/HEALTH_PATH 等环境变量
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

source "$PROJECT_ROOT/scripts/lib/log.sh"
source "$PROJECT_ROOT/scripts/manage-containers.sh"
source "$PROJECT_ROOT/scripts/lib/deploy-check.sh"

# ============================================
# 主函数
# ============================================
main() {
  local active_env
  active_env=$(get_active_env)

  # 回滚目标 = 非活跃环境
  local rollback_env
  if [ "$active_env" = "blue" ]; then
    rollback_env="green"
  else
    rollback_env="blue"
  fi

  local rollback_container
  rollback_container=$(get_container_name "$rollback_env")

  log_info "=========================================="
  log_info "紧急回滚 (${SERVICE_NAME})"
  log_info "=========================================="
  log_info "当前活跃: $active_env ($(get_container_name "$active_env"))"
  log_info "回滚目标: $rollback_env ($rollback_container)"
  log_info ""

  # 前置检查：nginx 容器
  if [ "$(is_container_running "$NGINX_CONTAINER")" != "true" ]; then
    log_error "nginx 容器 ($NGINX_CONTAINER) 未运行，无法回滚"
    exit 1
  fi

  # 检查回滚目标容器
  if [ "$(is_container_running "$rollback_container")" != "true" ]; then
    log_error "回滚目标容器 $rollback_container 未运行"
    log_error "无法回滚 -- 请手动启动旧容器或修复新版本"
    log_info "当前活跃容器 $(get_container_name "$active_env") 状态:"
    docker inspect --format='运行: {{.State.Running}} 健康: {{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}}' "$(get_container_name "$active_env")" 2>/dev/null || echo "  检查失败"
    exit 1
  fi

  # 步骤 1/4: 验证回滚目标容器健康
  log_info "步骤 1/4: 验证回滚目标容器健康"

  if ! http_health_check "$rollback_container" "${SERVICE_PORT}" "${HEALTH_PATH}" "${ROLLBACK_HEALTH_RETRIES:-10}" "${ROLLBACK_HEALTH_INTERVAL:-3}"; then
    log_error "回滚目标容器 $rollback_container 不健康，拒绝回滚"
    log_error "请手动检查容器状态: docker logs $rollback_container --tail 50"
    exit 1
  fi

  # 步骤 2/4: 切换流量
  log_info "步骤 2/4: 切换流量 $active_env -> $rollback_env"

  update_upstream "$rollback_env"

  if ! docker exec "$NGINX_CONTAINER" nginx -t; then
    log_error "nginx 配置验证失败，恢复 upstream"
    update_upstream "$active_env"
    exit 1
  fi

  reload_nginx
  set_active_env "$rollback_env"

  # 步骤 3/4: E2E 验证
  log_info "步骤 3/4: E2E 验证"

  if ! e2e_verify "$rollback_env" "${SERVICE_PORT}" "${HEALTH_PATH}" "${ROLLBACK_E2E_RETRIES:-5}" "${ROLLBACK_E2E_INTERVAL:-2}"; then
    log_error "回滚后 E2E 验证失败"
    log_error "流量已切换到 $rollback_env，请手动检查服务状态"
    exit 1
  fi

  # 步骤 4/4: 完成
  log_info "步骤 4/4: 完成"
  log_success "=========================================="
  log_success "回滚完成 (${SERVICE_NAME})"
  log_success "=========================================="
  log_success "$active_env -> $rollback_env"
  log_info ""
  log_info "新版本容器 $(get_container_name "$active_env") 仍在运行"
  log_info "确认无误后可手动停止: docker stop -t 30 $(get_container_name "$active_env")"
}

main "$@"
```

关键修改点：
- 所有 `"3001"` 替换为 `"${SERVICE_PORT}"`
- 所有 `"/api/health"` 替换为 `"${HEALTH_PATH}"`
- 重试次数/间隔通过 `ROLLBACK_HEALTH_RETRIES`/`ROLLBACK_HEALTH_INTERVAL`/`ROLLBACK_E2E_RETRIES`/`ROLLBACK_E2E_INTERVAL` 环境变量参数化
- 日志中使用 `${SERVICE_NAME}` 替代硬编码服务名
- 文件头注释更新为"统一蓝绿回滚脚本"

创建后运行 `chmod +x scripts/rollback-deploy.sh` 确保可执行。
  </action>
  <verify>
    <automated>bash -n /Users/dianwenwang/Project/noda-infra/scripts/rollback-deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/rollback-deploy.sh` 存在且可执行
    - `scripts/rollback-deploy.sh` 包含 `${SERVICE_PORT}` 字符串
    - `scripts/rollback-deploy.sh` 包含 `${HEALTH_PATH}` 字符串
    - `scripts/rollback-deploy.sh` 包含 `${ROLLBACK_HEALTH_RETRIES:-10}` 字符串
    - `scripts/rollback-deploy.sh` 包含 `${ROLLBACK_E2E_RETRIES:-5}` 字符串
    - `scripts/rollback-deploy.sh` 不包含硬编码的 `"3001"` 字符串
    - `scripts/rollback-deploy.sh` 不包含硬编码的 `"/api/health"` 字符串
    - `bash -n scripts/rollback-deploy.sh` 返回 0
  </acceptance_criteria>
  <done>
    统一回滚脚本通过 bash -n 语法检查；所有端口/路径通过 SERVICE_PORT/HEALTH_PATH 环境变量参数化；重试参数通过 ROLLBACK_* 环境变量参数化；无任何硬编码的端口或路径值
  </done>
</task>

<task type="auto">
  <name>Task 2: 改写 rollback-findclass.sh 为 wrapper + 创建 rollback-keycloak.sh wrapper</name>
  <files>scripts/rollback-findclass.sh, scripts/rollback-keycloak.sh</files>
  <read_first>
    - scripts/rollback-deploy.sh（Task 1 创建的统一回滚脚本）
    - scripts/rollback-findclass.sh（当前 findclass 回滚脚本，将改写为 wrapper）
    - scripts/keycloak-blue-green-deploy.sh（keycloak 参数参考，rollback-keycloak.sh 需要类似参数）
  </read_first>
  <action>
**改写** `scripts/rollback-findclass.sh` 为 findclass-ssr 回滚 wrapper。

内容如下：
```bash
#!/bin/bash
set -euo pipefail
# ============================================
# findclass-ssr 紧急回滚 wrapper
# ============================================
# 向后兼容入口，设置 findclass-ssr 专属参数后调用统一回滚脚本
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# findclass-ssr 使用 manage-containers.sh 默认值：
# SERVICE_NAME=findclass-ssr, SERVICE_PORT=3001, HEALTH_PATH=/api/health
# 无需额外覆盖

exec "$SCRIPT_DIR/rollback-deploy.sh" "$@"
```

**新建** `scripts/rollback-keycloak.sh` 作为 keycloak 回滚 wrapper（per D-04）。

内容如下：
```bash
#!/bin/bash
set -euo pipefail
# ============================================
# Keycloak 紧急回滚 wrapper
# ============================================
# 向后兼容入口，设置 Keycloak 专属参数后调用统一回滚脚本
# ============================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

# Keycloak 服务参数（覆盖 manage-containers.sh 默认值）
export SERVICE_NAME="${SERVICE_NAME:-keycloak}"
export SERVICE_PORT="${SERVICE_PORT:-8080}"
export UPSTREAM_NAME="${UPSTREAM_NAME:-keycloak_backend}"
export HEALTH_PATH="${HEALTH_PATH:-/realms/master}"
export ACTIVE_ENV_FILE="${ACTIVE_ENV_FILE:-/opt/noda/active-env-keycloak}"
export UPSTREAM_CONF="${UPSTREAM_CONF:-$PROJECT_ROOT/config/nginx/snippets/upstream-keycloak.conf}"

exec "$SCRIPT_DIR/rollback-deploy.sh" "$@"
```

关键注意事项：
- findclass wrapper 无需设置任何参数覆盖（manage-containers.sh 默认值就是 findclass-ssr 的值）
- keycloak wrapper 必须覆盖 SERVICE_NAME、SERVICE_PORT、HEALTH_PATH、ACTIVE_ENV_FILE、UPSTREAM_CONF（这些是回滚操作的必要参数）
- keycloak 回滚不需要 IMAGE_SOURCE（回滚不获取新镜像）
- keycloak 回滚不需要 CLEANUP_*（回滚不清理镜像）
- keycloak 回滚不需要 CONTAINER_* 参数（回滚不启动新容器）
- 创建后运行 `chmod +x scripts/rollback-keycloak.sh` 确保可执行
  </action>
  <verify>
    <automated>bash -n /Users/dianwenwang/Project/noda-infra/scripts/rollback-findclass.sh && bash -n /Users/dianwenwang/Project/noda-infra/scripts/rollback-keycloak.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/rollback-findclass.sh` 包含 `exec.*rollback-deploy\.sh`
    - `scripts/rollback-findclass.sh` 不包含 `main()` 函数定义
    - `scripts/rollback-findclass.sh` 文件行数小于 20 行
    - `scripts/rollback-keycloak.sh` 存在且可执行
    - `scripts/rollback-keycloak.sh` 包含 `exec.*rollback-deploy\.sh`
    - `scripts/rollback-keycloak.sh` 包含 `SERVICE_NAME.*keycloak`
    - `scripts/rollback-keycloak.sh` 包含 `SERVICE_PORT.*8080`
    - `scripts/rollback-keycloak.sh` 包含 `HEALTH_PATH.*realms/master`
    - `scripts/rollback-keycloak.sh` 文件行数小于 30 行
    - `bash -n` 两个 wrapper 文件都返回 0
  </acceptance_criteria>
  <done>
    findclass 和 keycloak 回滚 wrapper 都通过 bash -n 语法检查；findclass wrapper 为 thin wrapper（小于 20 行）；keycloak wrapper 设置正确的 keycloak 参数（SERVICE_NAME=keycloak, SERVICE_PORT=8080, HEALTH_PATH=/realms/master）；两个 wrapper 都使用 exec 调用统一回滚脚本
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper -> 统一 rollback 脚本 | 通过 exec 传递环境变量 |
| 统一 rollback 脚本 -> Docker daemon | 执行 docker inspect/stop/exec 等操作 |
| 统一 rollback 脚本 -> nginx 容器 | 通过 docker exec 修改 nginx 配置 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Tampering | SERVICE_NAME 环境变量 | accept | 回滚脚本操作容器（docker inspect/stop），不涉及特权操作；SERVICE_NAME 错误会因容器不存在而失败 |
| T-36-06 | Denial of Service | 回滚目标容器不健康 | mitigate | http_health_check 失败时拒绝回滚（exit 1），防止将流量切到不健康容器 |
</threat_model>

<verification>
```bash
# 语法检查
bash -n scripts/rollback-deploy.sh
bash -n scripts/rollback-findclass.sh
bash -n scripts/rollback-keycloak.sh

# 统一脚本无硬编码端口/路径
grep '"3001"' scripts/rollback-deploy.sh && echo "FAIL: 硬编码端口" || echo "OK"
grep '"/api/health"' scripts/rollback-deploy.sh && echo "FAIL: 硬编码路径" || echo "OK"

# wrapper 使用 exec 调用
grep 'exec.*rollback-deploy.sh' scripts/rollback-findclass.sh
grep 'exec.*rollback-deploy.sh' scripts/rollback-keycloak.sh

# keycloak wrapper 包含关键参数
grep 'SERVICE_NAME.*keycloak' scripts/rollback-keycloak.sh
grep 'SERVICE_PORT.*8080' scripts/rollback-keycloak.sh
grep 'HEALTH_PATH.*realms' scripts/rollback-keycloak.sh
```
</verification>

<success_criteria>
1. 统一回滚脚本 `scripts/rollback-deploy.sh` 通过 `bash -n`，无硬编码端口 "3001" 或路径 "/api/health"
2. findclass wrapper `scripts/rollback-findclass.sh` 通过 `bash -n`，使用 exec 调用统一脚本，小于 20 行
3. keycloak wrapper `scripts/rollback-keycloak.sh` 通过 `bash -n`，包含 SERVICE_NAME=keycloak, SERVICE_PORT=8080, HEALTH_PATH=/realms/master
4. 两个 wrapper 都使用 exec 调用 `rollback-deploy.sh`
5. 统一脚本使用 `${SERVICE_PORT}`, `${HEALTH_PATH}`, `${ROLLBACK_HEALTH_RETRIES:-10}` 等参数化变量
</success_criteria>

<output>
After completion, create `.planning/phases/36-blue-green-unify/36-02-SUMMARY.md`
</output>
