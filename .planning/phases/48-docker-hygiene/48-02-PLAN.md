---
phase: 48-docker-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - deploy/Dockerfile.noda-ops
  - deploy/Dockerfile.noda-site
  - deploy/Dockerfile.backup
  - scripts/backup/docker/Dockerfile.test-verify
autonomous: false
requirements:
  - HYGIENE-01
  - HYGIENE-02
  - HYGIENE-03

must_haves:
  truths:
    - "noda-ops 镜像构建成功，--chown 生效且层数未增加"
    - "noda-site 镜像构建成功，nginx 用户正确拥有文件"
    - "backup 镜像构建成功，.dockerignore 减少了构建上下文"
    - "test-verify 镜像构建成功，使用 postgres:17-alpine 基础镜像"
    - "构建上下文大小因 .dockerignore 而减小"
  artifacts:
    - path: "deploy/Dockerfile.noda-ops"
      provides: "构建验证通过的 noda-ops Dockerfile"
    - path: "deploy/Dockerfile.noda-site"
      provides: "构建验证通过的 noda-site Dockerfile"
    - path: "deploy/Dockerfile.backup"
      provides: "构建验证通过的 backup Dockerfile"
    - path: "scripts/backup/docker/Dockerfile.test-verify"
      provides: "构建验证通过的 test-verify Dockerfile"
  key_links:
    - from: ".dockerignore"
      to: "docker build"
      via: "构建上下文过滤"
      pattern: "构建上下文大小对比"
---

<objective>
验证 Plan 01 的所有改动在真实 Docker 构建中正确工作，包括 .dockerignore 效果确认和 4 个 Dockerfile 的构建测试。

Purpose: 确保修改后的 Dockerfile 能成功构建镜像，COPY --chown 和基础镜像升级无功能问题
Output: 4 个成功构建的 Docker 镜像 + 构建验证日志
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-docker-hygiene/48-CONTEXT.md
@.planning/phases/48-docker-hygiene/48-01-SUMMARY.md

<interfaces>
<!-- 构建上下文映射 -->
- deploy/Dockerfile.noda-ops: context=".." (项目根目录)
- deploy/Dockerfile.backup: context="." (项目根目录)
- deploy/Dockerfile.noda-site: context="../../noda-apps" (另一个仓库)
- scripts/backup/docker/Dockerfile.test-verify: context="." (项目根目录)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 构建 noda-ops、backup、test-verify 镜像并验证</name>
  <files>deploy/Dockerfile.noda-ops, deploy/Dockerfile.backup, scripts/backup/docker/Dockerfile.test-verify</files>
  <read_first>
    - .dockerignore（确认文件存在和内容）
    - deploy/Dockerfile.noda-ops（确认 COPY --chown 改动）
    - deploy/Dockerfile.backup（确认无破坏性改动）
    - scripts/backup/docker/Dockerfile.test-verify（确认 FROM postgres:17-alpine）
  </read_first>
  <action>
按顺序构建 3 个使用项目根目录作为构建上下文的 Dockerfile。noda-site 的构建上下文是 ../../noda-apps（另一个仓库），在本地无法构建，跳过本地构建验证（将在 checkpoint 中通过 Jenkins Pipeline 部署验证）。

**构建 noda-ops：**
```bash
cd /Users/dianwenwang/Project/noda-infra && docker build -f deploy/Dockerfile.noda-ops -t noda-ops:hygiene-test .
```

**构建 backup：**
```bash
cd /Users/dianwenwang/Project/noda-infra && docker build -f deploy/Dockerfile.backup -t noda-backup:hygiene-test .
```

**构建 test-verify：**
```bash
cd /Users/dianwenwang/Project/noda-infra && docker build -f scripts/backup/docker/Dockerfile.test-verify -t noda-backup-test:hygiene-test .
```

**验证构建结果：**
对每个镜像检查：
1. `docker history <image>` 确认层数合理（noda-ops 的 chown 层应从 3 减为 1）
2. `docker images <image>` 确认镜像构建成功
3. 对于 test-verify：`docker run --rm noda-backup-test:hygiene-test psql --version` 确认客户端工具可用

**清理测试镜像：**
```bash
docker rmi noda-ops:hygiene-test noda-backup:hygiene-test noda-backup-test:hygiene-test 2>/dev/null || true
```

如果构建失败，分析错误信息并修复 Dockerfile 或 .dockerignore 中的问题。常见失败原因：
- .dockerignore 排除了 COPY 需要的文件 -> 从排除列表中移除对应条目
- COPY --chown 中用户不存在 -> 检查基础镜像中的用户定义
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && bash -c '
echo "=== Building noda-ops ==="
docker build -f deploy/Dockerfile.noda-ops -t noda-ops:hygiene-test . 2>&1 | tail -5
echo "noda-ops build exit: $?"

echo "=== Building backup ==="
docker build -f deploy/Dockerfile.backup -t noda-backup:hygiene-test . 2>&1 | tail -5
echo "backup build exit: $?"

echo "=== Building test-verify ==="
docker build -f scripts/backup/docker/Dockerfile.test-verify -t noda-backup-test:hygiene-test . 2>&1 | tail -5
echo "test-verify build exit: $?"

echo "=== Verify test-verify psql version ==="
docker run --rm noda-backup-test:hygiene-test psql --version

echo "=== Cleanup test images ==="
docker rmi noda-ops:hygiene-test noda-backup:hygiene-test noda-backup-test:hygiene-test 2>/dev/null

echo "=== DONE ==="
'</automated>
  </verify>
  <done>
1. noda-ops 镜像构建成功，docker history 显示 chown 层数从 3 减少到 1
2. backup 镜像构建成功
3. test-verify 镜像构建成功，psql --version 输出 17.x 版本
4. 测试镜像已清理
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证部署结果</name>
  <files>deploy/Dockerfile.noda-ops, deploy/Dockerfile.noda-site, deploy/Dockerfile.backup, scripts/backup/docker/Dockerfile.test-verify</files>
  <action>等待人工确认以下验证结果。</action>
  <what-built>
已完成 Phase 48 的所有 Dockerfile 优化：
1. 项目根目录 .dockerignore（减少构建上下文大小）
2. noda-ops 的 COPY --chown 优化（层数减少）
3. noda-site 的 COPY --chown 优化（RUN chown 删除）
4. test-verify 基础镜像升级（postgres:15-alpine -> postgres:17-alpine）
5. 本地构建验证通过（noda-ops、backup、test-verify）
  </what-built>
  <how-to-verify>
**选项 A: 通过 Jenkins Pipeline 部署验证（推荐，per D-08）**

1. 触发基础设施部署 Pipeline（会重建 noda-ops 和 backup 镜像）：
   - 浏览器访问 Jenkins（http://服务器IP:8888）
   - 点击 `infra-deploy` 任务
   - 点击 "Build Now"

2. 等待 Pipeline 完成后，检查容器状态：
   ```bash
   docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml ps
   ```
   确认 noda-ops 容器状态为 `healthy`。

3. 检查 noda-ops 镜像层数：
   ```bash
   docker history noda-infra-noda-ops:latest --no-trunc | grep chown
   ```
   应该只有 1 行包含 chown（合并后的 RUN 层）。

**选项 B: 仅验证本地构建（如果不方便触发部署）**

1. 确认上面的自动构建验证输出全部显示 "build exit: 0"
2. 确认 test-verify 的 psql --version 显示 17.x
  </how-to-verify>
  <verify>人工确认 Jenkins Pipeline 部署成功或本地构建验证通过</verify>
  <done>人工确认所有改动在生产环境中正常工作，无功能回归</done>
  <resume-signal>输入 "approved" 确认验证通过，或描述遇到的问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地构建 -> 生产部署 | 需要通过 Jenkins Pipeline 部署验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Tampering | 构建验证 -> 生产部署 | mitigate | 通过 Jenkins Pipeline 部署，Pipeline 包含健康检查门禁 |
</threat_model>

<verification>
1. 3 个本地镜像构建成功（noda-ops、backup、test-verify）
2. test-verify 的 psql --version 显示 17.x
3. noda-ops 的 docker history 中 chown 层数为 1
4. 人工确认部署验证通过
</verification>

<success_criteria>
1. noda-ops、backup、test-verify 三个镜像本地构建成功
2. 人工确认部署验证通过（通过 Jenkins 或手动检查）
3. 无构建错误，无功能回归
</success_criteria>

<output>
After completion, create `.planning/phases/48-docker-hygiene/48-02-SUMMARY.md`
</output>
