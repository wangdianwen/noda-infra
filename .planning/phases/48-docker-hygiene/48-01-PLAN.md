---
phase: 48-docker-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .dockerignore
  - deploy/Dockerfile.noda-ops
  - deploy/Dockerfile.noda-site
  - deploy/Dockerfile.backup
  - scripts/backup/docker/Dockerfile.test-verify
autonomous: true
requirements:
  - HYGIENE-01
  - HYGIENE-02
  - HYGIENE-03

must_haves:
  truths:
    - "项目根目录存在 .dockerignore，排除了 .git、.planning、node_modules 等无关文件"
    - "所有 COPY 指令使用 --chown 标志，不再有单独的 RUN chown 层"
    - "test-verify 基础镜像从 postgres:15-alpine 更新为 postgres:17-alpine"
    - "4 个 Dockerfile 的构建上下文所需的文件没有被 .dockerignore 排除"
  artifacts:
    - path: ".dockerignore"
      provides: "综合 .dockerignore 覆盖 noda-ops、backup、test-verify 三个构建上下文"
      contains: ".git"
    - path: "deploy/Dockerfile.noda-ops"
      provides: "noda-ops 容器构建定义（COPY --chown 优化）"
      contains: "COPY --chown=nodaops:nodaops"
    - path: "deploy/Dockerfile.noda-site"
      provides: "noda-site 容器构建定义（COPY --chown 优化）"
      contains: "COPY --from=builder --chown=nginx:nginx"
    - path: "scripts/backup/docker/Dockerfile.test-verify"
      provides: "test-verify 容器构建定义（基础镜像升级）"
      contains: "postgres:17-alpine"
  key_links:
    - from: ".dockerignore"
      to: "deploy/Dockerfile.noda-ops"
      via: "构建上下文过滤"
      pattern: "不排除 scripts/backup/ 和 deploy/"
    - from: ".dockerignore"
      to: "scripts/backup/docker/Dockerfile.test-verify"
      via: "构建上下文过滤"
      pattern: "不排除 scripts/backup/"
    - from: "deploy/Dockerfile.noda-site"
      to: "nginx:1.25-alpine"
      via: "基础镜像提供 nginx 用户"
      pattern: "FROM nginx:1.25-alpine"
---

<objective>
为 noda-infra 的 4 个自建 Dockerfile 实施 Docker 最佳实践优化：添加项目根目录 .dockerignore、COPY --chown 替代 RUN chown、test-verify 基础镜像升级。

Purpose: 减少镜像层数和构建上下文大小，统一基础镜像版本以共享层缓存
Output: 修改后的 4 个 Dockerfile + 新建的 .dockerignore
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-docker-hygiene/48-CONTEXT.md
@.planning/phases/48-docker-hygiene/48-RESEARCH.md

<interfaces>
<!-- 关键构建上下文信息，executor 必须了解 -->

构建上下文映射（来自 RESEARCH.md + docker-compose.yml）：
- deploy/Dockerfile.noda-ops: context=".."（项目根目录）, dockerfile="deploy/Dockerfile.noda-ops"
- deploy/Dockerfile.backup: context="."（项目根目录，通过 deploy.sh）
- deploy/Dockerfile.noda-site: context="../../noda-apps"（另一个仓库，本 phase 不创建 .dockerignore）
- scripts/backup/docker/Dockerfile.test-verify: context="."（项目根目录，通过 docker build -f ... .）

关键结论：noda-ops、backup、test-verify 都使用项目根目录作为构建上下文，所以 .dockerignore 必须放在项目根目录。
noda-site 的构建上下文是 ../../noda-apps（另一个仓库），本 phase 不处理其 .dockerignore，仅做 Dockerfile 内的 COPY --chown 优化。

noda-site 是多阶段构建：
- Stage 1 (builder): node:20-alpine -- 不涉及 COPY --chown
- Stage 2 (runner): nginx:1.25-alpine -- 需要对 COPY --from=builder 添加 --chown=nginx:nginx

现有 RUN chown 位置：
1. Dockerfile.noda-site 第 51 行: `RUN chown -R nginx:nginx /usr/share/nginx/html /var/cache/nginx`
2. Dockerfile.noda-ops 第 65 行: `RUN chown -R nodaops:nodaops /app /var/log/supervisor /var/log/noda-backup /app/history /run/supervisor`
3. Dockerfile.noda-ops 第 66 行: `RUN mkdir -p /home/nodaops/.config/rclone && chown -R nodaops:nodaops /home/nodaops`
4. Dockerfile.backup: 无 RUN chown
5. Dockerfile.test-verify: 无 RUN chown
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建项目根目录 .dockerignore 并优化 4 个 Dockerfile</name>
  <files>.dockerignore, deploy/Dockerfile.noda-ops, deploy/Dockerfile.noda-site, deploy/Dockerfile.backup, scripts/backup/docker/Dockerfile.test-verify</files>
  <read_first>
    - deploy/Dockerfile.noda-ops（当前第 50-66 行有 COPY 和 chown 指令）
    - deploy/Dockerfile.noda-site（当前第 44-51 行有 COPY 和 chown 指令）
    - deploy/Dockerfile.backup（确认无 chown 指令，但需要 .dockerignore 覆盖）
    - scripts/backup/docker/Dockerfile.test-verify（当前第 1 行 FROM postgres:15-alpine）
  </read_first>
  <action>
按以下步骤执行所有改动：

**步骤 1: 创建 .dockerignore（项目根目录）**

在项目根目录 `/Users/dianwenwang/Project/noda-infra/.dockerignore` 创建新文件。内容必须精确匹配以下排除列表（per D-01, D-02, D-03）：

```
# 版本控制
.git
.gitignore

# 开发工具配置
.editorconfig
.shellcheckrc
.claude/

# 规划文档
.planning/

# 文档（不需要进入镜像）
docs/
*.md
LICENSE

# Docker 配置文件（防止递归）
docker/
docker-compose*.yml
.dockerignore

# 部署脚本（构建产物由 Dockerfile 控制）
deploy.sh
setup-dev.sh

# Jenkins
jenkins/

# 示例和测试（backup 的 tests/ 不需要进入镜像）
examples/
scripts/backup/tests/

# 备份的敏感配置和文档
scripts/backup/.env.backup
scripts/backup/.env.example
scripts/backup/.gitignore
scripts/backup/TEST_REPORT.md
scripts/backup/DEPLOYMENT_SUMMARY.md
scripts/backup/DATA_VOLUME_CHECK.md

# 其他不需要的目录
services/
config/

# 临时文件
*.tmp
*.bak
*.swp
*~
```

关键验证：以下路径绝不能被排除（因为 Dockerfile 的 COPY 需要它们）：
- `scripts/backup/` -- noda-ops (COPY scripts/backup/ /app/backup/) 和 backup (COPY scripts/backup/ /app/) 需要
- `scripts/backup/lib/` -- test-verify (COPY scripts/backup/lib/*.sh /scripts/lib/) 需要
- `scripts/backup/test-verify-weekly.sh` -- test-verify (COPY scripts/backup/test-verify-weekly.sh /scripts/) 需要
- `deploy/crontab` -- noda-ops 和 backup 需要
- `deploy/entrypoint.sh` -- backup 需要
- `deploy/entrypoint-ops.sh` -- noda-ops 需要
- `deploy/supervisord.conf` -- noda-ops 需要

**步骤 2: 优化 deploy/Dockerfile.noda-site（per D-05, D-06）**

将第 48 行：
```dockerfile
COPY --from=builder /app/apps/site/dist /usr/share/nginx/html
```
改为：
```dockerfile
COPY --from=builder --chown=nginx:nginx /app/apps/site/dist /usr/share/nginx/html
```

将第 44-45 行的两个 COPY 添加 --chown：
```dockerfile
COPY --chown=nginx:nginx deploy/nginx/nginx.conf /etc/nginx/nginx.conf
COPY --chown=nginx:nginx deploy/nginx/default.conf /etc/nginx/conf.d/default.conf
```

删除第 50-51 行（RUN chown + 其上方注释）：
```dockerfile
# 设置目录权限（Phase 48 可优化为 COPY --chown）
RUN chown -R nginx:nginx /usr/share/nginx/html /var/cache/nginx
```
（/var/cache/nginx 的权限由 nginx 基础镜像管理，不需要手动 chown）

**步骤 3: 优化 deploy/Dockerfile.noda-ops（per D-05）**

将第 50 行改为：
```dockerfile
COPY --chown=nodaops:nodaops scripts/backup/ /app/backup/
```

将第 54 行改为：
```dockerfile
COPY --chown=nodaops:nodaops deploy/crontab /etc/crontabs/nodaops
```

将第 57 行改为：
```dockerfile
COPY --chown=nodaops:nodaops deploy/supervisord.conf /etc/supervisord.conf
```

将第 60 行改为：
```dockerfile
COPY --chown=nodaops:nodaops deploy/entrypoint-ops.sh /app/entrypoint.sh
```

将第 64-66 行的三个 RUN 指令合并为一个：
```dockerfile
RUN mkdir -p /var/log/supervisor /var/log/noda-backup /app/history /run/supervisor /home/nodaops/.config/rclone && \
    chown -R nodaops:nodaops /var/log/supervisor /var/log/noda-backup /app/history /run/supervisor /home/nodaops
```

（原来第 65 行对 /app 的 chown 不再需要，因为所有 COPY 到 /app 的文件已通过 --chown 设置了正确的所有权。原来第 66 行对 /home/nodaops 的 mkdir + chown 合并到上面的 RUN 中。）

**步骤 4: 升级 scripts/backup/docker/Dockerfile.test-verify（per D-07）**

将第 1 行：
```dockerfile
FROM postgres:15-alpine
```
改为：
```dockerfile
FROM postgres:17-alpine
```

Dockerfile.backup 无 RUN chown 指令，不需要 COPY --chown 改动。
Dockerfile.test-verify 以 root 运行且无 RUN chown，不需要 COPY --chown 改动。
  </action>
  <verify>
    <automated>cd /Users/dianwenwang/Project/noda-infra && bash -c '
echo "=== .dockerignore exists ==="
test -f .dockerignore && echo "PASS" || echo "FAIL"

echo "=== .dockerignore excludes .git ==="
grep -q "^\.git$" .dockerignore && echo "PASS" || echo "FAIL"

echo "=== .dockerignore excludes .planning ==="
grep -q "^\.planning/$" .dockerignore && echo "PASS" || echo "FAIL"

echo "=== .dockerignore does NOT exclude scripts/backup/ ==="
! grep -q "^scripts/backup/$" .dockerignore && echo "PASS" || echo "FAIL"

echo "=== .dockerignore does NOT exclude deploy/ ==="
(! grep -q "^deploy/$" .dockerignore || grep -q "^deploy/$" .dockerignore) && echo "PASS" || echo "FAIL"
# deploy/ itself is NOT excluded - only docker/ is excluded

echo "=== noda-site: COPY --chown=nginx:nginx on --from=builder ==="
grep -q "COPY --from=builder --chown=nginx:nginx" deploy/Dockerfile.noda-site && echo "PASS" || echo "FAIL"

echo "=== noda-site: no standalone RUN chown ==="
! grep -q "^RUN chown" deploy/Dockerfile.noda-site && echo "PASS" || echo "FAIL"

echo "=== noda-ops: COPY --chown=nodaops:nodaops on scripts/backup/ ==="
grep -q "COPY --chown=nodaops:nodaops scripts/backup/" deploy/Dockerfile.noda-ops && echo "PASS" || echo "FAIL"

echo "=== noda-ops: COPY --chown on crontab ==="
grep -q "COPY --chown=nodaops:nodaops deploy/crontab" deploy/Dockerfile.noda-ops && echo "PASS" || echo "FAIL"

echo "=== noda-ops: single merged RUN mkdir+chown ==="
chown_count=$(grep -c "chown.*nodaops" deploy/Dockerfile.noda-ops)
echo "chown lines: $chown_count (expected: 1)"
[ "$chown_count" -eq 1 ] && echo "PASS" || echo "FAIL"

echo "=== test-verify: FROM postgres:17-alpine ==="
grep -q "FROM postgres:17-alpine" scripts/backup/docker/Dockerfile.test-verify && echo "PASS" || echo "FAIL"

echo "=== test-verify: no postgres:15-alpine ==="
! grep -q "postgres:15-alpine" scripts/backup/docker/Dockerfile.test-verify && echo "PASS" || echo "FAIL"

echo "=== noda-site deploy/ nginx configs still have --chown ==="
grep -q "COPY --chown=nginx:nginx deploy/nginx/nginx.conf" deploy/Dockerfile.noda-site && echo "PASS" || echo "FAIL"
grep -q "COPY --chown=nginx:nginx deploy/nginx/default.conf" deploy/Dockerfile.noda-site && echo "PASS" || echo "FAIL"

echo "=== ALL CHECKS COMPLETE ==="
'</automated>
  </verify>
  <done>
1. 项目根目录存在 .dockerignore，排除了 .git、.planning、docs/、docker/、jenkins/ 等，但不排除 scripts/backup/ 和 deploy/ 下的构建所需文件
2. deploy/Dockerfile.noda-site 的 COPY --from=builder 和两个 nginx 配置 COPY 都使用 --chown=nginx:nginx，独立 RUN chown 已删除
3. deploy/Dockerfile.noda-ops 的 4 个 COPY 指令都使用 --chown=nodaops:nodaops，原来的 3 个 RUN chown/mkdir+chown 合并为 1 个 RUN 指令
4. scripts/backup/docker/Dockerfile.test-verify 的 FROM 已改为 postgres:17-alpine
5. deploy/Dockerfile.backup 无改动（确认无 RUN chown）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 构建 context -> Docker daemon | .dockerignore 控制发送到 daemon 的文件，排除敏感文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Information Disclosure | .dockerignore -> 镜像内容 | mitigate | .dockerignore 排除 .planning/、.env、密钥文件等敏感信息 |
| T-48-02 | Information Disclosure | .dockerignore -> 构建上下文日志 | accept | 构建日志在服务器本地，低价值目标 |
</threat_model>

<verification>
1. `.dockerignore` 文件存在且内容正确（排除 .git、.planning，不排除 scripts/backup/、deploy/）
2. `deploy/Dockerfile.noda-ops` 中无独立 `RUN chown` 行，所有 COPY 带 --chown
3. `deploy/Dockerfile.noda-site` 中无独立 `RUN chown` 行，COPY --from=builder 带 --chown=nginx:nginx
4. `scripts/backup/docker/Dockerfile.test-verify` 使用 `FROM postgres:17-alpine`
5. 所有 4 个 Dockerfile 可通过 `docker build --check` 语法检查（或 dry run）
</verification>

<success_criteria>
1. 项目根目录 .dockerignore 存在，包含 .git、.planning 排除规则，不包含构建所需文件的排除规则
2. noda-ops 和 noda-site 的 Dockerfile 中不存在独立的 `RUN chown` 行
3. noda-ops 的 COPY 指令共 4 处使用 `--chown=nodaops:nodaops`
4. noda-site 的 COPY --from=builder 使用 `--chown=nginx:nginx`
5. test-verify 基础镜像是 `postgres:17-alpine`
6. noda-ops 镜像层数减少（从 3 个 RUN chown/mkdir+chown 合并为 1 个 RUN）
</success_criteria>

<output>
After completion, create `.planning/phases/48-docker-hygiene/48-01-SUMMARY.md`
</output>
