---
phase: 32-sudoers-breakglass
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - scripts/break-glass.sh
autonomous: true
requirements: [BREAK-03, BREAK-04]

must_haves:
  truths:
    - "Break-Glass 脚本在 Jenkins 正常运行（HTTP 200）时拒绝执行"
    - "Break-Glass 脚本在 Jenkins 不可用时，验证 sudo 密码后可执行紧急部署"
    - "Break-Glass 操作被记录到审计日志文件"
    - "Break-Glass 仅允许调用 deploy-apps-prod.sh 或 deploy-infrastructure-prod.sh"
  artifacts:
    - path: "scripts/break-glass.sh"
      provides: "Break-Glass 紧急部署入口脚本"
      contains: "check_jenkins_available"
    - path: "/var/log/noda/break-glass.log"
      provides: "Break-Glass 操作审计日志"
  key_links:
    - from: "scripts/break-glass.sh"
      to: "http://localhost:8888/login"
      via: "curl HTTP 健康检查"
      pattern: "curl.*8888.*login"
    - from: "scripts/break-glass.sh"
      to: "scripts/deploy/deploy-apps-prod.sh"
      via: "sudo -u jenkins bash deploy-apps-prod.sh"
      pattern: "sudo -u jenkins.*deploy"
    - from: "scripts/break-glass.sh"
      to: "/var/log/noda/break-glass.log"
      via: "tee -a 审计日志"
      pattern: "tee -a.*break-glass.log"
---

<objective>
创建 Break-Glass 紧急部署脚本。当 Jenkins 不可用时，管理员可通过此脚本验证身份后执行紧急部署。脚本内置 Jenkins 可用性检查防止滥用，所有操作记录到审计日志。

Purpose: Phase 31 权限收敛后，Jenkins 是唯一的部署入口。当 Jenkins 进程崩溃、OOM、磁盘满等故障时，需要受控的紧急通道执行部署，同时防止在 Jenkins 正常时绕过 Pipeline。

Output: scripts/break-glass.sh（紧急部署入口 + Jenkins 健康检查 + PAM 认证 + 审计日志）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-sudoers-breakglass/32-CONTEXT.md
@.planning/phases/32-sudoers-breakglass/32-01-SUMMARY.md

<interfaces>
<!-- Phase 31 已锁定的部署脚本（Break-Glass 调用目标） -->

From scripts/deploy/deploy-apps-prod.sh:
```bash
# 应用服务部署脚本（findclass-ssr + noda-site）
# 权限: 750 root:jenkins（仅 jenkins 用户可执行）
# 入口: bash scripts/deploy/deploy-apps-prod.sh [IMAGE_TAG]
```

From scripts/deploy/deploy-infrastructure-prod.sh:
```bash
# 基础设施部署脚本（postgres）
# 权限: 750 root:jenkins（仅 jenkins 用户可执行）
# 入口: bash scripts/deploy/deploy-infrastructure-prod.sh [--skip-backup]
```

From scripts/setup-jenkins.sh (wait_for_jenkins 函数):
```bash
# Jenkins 健康检查模式
curl -sf "http://localhost:${JENKINS_PORT}/login" > /dev/null 2>&1
# JENKINS_PORT=8888
```

From scripts/lib/log.sh:
```bash
log_info()    # 黄色信息
log_success() # 绿色成功
log_error()   # 红色错误
log_warn()    # 黄色警告
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 break-glass.sh 紧急部署入口脚本</name>
  <files>scripts/break-glass.sh</files>
  <read_first>
    - scripts/setup-jenkins.sh（复用 wait_for_jenkins HTTP 检查模式，JENKINS_PORT=8888）
    - scripts/deploy/deploy-apps-prod.sh（了解调用接口）
    - scripts/deploy/deploy-infrastructure-prod.sh（了解调用接口）
    - scripts/apply-file-permissions.sh（复用平台检测、子命令模式）
    - scripts/lib/log.sh（日志函数）
  </read_first>
  <action>
创建 `scripts/break-glass.sh`，实现紧急部署入口。

**脚本头部：**
```bash
#!/bin/bash
set -euo pipefail
```
- source `$PROJECT_ROOT/scripts/lib/log.sh`
- 复用平台检测模式（"macos" / "linux"）
- 常量定义：
  - `JENKINS_PORT=8888`
  - `JENKINS_HEALTH_URL="http://localhost:${JENKINS_PORT}/login"`
  - `JENKINS_HEALTH_TIMEOUT=10`
  - `AUDIT_LOG="/var/log/noda/break-glass.log"`
  - `PROJECT_ROOT` 从 BASH_SOURCE 推导
  - `ALLOWED_SCRIPTS` 数组包含：
    - `"$PROJECT_ROOT/scripts/deploy/deploy-apps-prod.sh"`
    - `"$PROJECT_ROOT/scripts/deploy/deploy-infrastructure-prod.sh"`

**函数: check_jenkins_available() — Jenkins 可用性检查（D-02, BREAK-04）**

```bash
check_jenkins_available() {
    # 检查 Jenkins HTTP 端点
    # 返回 0=可用（拒绝 Break-Glass），1=不可用（允许 Break-Glass）

    log_info "检查 Jenkins 可用性..."
    log_info "端点: $JENKINS_HEALTH_URL (超时: ${JENKINS_HEALTH_TIMEOUT}s)"

    local http_code
    http_code=$(curl -s -o /dev/null -w "%{http_code}" \
        --connect-timeout "$JENKINS_HEALTH_TIMEOUT" \
        --max-time "$JENKINS_HEALTH_TIMEOUT" \
        "$JENKINS_HEALTH_URL" 2>/dev/null) || http_code="000"

    if [ "$http_code" = "200" ] || [ "$http_code" = "302" ] || [ "$http_code" = "403" ]; then
        # 200/302/403 都说明 Jenkins 进程在响应（403 是需要登录但进程正常）
        log_success "Jenkins 正在运行 (HTTP $http_code)"
        return 0
    else
        log_info "Jenkins 不可用 (HTTP $http_code)"
        return 1
    fi
}
```

**函数: verify_identity() — PAM 身份验证（D-01, BREAK-03）**

```bash
verify_identity() {
    # 使用 sudo -v 验证用户身份（触发 PAM 密码验证）
    # sudo -v 会刷新 sudo 时间戳或要求输入密码
    # 返回 0=验证通过，1=验证失败

    log_info "身份验证（需要 sudo 密码）..."

    if sudo -v; then
        log_success "身份验证通过: $(whoami)"
        return 0
    else
        log_error "身份验证失败"
        return 1
    fi
}
```

**函数: log_audit() — 审计日志记录（BREAK-03）**

```bash
log_audit() {
    local action="$1"
    local detail="${2:-}"

    local timestamp
    timestamp=$(date -Iseconds 2>/dev/null || date +"%Y-%m-%dT%H:%M:%S%z")

    # 确保 /var/log/noda/ 目录存在
    sudo mkdir -p "$(dirname "$AUDIT_LOG")"

    local log_entry
    log_entry="[$timestamp] user=$(whoami) action=$action detail=$detail"

    echo "$log_entry" | sudo tee -a "$AUDIT_LOG" > /dev/null
    sudo chmod 640 "$AUDIT_LOG"
    sudo chown root:jenkins "$AUDIT_LOG" 2>/dev/null || true
}
```

**函数: validate_script() — 验证目标脚本在允许列表中（D-03）**

```bash
validate_script() {
    local target_script="$1"

    for allowed in "${ALLOWED_SCRIPTS[@]}"; do
        if [ "$target_script" = "$allowed" ] || [ "$target_script" = "$(basename "$allowed")" ]; then
            return 0
        fi
    done

    log_error "脚本不在允许列表中: $target_script"
    log_info "允许的脚本:"
    for allowed in "${ALLOWED_SCRIPTS[@]}"; do
        log_info "  - $(basename "$allowed")"
    done
    return 1
}
```

**函数: cmd_deploy() — 执行紧急部署（核心逻辑）**

```bash
cmd_deploy() {
    local target_script="${1:-}"
    local script_args="${@:2}"

    if [ -z "$target_script" ]; then
        log_error "请指定部署脚本"
        log_info "用法: $0 deploy <脚本名> [参数...]"
        log_info "允许的脚本:"
        for allowed in "${ALLOWED_SCRIPTS[@]}"; do
            log_info "  - $(basename "$allowed")"
        done
        exit 1
    fi

    # macOS 检查
    if [[ "$(uname)" == "Darwin" ]]; then
        log_warn "macOS 环境：跳过 Jenkins 可用性检查"
    else
        # 步骤 1/4: 检查 Jenkins 不可用（BREAK-04, D-02）
        if check_jenkins_available; then
            log_error "=========================================="
            log_error "Jenkins 正在运行！请使用 Jenkins Pipeline 部署。"
            log_error "Break-Glass 仅在 Jenkins 不可用时使用。"
            log_error "=========================================="
            log_audit "BREAK_GLASS_DENIED" "jenkins_available"
            exit 1
        fi
    fi

    # 步骤 2/4: 验证目标脚本（D-03）
    # 支持简写名或完整路径
    local resolved_script=""
    for allowed in "${ALLOWED_SCRIPTS[@]}"; do
        if [ "$target_script" = "$allowed" ] || [ "$target_script" = "$(basename "$allowed")" ]; then
            resolved_script="$allowed"
            break
        fi
    done

    if [ -z "$resolved_script" ]; then
        log_error "脚本不在允许列表中: $target_script"
        log_info "允许的脚本:"
        for allowed in "${ALLOWED_SCRIPTS[@]}"; do
            log_info "  - $(basename "$allowed")"
        done
        log_audit "BREAK_GLASS_DENIED" "script_not_allowed:$target_script"
        exit 1
    fi

    if [ ! -f "$resolved_script" ]; then
        log_error "脚本文件不存在: $resolved_script"
        exit 1
    fi

    # 步骤 3/4: 身份验证（D-01, BREAK-03）
    if ! verify_identity; then
        log_audit "BREAK_GLASS_DENIED" "auth_failed"
        exit 1
    fi

    # 步骤 4/4: 执行紧急部署
    log_warn "=========================================="
    log_warn "BREAK-GLASS 紧急部署"
    log_warn "=========================================="
    log_warn "操作人: $(whoami)"
    log_warn "目标脚本: $resolved_script"
    log_warn "脚本参数: ${script_args:-无}"
    log_warn "时间: $(date -Iseconds 2>/dev/null || date)"
    log_warn "=========================================="

    log_audit "BREAK_GLASS_START" "script=$(basename "$resolved_script") args=$script_args"

    # 以 jenkins 用户身份执行部署脚本（per Phase 31 权限锁定）
    # 部署脚本权限为 750 root:jenkins，仅 jenkins 用户可执行
    local exit_code=0
    if [[ "$(uname)" == "Darwin" ]]; then
        # macOS: 直接执行（无 jenkins 用户）
        bash "$resolved_script" $script_args || exit_code=$?
    else
        # Linux: 以 jenkins 用户执行
        sudo -u jenkins bash "$resolved_script" $script_args || exit_code=$?
    fi

    if [ $exit_code -eq 0 ]; then
        log_success "=========================================="
        log_success "BREAK-GLASS 紧急部署完成"
        log_success "=========================================="
        log_audit "BREAK_GLASS_SUCCESS" "script=$(basename "$resolved_script")"
    else
        log_error "=========================================="
        log_error "BREAK-GLASS 紧急部署失败 (exit code: $exit_code)"
        log_error "=========================================="
        log_audit "BREAK_GLASS_FAILED" "script=$(basename "$resolved_script") exit_code=$exit_code"
        exit $exit_code
    fi
}
```

**函数: cmd_status() — 显示 Break-Glass 状态**

```bash
cmd_status() {
    log_info "=========================================="
    log_info "Break-Glass 状态"
    log_info "=========================================="

    # Jenkins 状态
    if [[ "$(uname)" == "Darwin" ]]; then
        log_warn "平台: macOS（生产环境功能，此处仅供参考）"
    else
        if check_jenkins_available; then
            log_success "Jenkins: 运行中（Break-Glass 将被拒绝）"
        else
            log_warn "Jenkins: 不可用（Break-Glass 可用）"
        fi
    fi

    # 审计日志
    if [ -f "$AUDIT_LOG" ]; then
        log_info "审计日志: $AUDIT_LOG"
        log_info "最近 5 条记录:"
        sudo tail -5 "$AUDIT_LOG" 2>/dev/null | sed 's/^/  /'
    else
        log_info "审计日志: 无记录"
    fi

    # 允许的脚本
    log_info "允许的部署脚本:"
    for allowed in "${ALLOWED_SCRIPTS[@]}"; do
        if [ -f "$allowed" ]; then
            log_success "  $(basename "$allowed") — 存在"
        else
            log_error "  $(basename "$allowed") — 不存在"
        fi
    done
}
```

**函数: cmd_log() — 查看审计日志**

```bash
cmd_log() {
    if [ ! -f "$AUDIT_LOG" ]; then
        log_info "审计日志为空: $AUDIT_LOG"
        return 0
    fi

    log_info "Break-Glass 审计日志: $AUDIT_LOG"
    log_info "=========================================="
    sudo cat "$AUDIT_LOG"
}
```

**usage() 函数：**
```
用法: break-glass.sh <命令> [参数]

命令:
  deploy <脚本名> [参数...]  执行紧急部署（仅 Jenkins 不可用时可用）
  status                     显示 Break-Glass 状态和 Jenkins 可用性
  log                        查看审计日志

允许的部署脚本 (D-03):
  deploy-apps-prod.sh        部署应用服务（findclass-ssr, noda-site）
  deploy-infrastructure-prod.sh  部署基础设施（PostgreSQL 等）

安全机制:
  - Jenkins 可用时拒绝执行（D-02）
  - 需要 sudo 密码验证（D-01）
  - 所有操作记录到审计日志（BREAK-03）
  - 仅允许调用指定部署脚本（D-03）

审计日志: /var/log/noda/break-glass.log

示例:
  break-glass.sh status
  break-glass.sh deploy deploy-apps-prod.sh
  break-glass.sh deploy deploy-infrastructure-prod.sh --skip-backup
  break-glass.sh log
```

**子命令分发：**
```bash
case "${1:-}" in
  deploy)  cmd_deploy "${@:2}" ;;
  status)  cmd_status ;;
  log)     cmd_log ;;
  help|--help|-h) usage ;;
  *)       usage && exit 1 ;;
esac
```

脚本文件权限设为 `755`。
  </action>
  <acceptance_criteria>
    - `grep -c 'check_jenkins_available' scripts/break-glass.sh` 返回 >= 2（定义 + 调用）
    - `grep -c 'curl.*8888.*login' scripts/break-glass.sh` 返回 >= 1（D-02: HTTP 健康检查）
    - `grep -c 'sudo -v' scripts/break-glass.sh` 返回 >= 1（D-01: PAM 密码验证）
    - `grep -c 'deploy-apps-prod.sh' scripts/break-glass.sh` 返回 >= 1（D-03: 允许调用）
    - `grep -c 'deploy-infrastructure-prod.sh' scripts/break-glass.sh` 返回 >= 1（D-03: 允许调用）
    - `grep -c 'break-glass.log' scripts/break-glass.sh` 返回 >= 2（BREAK-03: 审计日志）
    - `grep -c 'sudo -u jenkins' scripts/break-glass.sh` 返回 >= 1（以 jenkins 用户执行部署）
    - `grep -c 'log_audit' scripts/break-glass.sh` 返回 >= 5（多处审计记录：DENIED/START/SUCCESS/FAILED）
    - `grep -c 'BREAK_GLASS_DENIED' scripts/break-glass.sh` 返回 >= 1（拒绝时记录审计）
    - `head -1 scripts/break-glass.sh` 为 `#!/bin/bash`
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/break-glass.sh && echo "SYNTAX OK"</automated>
  </verify>
  <done>
    scripts/break-glass.sh 存在且语法正确，包含 deploy/status/log 子命令。
    - Jenkins 可用时拒绝执行（D-02, BREAK-04）
    - sudo -v 密码验证（D-01, BREAK-03）
    - 仅允许 deploy-apps-prod.sh 和 deploy-infrastructure-prod.sh（D-03）
    - 所有操作记录到 /var/log/noda/break-glass.log（BREAK-03）
    - 以 jenkins 用户身份执行部署脚本
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin -> Break-Glass 脚本 | 管理员通过 Break-Glass 获得紧急部署能力 |
| Break-Glass -> jenkins 用户 | 脚本以 jenkins 身份执行部署，不直接 root 操作 |
| Break-Glass -> 审计日志 | 所有操作记录到不可篡改的日志文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Elevation of Privilege | Break-Glass 绕过 Jenkins | mitigate | HTTP 健康检查验证 Jenkins 确实不可用；200/302/403 都视为"可用"（D-02） |
| T-32-06 | Repudiation | Break-Glass 操作无记录 | mitigate | log_audit() 记录所有操作（DENIED/START/SUCCESS/FAILED）到 /var/log/noda/break-glass.log |
| T-32-07 | Spoofing | 伪造 Jenkins 不可用 | mitigate | 使用 curl 直连 localhost:8888，不经 DNS；10 秒超时避免误判 |
| T-32-08 | Elevation of Privilege | Break-Glass 执行任意脚本 | mitigate | validate_script() 严格白名单，仅允许 2 个部署脚本（D-03） |
| T-32-09 | Tampering | 审计日志被篡改 | accept | 当前日志文件 640 root:jenkins；Phase 33 auditd 将提供内核级不可篡改审计 |
</threat_model>

<verification>
## Plan 32-02 验证

1. `bash -n scripts/break-glass.sh` — 语法正确
2. `grep 'curl.*8888.*login' scripts/break-glass.sh` — Jenkins HTTP 检查存在
3. `grep 'sudo -v' scripts/break-glass.sh` — PAM 验证存在
4. `grep 'sudo -u jenkins' scripts/break-glass.sh` — 以 jenkins 用户执行
5. `grep 'break-glass.log' scripts/break-glass.sh` — 审计日志路径存在
6. `grep 'ALLOWED_SCRIPTS' scripts/break-glass.sh` — 脚本白名单存在
7. `grep 'BREAK_GLASS_DENIED' scripts/break-glass.sh` — 拒绝时审计记录存在
</verification>

<success_criteria>
1. break-glass.sh 存在，支持 deploy/status/log 子命令
2. Jenkins 可用时 deploy 被拒绝（BREAK-04, D-02）
3. 需要密码验证（BREAK-03, D-01）
4. 仅允许 2 个部署脚本（BREAK-03, D-03）
5. 所有操作记录到审计日志（BREAK-03）
6. 以 jenkins 用户执行部署（Phase 31 权限兼容）
7. 脚本语法正确（bash -n 通过）
</success_criteria>

<output>
After completion, create `.planning/phases/32-sudoers-breakglass/32-02-SUMMARY.md`
</output>
