# ============================================
# noda-apps preprod 环境变量文件
# ============================================
# 用途：pipeline_deploy_preprod() 使用 envsubst 替换后传给 docker run --env-file
# 与 prod 的区别：DATABASE_URL 指向 noda_preprod 数据库
# Keycloak 配置与 prod 一致，确保 Build Once 镜像兼容性

NODE_ENV=production
CRAWL_POST_LIMIT=10
NEXT_PUBLIC_LOCAL_API_URL=http://localhost:3001
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@noda-infra-postgres-prod:5432/noda_preprod
DIRECT_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@noda-infra-postgres-prod:5432/noda_preprod
KEYCLOAK_URL=https://auth.noda.co.nz
KEYCLOAK_INTERNAL_URL=http://noda-infra-nginx
KEYCLOAK_REALM=noda
KEYCLOAK_CLIENT_ID=noda-frontend
KEYCLOAK_CLIENT_SECRET=${KEYCLOAK_CLIENT_SECRET}
RESEND_API_KEY=${RESEND_API_KEY}
ANTHROPIC_AUTH_TOKEN=${ANTHROPIC_AUTH_TOKEN}
ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL}
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# Email Service（运行时环境变量，非构建时）
KEYCLOAK_ADMIN_URL=http://noda-infra-nginx
KEYCLOAK_ADMIN_USER=${KEYCLOAK_ADMIN_USER}
KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD}
TOKEN_SECRET=${TOKEN_SECRET}
EMAIL_SERVICE_API_KEY=${EMAIL_SERVICE_API_KEY}
AUTH_APP_URL=http://localhost:3004
NEXT_PUBLIC_AUTH_APP_URL=https://auth-preprod.noda.co.nz
RESEND_FROM_EMAIL=Noda <noreply@email.noda.co.nz>

# 2026-09 auth/comment 迁移（preprod：独立 cookie 名避免与 prod 共享父域会话）
SESSION_COOKIE_NAME=noda_session_preprod
GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
AUTH_STATE_SECRET=${AUTH_STATE_SECRET}
COMMENT_SERVICE_KEY=${COMMENT_SERVICE_KEY}
COMMENT_API_INTERNAL_URL=http://localhost:3012
EMAIL_TOKEN_SECRET=${TOKEN_SECRET}
