# ============================================
# Noda 应用服务配置
# ============================================

name: noda-app

services:
  # ----------------------------------------
  # Findclass 前端（Nginx 静态文件）
  # ----------------------------------------
  web:
    build:
      context: ../../noda-apps
      dockerfile: infra/docker/Dockerfile.findclass
      args:
        VITE_KEYCLOAK_URL: "https://auth.noda.co.nz"
        VITE_KEYCLOAK_REALM: noda
        VITE_KEYCLOAK_CLIENT_ID: noda-frontend
    image: findclass-web:latest
    container_name: findclass-web
    restart: unless-stopped
    networks:
      noda-network:
        aliases:
          - findclass-web
    depends_on:
      - api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ----------------------------------------
  # Express API 服务器
  # ----------------------------------------
  api:
    build:
      context: ../../noda-apps
      dockerfile: infra/docker/Dockerfile.api
    image: findclass-api:latest
    container_name: findclass-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://postgres:postgres_password_change_me@postgres:5432/noda_prod
      DIRECT_URL: postgresql://postgres:postgres_password_change_me@postgres:5432/noda_prod
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_REALM: noda
      KEYCLOAK_CLIENT_ID: noda-frontend
    networks:
      noda-network:
        aliases:
          - findclass-api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

networks:
  noda-network:
    external: true
    name: noda-network
