# 评论服务 upstream 变量 — 在 server 块中 include（兼容保留）
# 2026-09 auth/comment 迁移：Remark42 容器下线，comment 应用接管
# comments.noda.co.nz（remark42 兼容 API 形状，:3012）
# 2026-09 三容器拆分：comment 应用落在 noda-frontend-prod（:3012）
# 注意：default.conf 的 comments server 块已改用 upstream-class.conf 的
#   $comment_upstream（同一目标）；本文件仅为手工运维/回滚引用保留
set $remark42_upstream noda-frontend-prod:3012;
