# SeaweedFS S3 upstream 变量 — 在 cdn server 块中 include（Phase 2, 2026-09-12）
# 使用 resolver 127.0.0.11 动态解析 DNS，容器重建后自动刷新 IP
# 容器名: seaweedfs（R4S prod 实例；Mac preprod 实例同名但仅存在各自网络内）
set $seaweedfs_upstream seaweedfs:8333;
