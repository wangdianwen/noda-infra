# ⛔ 已下线（2026-09-12）：Keycloak 容器退役，本文件仅为回滚参考保留。
# 下线证据：nginx access log 连续 3 天+零成功 /realms/* 流量（仅爬虫 404 噪音）；
# DB 已最终归档（backup/2026/decommission/keycloak-final-20260912.dump）后 drop。
# Keycloak upstream 变量 — 在 server 块中 include
# 使用 resolver 127.0.0.11 动态解析 DNS，容器重建后自动刷新 IP
# 容器名: noda-infra-keycloak（单容器，直接替换部署）
set $keycloak_upstream noda-infra-keycloak:8080;
